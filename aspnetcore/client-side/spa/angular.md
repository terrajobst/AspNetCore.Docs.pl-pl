---
title: Szablon projektu biblioteki Angular za pomocą platformy ASP.NET Core
author: SteveSandersonMS
description: Dowiedz się, jak rozpocząć pracę przy użyciu szablonu projektu ASP.NET Core jednej strony aplikacji (SPA) do usługi Angular i Angular interfejsu wiersza polecenia.
monikerRange: '>= aspnetcore-2.0'
ms.author: scaddie
ms.custom: mvc
ms.date: 02/21/2018
uid: spa/angular
ms.openlocfilehash: 763b4fff7c64432328af660c66e6ff3f8f697f71
ms.sourcegitcommit: b2723654af4969a24545f09ebe32004cb5e84a96
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/18/2018
ms.locfileid: "46011362"
---
# <a name="use-the-angular-project-template-with-aspnet-core"></a><span data-ttu-id="127f7-103">Szablon projektu biblioteki Angular za pomocą platformy ASP.NET Core</span><span class="sxs-lookup"><span data-stu-id="127f7-103">Use the Angular project template with ASP.NET Core</span></span>

::: moniker range="= aspnetcore-2.0"

> [!NOTE]
> <span data-ttu-id="127f7-104">Ta dokumentacja nie jest o szablon projektu biblioteki Angular zawarty w ASP.NET Core 2.0.</span><span class="sxs-lookup"><span data-stu-id="127f7-104">This documentation isn't about the Angular project template included in ASP.NET Core 2.0.</span></span> <span data-ttu-id="127f7-105">Chodzi o nowszych szablonu Angular, do której można zaktualizować ręcznie.</span><span class="sxs-lookup"><span data-stu-id="127f7-105">It's about the newer Angular template to which you can update manually.</span></span> <span data-ttu-id="127f7-106">Szablon jest domyślnie w programie ASP.NET Core 2.1.</span><span class="sxs-lookup"><span data-stu-id="127f7-106">The template is included in ASP.NET Core 2.1 by default.</span></span>

::: moniker-end

<span data-ttu-id="127f7-107">Szablon projektu biblioteki Angular zaktualizowane udostępnia dogodny punkt początkowy dla platformy ASP.NET Core aplikacji za pomocą usługi Angular i Angular interfejsu wiersza polecenia, aby zaimplementować interfejs rozbudowane, po stronie klienta użytkownika (UI).</span><span class="sxs-lookup"><span data-stu-id="127f7-107">The updated Angular project template provides a convenient starting point for ASP.NET Core apps using Angular and the Angular CLI to implement a rich, client-side user interface (UI).</span></span>

<span data-ttu-id="127f7-108">Szablon jest odpowiednikiem Tworzenie projektu ASP.NET Core jako zaplecza interfejsu API i projekt usługi Angular interfejsu wiersza polecenia pełnić rolę interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="127f7-108">The template is equivalent to creating an ASP.NET Core project to act as an API backend and an Angular CLI project to act as a UI.</span></span> <span data-ttu-id="127f7-109">Ten szablon oferuje wygodne hostingu obu typów projektów w projekcie pojedynczej aplikacji.</span><span class="sxs-lookup"><span data-stu-id="127f7-109">The template offers the convenience of hosting both project types in a single app project.</span></span> <span data-ttu-id="127f7-110">W związku z tym projekt aplikacji można skompilować i opublikowane jako pojedyncza jednostka.</span><span class="sxs-lookup"><span data-stu-id="127f7-110">Consequently, the app project can be built and published as a single unit.</span></span>

## <a name="create-a-new-app"></a><span data-ttu-id="127f7-111">Utwórz nową aplikację</span><span class="sxs-lookup"><span data-stu-id="127f7-111">Create a new app</span></span>

::: moniker range="= aspnetcore-2.0"

<span data-ttu-id="127f7-112">Jeśli używasz programu ASP.NET Core 2.0, upewnij się, masz [zainstalowany zaktualizowany szablon projektu platformy React](xref:spa/index#installation).</span><span class="sxs-lookup"><span data-stu-id="127f7-112">If using ASP.NET Core 2.0, ensure you've [installed the updated React project template](xref:spa/index#installation).</span></span>

::: moniker-end

::: moniker range=">= aspnetcore-2.1"

<span data-ttu-id="127f7-113">Jeśli masz zainstalowany program ASP.NET Core 2.1, nie ma potrzeby instalowania szablon projektu biblioteki Angular.</span><span class="sxs-lookup"><span data-stu-id="127f7-113">If you have ASP.NET Core 2.1 installed, there's no need to install the Angular project template.</span></span>

::: moniker-end

<span data-ttu-id="127f7-114">Utwórz nowy projekt z wiersza polecenia za pomocą polecenia `dotnet new angular` w pustym katalogu.</span><span class="sxs-lookup"><span data-stu-id="127f7-114">Create a new project from a command prompt using the command `dotnet new angular` in an empty directory.</span></span> <span data-ttu-id="127f7-115">Na przykład, następujące polecenia tworzą aplikację w *Moje aplikacyjnego nowe* katalogu i przełącz się do tego katalogu:</span><span class="sxs-lookup"><span data-stu-id="127f7-115">For example, the following commands create the app in a *my-new-app* directory and switch to that directory:</span></span>

```console
dotnet new angular -o my-new-app
cd my-new-app
```

<span data-ttu-id="127f7-116">Uruchom aplikację z programu Visual Studio lub platformy .NET Core interfejsu wiersza polecenia:</span><span class="sxs-lookup"><span data-stu-id="127f7-116">Run the app from either Visual Studio or the .NET Core CLI:</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="127f7-117">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="127f7-117">Visual Studio</span></span>](#tab/visual-studio/)

<span data-ttu-id="127f7-118">Otwórz wygenerowany *.csproj* pliku i uruchom aplikację w zwykły z tego miejsca.</span><span class="sxs-lookup"><span data-stu-id="127f7-118">Open the generated *.csproj* file, and run the app as normal from there.</span></span>

<span data-ttu-id="127f7-119">Proces kompilacji przywraca zależności rozwiązania npm przy pierwszym uruchomieniu może potrwać kilka minut.</span><span class="sxs-lookup"><span data-stu-id="127f7-119">The build process restores npm dependencies on the first run, which can take several minutes.</span></span> <span data-ttu-id="127f7-120">Kolejne kompilacje są znacznie szybciej.</span><span class="sxs-lookup"><span data-stu-id="127f7-120">Subsequent builds are much faster.</span></span>

# <a name="net-core-clitabnetcore-cli"></a>[<span data-ttu-id="127f7-121">.NET Core CLI</span><span class="sxs-lookup"><span data-stu-id="127f7-121">.NET Core CLI</span></span>](#tab/netcore-cli/)

<span data-ttu-id="127f7-122">Upewnij się, że zmienna środowiskowa o nazwie `ASPNETCORE_Environment` o wartości `Development`.</span><span class="sxs-lookup"><span data-stu-id="127f7-122">Ensure you have an environment variable called `ASPNETCORE_Environment` with a value of `Development`.</span></span> <span data-ttu-id="127f7-123">Na Windows (w monity-PowerShell), uruchom `SET ASPNETCORE_Environment=Development`.</span><span class="sxs-lookup"><span data-stu-id="127f7-123">On Windows (in non-PowerShell prompts), run `SET ASPNETCORE_Environment=Development`.</span></span> <span data-ttu-id="127f7-124">W systemie Linux lub macOS Uruchom `export ASPNETCORE_Environment=Development`.</span><span class="sxs-lookup"><span data-stu-id="127f7-124">On Linux or macOS, run `export ASPNETCORE_Environment=Development`.</span></span>

<span data-ttu-id="127f7-125">Uruchom [kompilacji dotnet](/dotnet/core/tools/dotnet-build) Aby sprawdzić, aplikacja poprawnie się kompiluje.</span><span class="sxs-lookup"><span data-stu-id="127f7-125">Run [dotnet build](/dotnet/core/tools/dotnet-build) to verify the app builds correctly.</span></span> <span data-ttu-id="127f7-126">Przy pierwszym uruchomieniu procesu kompilacji przywraca zależności rozwiązania npm, co może zająć kilka minut.</span><span class="sxs-lookup"><span data-stu-id="127f7-126">On the first run, the build process restores npm dependencies, which can take several minutes.</span></span> <span data-ttu-id="127f7-127">Kolejne kompilacje są znacznie szybciej.</span><span class="sxs-lookup"><span data-stu-id="127f7-127">Subsequent builds are much faster.</span></span>

<span data-ttu-id="127f7-128">Uruchom [dotnet, uruchom](/dotnet/core/tools/dotnet-run) Aby uruchomić aplikację.</span><span class="sxs-lookup"><span data-stu-id="127f7-128">Run [dotnet run](/dotnet/core/tools/dotnet-run) to start the app.</span></span> <span data-ttu-id="127f7-129">Zostanie zarejestrowany komunikat podobny do następującego:</span><span class="sxs-lookup"><span data-stu-id="127f7-129">A message similar to the following is logged:</span></span>

```console
Now listening on: http://localhost:<port>
```

<span data-ttu-id="127f7-130">Przejdź do tego adresu URL w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="127f7-130">Navigate to this URL in a browser.</span></span>

<span data-ttu-id="127f7-131">Wystąpienie serwera Angular interfejsu wiersza polecenia w tle uruchamiania aplikacji.</span><span class="sxs-lookup"><span data-stu-id="127f7-131">The app starts up an instance of the Angular CLI server in the background.</span></span> <span data-ttu-id="127f7-132">Zostanie zarejestrowany komunikat podobny do następującego: *NG Live Development Server nasłuchuje na hoście lokalnym:&lt;otherport&gt;, otwórz przeglądarkę na http://localhost:&lt; otherport&gt; /*  .</span><span class="sxs-lookup"><span data-stu-id="127f7-132">A message similar to the following is logged: *NG Live Development Server is listening on localhost:&lt;otherport&gt;, open your browser on http://localhost:&lt;otherport&gt;/*.</span></span> <span data-ttu-id="127f7-133">Zignoruj ten komunikat&mdash;ma **nie** adres URL dla połączonych aplikacji platformy ASP.NET Core i wiersza polecenia usługi Angular.</span><span class="sxs-lookup"><span data-stu-id="127f7-133">Ignore this message&mdash;it's **not** the URL for the combined ASP.NET Core and Angular CLI app.</span></span>

---

<span data-ttu-id="127f7-134">Szablon projektu umożliwia utworzenie aplikacji ASP.NET Core oraz aplikacji Angular.</span><span class="sxs-lookup"><span data-stu-id="127f7-134">The project template creates an ASP.NET Core app and an Angular app.</span></span> <span data-ttu-id="127f7-135">Aplikacja platformy ASP.NET Core jest przeznaczona do użytku z dostępem do danych, autoryzację i inne problemy po stronie serwera.</span><span class="sxs-lookup"><span data-stu-id="127f7-135">The ASP.NET Core app is intended to be used for data access, authorization, and other server-side concerns.</span></span> <span data-ttu-id="127f7-136">Platformy Angular aplikacji znajdujących się w *ClientApp* podkatalogu, jest przeznaczona do użycia dla wszystkich obaw interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="127f7-136">The Angular app, residing in the *ClientApp* subdirectory, is intended to be used for all UI concerns.</span></span>

## <a name="add-pages-images-styles-modules-etc"></a><span data-ttu-id="127f7-137">Dodawanie stron, obrazów, style, moduły, itp.</span><span class="sxs-lookup"><span data-stu-id="127f7-137">Add pages, images, styles, modules, etc.</span></span>

<span data-ttu-id="127f7-138">*ClientApp* katalog zawiera standardowe aplikacji Angular interfejsu wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="127f7-138">The *ClientApp* directory contains a standard Angular CLI app.</span></span> <span data-ttu-id="127f7-139">Można znaleźć w oficjalnej [dokumentacji usługi Angular](https://github.com/angular/angular-cli/wiki) Aby uzyskać więcej informacji.</span><span class="sxs-lookup"><span data-stu-id="127f7-139">See the official [Angular documentation](https://github.com/angular/angular-cli/wiki) for more information.</span></span>

<span data-ttu-id="127f7-140">Istnieją drobne różnice między Angular aplikację utworzoną za pomocą tego szablonu, a ten, który został utworzony przez wierszem polecenia platformy Angular (za pośrednictwem `ng new`); jednak możliwości aplikacji nie ulegną zmianie.</span><span class="sxs-lookup"><span data-stu-id="127f7-140">There are slight differences between the Angular app created by this template and the one created by Angular CLI itself (via `ng new`); however, the app's capabilities are unchanged.</span></span> <span data-ttu-id="127f7-141">Aplikacja utworzona przez szablon zawiera [Bootstrap](https://getbootstrap.com/)— na podstawie układu oraz przykład podstawowej routingu.</span><span class="sxs-lookup"><span data-stu-id="127f7-141">The app created by the template contains a [Bootstrap](https://getbootstrap.com/)-based layout and a basic routing example.</span></span>

## <a name="run-ng-commands"></a><span data-ttu-id="127f7-142">Uruchom polecenia ng</span><span class="sxs-lookup"><span data-stu-id="127f7-142">Run ng commands</span></span>

<span data-ttu-id="127f7-143">W wierszu polecenia przejdź do *ClientApp* podkatalogu:</span><span class="sxs-lookup"><span data-stu-id="127f7-143">In a command prompt, switch to the *ClientApp* subdirectory:</span></span>

```console
cd ClientApp
```

<span data-ttu-id="127f7-144">Jeśli masz `ng` narzędzie jest zainstalowane globalnie, możesz uruchomić dowolne z jego poleceń.</span><span class="sxs-lookup"><span data-stu-id="127f7-144">If you have the `ng` tool installed globally, you can run any of its commands.</span></span> <span data-ttu-id="127f7-145">Na przykład, można uruchomić `ng lint`, `ng test`, ani żadnego innego [Angular polecenia interfejsu wiersza polecenia](https://github.com/angular/angular-cli/wiki#additional-commands).</span><span class="sxs-lookup"><span data-stu-id="127f7-145">For example, you can run `ng lint`, `ng test`, or any of the other [Angular CLI commands](https://github.com/angular/angular-cli/wiki#additional-commands).</span></span> <span data-ttu-id="127f7-146">Nie ma potrzeby uruchomić `ng serve` , ponieważ dotyczy zarówno po stronie serwera i klienta części Twojej aplikacji obsługujących aplikacji platformy ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="127f7-146">There's no need to run `ng serve` though, because your ASP.NET Core app deals with serving both server-side and client-side parts of your app.</span></span> <span data-ttu-id="127f7-147">Używa wewnętrznie, `ng serve` w trakcie opracowywania.</span><span class="sxs-lookup"><span data-stu-id="127f7-147">Internally, it uses `ng serve` in development.</span></span>

<span data-ttu-id="127f7-148">Jeśli nie masz `ng` narzędzie jest zainstalowane, uruchom `npm run ng` zamiast tego.</span><span class="sxs-lookup"><span data-stu-id="127f7-148">If you don't have the `ng` tool installed, run `npm run ng` instead.</span></span> <span data-ttu-id="127f7-149">Na przykład, można uruchomić `npm run ng lint` lub `npm run ng test`.</span><span class="sxs-lookup"><span data-stu-id="127f7-149">For example, you can run `npm run ng lint` or `npm run ng test`.</span></span>

## <a name="install-npm-packages"></a><span data-ttu-id="127f7-150">Instalowanie pakietów npm</span><span class="sxs-lookup"><span data-stu-id="127f7-150">Install npm packages</span></span>

<span data-ttu-id="127f7-151">Aby zainstalować pakiety npm innych firm, należy użyć wiersza polecenia w *ClientApp* podkatalogu.</span><span class="sxs-lookup"><span data-stu-id="127f7-151">To install third-party npm packages, use a command prompt in the *ClientApp* subdirectory.</span></span> <span data-ttu-id="127f7-152">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="127f7-152">For example:</span></span>

```console
cd ClientApp
npm install --save <package_name>
```

## <a name="publish-and-deploy"></a><span data-ttu-id="127f7-153">Publikowanie i wdrażanie</span><span class="sxs-lookup"><span data-stu-id="127f7-153">Publish and deploy</span></span>

<span data-ttu-id="127f7-154">Na etapie opracowywania aplikacji jest uruchamiany w trybie zoptymalizowane pod kątem dla wygody deweloperów.</span><span class="sxs-lookup"><span data-stu-id="127f7-154">In development, the app runs in a mode optimized for developer convenience.</span></span> <span data-ttu-id="127f7-155">Na przykład pakiety języka JavaScript obejmują map źródeł (tak, aby podczas debugowania, możesz zobaczyć oryginalnego kodu TypeScript).</span><span class="sxs-lookup"><span data-stu-id="127f7-155">For example, JavaScript bundles include source maps (so that when debugging, you can see your original TypeScript code).</span></span> <span data-ttu-id="127f7-156">Aplikacja oczekuje na TypeScript, HTML i CSS zmian plików na dysku i automatycznie następuje rekompilacja i ponowne załadowanie po wykryciu tych plików, zmiany.</span><span class="sxs-lookup"><span data-stu-id="127f7-156">The app watches for TypeScript, HTML, and CSS file changes on disk and automatically recompiles and reloads when it sees those files change.</span></span>

<span data-ttu-id="127f7-157">W środowisku produkcyjnym obsługiwać wersję aplikacji, która jest zoptymalizowana pod kątem wydajności.</span><span class="sxs-lookup"><span data-stu-id="127f7-157">In production, serve a version of your app that's optimized for performance.</span></span> <span data-ttu-id="127f7-158">Jest ona konfigurowana automatycznie.</span><span class="sxs-lookup"><span data-stu-id="127f7-158">This is configured to happen automatically.</span></span> <span data-ttu-id="127f7-159">Podczas publikowania, zminimalizowany emituje konfigurację kompilacji, z wyprzedzeniem of-time (AoT) skompilowany kompilację kodu po stronie klienta.</span><span class="sxs-lookup"><span data-stu-id="127f7-159">When you publish, the build configuration emits a minified, ahead-of-time (AoT) compiled build of your client-side code.</span></span> <span data-ttu-id="127f7-160">W przeciwieństwie do tworzenia kompilacji, kompilacja w produkcji nie wymaga środowiska Node.js można zainstalować na serwerze (Jeśli nie włączono [prerendering po stronie serwera](#server-side-rendering)).</span><span class="sxs-lookup"><span data-stu-id="127f7-160">Unlike the development build, the production build doesn't require Node.js to be installed on the server (unless you have enabled [server-side prerendering](#server-side-rendering)).</span></span>

<span data-ttu-id="127f7-161">Można użyć standardowego [metody hosting i wdrażanie platformy ASP.NET Core](xref:host-and-deploy/index).</span><span class="sxs-lookup"><span data-stu-id="127f7-161">You can use standard [ASP.NET Core hosting and deployment methods](xref:host-and-deploy/index).</span></span>

## <a name="run-ng-serve-independently"></a><span data-ttu-id="127f7-162">Uruchom niezależnie "ng służą"</span><span class="sxs-lookup"><span data-stu-id="127f7-162">Run "ng serve" independently</span></span>

<span data-ttu-id="127f7-163">Projekt jest skonfigurowana do uruchamiania własne wystąpienie serwera, usługi Angular interfejsu wiersza polecenia w tle, gdy aplikacja platformy ASP.NET Core jest uruchamiany w trybie projektowania.</span><span class="sxs-lookup"><span data-stu-id="127f7-163">The project is configured to start its own instance of the Angular CLI server in the background when the ASP.NET Core app starts in development mode.</span></span> <span data-ttu-id="127f7-164">Jest to wygodne, ponieważ nie trzeba ręcznie uruchomić oddzielny serwer.</span><span class="sxs-lookup"><span data-stu-id="127f7-164">This is convenient because you don't have to run a separate server manually.</span></span>

<span data-ttu-id="127f7-165">Brak wadą tego ustawienia domyślnego.</span><span class="sxs-lookup"><span data-stu-id="127f7-165">There's a drawback to this default setup.</span></span> <span data-ttu-id="127f7-166">Po każdej zmianie kodu C# i ASP.NET Core aplikacja wymaga ponownego uruchomienia, powoduje ponowne uruchomienie serwera Angular interfejsu wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="127f7-166">Each time you modify your C# code and your ASP.NET Core app needs to restart, the Angular CLI server restarts.</span></span> <span data-ttu-id="127f7-167">Około 10 sekund jest wymagana, aby rozpocząć tworzenie kopii zapasowej.</span><span class="sxs-lookup"><span data-stu-id="127f7-167">Around 10 seconds is required to start back up.</span></span> <span data-ttu-id="127f7-168">Jeśli wprowadzasz częste C# kodu edycji i nie chcesz czekać na Angular interfejsu wiersza polecenia ponownie uruchomić, uruchom serwer platformy Angular interfejsu wiersza polecenia w zewnętrznie, niezależnie od procesu, ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="127f7-168">If you're making frequent C# code edits and don't want to wait for Angular CLI to restart, run the Angular CLI server externally, independently of the ASP.NET Core process.</span></span> <span data-ttu-id="127f7-169">Aby to zrobić:</span><span class="sxs-lookup"><span data-stu-id="127f7-169">To do so:</span></span>

1. <span data-ttu-id="127f7-170">W wierszu polecenia przejdź do *ClientApp* podkatalogu, a następnie uruchom serwera wdrożeniowego programu Angular interfejsu wiersza polecenia:</span><span class="sxs-lookup"><span data-stu-id="127f7-170">In a command prompt, switch to the *ClientApp* subdirectory, and launch the Angular CLI development server:</span></span>

    ```console
    cd ClientApp
    npm start
    ```

    > [!IMPORTANT]
    > <span data-ttu-id="127f7-171">Użyj `npm start` można uruchomić serwera wdrożeniowego programu Angular interfejsu wiersza polecenia nie `ng serve`, dzięki czemu konfiguracji w *package.json* jest zachowana.</span><span class="sxs-lookup"><span data-stu-id="127f7-171">Use `npm start` to launch the Angular CLI development server, not `ng serve`, so that the configuration in *package.json* is respected.</span></span> <span data-ttu-id="127f7-172">Aby przekazywać dodatkowych parametrów do serwera usługi Angular interfejsu wiersza polecenia, dodaj je do odpowiedniego `scripts` wiersz w Twojej *package.json* pliku.</span><span class="sxs-lookup"><span data-stu-id="127f7-172">To pass additional parameters to the Angular CLI server, add them to the relevant `scripts` line in your *package.json* file.</span></span>

2. <span data-ttu-id="127f7-173">Zmodyfikuj do korzystania z zewnętrznego wystąpienia usługi Angular interfejsu wiersza polecenia zamiast uruchamiania jednego z własnych aplikacji platformy ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="127f7-173">Modify your ASP.NET Core app to use the external Angular CLI instance instead of launching one of its own.</span></span> <span data-ttu-id="127f7-174">W swojej *uruchamiania* klasy, Zastąp `spa.UseAngularCliServer` wywołania następującym kodem:</span><span class="sxs-lookup"><span data-stu-id="127f7-174">In your *Startup* class, replace the `spa.UseAngularCliServer` invocation with the following:</span></span>

    ```csharp
    spa.UseProxyToSpaDevelopmentServer("http://localhost:4200");
    ```

<span data-ttu-id="127f7-175">Po uruchomieniu aplikacji platformy ASP.NET Core, nie będzie go uruchomić serwera Angular interfejsu wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="127f7-175">When you start your ASP.NET Core app, it won't launch an Angular CLI server.</span></span> <span data-ttu-id="127f7-176">Wystąpienie, które można uruchomić ręcznie jest używana zamiast tego.</span><span class="sxs-lookup"><span data-stu-id="127f7-176">The instance you started manually is used instead.</span></span> <span data-ttu-id="127f7-177">To umożliwia uruchamianie i ponowne uruchamianie szybciej.</span><span class="sxs-lookup"><span data-stu-id="127f7-177">This enables it to start and restart faster.</span></span> <span data-ttu-id="127f7-178">Oczekuje się już na Angular interfejsu wiersza polecenia ponownie skompilować aplikację kliencką każdorazowo.</span><span class="sxs-lookup"><span data-stu-id="127f7-178">It's no longer waiting for Angular CLI to rebuild your client app each time.</span></span>

## <a name="server-side-rendering"></a><span data-ttu-id="127f7-179">Renderowanie po stronie serwera</span><span class="sxs-lookup"><span data-stu-id="127f7-179">Server-side rendering</span></span>

<span data-ttu-id="127f7-180">Jako funkcja wydajności można wstępnie renderowane Angular aplikacji na serwerze, jak i uruchamiając go na komputerze klienckim.</span><span class="sxs-lookup"><span data-stu-id="127f7-180">As a performance feature, you can choose to pre-render your Angular app on the server as well as running it on the client.</span></span> <span data-ttu-id="127f7-181">Oznacza to, że przeglądarki odbierają kod znaczników HTML reprezentujący początkowej interfejsu użytkownika aplikacji, dzięki czemu są wyświetlane nawet przed pobierania i wykonywania pakietów usługi języka JavaScript.</span><span class="sxs-lookup"><span data-stu-id="127f7-181">This means that browsers receive HTML markup representing your app's initial UI, so they display it even before downloading and executing your JavaScript bundles.</span></span> <span data-ttu-id="127f7-182">Większość implementacji tego pochodzi z usługi Angular funkcję o nazwie [Angular Universal](https://universal.angular.io/).</span><span class="sxs-lookup"><span data-stu-id="127f7-182">Most of the implementation of this comes from an Angular feature called [Angular Universal](https://universal.angular.io/).</span></span>

> [!TIP]
> <span data-ttu-id="127f7-183">Włączanie renderowania po stronie serwera (SSR) wprowadzono szereg dodatkowych komplikacji zarówno podczas tworzenia i wdrażania.</span><span class="sxs-lookup"><span data-stu-id="127f7-183">Enabling server-side rendering (SSR) introduces a number of extra complications both during development and deployment.</span></span> <span data-ttu-id="127f7-184">Odczyt [wady SSR](#drawbacks-of-ssr) do ustalenia, czy SSR jest odpowiednia dla wymagań.</span><span class="sxs-lookup"><span data-stu-id="127f7-184">Read [drawbacks of SSR](#drawbacks-of-ssr) to determine if SSR is a good fit for your requirements.</span></span>

<span data-ttu-id="127f7-185">Aby włączyć SSR, należy wykonać liczbę ma zostać dodany do projektu.</span><span class="sxs-lookup"><span data-stu-id="127f7-185">To enable SSR, you need to make a number of additions to your project.</span></span>

<span data-ttu-id="127f7-186">W *uruchamiania* klasy *po* wiersza, który konfiguruje `spa.Options.SourcePath`, i *przed* wywołanie `UseAngularCliServer` lub `UseProxyToSpaDevelopmentServer`, Dodaj następujący kod:</span><span class="sxs-lookup"><span data-stu-id="127f7-186">In the *Startup* class, *after* the line that configures `spa.Options.SourcePath`, and *before* the call to `UseAngularCliServer` or `UseProxyToSpaDevelopmentServer`, add the following:</span></span>

[!code-csharp[](sample/AngularServerSideRendering/Startup.cs?name=snippet_Call_UseSpa&highlight=5-12)]

<span data-ttu-id="127f7-187">W trybie projektowania, ten kod próbuje kompilacji pakietu SSR, uruchamiając skrypt `build:ssr`, który jest zdefiniowany w *ClientApp\package.json*.</span><span class="sxs-lookup"><span data-stu-id="127f7-187">In development mode, this code attempts to build the SSR bundle by running the script `build:ssr`, which is defined in *ClientApp\package.json*.</span></span> <span data-ttu-id="127f7-188">Powoduje to skompilowanie aplikacji Angular o nazwie `ssr`, która nie jest jeszcze zdefiniowana.</span><span class="sxs-lookup"><span data-stu-id="127f7-188">This builds an Angular app named `ssr`, which isn't yet defined.</span></span>

<span data-ttu-id="127f7-189">Na koniec `apps` tablicy w *ClientApp/.angular-cli.json*, definiowania dodatkowych aplikacji o nazwie `ssr`.</span><span class="sxs-lookup"><span data-stu-id="127f7-189">At the end of the `apps` array in *ClientApp/.angular-cli.json*, define an extra app with name `ssr`.</span></span> <span data-ttu-id="127f7-190">Użyj następujących opcji:</span><span class="sxs-lookup"><span data-stu-id="127f7-190">Use the following options:</span></span>

[!code-json[](sample/AngularServerSideRendering/ClientApp/.angular-cli.json?range=24-41)]

<span data-ttu-id="127f7-191">Nową konfigurację aplikacji z włączoną obsługą SSR wymagane są dwa pliki dalsze: *tsconfig.server.json* i *main.server.ts*.</span><span class="sxs-lookup"><span data-stu-id="127f7-191">This new SSR-enabled app configuration requires two further files: *tsconfig.server.json* and *main.server.ts*.</span></span> <span data-ttu-id="127f7-192">*Tsconfig.server.json* plik Określa opcje kompilacji TypeScript.</span><span class="sxs-lookup"><span data-stu-id="127f7-192">The *tsconfig.server.json* file specifies TypeScript compilation options.</span></span> <span data-ttu-id="127f7-193">*Main.server.ts* pliku służy jako punkt wejścia kodu podczas SSR.</span><span class="sxs-lookup"><span data-stu-id="127f7-193">The *main.server.ts* file serves as the code entry point during SSR.</span></span>

<span data-ttu-id="127f7-194">Dodaj nowy plik o nazwie *tsconfig.server.json* wewnątrz *ClientApp/src* (wraz z istniejącą *tsconfig.app.json*), które zawierają:</span><span class="sxs-lookup"><span data-stu-id="127f7-194">Add a new file called *tsconfig.server.json* inside *ClientApp/src* (alongside the existing *tsconfig.app.json*), containing the following:</span></span>

[!code-json[](sample/AngularServerSideRendering/ClientApp/src/tsconfig.server.json)]

<span data-ttu-id="127f7-195">Ten plik konfiguruje kompilator AoT Angular do wyszukania moduł o nazwie `app.server.module`.</span><span class="sxs-lookup"><span data-stu-id="127f7-195">This file configures Angular's AoT compiler to look for a module called `app.server.module`.</span></span> <span data-ttu-id="127f7-196">Dodaj to przez utworzenie nowego pliku w *ClientApp/src/app/app.server.module.ts* (wraz z istniejącą *app.module.ts*) zawierający następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="127f7-196">Add this by creating a new file at *ClientApp/src/app/app.server.module.ts* (alongside the existing *app.module.ts*) containing the following:</span></span>

[!code-typescript[](sample/AngularServerSideRendering/ClientApp/src/app/app.server.module.ts)]

<span data-ttu-id="127f7-197">Ten moduł dziedziczy po stronie klienta `app.module` i definiuje, które bardzo Angular moduły są dostępne podczas SSR.</span><span class="sxs-lookup"><span data-stu-id="127f7-197">This module inherits from your client-side `app.module` and defines which extra Angular modules are available during SSR.</span></span>

<span data-ttu-id="127f7-198">Pamiętaj, że nowy `ssr` wpis *.angular cli.json* odwołanie do pliku punktu wejścia o nazwie *main.server.ts*.</span><span class="sxs-lookup"><span data-stu-id="127f7-198">Recall that the new `ssr` entry in *.angular-cli.json* referenced an entry point file called *main.server.ts*.</span></span> <span data-ttu-id="127f7-199">Nie dodano jeszcze tego pliku, a następnie nadszedł czas, aby to zrobić.</span><span class="sxs-lookup"><span data-stu-id="127f7-199">You haven't yet added that file, and now is time to do so.</span></span> <span data-ttu-id="127f7-200">Utwórz nowy plik w *ClientApp/src/main.server.ts* (wraz z istniejącą *main.ts*), które zawierają:</span><span class="sxs-lookup"><span data-stu-id="127f7-200">Create a new file at *ClientApp/src/main.server.ts* (alongside the existing *main.ts*), containing the following:</span></span>

[!code-typescript[](sample/AngularServerSideRendering/ClientApp/src/main.server.ts)]

<span data-ttu-id="127f7-201">Kod ten plik znajduje się co platformy ASP.NET Core jest wykonywany dla każdego żądania po jego uruchomieniu `UseSpaPrerendering` oprogramowania pośredniczącego, który został dodany do *uruchamiania* klasy.</span><span class="sxs-lookup"><span data-stu-id="127f7-201">This file's code is what ASP.NET Core executes for each request when it runs the `UseSpaPrerendering` middleware that you added to the *Startup* class.</span></span> <span data-ttu-id="127f7-202">Dotyczy on odbierające `params` kodu platformy .NET (na przykład adres URL żądanego) i wykonywania wywołań do usługi Angular SSR interfejsów API można pobrać wynikowy kod HTML.</span><span class="sxs-lookup"><span data-stu-id="127f7-202">It deals with receiving `params` from the .NET code (such as the URL being requested), and making calls to Angular SSR APIs to get the resulting HTML.</span></span>

<span data-ttu-id="127f7-203">Ściśle mówiąc, jest wystarczające, aby umożliwić SSR w trybie projektowania.</span><span class="sxs-lookup"><span data-stu-id="127f7-203">Strictly-speaking, this is sufficient to enable SSR in development mode.</span></span> <span data-ttu-id="127f7-204">Istotne jest zapewnienie jednego ostateczna zmiana tak, aby aplikacja działa prawidłowo po opublikowaniu.</span><span class="sxs-lookup"><span data-stu-id="127f7-204">It's essential to make one final change so that your app works correctly when published.</span></span> <span data-ttu-id="127f7-205">W głównym Twojej aplikacji *.csproj* plików, należy ustawić `BuildServerSideRenderer` wartość właściwości `true`:</span><span class="sxs-lookup"><span data-stu-id="127f7-205">In your app's main *.csproj* file, set the `BuildServerSideRenderer` property value to `true`:</span></span>

[!code-xml[](sample/AngularServerSideRendering/AngularServerSideRendering.csproj?name=snippet_EnableBuildServerSideRenderer)]

<span data-ttu-id="127f7-206">Pozwoli to na skonfigurowanie procesu kompilacji, aby uruchomić `build:ssr` podczas publikowania i wdrażanie plików SSR do serwera.</span><span class="sxs-lookup"><span data-stu-id="127f7-206">This configures the build process to run `build:ssr` during publishing and deploy the SSR files to the server.</span></span> <span data-ttu-id="127f7-207">Jeśli nie zostanie włączone, SSR kończy się niepowodzeniem w środowisku produkcyjnym.</span><span class="sxs-lookup"><span data-stu-id="127f7-207">If you don't enable this, SSR fails in production.</span></span>

<span data-ttu-id="127f7-208">Gdy aplikacja działa w trybie rozwoju i produkcji, usługi Angular kodu wstępnie renderowany jako HTML na serwerze.</span><span class="sxs-lookup"><span data-stu-id="127f7-208">When your app runs in either development or production mode, the Angular code pre-renders as HTML on the server.</span></span> <span data-ttu-id="127f7-209">Kod po stronie klienta jest wykonywany w zwykły sposób.</span><span class="sxs-lookup"><span data-stu-id="127f7-209">The client-side code executes as normal.</span></span>

### <a name="pass-data-from-net-code-into-typescript-code"></a><span data-ttu-id="127f7-210">Przekazywanie danych od kodu platformy .NET do kodu TypeScript</span><span class="sxs-lookup"><span data-stu-id="127f7-210">Pass data from .NET code into TypeScript code</span></span>

<span data-ttu-id="127f7-211">Podczas SSR można przekazać dane na żądanie z aplikacji platformy ASP.NET Core w swojej aplikacji Angular.</span><span class="sxs-lookup"><span data-stu-id="127f7-211">During SSR, you might want to pass per-request data from your ASP.NET Core app into your Angular app.</span></span> <span data-ttu-id="127f7-212">Na przykład można przekazywać informacje dotyczące pliku cookie lub coś odczytu z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="127f7-212">For example, you could pass cookie information or something read from a database.</span></span> <span data-ttu-id="127f7-213">Aby to zrobić, Edytuj swoje *uruchamiania* klasy.</span><span class="sxs-lookup"><span data-stu-id="127f7-213">To do this, edit your *Startup* class.</span></span> <span data-ttu-id="127f7-214">Podczas wywołania zwrotnego dla `UseSpaPrerendering`, ustaw wartość `options.SupplyData` podobny do następującego:</span><span class="sxs-lookup"><span data-stu-id="127f7-214">In the callback for `UseSpaPrerendering`, set a value for `options.SupplyData` such as the following:</span></span>

```csharp
options.SupplyData = (context, data) =>
{
    // Creates a new value called isHttpsRequest that's passed to TypeScript code
    data["isHttpsRequest"] = context.Request.IsHttps;
};
```

<span data-ttu-id="127f7-215">`SupplyData` Umożliwia wywołanie zwrotne przekazania dowolnych, żądań, możliwy do serializacji JSON danych (na przykład ciągi, wartości logicznych lub liczb).</span><span class="sxs-lookup"><span data-stu-id="127f7-215">The `SupplyData` callback lets you pass arbitrary, per-request, JSON-serializable data (for example, strings, booleans, or numbers).</span></span> <span data-ttu-id="127f7-216">Twoje *main.server.ts* kodu odbiera jako `params.data`.</span><span class="sxs-lookup"><span data-stu-id="127f7-216">Your *main.server.ts* code receives this as `params.data`.</span></span> <span data-ttu-id="127f7-217">Na przykład w poprzednim przykładzie kodu przekazuje wartość typu boolean jako `params.data.isHttpsRequest` do `createServerRenderer` wywołania zwrotnego.</span><span class="sxs-lookup"><span data-stu-id="127f7-217">For example, the preceding code sample passes a boolean value as `params.data.isHttpsRequest` into the `createServerRenderer` callback.</span></span> <span data-ttu-id="127f7-218">Możesz przekazać ten do innych części aplikacji w jakikolwiek sposób obsługiwane przez usługi Angular.</span><span class="sxs-lookup"><span data-stu-id="127f7-218">You can pass this to other parts of your app in any way supported by Angular.</span></span> <span data-ttu-id="127f7-219">Na przykład, zobacz temat jak *main.server.ts* przekazuje `BASE_URL` wartość każdego składnika, której Konstruktor jest zadeklarowany do jej otrzymania.</span><span class="sxs-lookup"><span data-stu-id="127f7-219">For example, see how *main.server.ts* passes the `BASE_URL` value to any component whose constructor is declared to receive it.</span></span>

### <a name="drawbacks-of-ssr"></a><span data-ttu-id="127f7-220">Wady SSR</span><span class="sxs-lookup"><span data-stu-id="127f7-220">Drawbacks of SSR</span></span>

<span data-ttu-id="127f7-221">Nie wszystkie aplikacje korzystają z SSR.</span><span class="sxs-lookup"><span data-stu-id="127f7-221">Not all apps benefit from SSR.</span></span> <span data-ttu-id="127f7-222">Główną Korzyścią płynącą jest traktowany wydajności.</span><span class="sxs-lookup"><span data-stu-id="127f7-222">The primary benefit is perceived performance.</span></span> <span data-ttu-id="127f7-223">Osoby odwiedzające osiągnięcia swojej aplikacji za pośrednictwem z wolnym połączeniem sieciowym lub na urządzeniach przenośnych powolne początkowej interfejsu użytkownika szybkie wyświetlenie, nawet jeśli zajmuje trochę czasu, aby pobrać lub przeanalizować pakiety języka JavaScript.</span><span class="sxs-lookup"><span data-stu-id="127f7-223">Visitors reaching your app over a slow network connection or on slow mobile devices see the initial UI quickly, even if it takes a while to fetch or parse the JavaScript bundles.</span></span> <span data-ttu-id="127f7-224">Jednak wiele aplikacji jednostronicowych są używane głównie za pośrednictwem sieci firmy szybkie, wewnętrzne na komputerach z szybkich, gdzie aplikacja będzie wyświetlana niemal natychmiast.</span><span class="sxs-lookup"><span data-stu-id="127f7-224">However, many SPAs are mainly used over fast, internal company networks on fast computers where the app appears almost instantly.</span></span>

<span data-ttu-id="127f7-225">W tym samym czasie istnieją istotne wady włączania SSR.</span><span class="sxs-lookup"><span data-stu-id="127f7-225">At the same time, there are significant drawbacks to enabling SSR.</span></span> <span data-ttu-id="127f7-226">Dodaje złożoności do procesu opracowywania.</span><span class="sxs-lookup"><span data-stu-id="127f7-226">It adds complexity to your development process.</span></span> <span data-ttu-id="127f7-227">Kod, należy uruchomić w dwóch różnych środowiskach: po stronie klienta i po stronie serwera (w środowisku Node.js, wywoływana z platformą ASP.NET Core).</span><span class="sxs-lookup"><span data-stu-id="127f7-227">Your code must run in two different environments: client-side and server-side (in a Node.js environment invoked from ASP.NET Core).</span></span> <span data-ttu-id="127f7-228">Oto kilka rzeczy, aby mieć na uwadze:</span><span class="sxs-lookup"><span data-stu-id="127f7-228">Here are some things to bear in mind:</span></span>

* <span data-ttu-id="127f7-229">SSR wymaga instalacji środowiska Node.js na serwerach produkcyjnych.</span><span class="sxs-lookup"><span data-stu-id="127f7-229">SSR requires a Node.js installation on your production servers.</span></span> <span data-ttu-id="127f7-230">Automatycznie jest przypadek, w przypadku niektórych scenariuszy wdrażania, takie jak usługi Azure App Services, ale nie dla inne, takie jak usługi Azure Service Fabric.</span><span class="sxs-lookup"><span data-stu-id="127f7-230">This is automatically the case for some deployment scenarios, such as Azure App Services, but not for others, such as Azure Service Fabric.</span></span>
* <span data-ttu-id="127f7-231">Włączanie `BuildServerSideRenderer` kompilacji flaga powoduje, że Twoje *node_modules* katalogu publikowania.</span><span class="sxs-lookup"><span data-stu-id="127f7-231">Enabling the `BuildServerSideRenderer` build flag causes your *node_modules* directory to publish.</span></span> <span data-ttu-id="127f7-232">Ten folder zawiera 20 000 + plików, co zwiększa czas wdrażania.</span><span class="sxs-lookup"><span data-stu-id="127f7-232">This folder contains 20,000+ files, which increases deployment time.</span></span>
* <span data-ttu-id="127f7-233">Aby uruchomić kod w środowisku Node.js, go nie można polegać na istnienie interfejsów API języka JavaScript w przeglądarce specyficzne takich jak `window` lub `localStorage`.</span><span class="sxs-lookup"><span data-stu-id="127f7-233">To run your code in a Node.js environment, it can't rely on the existence of browser-specific JavaScript APIs such as `window` or `localStorage`.</span></span> <span data-ttu-id="127f7-234">Jeśli kod (lub niektórych biblioteki innych firm, którą możesz odwoływać się) próbuje użyć tych interfejsów API, otrzymasz błąd podczas SSR.</span><span class="sxs-lookup"><span data-stu-id="127f7-234">If your code (or some third-party library you reference) tries to use these APIs, you'll get an error during SSR.</span></span> <span data-ttu-id="127f7-235">Na przykład nie używaj jQuery, ponieważ widok odwołuje się interfejsami API właściwymi dla przeglądarki w wielu miejscach.</span><span class="sxs-lookup"><span data-stu-id="127f7-235">For example, don't use jQuery because it references browser-specific APIs in many places.</span></span> <span data-ttu-id="127f7-236">Aby zapobiec wystąpieniu błędów, możesz uniknąć SSR lub uniknąć interfejsów API specyficznych dla przeglądarki lub biblioteki.</span><span class="sxs-lookup"><span data-stu-id="127f7-236">To prevent errors, you must either avoid SSR or avoid browser-specific APIs or libraries.</span></span> <span data-ttu-id="127f7-237">Wszelkie wywołania do tych interfejsów API można opakować w kontrole, aby upewnić się, że nie są one wywoływane podczas SSR.</span><span class="sxs-lookup"><span data-stu-id="127f7-237">You can wrap any calls to such APIs in checks to ensure they aren't invoked during SSR.</span></span> <span data-ttu-id="127f7-238">W kodzie JavaScript i TypeScript, na przykład użyć wyboru podobny do następującego:</span><span class="sxs-lookup"><span data-stu-id="127f7-238">For example, use a check such as the following in JavaScript or TypeScript code:</span></span>

    ```javascript
    if (typeof window !== 'undefined') {
        // Call browser-specific APIs here
    }
    ```
