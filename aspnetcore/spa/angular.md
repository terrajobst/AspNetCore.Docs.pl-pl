---
title: Należy użyć szablonu projektu kątowego z platformy ASP.NET Core
author: SteveSandersonMS
description: Dowiedz się, jak rozpocząć pracę z szablonem projektu platformy ASP.NET Core jednej strony aplikacji JEDNOSTRONICOWEJ kątową i kątowego interfejsu wiersza polecenia.
manager: wpickett
monikerRange: '>= aspnetcore-2.0'
ms.author: scaddie
ms.custom: mvc
ms.date: 02/21/2018
ms.devlang: csharp
ms.prod: aspnet-core
ms.technology: aspnet
ms.topic: article
uid: spa/angular
ms.openlocfilehash: 244fece83279ae4d9ead9b345fcdd66ad6ed4225
ms.sourcegitcommit: 466300d32f8c33e64ee1b419a2cbffe702863cdf
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 05/27/2018
---
# <a name="use-the-angular-project-template-with-aspnet-core"></a><span data-ttu-id="d13ab-103">Należy użyć szablonu projektu kątowego z platformy ASP.NET Core</span><span class="sxs-lookup"><span data-stu-id="d13ab-103">Use the Angular project template with ASP.NET Core</span></span>

::: moniker range="= aspnetcore-2.0"

> [!NOTE]
> <span data-ttu-id="d13ab-104">O szablonie projektu kątowego tej dokumentacji nie jest zawarty w programie ASP.NET 2.0 Core.</span><span class="sxs-lookup"><span data-stu-id="d13ab-104">This documentation isn't about the Angular project template included in ASP.NET Core 2.0.</span></span> <span data-ttu-id="d13ab-105">Chodzi o nowszej kątowego szablonu, do której można zaktualizować ręcznie.</span><span class="sxs-lookup"><span data-stu-id="d13ab-105">It's about the newer Angular template to which you can update manually.</span></span> <span data-ttu-id="d13ab-106">Domyślnie znajduje szablonu platformy ASP.NET Core 2.1.</span><span class="sxs-lookup"><span data-stu-id="d13ab-106">The template is included in ASP.NET Core 2.1 by default.</span></span>

::: moniker-end

<span data-ttu-id="d13ab-107">Szablon zaktualizowany projekt kątowego udostępnia dogodny punkt początkowy dla platformy ASP.NET Core aplikacji przy użyciu kątową i kątowego interfejsu wiersza polecenia, aby zaimplementować rozbudowanego klienta interfejsu użytkownika (UI).</span><span class="sxs-lookup"><span data-stu-id="d13ab-107">The updated Angular project template provides a convenient starting point for ASP.NET Core apps using Angular and the Angular CLI to implement a rich, client-side user interface (UI).</span></span>

<span data-ttu-id="d13ab-108">Szablon jest odpowiednikiem Tworzenie projektu platformy ASP.NET Core do działania jako zaplecza interfejsu API i projektu kątowego interfejsu wiersza polecenia do działania jako interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="d13ab-108">The template is equivalent to creating an ASP.NET Core project to act as an API backend and an Angular CLI project to act as a UI.</span></span> <span data-ttu-id="d13ab-109">Szablon oferuje wygodne hosting obu typów projektów w projekcie jednej aplikacji.</span><span class="sxs-lookup"><span data-stu-id="d13ab-109">The template offers the convenience of hosting both project types in a single app project.</span></span> <span data-ttu-id="d13ab-110">W rezultacie projektu aplikacji można wbudowane i opublikowane jako pojedyncza jednostka.</span><span class="sxs-lookup"><span data-stu-id="d13ab-110">Consequently, the app project can be built and published as a single unit.</span></span>

## <a name="create-a-new-app"></a><span data-ttu-id="d13ab-111">Tworzenie nowej aplikacji</span><span class="sxs-lookup"><span data-stu-id="d13ab-111">Create a new app</span></span>

<span data-ttu-id="d13ab-112">Jeśli za pomocą platformy ASP.NET Core 2.0, upewnij się, które zostały [zainstalowane szablonu zaktualizowany projekt kątowego](xref:spa/index#installation).</span><span class="sxs-lookup"><span data-stu-id="d13ab-112">If using ASP.NET Core 2.0, ensure you've [installed the updated Angular project template](xref:spa/index#installation).</span></span> <span data-ttu-id="d13ab-113">Jeśli masz program ASP.NET Core 2.1 jest niepotrzebna go zainstalować.</span><span class="sxs-lookup"><span data-stu-id="d13ab-113">If you have ASP.NET Core 2.1, there's no need to install it.</span></span>

<span data-ttu-id="d13ab-114">Utwórz nowy projekt z wiersza polecenia przy użyciu polecenia `dotnet new angular` w pustych katalogów.</span><span class="sxs-lookup"><span data-stu-id="d13ab-114">Create a new project from a command prompt using the command `dotnet new angular` in an empty directory.</span></span> <span data-ttu-id="d13ab-115">Na przykład poniższe polecenia Utwórz aplikację w *— nowy — aplikacji my* katalogu i przełącznika do tego katalogu:</span><span class="sxs-lookup"><span data-stu-id="d13ab-115">For example, the following commands create the app in a *my-new-app* directory and switch to that directory:</span></span>

```console
dotnet new angular -o my-new-app
cd my-new-app
```

<span data-ttu-id="d13ab-116">Uruchamianie aplikacji z programu Visual Studio lub platformy .NET Core interfejsu wiersza polecenia:</span><span class="sxs-lookup"><span data-stu-id="d13ab-116">Run the app from either Visual Studio or the .NET Core CLI:</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="d13ab-117">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="d13ab-117">Visual Studio</span></span>](#tab/visual-studio/)

<span data-ttu-id="d13ab-118">Otwórz wygenerowany *.csproj* pliku i uruchom aplikację w zwykły stamtąd.</span><span class="sxs-lookup"><span data-stu-id="d13ab-118">Open the generated *.csproj* file, and run the app as normal from there.</span></span>

<span data-ttu-id="d13ab-119">Proces kompilacji przywraca npm zależności przy pierwszym uruchomieniu, co może zająć kilka minut.</span><span class="sxs-lookup"><span data-stu-id="d13ab-119">The build process restores npm dependencies on the first run, which can take several minutes.</span></span> <span data-ttu-id="d13ab-120">Kolejne kompilacje są znacznie szybsze.</span><span class="sxs-lookup"><span data-stu-id="d13ab-120">Subsequent builds are much faster.</span></span>

# <a name="net-core-clitabnetcore-cli"></a>[<span data-ttu-id="d13ab-121">.NET Core CLI</span><span class="sxs-lookup"><span data-stu-id="d13ab-121">.NET Core CLI</span></span>](#tab/netcore-cli/)

<span data-ttu-id="d13ab-122">Upewnij się, masz zmienną środowiskową o nazwie `ASPNETCORE_Environment` o wartości `Development`.</span><span class="sxs-lookup"><span data-stu-id="d13ab-122">Ensure you have an environment variable called `ASPNETCORE_Environment` with a value of `Development`.</span></span> <span data-ttu-id="d13ab-123">W systemie Windows (w monity-PowerShell), uruchom `SET ASPNETCORE_Environment=Development`.</span><span class="sxs-lookup"><span data-stu-id="d13ab-123">On Windows (in non-PowerShell prompts), run `SET ASPNETCORE_Environment=Development`.</span></span> <span data-ttu-id="d13ab-124">W systemie Linux lub macOS Uruchom `export ASPNETCORE_Environment=Development`.</span><span class="sxs-lookup"><span data-stu-id="d13ab-124">On Linux or macOS, run `export ASPNETCORE_Environment=Development`.</span></span>

<span data-ttu-id="d13ab-125">Uruchom [kompilacji dotnet](/dotnet/core/tools/dotnet-build) można zweryfikować aplikacji kompilacje poprawnie.</span><span class="sxs-lookup"><span data-stu-id="d13ab-125">Run [dotnet build](/dotnet/core/tools/dotnet-build) to verify the app builds correctly.</span></span> <span data-ttu-id="d13ab-126">Przy pierwszym uruchomieniu procesu kompilacji przywraca npm zależności, co może potrwać kilka minut.</span><span class="sxs-lookup"><span data-stu-id="d13ab-126">On the first run, the build process restores npm dependencies, which can take several minutes.</span></span> <span data-ttu-id="d13ab-127">Kolejne kompilacje są znacznie szybsze.</span><span class="sxs-lookup"><span data-stu-id="d13ab-127">Subsequent builds are much faster.</span></span>

<span data-ttu-id="d13ab-128">Uruchom [dotnet Uruchom](/dotnet/core/tools/dotnet-run) do uruchomienia aplikacji.</span><span class="sxs-lookup"><span data-stu-id="d13ab-128">Run [dotnet run](/dotnet/core/tools/dotnet-run) to start the app.</span></span> <span data-ttu-id="d13ab-129">Zostanie zarejestrowany komunikat podobny do następującego:</span><span class="sxs-lookup"><span data-stu-id="d13ab-129">A message similar to the following is logged:</span></span>

```console
Now listening on: http://localhost:<port>
```

<span data-ttu-id="d13ab-130">Przejdź do tego adresu URL w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="d13ab-130">Navigate to this URL in a browser.</span></span>

<span data-ttu-id="d13ab-131">Wystąpienie serwera kątowego interfejsu wiersza polecenia w tle uruchamiania aplikacji.</span><span class="sxs-lookup"><span data-stu-id="d13ab-131">The app starts up an instance of the Angular CLI server in the background.</span></span> <span data-ttu-id="d13ab-132">Zostanie zarejestrowany komunikat podobny do następującego: *NG Live Development Server nasłuchuje na localhost:&lt;otherport&gt;, otwórz przeglądarkę na http://localhost:&lt; otherport&gt; /*  .</span><span class="sxs-lookup"><span data-stu-id="d13ab-132">A message similar to the following is logged: *NG Live Development Server is listening on localhost:&lt;otherport&gt;, open your browser on http://localhost:&lt;otherport&gt;/*.</span></span> <span data-ttu-id="d13ab-133">Zignoruj ten komunikat&mdash;ma **nie** adres URL dla połączonych aplikacji platformy ASP.NET Core i kątowego interfejsu wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="d13ab-133">Ignore this message&mdash;it's **not** the URL for the combined ASP.NET Core and Angular CLI app.</span></span>

---

<span data-ttu-id="d13ab-134">Szablon projektu tworzy aplikację platformy ASP.NET Core i kątowego aplikacji.</span><span class="sxs-lookup"><span data-stu-id="d13ab-134">The project template creates an ASP.NET Core app and an Angular app.</span></span> <span data-ttu-id="d13ab-135">Aplikacja platformy ASP.NET Core jest przeznaczona do użycia dla dostępu do danych, autoryzację i inne problemy po stronie serwera.</span><span class="sxs-lookup"><span data-stu-id="d13ab-135">The ASP.NET Core app is intended to be used for data access, authorization, and other server-side concerns.</span></span> <span data-ttu-id="d13ab-136">Dyrektywy Angular aplikacji, znajdującej się w *ClientApp* podkatalogu, jest przeznaczona do użycia dla wszystkich problemów w zakresie interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="d13ab-136">The Angular app, residing in the *ClientApp* subdirectory, is intended to be used for all UI concerns.</span></span>

## <a name="add-pages-images-styles-modules-etc"></a><span data-ttu-id="d13ab-137">Dodawanie stron, obrazy, style, modułów, itp.</span><span class="sxs-lookup"><span data-stu-id="d13ab-137">Add pages, images, styles, modules, etc.</span></span>

<span data-ttu-id="d13ab-138">*ClientApp* katalog zawiera standardowe aplikacji kątowego interfejsu wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="d13ab-138">The *ClientApp* directory contains a standard Angular CLI app.</span></span> <span data-ttu-id="d13ab-139">Można znaleźć w oficjalnej [kątowego dokumentacji](https://github.com/angular/angular-cli/wiki) Aby uzyskać więcej informacji.</span><span class="sxs-lookup"><span data-stu-id="d13ab-139">See the official [Angular documentation](https://github.com/angular/angular-cli/wiki) for more information.</span></span>

<span data-ttu-id="d13ab-140">Istnieją niewielkich różnic między kątowego aplikacji utworzonych przez ten szablon i utworzonym przez kątowego interfejsu wiersza polecenia, sam (za pośrednictwem `ng new`), jednak nie uległy zmianie możliwości aplikacji.</span><span class="sxs-lookup"><span data-stu-id="d13ab-140">There are slight differences between the Angular app created by this template and the one created by Angular CLI itself (via `ng new`); however, the app's capabilities are unchanged.</span></span> <span data-ttu-id="d13ab-141">Utworzona przez szablon aplikacja zawiera [Bootstrap](https://getbootstrap.com/)— na podstawie układu i prosty przykład routingu.</span><span class="sxs-lookup"><span data-stu-id="d13ab-141">The app created by the template contains a [Bootstrap](https://getbootstrap.com/)-based layout and a basic routing example.</span></span>

## <a name="run-ng-commands"></a><span data-ttu-id="d13ab-142">Uruchom polecenia ng</span><span class="sxs-lookup"><span data-stu-id="d13ab-142">Run ng commands</span></span>

<span data-ttu-id="d13ab-143">W wierszu polecenia, przełącz się do *ClientApp* podkatalogu:</span><span class="sxs-lookup"><span data-stu-id="d13ab-143">In a command prompt, switch to the *ClientApp* subdirectory:</span></span>

```console
cd ClientApp
```

<span data-ttu-id="d13ab-144">Jeśli masz `ng` narzędzie zainstalowane globalnie, możesz uruchomić dowolne z jego poleceń.</span><span class="sxs-lookup"><span data-stu-id="d13ab-144">If you have the `ng` tool installed globally, you can run any of its commands.</span></span> <span data-ttu-id="d13ab-145">Na przykład można uruchomić `ng lint`, `ng test`, ani żadnych innych [kątowego polecenia interfejsu wiersza polecenia](https://github.com/angular/angular-cli/wiki#additional-commands).</span><span class="sxs-lookup"><span data-stu-id="d13ab-145">For example, you can run `ng lint`, `ng test`, or any of the other [Angular CLI commands](https://github.com/angular/angular-cli/wiki#additional-commands).</span></span> <span data-ttu-id="d13ab-146">Nie istnieje potrzeba do uruchamiania `ng serve` , ponieważ aplikacja platformy ASP.NET Core dotyczy obsługująca zarówno po stronie serwera i klienta części aplikacji.</span><span class="sxs-lookup"><span data-stu-id="d13ab-146">There's no need to run `ng serve` though, because your ASP.NET Core app deals with serving both server-side and client-side parts of your app.</span></span> <span data-ttu-id="d13ab-147">Wewnętrznie, używa `ng serve` do rozwoju.</span><span class="sxs-lookup"><span data-stu-id="d13ab-147">Internally, it uses `ng serve` in development.</span></span>

<span data-ttu-id="d13ab-148">Jeśli nie masz `ng` narzędzie zainstalowane, uruchom `npm run ng` zamiast tego.</span><span class="sxs-lookup"><span data-stu-id="d13ab-148">If you don't have the `ng` tool installed, run `npm run ng` instead.</span></span> <span data-ttu-id="d13ab-149">Na przykład można uruchomić `npm run ng lint` lub `npm run ng test`.</span><span class="sxs-lookup"><span data-stu-id="d13ab-149">For example, you can run `npm run ng lint` or `npm run ng test`.</span></span>

## <a name="install-npm-packages"></a><span data-ttu-id="d13ab-150">Instalowanie pakietów npm</span><span class="sxs-lookup"><span data-stu-id="d13ab-150">Install npm packages</span></span>

<span data-ttu-id="d13ab-151">Aby zainstalować pakiety innych firm npm, przy użyciu wiersza polecenia w *ClientApp* podkatalogu.</span><span class="sxs-lookup"><span data-stu-id="d13ab-151">To install third-party npm packages, use a command prompt in the *ClientApp* subdirectory.</span></span> <span data-ttu-id="d13ab-152">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="d13ab-152">For example:</span></span>

```console
cd ClientApp
npm install --save <package_name>
```

## <a name="publish-and-deploy"></a><span data-ttu-id="d13ab-153">Publikowanie i wdrażanie</span><span class="sxs-lookup"><span data-stu-id="d13ab-153">Publish and deploy</span></span>

<span data-ttu-id="d13ab-154">W rozwoju aplikacji jest uruchamiany w trybie zoptymalizowane pod kątem wygody deweloperów.</span><span class="sxs-lookup"><span data-stu-id="d13ab-154">In development, the app runs in a mode optimized for developer convenience.</span></span> <span data-ttu-id="d13ab-155">Na przykład pakiety JavaScript obejmują map źródeł (tak, aby podczas debugowania, możesz zobaczyć oryginalnego kodu TypeScript).</span><span class="sxs-lookup"><span data-stu-id="d13ab-155">For example, JavaScript bundles include source maps (so that when debugging, you can see your original TypeScript code).</span></span> <span data-ttu-id="d13ab-156">Aplikacja oczekuje na maszynie, HTML i CSS zmian plików na dysku i automatycznie rekompiluje i ponowne załadowanie po wykryciu tych plików, zmiany.</span><span class="sxs-lookup"><span data-stu-id="d13ab-156">The app watches for TypeScript, HTML, and CSS file changes on disk and automatically recompiles and reloads when it sees those files change.</span></span>

<span data-ttu-id="d13ab-157">W środowisku produkcyjnym obsługiwać wersji aplikacji jest zoptymalizowany pod kątem wydajności.</span><span class="sxs-lookup"><span data-stu-id="d13ab-157">In production, serve a version of your app that's optimized for performance.</span></span> <span data-ttu-id="d13ab-158">To jest skonfigurowany automatycznie.</span><span class="sxs-lookup"><span data-stu-id="d13ab-158">This is configured to happen automatically.</span></span> <span data-ttu-id="d13ab-159">Podczas publikowania, zminimalizowany emituje konfigurację kompilacji, kompilacja kodu po stronie klienta skompilowane z wyprzedzeniem elementu time (drzewa obiektów aplikacji).</span><span class="sxs-lookup"><span data-stu-id="d13ab-159">When you publish, the build configuration emits a minified, ahead-of-time (AoT) compiled build of your client-side code.</span></span> <span data-ttu-id="d13ab-160">W przeciwieństwie do kompilacji programowanie kompilacji produkcyjnym nie wymaga Node.js do zainstalowania na serwerze (Jeśli nie włączono [prerendering po stronie serwera](#server-side-rendering)).</span><span class="sxs-lookup"><span data-stu-id="d13ab-160">Unlike the development build, the production build doesn't require Node.js to be installed on the server (unless you have enabled [server-side prerendering](#server-side-rendering)).</span></span>

<span data-ttu-id="d13ab-161">Można użyć standardowego [metody wdrażania i hostingu ASP.NET Core](xref:host-and-deploy/index).</span><span class="sxs-lookup"><span data-stu-id="d13ab-161">You can use standard [ASP.NET Core hosting and deployment methods](xref:host-and-deploy/index).</span></span>

## <a name="run-ng-serve-independently"></a><span data-ttu-id="d13ab-162">Uruchom niezależnie "ng służą"</span><span class="sxs-lookup"><span data-stu-id="d13ab-162">Run "ng serve" independently</span></span>

<span data-ttu-id="d13ab-163">Projekt jest skonfigurowany do uruchamiania własnego wystąpienia serwera kątowego interfejsu wiersza polecenia w tle po uruchomieniu aplikacji platformy ASP.NET Core w trybie projektowania.</span><span class="sxs-lookup"><span data-stu-id="d13ab-163">The project is configured to start its own instance of the Angular CLI server in the background when the ASP.NET Core app starts in development mode.</span></span> <span data-ttu-id="d13ab-164">Ta opcja jest przydatna, ponieważ nie trzeba ręcznie uruchomić oddzielny serwer.</span><span class="sxs-lookup"><span data-stu-id="d13ab-164">This is convenient because you don't have to run a separate server manually.</span></span>

<span data-ttu-id="d13ab-165">Brak wadą tego ustawienia domyślne.</span><span class="sxs-lookup"><span data-stu-id="d13ab-165">There's a drawback to this default setup.</span></span> <span data-ttu-id="d13ab-166">Po każdej zmianie kodu C# i ASP.NET podstawowych aplikacja musi ponownie uruchomić, ponowne uruchomienie serwera kątowego interfejsu wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="d13ab-166">Each time you modify your C# code and your ASP.NET Core app needs to restart, the Angular CLI server restarts.</span></span> <span data-ttu-id="d13ab-167">Około 10 sekund jest wymagany, aby rozpocząć tworzenie kopii zapasowej.</span><span class="sxs-lookup"><span data-stu-id="d13ab-167">Around 10 seconds is required to start back up.</span></span> <span data-ttu-id="d13ab-168">Jeśli wprowadzania częste zmiany kodu C# i nie chcesz czekać na kątowego interfejsu wiersza polecenia uruchomić ponownie, uruchom serwer kątowego CLI zewnętrznie, niezależnie od procesu ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="d13ab-168">If you're making frequent C# code edits and don't want to wait for Angular CLI to restart, run the Angular CLI server externally, independently of the ASP.NET Core process.</span></span> <span data-ttu-id="d13ab-169">Aby to zrobić:</span><span class="sxs-lookup"><span data-stu-id="d13ab-169">To do so:</span></span>

1. <span data-ttu-id="d13ab-170">W wierszu polecenia, przełącz się do *ClientApp* podkatalogu, a następnie uruchom serwera wdrożeniowego kątowego interfejsu wiersza polecenia:</span><span class="sxs-lookup"><span data-stu-id="d13ab-170">In a command prompt, switch to the *ClientApp* subdirectory, and launch the Angular CLI development server:</span></span>

    ```console
    cd ClientApp
    npm start
    ```

    > [!IMPORTANT]
    > <span data-ttu-id="d13ab-171">Użyj `npm start` można uruchomić serwera wdrożeniowego programu kątowego interfejsu wiersza polecenia nie `ng serve`, dzięki czemu konfiguracji w *package.json* jest przestrzegana.</span><span class="sxs-lookup"><span data-stu-id="d13ab-171">Use `npm start` to launch the Angular CLI development server, not `ng serve`, so that the configuration in *package.json* is respected.</span></span> <span data-ttu-id="d13ab-172">Aby przekazywać dodatkowych parametrów do serwera kątowego interfejsu wiersza polecenia, dodaj go do odpowiedniego `scripts` wiersz w Twojej *package.json* pliku.</span><span class="sxs-lookup"><span data-stu-id="d13ab-172">To pass additional parameters to the Angular CLI server, add them to the relevant `scripts` line in your *package.json* file.</span></span>

2. <span data-ttu-id="d13ab-173">Zmodyfikuj do korzystania z zewnętrznego wystąpienia kątowego CLI zamiast uruchamiania jednego z jego własnej aplikacji platformy ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="d13ab-173">Modify your ASP.NET Core app to use the external Angular CLI instance instead of launching one of its own.</span></span> <span data-ttu-id="d13ab-174">W Twojej *uruchamiania* klasy, Zastąp `spa.UseAngularCliServer` wywołania z następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="d13ab-174">In your *Startup* class, replace the `spa.UseAngularCliServer` invocation with the following:</span></span>

    ```csharp
    spa.UseProxyToSpaDevelopmentServer("http://localhost:4200");
    ```

<span data-ttu-id="d13ab-175">Po uruchomieniu aplikacji platformy ASP.NET Core, nie będzie go uruchomić serwera kątowego interfejsu wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="d13ab-175">When you start your ASP.NET Core app, it won't launch an Angular CLI server.</span></span> <span data-ttu-id="d13ab-176">Zamiast niego jest używana wystąpienia, które zostanie uruchomione ręcznie.</span><span class="sxs-lookup"><span data-stu-id="d13ab-176">The instance you started manually is used instead.</span></span> <span data-ttu-id="d13ab-177">Dzięki temu może uruchomić i ponownie uruchom szybciej.</span><span class="sxs-lookup"><span data-stu-id="d13ab-177">This enables it to start and restart faster.</span></span> <span data-ttu-id="d13ab-178">Nie jest już oczekuje na kątowego CLI odbudować zawsze aplikację klienta.</span><span class="sxs-lookup"><span data-stu-id="d13ab-178">It's no longer waiting for Angular CLI to rebuild your client app each time.</span></span>

## <a name="server-side-rendering"></a><span data-ttu-id="d13ab-179">Renderowanie po stronie serwera</span><span class="sxs-lookup"><span data-stu-id="d13ab-179">Server-side rendering</span></span>

<span data-ttu-id="d13ab-180">Jako funkcja wydajności można przed renderowaniem kątowego aplikacji na serwerze, a także uruchomiony na kliencie.</span><span class="sxs-lookup"><span data-stu-id="d13ab-180">As a performance feature, you can choose to pre-render your Angular app on the server as well as running it on the client.</span></span> <span data-ttu-id="d13ab-181">Oznacza to, że przeglądarka otrzyma kod znaczników HTML reprezentujący początkowej interfejsu użytkownika aplikacji, tak aby wyświetlały nawet przed pobraniem i wykonywanie programu pakiety języka JavaScript.</span><span class="sxs-lookup"><span data-stu-id="d13ab-181">This means that browsers receive HTML markup representing your app's initial UI, so they display it even before downloading and executing your JavaScript bundles.</span></span> <span data-ttu-id="d13ab-182">Większość implementacji tego pochodzi z kątowego funkcję [kątowego uniwersalnych](https://universal.angular.io/).</span><span class="sxs-lookup"><span data-stu-id="d13ab-182">Most of the implementation of this comes from an Angular feature called [Angular Universal](https://universal.angular.io/).</span></span>

> [!TIP]
> <span data-ttu-id="d13ab-183">Włączanie renderowania po stronie serwera (SSR) przedstawiono kilka dodatkowych komplikacji zarówno podczas opracowywania i wdrażania.</span><span class="sxs-lookup"><span data-stu-id="d13ab-183">Enabling server-side rendering (SSR) introduces a number of extra complications both during development and deployment.</span></span> <span data-ttu-id="d13ab-184">Odczyt [wady SSR](#drawbacks-of-ssr) do ustalenia, czy SSR jest odpowiedni dla wymagań.</span><span class="sxs-lookup"><span data-stu-id="d13ab-184">Read [drawbacks of SSR](#drawbacks-of-ssr) to determine if SSR is a good fit for your requirements.</span></span>

<span data-ttu-id="d13ab-185">Aby włączyć SSR, należy wprowadzić numer ma zostać dodany do projektu.</span><span class="sxs-lookup"><span data-stu-id="d13ab-185">To enable SSR, you need to make a number of additions to your project.</span></span>

<span data-ttu-id="d13ab-186">W *uruchamiania* klasy *po* wiersza, który konfiguruje `spa.Options.SourcePath`, i *przed* wywołanie `UseAngularCliServer` lub `UseProxyToSpaDevelopmentServer`, Dodaj następujący kod:</span><span class="sxs-lookup"><span data-stu-id="d13ab-186">In the *Startup* class, *after* the line that configures `spa.Options.SourcePath`, and *before* the call to `UseAngularCliServer` or `UseProxyToSpaDevelopmentServer`, add the following:</span></span>

[!code-csharp[](sample/AngularServerSideRendering/Startup.cs?name=snippet_Call_UseSpa&highlight=5-12)]

<span data-ttu-id="d13ab-187">W trybie projektowania, ten kod próbuje kompilacji pakietu SSR przez uruchomienie skryptu `build:ssr`, która jest zdefiniowana w *ClientApp\package.json*.</span><span class="sxs-lookup"><span data-stu-id="d13ab-187">In development mode, this code attempts to build the SSR bundle by running the script `build:ssr`, which is defined in *ClientApp\package.json*.</span></span> <span data-ttu-id="d13ab-188">Powoduje to skompilowanie kątowego aplikacji o nazwie `ssr`, która nie jest jeszcze zdefiniowana.</span><span class="sxs-lookup"><span data-stu-id="d13ab-188">This builds an Angular app named `ssr`, which isn't yet defined.</span></span>

<span data-ttu-id="d13ab-189">Na koniec `apps` tablicy w *ClientApp/.angular-cli.json*, zdefiniuj dodatkowy aplikacji o nazwie `ssr`.</span><span class="sxs-lookup"><span data-stu-id="d13ab-189">At the end of the `apps` array in *ClientApp/.angular-cli.json*, define an extra app with name `ssr`.</span></span> <span data-ttu-id="d13ab-190">Użyj następujących opcji:</span><span class="sxs-lookup"><span data-stu-id="d13ab-190">Use the following options:</span></span>

[!code-json[](sample/AngularServerSideRendering/ClientApp/.angular-cli.json?range=24-41)]

<span data-ttu-id="d13ab-191">Tę nową konfigurację aplikacji z włączoną obsługą SSR wymaga dwóch plików dalsze: *tsconfig.server.json* i *main.server.ts*.</span><span class="sxs-lookup"><span data-stu-id="d13ab-191">This new SSR-enabled app configuration requires two further files: *tsconfig.server.json* and *main.server.ts*.</span></span> <span data-ttu-id="d13ab-192">*Tsconfig.server.json* plik Określa opcje kompilacji języka TypeScript.</span><span class="sxs-lookup"><span data-stu-id="d13ab-192">The *tsconfig.server.json* file specifies TypeScript compilation options.</span></span> <span data-ttu-id="d13ab-193">*Main.server.ts* plik służy jako punkt wejścia kodu podczas SSR.</span><span class="sxs-lookup"><span data-stu-id="d13ab-193">The *main.server.ts* file serves as the code entry point during SSR.</span></span>

<span data-ttu-id="d13ab-194">Dodaj nowy plik o nazwie *tsconfig.server.json* wewnątrz *ClientApp/src* (obok istniejącej *tsconfig.app.json*), zawierających następujące:</span><span class="sxs-lookup"><span data-stu-id="d13ab-194">Add a new file called *tsconfig.server.json* inside *ClientApp/src* (alongside the existing *tsconfig.app.json*), containing the following:</span></span>

[!code-json[](sample/AngularServerSideRendering/ClientApp/src/tsconfig.server.json)]

<span data-ttu-id="d13ab-195">Ten plik konfiguruje kątową przez kompilator drzewa obiektów aplikacji do wyszukania modułu o nazwie `app.server.module`.</span><span class="sxs-lookup"><span data-stu-id="d13ab-195">This file configures Angular's AoT compiler to look for a module called `app.server.module`.</span></span> <span data-ttu-id="d13ab-196">Dodaj tę przez utworzenie nowego pliku w *ClientApp/src/app/app.server.module.ts* (obok istniejącej *app.module.ts*), które zawierają:</span><span class="sxs-lookup"><span data-stu-id="d13ab-196">Add this by creating a new file at *ClientApp/src/app/app.server.module.ts* (alongside the existing *app.module.ts*) containing the following:</span></span>

[!code-typescript[](sample/AngularServerSideRendering/ClientApp/src/app/app.server.module.ts)]

<span data-ttu-id="d13ab-197">Ten moduł dziedziczy od strony klienta `app.module` i definiuje, które moduły kątową bardzo są dostępne podczas SSR.</span><span class="sxs-lookup"><span data-stu-id="d13ab-197">This module inherits from your client-side `app.module` and defines which extra Angular modules are available during SSR.</span></span>

<span data-ttu-id="d13ab-198">Odwołania, który nowy `ssr` wpis w *.angular cli.json* odwołuje się do pliku punktu wejścia o nazwie *main.server.ts*.</span><span class="sxs-lookup"><span data-stu-id="d13ab-198">Recall that the new `ssr` entry in *.angular-cli.json* referenced an entry point file called *main.server.ts*.</span></span> <span data-ttu-id="d13ab-199">Nie dodano jeszcze tego pliku, a nadszedł czas, aby to zrobić.</span><span class="sxs-lookup"><span data-stu-id="d13ab-199">You haven't yet added that file, and now is time to do so.</span></span> <span data-ttu-id="d13ab-200">Utwórz nowy plik w *ClientApp/src/main.server.ts* (obok istniejącej *main.ts*), zawierających następujące:</span><span class="sxs-lookup"><span data-stu-id="d13ab-200">Create a new file at *ClientApp/src/main.server.ts* (alongside the existing *main.ts*), containing the following:</span></span>

[!code-typescript[](sample/AngularServerSideRendering/ClientApp/src/main.server.ts)]

<span data-ttu-id="d13ab-201">Kod ten plik znajduje się co platformy ASP.NET Core wykonuje dla każdego żądania po uruchomieniu `UseSpaPrerendering` oprogramowanie pośredniczące, który został dodany do *uruchamiania* klasy.</span><span class="sxs-lookup"><span data-stu-id="d13ab-201">This file's code is what ASP.NET Core executes for each request when it runs the `UseSpaPrerendering` middleware that you added to the *Startup* class.</span></span> <span data-ttu-id="d13ab-202">Dotyczy on odbieranie `params` z kodu platformy .NET (na przykład adres URL żądanego) i wykonywania wywołań do dyrektywy Angular SSR interfejsów API można pobrać wynikowy kod HTML.</span><span class="sxs-lookup"><span data-stu-id="d13ab-202">It deals with receiving `params` from the .NET code (such as the URL being requested), and making calls to Angular SSR APIs to get the resulting HTML.</span></span>

<span data-ttu-id="d13ab-203">Ściśle mówiąc, jest wystarczające, aby umożliwić SSR w trybie projektowania.</span><span class="sxs-lookup"><span data-stu-id="d13ab-203">Strictly-speaking, this is sufficient to enable SSR in development mode.</span></span> <span data-ttu-id="d13ab-204">Należy zmienić jedną końcowego tak, aby aplikacja działa prawidłowo po opublikowaniu.</span><span class="sxs-lookup"><span data-stu-id="d13ab-204">It's essential to make one final change so that your app works correctly when published.</span></span> <span data-ttu-id="d13ab-205">W głównym aplikacji *.csproj* pliku, ustaw `BuildServerSideRenderer` wartości właściwości do `true`:</span><span class="sxs-lookup"><span data-stu-id="d13ab-205">In your app's main *.csproj* file, set the `BuildServerSideRenderer` property value to `true`:</span></span>

[!code-xml[](sample/AngularServerSideRendering/AngularServerSideRendering.csproj?name=snippet_EnableBuildServerSideRenderer)]

<span data-ttu-id="d13ab-206">Spowoduje to skonfigurowanie proces kompilacji, aby uruchomić `build:ssr` podczas publikowania i wdrożyć SSR plików na serwerze.</span><span class="sxs-lookup"><span data-stu-id="d13ab-206">This configures the build process to run `build:ssr` during publishing and deploy the SSR files to the server.</span></span> <span data-ttu-id="d13ab-207">Jeśli nie zostało włączone, SSR kończy się niepowodzeniem w środowisku produkcyjnym.</span><span class="sxs-lookup"><span data-stu-id="d13ab-207">If you don't enable this, SSR fails in production.</span></span>

<span data-ttu-id="d13ab-208">Gdy aplikacja będzie działać w trybie rozwoju lub produkcji, kod dyrektywy Angular wstępnie renderuje jako HTML na serwerze.</span><span class="sxs-lookup"><span data-stu-id="d13ab-208">When your app runs in either development or production mode, the Angular code pre-renders as HTML on the server.</span></span> <span data-ttu-id="d13ab-209">Kod po stronie klienta wykonywany jako standardowa.</span><span class="sxs-lookup"><span data-stu-id="d13ab-209">The client-side code executes as normal.</span></span>

### <a name="pass-data-from-net-code-into-typescript-code"></a><span data-ttu-id="d13ab-210">Przekazywanie danych z kodu platformy .NET do kodu TypeScript</span><span class="sxs-lookup"><span data-stu-id="d13ab-210">Pass data from .NET code into TypeScript code</span></span>

<span data-ttu-id="d13ab-211">Podczas SSR możesz przekazać dane na żądanie z aplikacji platformy ASP.NET Core w kątowego aplikacji.</span><span class="sxs-lookup"><span data-stu-id="d13ab-211">During SSR, you might want to pass per-request data from your ASP.NET Core app into your Angular app.</span></span> <span data-ttu-id="d13ab-212">Na przykład możesz przesłać informacje o pliku cookie lub coś odczytu z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="d13ab-212">For example, you could pass cookie information or something read from a database.</span></span> <span data-ttu-id="d13ab-213">Aby to zrobić, Edytuj Twojej *uruchamiania* klasy.</span><span class="sxs-lookup"><span data-stu-id="d13ab-213">To do this, edit your *Startup* class.</span></span> <span data-ttu-id="d13ab-214">Podczas wywołania zwrotnego dla `UseSpaPrerendering`, ustaw wartość `options.SupplyData` podobny do następującego:</span><span class="sxs-lookup"><span data-stu-id="d13ab-214">In the callback for `UseSpaPrerendering`, set a value for `options.SupplyData` such as the following:</span></span>

```csharp
options.SupplyData = (context, data) =>
{
    // Creates a new value called isHttpsRequest that's passed to TypeScript code
    data["isHttpsRequest"] = context.Request.IsHttps;
};
```

<span data-ttu-id="d13ab-215">`SupplyData` Przekazania dowolnych umożliwia wywołanie zwrotne, na żądanie, dane do serializacji JSON (na przykład ciągi, wartości logiczne lub cyfry).</span><span class="sxs-lookup"><span data-stu-id="d13ab-215">The `SupplyData` callback lets you pass arbitrary, per-request, JSON-serializable data (for example, strings, booleans, or numbers).</span></span> <span data-ttu-id="d13ab-216">Twoje *main.server.ts* kod odbiera jako `params.data`.</span><span class="sxs-lookup"><span data-stu-id="d13ab-216">Your *main.server.ts* code receives this as `params.data`.</span></span> <span data-ttu-id="d13ab-217">Na przykład w poprzednim przykładzie kodu przekazuje wartość logiczną jako `params.data.isHttpsRequest` do `createServerRenderer` wywołania zwrotnego.</span><span class="sxs-lookup"><span data-stu-id="d13ab-217">For example, the preceding code sample passes a boolean value as `params.data.isHttpsRequest` into the `createServerRenderer` callback.</span></span> <span data-ttu-id="d13ab-218">To można przekazać do innych części aplikacji w żaden sposób obsługiwane przez kątową.</span><span class="sxs-lookup"><span data-stu-id="d13ab-218">You can pass this to other parts of your app in any way supported by Angular.</span></span> <span data-ttu-id="d13ab-219">Na przykład, zobacz temat jak *main.server.ts* przekazuje `BASE_URL` wartość każdego składnika, którego konstruktor jest zadeklarowana, aby go otrzymają.</span><span class="sxs-lookup"><span data-stu-id="d13ab-219">For example, see how *main.server.ts* passes the `BASE_URL` value to any component whose constructor is declared to receive it.</span></span>

### <a name="drawbacks-of-ssr"></a><span data-ttu-id="d13ab-220">Wady SSR</span><span class="sxs-lookup"><span data-stu-id="d13ab-220">Drawbacks of SSR</span></span>

<span data-ttu-id="d13ab-221">Nie wszystkie aplikacje korzystają z SSR.</span><span class="sxs-lookup"><span data-stu-id="d13ab-221">Not all apps benefit from SSR.</span></span> <span data-ttu-id="d13ab-222">Podstawowy korzyści jest traktowany wydajności.</span><span class="sxs-lookup"><span data-stu-id="d13ab-222">The primary benefit is perceived performance.</span></span> <span data-ttu-id="d13ab-223">Osoby odwiedzające osiągnięcia aplikacji za pośrednictwem wolnym połączeniem sieciowym lub na urządzeniach przenośnych powolne początkowej interfejsu użytkownika szybkie wyświetlenie, nawet wtedy, gdy trwa trochę czasu, aby pobrać i przeanalizować pakiety języka JavaScript.</span><span class="sxs-lookup"><span data-stu-id="d13ab-223">Visitors reaching your app over a slow network connection or on slow mobile devices see the initial UI quickly, even if it takes a while to fetch or parse the JavaScript bundles.</span></span> <span data-ttu-id="d13ab-224">Jednak wiele źródła są używane głównie w sieciach szybkie, wewnętrzne firmy na komputerach szybkie, gdzie aplikacja będzie wyświetlana niemal natychmiast.</span><span class="sxs-lookup"><span data-stu-id="d13ab-224">However, many SPAs are mainly used over fast, internal company networks on fast computers where the app appears almost instantly.</span></span>

<span data-ttu-id="d13ab-225">W tym samym czasie istnieją znaczne wady włączenia SSR.</span><span class="sxs-lookup"><span data-stu-id="d13ab-225">At the same time, there are significant drawbacks to enabling SSR.</span></span> <span data-ttu-id="d13ab-226">Dodaje złożoność do procesie tworzenia aplikacji.</span><span class="sxs-lookup"><span data-stu-id="d13ab-226">It adds complexity to your development process.</span></span> <span data-ttu-id="d13ab-227">Kod muszą działać w dwóch różnych środowiskach: po stronie klienta i po stronie serwera (w środowisku Node.js wywoływany z platformy ASP.NET Core).</span><span class="sxs-lookup"><span data-stu-id="d13ab-227">Your code must run in two different environments: client-side and server-side (in a Node.js environment invoked from ASP.NET Core).</span></span> <span data-ttu-id="d13ab-228">Poniżej przedstawiono kilka sposobów pamiętać:</span><span class="sxs-lookup"><span data-stu-id="d13ab-228">Here are some things to bear in mind:</span></span>

* <span data-ttu-id="d13ab-229">SSR wymaga instalacji środowiska Node.js na serwerach produkcyjnych.</span><span class="sxs-lookup"><span data-stu-id="d13ab-229">SSR requires a Node.js installation on your production servers.</span></span> <span data-ttu-id="d13ab-230">Jest automatycznie dla niektórych scenariuszy wdrażania, takie jak usługi aplikacji Azure, ale nie dla innych osób, takich jak sieć szkieletowa usług Azure.</span><span class="sxs-lookup"><span data-stu-id="d13ab-230">This is automatically the case for some deployment scenarios, such as Azure App Services, but not for others, such as Azure Service Fabric.</span></span>
* <span data-ttu-id="d13ab-231">Włączanie `BuildServerSideRenderer` flagi przyczyny kompilacji z *node_modules* katalogu publikowania.</span><span class="sxs-lookup"><span data-stu-id="d13ab-231">Enabling the `BuildServerSideRenderer` build flag causes your *node_modules* directory to publish.</span></span> <span data-ttu-id="d13ab-232">Ten folder zawiera 20 000 + plików, co zwiększa czas wdrażania.</span><span class="sxs-lookup"><span data-stu-id="d13ab-232">This folder contains 20,000+ files, which increases deployment time.</span></span>
* <span data-ttu-id="d13ab-233">Do uruchomienia kodu w środowisku Node.js, go nie może polegać na istnienie specyficzne dla przeglądarki JavaScript API takich jak `window` lub `localStorage`.</span><span class="sxs-lookup"><span data-stu-id="d13ab-233">To run your code in a Node.js environment, it can't rely on the existence of browser-specific JavaScript APIs such as `window` or `localStorage`.</span></span> <span data-ttu-id="d13ab-234">Jeśli kodu (lub niektóre biblioteki możesz odwoływać się do innych firm) podejmie próbę użycia tych interfejsów API, zostanie wyświetlony błąd podczas SSR.</span><span class="sxs-lookup"><span data-stu-id="d13ab-234">If your code (or some third-party library you reference) tries to use these APIs, you'll get an error during SSR.</span></span> <span data-ttu-id="d13ab-235">Na przykład nie używaj jQuery, ponieważ odwołuje się do interfejsów API specyficzne dla przeglądarki w wielu miejscach.</span><span class="sxs-lookup"><span data-stu-id="d13ab-235">For example, don't use jQuery because it references browser-specific APIs in many places.</span></span> <span data-ttu-id="d13ab-236">Aby zapobiec błędom, możesz uniknąć SSR lub uniknąć interfejsów API specyficzne dla przeglądarki lub biblioteki.</span><span class="sxs-lookup"><span data-stu-id="d13ab-236">To prevent errors, you must either avoid SSR or avoid browser-specific APIs or libraries.</span></span> <span data-ttu-id="d13ab-237">W kontroli w celu zapewnienia, że nie są one wywoływane podczas SSR może zawijać się żadnych wywołań do tych interfejsów API.</span><span class="sxs-lookup"><span data-stu-id="d13ab-237">You can wrap any calls to such APIs in checks to ensure they aren't invoked during SSR.</span></span> <span data-ttu-id="d13ab-238">W kodzie JavaScript lub maszynie, na przykład użyć wyboru podobny do następującego:</span><span class="sxs-lookup"><span data-stu-id="d13ab-238">For example, use a check such as the following in JavaScript or TypeScript code:</span></span>

    ```javascript
    if (typeof window !== 'undefined') {
        // Call browser-specific APIs here
    }
    ```
