---
uid: web-pages/overview/ui-layouts-and-themes/9-working-with-images
title: Praca z obrazami w witryny ASP.NET Web Pages (Razor) | Dokumentacja firmy Microsoft
author: tfitzmac
description: W tym rozdziale przedstawiono sposób dodawania, wyświetlania i modyfikowania obrazów (zmienianie rozmiaru, przerzucania i dodać znaki wodne) w witrynie sieci Web.
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/20/2014
ms.topic: article
ms.assetid: 778c4e58-4372-4d25-bab9-aec4a8d8e38d
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/ui-layouts-and-themes/9-working-with-images
msc.type: authoredcontent
ms.openlocfilehash: 74838dd364a43f3f4c966c1417d0f0b2cc242f28
ms.sourcegitcommit: 9a9483aceb34591c97451997036a9120c3fe2baf
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 11/10/2017
ms.locfileid: "26573350"
---
<a name="working-with-images-in-an-aspnet-web-pages-razor-site"></a><span data-ttu-id="a8aea-103">Praca z obrazami w lokacji (Razor) stron sieci Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="a8aea-103">Working with Images in an ASP.NET Web Pages (Razor) Site</span></span>
====================
<span data-ttu-id="a8aea-104">przez [FitzMacken niestandardowy](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="a8aea-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="a8aea-105">W tym artykule przedstawiono sposób dodawania, wyświetlania i modyfikowania obrazów (zmienianie rozmiaru, przerzucania i dodać znaki wodne) w witrynie sieci Web platformy ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="a8aea-105">This article shows you how to add, display, and manipulate images (resize, flip, and add watermarks) in an ASP.NET Web Pages (Razor) website.</span></span>
> 
> <span data-ttu-id="a8aea-106">Zawartość:</span><span class="sxs-lookup"><span data-stu-id="a8aea-106">What you'll learn:</span></span>
> 
> - <span data-ttu-id="a8aea-107">Jak dodać obraz do strony dynamicznie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-107">How to add an image to a page dynamically.</span></span>
> - <span data-ttu-id="a8aea-108">Jak umożliwić użytkownikom przekazywanie obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-108">How to let users upload an image.</span></span>
> - <span data-ttu-id="a8aea-109">Jak zmienić rozmiar obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-109">How to resize an image.</span></span>
> - <span data-ttu-id="a8aea-110">Jak przerzucanie i obracanie obrazów.</span><span class="sxs-lookup"><span data-stu-id="a8aea-110">How to flip or rotate an image.</span></span>
> - <span data-ttu-id="a8aea-111">Jak dodać do obrazu znaku wodnego.</span><span class="sxs-lookup"><span data-stu-id="a8aea-111">How to add a watermark to an image.</span></span>
> - <span data-ttu-id="a8aea-112">Jak używać obrazu jako znaku wodnego.</span><span class="sxs-lookup"><span data-stu-id="a8aea-112">How to use an image as a watermark.</span></span>
> 
> <span data-ttu-id="a8aea-113">Są to programowania funkcje dodane w artykule programu ASP.NET:</span><span class="sxs-lookup"><span data-stu-id="a8aea-113">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="a8aea-114">`WebImage` Pomocnika.</span><span class="sxs-lookup"><span data-stu-id="a8aea-114">The `WebImage` helper.</span></span>
> - <span data-ttu-id="a8aea-115">`Path` Obiektu, który udostępnia metody, które umożliwiają manipulowania nazwę pliku i ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="a8aea-115">The `Path` object, which provides methods that let you manipulate path and file names.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="a8aea-116">Używane w samouczku wersje oprogramowania</span><span class="sxs-lookup"><span data-stu-id="a8aea-116">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="a8aea-117">Strony sieci Web platformy ASP.NET (Razor) 2</span><span class="sxs-lookup"><span data-stu-id="a8aea-117">ASP.NET Web Pages (Razor) 2</span></span>
> - <span data-ttu-id="a8aea-118">Program WebMatrix 2</span><span class="sxs-lookup"><span data-stu-id="a8aea-118">WebMatrix 2</span></span>
>   
> 
> <span data-ttu-id="a8aea-119">W tym samouczku współdziała również z 3 programu WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="a8aea-119">This tutorial also works with WebMatrix 3.</span></span>


<a id="Adding_an_Image"></a>
## <a name="adding-an-image-to-a-web-page-dynamically"></a><span data-ttu-id="a8aea-120">Dodawanie obrazu do strony sieci Web dynamicznie</span><span class="sxs-lookup"><span data-stu-id="a8aea-120">Adding an Image to a Web Page Dynamically</span></span>

<span data-ttu-id="a8aea-121">Można dodać obrazy do witryny sieci Web i poszczególnych stron, podczas projektujesz witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="a8aea-121">You can add images to your website and to individual pages while you're developing the website.</span></span> <span data-ttu-id="a8aea-122">Można także pozwolić użytkownikom przekazywanie obrazów, które mogą być przydatne dla zadania, takie jak pozwalając im na dodawanie zdjęcia profilu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-122">You can also let users upload images, which might be useful for tasks like letting them add a profile photo.</span></span>

<span data-ttu-id="a8aea-123">Jeśli obraz nie jest jeszcze dostępna w witrynie i chcesz go wyświetlić na stronie, użyj HTML `<img>` element następująco:</span><span class="sxs-lookup"><span data-stu-id="a8aea-123">If an image is already available on your site and you just want to display it on a page, you use an HTML `<img>` element like this:</span></span>

[!code-html[Main](9-working-with-images/samples/sample1.html)]

<span data-ttu-id="a8aea-124">Czasami jednak musisz mieć możliwość wyświetlania obrazów dynamicznie & #8212; oznacza to nie wiadomo, jakie obraz do wyświetlania aż do strony jest uruchomiona.</span><span class="sxs-lookup"><span data-stu-id="a8aea-124">Sometimes, though, you need to be able to display images dynamically &#8212; that is, you don't know what image to display until the page is running.</span></span>

<span data-ttu-id="a8aea-125">Procedura w tej sekcji przedstawiono sposób wyświetlania obrazu na bieżąco, w którym użytkownicy określić nazwę pliku obrazu z listy nazw obrazów.</span><span class="sxs-lookup"><span data-stu-id="a8aea-125">The procedure in this section shows how to display an image on the fly where users specify the image file name from a list of image names.</span></span> <span data-ttu-id="a8aea-126">Nazwa obrazu one wybierz z listy rozwijanej i po przesyłają strony one wybranego obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-126">They select the name of the image from a drop-down list, and when they submit the page, the image they selected is displayed.</span></span>

<span data-ttu-id="a8aea-127">![[Obraz] ](9-working-with-images/_static/image1.jpg "ch9images 1.jpg")</span><span class="sxs-lookup"><span data-stu-id="a8aea-127">![[image]](9-working-with-images/_static/image1.jpg "ch9images-1.jpg")</span></span>

1. <span data-ttu-id="a8aea-128">W programie WebMatrix Utwórz nową witrynę sieci Web.</span><span class="sxs-lookup"><span data-stu-id="a8aea-128">In WebMatrix, create a new website.</span></span>
2. <span data-ttu-id="a8aea-129">Dodaj nową stronę o nazwie *DynamicImage.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-129">Add a new page named *DynamicImage.cshtml*.</span></span>
3. <span data-ttu-id="a8aea-130">W folderze głównym witryny sieci Web, należy dodać nowy folder i nadaj mu nazwę *obrazów*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-130">In the root folder of the website, add a new folder and name it *images*.</span></span>
4. <span data-ttu-id="a8aea-131">Dodaj cztery obrazy do *obrazów* właśnie utworzony folder.</span><span class="sxs-lookup"><span data-stu-id="a8aea-131">Add four images to the *images* folder you just created.</span></span> <span data-ttu-id="a8aea-132">(Wszystkie obrazy masz będą przydatne czy, ale powinny mieścić się na stronie). Zmień nazwę obrazy *Photo1.jpg*, *Photo2.jpg*, *Photo3.jpg*, i *Photo4.jpg*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-132">(Any images you have handy will do, but they should fit onto a page.) Rename the images *Photo1.jpg*, *Photo2.jpg*, *Photo3.jpg*, and *Photo4.jpg*.</span></span> <span data-ttu-id="a8aea-133">(Nie będzie używać *Photo4.jpg* w tym procedura, ale będzie używać go w dalszej części tego artykułu.)</span><span class="sxs-lookup"><span data-stu-id="a8aea-133">(You won't use *Photo4.jpg* in this procedure, but you'll use it later in the article.)</span></span>
5. <span data-ttu-id="a8aea-134">Upewnij się, że cztery obrazy nie są oznaczone jako tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-134">Verify that the four images are not marked as read-only.</span></span>
6. <span data-ttu-id="a8aea-135">Zastąp istniejącą zawartość na stronie następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="a8aea-135">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](9-working-with-images/samples/sample2.cshtml)]

    <span data-ttu-id="a8aea-136">Ciała strony ma listy rozwijanej ( `<select>` element) o nazwie `photoChoice`.</span><span class="sxs-lookup"><span data-stu-id="a8aea-136">The body of the page has a drop-down list (a `<select>` element) that's named `photoChoice`.</span></span> <span data-ttu-id="a8aea-137">Lista zawiera trzy opcje i `value` atrybut każdej opcji listy ma nazwę jednego z obrazów, które należy umieścić w *obrazów* folderu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-137">The list has three options, and the `value` attribute of each list option has the name of one of the images that you put in the *images* folder.</span></span> <span data-ttu-id="a8aea-138">Zasadniczo listy umożliwia użytkownikowi wybieranie przyjaznej nazwy, takie jak &quot;1 fotografii&quot;, a następnie przekazuje *.jpg* nazwę pliku po przesłaniu strony.</span><span class="sxs-lookup"><span data-stu-id="a8aea-138">Essentially, the list lets the user select a friendly name like &quot;Photo 1&quot;, and it then passes the *.jpg* file name when the page is submitted.</span></span>

    <span data-ttu-id="a8aea-139">W kodzie, można uzyskać określonego przez użytkownika (innymi słowy, nazwa pliku obrazu) z listy, odczytując `Request["photoChoice"]`.</span><span class="sxs-lookup"><span data-stu-id="a8aea-139">In the code, you can get the user's selection (in other words, the image file name) from the list by reading `Request["photoChoice"]`.</span></span> <span data-ttu-id="a8aea-140">Należy najpierw sprawdzić, czy zaznaczenie w ogóle.</span><span class="sxs-lookup"><span data-stu-id="a8aea-140">You first see if there's a selection at all.</span></span> <span data-ttu-id="a8aea-141">Jeśli istnieje, można zbudować ścieżki dla obrazu, który składa się z nazwy folderu obrazów i nazwę pliku obrazu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="a8aea-141">If there is, you construct a path for the image that consists of the name of the folder for the images and the user's image file name.</span></span> <span data-ttu-id="a8aea-142">(Jeśli podjęto próbę utworzenia ścieżki, ale nie było postanowienia `Request["photoChoice"]`, będzie wyświetlany komunikat o błędzie.) Powoduje to ścieżka względna następująco:</span><span class="sxs-lookup"><span data-stu-id="a8aea-142">(If you tried to construct a path but there was nothing in `Request["photoChoice"]`, you'd get an error.) This results in a relative path like this:</span></span>

    <span data-ttu-id="a8aea-143">*obrazy/Photo1.jpg*</span><span class="sxs-lookup"><span data-stu-id="a8aea-143">*images/Photo1.jpg*</span></span>

    <span data-ttu-id="a8aea-144">Ścieżka jest przechowywany w zmiennej o nazwie `imagePath` będą potrzebne później na stronie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-144">The path is stored in variable named `imagePath` that you'll need later in the page.</span></span>

    <span data-ttu-id="a8aea-145">W treści, dostępna jest również `<img>` element, który służy do wyświetlania obrazu, który zostanie pobrana.</span><span class="sxs-lookup"><span data-stu-id="a8aea-145">In the body, there's also an `<img>` element that's used to display the image that the user picked.</span></span> <span data-ttu-id="a8aea-146">`src` Atrybut nie jest ustawiony na nazwę pliku lub adres URL, tak, aby wyświetlić element statyczny, należy wykonać.</span><span class="sxs-lookup"><span data-stu-id="a8aea-146">The `src` attribute isn't set to a file name or URL, like you'd do to display a static element.</span></span> <span data-ttu-id="a8aea-147">Zamiast tego jest równa `@imagePath`, co oznacza, że pobiera ona swoją wartość ze ścieżki ustawić w kodzie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-147">Instead, it's set to `@imagePath`, meaning that it gets its value from the path you set in code.</span></span>

    <span data-ttu-id="a8aea-148">Po raz pierwszy uruchamia strony, jednak nie ma żadnego obrazu do wyświetlenia, ponieważ użytkownik nie wybrał żadnych czynności.</span><span class="sxs-lookup"><span data-stu-id="a8aea-148">The first time that the page runs, though, there's no image to display, because the user hasn't selected anything.</span></span> <span data-ttu-id="a8aea-149">To będzie zwykle oznaczają, że `src` atrybut może być pusty i obrazu będzie wyświetlany jako czerwony &quot;x&quot; (lub niezależnie od przeglądarki renderuje, gdy nie można odnaleźć obrazu).</span><span class="sxs-lookup"><span data-stu-id="a8aea-149">This would normally mean that the `src` attribute would be empty and the image would show up as a red &quot;x&quot; (or whatever the browser renders when it can't find an image).</span></span> <span data-ttu-id="a8aea-150">Aby tego uniknąć, możesz zaznaczyć `<img>` element `if` bloku, który umożliwia sprawdzenie, aby sprawdzić, czy `imagePath` zmienna ma niczego w nim.</span><span class="sxs-lookup"><span data-stu-id="a8aea-150">To prevent this, you put the `<img>` element in an `if` block that tests to see whether the `imagePath` variable has anything in it.</span></span> <span data-ttu-id="a8aea-151">Jeśli użytkownik niczego, `imagePath` zawiera ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="a8aea-151">If the user made a selection, `imagePath` contains the path.</span></span> <span data-ttu-id="a8aea-152">Jeśli użytkownik nie wybierz obraz lub ta strona jest wyświetlana, jeśli po raz pierwszy, `<img>` elementu nawet nie jest renderowany.</span><span class="sxs-lookup"><span data-stu-id="a8aea-152">If the user didn't pick an image or if this is the first time the page is displayed, the `<img>` element isn't even rendered.</span></span>
7. <span data-ttu-id="a8aea-153">Zapisz plik i uruchomić strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="a8aea-153">Save the file and run the page in a browser.</span></span> <span data-ttu-id="a8aea-154">(Upewnij się, że strona jest zaznaczona w **pliki** obszar roboczy przed jej uruchomieniem.)</span><span class="sxs-lookup"><span data-stu-id="a8aea-154">(Make sure the page is selected in the **Files** workspace before you run it.)</span></span>
8. <span data-ttu-id="a8aea-155">Wybierz obraz z listy rozwijanej, a następnie kliknij przycisk **obraz przykładowy**.</span><span class="sxs-lookup"><span data-stu-id="a8aea-155">Select an image from the drop-down list and then click **Sample Image**.</span></span> <span data-ttu-id="a8aea-156">Upewnij się, że widoczny różnych obrazów dla różnych opcji.</span><span class="sxs-lookup"><span data-stu-id="a8aea-156">Make sure that you see different images for different choices.</span></span>

<a id="Uploading_an_Image"></a>
## <a name="uploading-an-image"></a><span data-ttu-id="a8aea-157">Przekazywanie obrazu</span><span class="sxs-lookup"><span data-stu-id="a8aea-157">Uploading an Image</span></span>

<span data-ttu-id="a8aea-158">W poprzednim przykładzie pokazano sposób wyświetlania obrazu dynamicznie, ale działał tylko z obrazami, które zostały już w witrynie sieci Web.</span><span class="sxs-lookup"><span data-stu-id="a8aea-158">The previous example showed you how to display an image dynamically, but it worked only with images that were already on your website.</span></span> <span data-ttu-id="a8aea-159">Za pomocą poniższej procedury zezwolić użytkownikom na przekazywanie obrazu, który następnie jest wyświetlany na stronie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-159">This procedure shows how to let users upload an image, which is then displayed on the page.</span></span> <span data-ttu-id="a8aea-160">W programie ASP.NET, można manipulować obrazów na bieżąco przy użyciu `WebImage` pomocnika, mającej metody, które umożliwiają tworzenia, modyfikowania i zapisywania obrazów.</span><span class="sxs-lookup"><span data-stu-id="a8aea-160">In ASP.NET, you can manipulate images on the fly using the `WebImage` helper, which has methods that let you create, manipulate, and save images.</span></span> <span data-ttu-id="a8aea-161">`WebImage` Pomocnika obsługuje wszystkie wspólne web obrazu typy plików, łącznie z *.jpg*, *.png*, i *.bmp*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-161">The `WebImage` helper supports all the common web image file types, including *.jpg*, *.png*, and *.bmp*.</span></span> <span data-ttu-id="a8aea-162">W tym artykule użyjesz *.jpg* obrazów, ale można użyć dowolnego typu obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-162">Throughout this article, you'll use *.jpg* images, but you can use any of the image types.</span></span>

<span data-ttu-id="a8aea-163">![[Obraz] ](9-working-with-images/_static/image2.jpg "ch9images 2.jpg")</span><span class="sxs-lookup"><span data-stu-id="a8aea-163">![[image]](9-working-with-images/_static/image2.jpg "ch9images-2.jpg")</span></span>

1. <span data-ttu-id="a8aea-164">Dodaj nową stronę i nadaj mu nazwę *UploadImage.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-164">Add a new page and name it *UploadImage.cshtml*.</span></span>
2. <span data-ttu-id="a8aea-165">Zastąp istniejącą zawartość na stronie następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="a8aea-165">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample3.cshtml)]

    <span data-ttu-id="a8aea-166">Jednostka tekstu `<input type="file">` element, który umożliwia użytkownikom, wybierz plik do przekazania.</span><span class="sxs-lookup"><span data-stu-id="a8aea-166">The body of the text has an `<input type="file">` element, which lets users select a file to upload.</span></span> <span data-ttu-id="a8aea-167">Gdy użytkownik klika polecenie **przesyłania**, przesyłania pliku, są pobierane z formularza.</span><span class="sxs-lookup"><span data-stu-id="a8aea-167">When they click **Submit**, the file they picked is submitted along with the form.</span></span>

    <span data-ttu-id="a8aea-168">Aby uzyskać załadowanego obrazu, należy użyć `WebImage` Pomocnik, którego szerokiej gamy przydatne metody do pracy z obrazami.</span><span class="sxs-lookup"><span data-stu-id="a8aea-168">To get the uploaded image, you use the `WebImage` helper, which has all sorts of useful methods for working with images.</span></span> <span data-ttu-id="a8aea-169">W szczególności należy używać `WebImage.GetImageFromRequest` uzyskać załadowanego obrazu (jeśli istnieje) i zapisz go w zmiennej o nazwie `photo`.</span><span class="sxs-lookup"><span data-stu-id="a8aea-169">Specifically, you use `WebImage.GetImageFromRequest` to get the uploaded image (if any) and store it in a variable named `photo`.</span></span>

    <span data-ttu-id="a8aea-170">Dużo pracy w tym przykładzie obejmuje pobieranie i ustawianie nazwy pliku i ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="a8aea-170">A lot of the work in this example involves getting and setting file and path names.</span></span> <span data-ttu-id="a8aea-171">Problem polega na tym, czy chcesz pobrać nazwę (i tylko nazwę) obraz, który zostanie przekazany, a następnie utwórz nową ścieżkę, do których użytkownik chce przechowywania obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-171">The issue is that you want to get the name (and just the name) of the image that the user uploaded, and then create a new path for where you're going to store the image.</span></span> <span data-ttu-id="a8aea-172">Ponieważ użytkownicy potencjalnie może przekazać wiele obrazów, które mają taką samą nazwę, służy do tworzenia unikatowych nazw i upewnij się, że użytkownicy nie zastępuj istniejący obraz bit dodatkowy kod.</span><span class="sxs-lookup"><span data-stu-id="a8aea-172">Because users could potentially upload multiple images that have the same name, you use a bit of extra code to create unique names and make sure that users don't overwrite existing pictures.</span></span>

    <span data-ttu-id="a8aea-173">Jeśli obraz faktycznie został przekazany (test `if (photo != null)`), uzyskać nazwę obrazu z obrazu `FileName` właściwości.</span><span class="sxs-lookup"><span data-stu-id="a8aea-173">If an image actually has been uploaded (the test `if (photo != null)`), you get the image name from the image's `FileName` property.</span></span> <span data-ttu-id="a8aea-174">Gdy użytkownik przesyła obraz, `FileName` zawiera oryginalna nazwa użytkownika, który zawiera ścieżki z komputera użytkownika.</span><span class="sxs-lookup"><span data-stu-id="a8aea-174">When the user uploads the image, `FileName` contains the user's original name, which includes the path from the user's computer.</span></span> <span data-ttu-id="a8aea-175">Go może wyglądać następująco:</span><span class="sxs-lookup"><span data-stu-id="a8aea-175">It might look like this:</span></span>

    <span data-ttu-id="a8aea-176">*C:\Users\Joe\Pictures\SamplePhoto1.jpg*</span><span class="sxs-lookup"><span data-stu-id="a8aea-176">*C:\Users\Joe\Pictures\SamplePhoto1.jpg*</span></span>

    <span data-ttu-id="a8aea-177">Nie ma jednak te informacje Ścieżka & #8212; chcesz rzeczywiste nazwy plików (*SamplePhoto1.jpg*).</span><span class="sxs-lookup"><span data-stu-id="a8aea-177">You don't want all that path information, though &#8212; you just want the actual file name (*SamplePhoto1.jpg*).</span></span> <span data-ttu-id="a8aea-178">Tylko plik ze ścieżki można usuwają przy użyciu `Path.GetFileName` metody następująco:</span><span class="sxs-lookup"><span data-stu-id="a8aea-178">You can strip out just the file from a path by using the `Path.GetFileName` method, like this:</span></span>

    [!code-csharp[Main](9-working-with-images/samples/sample4.cs)]

    <span data-ttu-id="a8aea-179">Następnie można utworzyć nowego unikatową nazwę pliku, dodając identyfikator GUID oryginalną nazwę.</span><span class="sxs-lookup"><span data-stu-id="a8aea-179">You then create a new unique file name by adding a GUID to the original name.</span></span> <span data-ttu-id="a8aea-180">(Aby uzyskać więcej informacji na temat identyfikatorów GUID, zobacz [o identyfikatorów GUID](#SB_AboutGUIDs) dalszej części tego artykułu.) Następnie należy utworzyć pełną ścieżkę, która służy do zapisania obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-180">(For more about GUIDs, see [About GUIDs](#SB_AboutGUIDs) later in this article.) Then you construct a complete path that you can use to save the image.</span></span> <span data-ttu-id="a8aea-181">Zapisz ścieżkę składa się z nową nazwę pliku, folderu (obrazy) i bieżącej lokalizacji witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="a8aea-181">The save path is made up of the new file name, the folder (images), and the current website location.</span></span>

    > [!NOTE]
    > <span data-ttu-id="a8aea-182">Aby swój kod, aby zapisać pliki w *obrazów* folderu, aplikacja musi odczytu i zapisu uprawnienia dla tego folderu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-182">In order for your code to save files in the *images* folder, the application needs read-write permissions for that folder.</span></span> <span data-ttu-id="a8aea-183">Na komputerze deweloperskim to nie jest zazwyczaj problem.</span><span class="sxs-lookup"><span data-stu-id="a8aea-183">On your development computer this is not typically an issue.</span></span> <span data-ttu-id="a8aea-184">Jednak opublikowanie witryny dostawcy hostingu serwera sieci web, należy jawnie ustawić te uprawnienia.</span><span class="sxs-lookup"><span data-stu-id="a8aea-184">However, when you publish your site to a hosting provider's web server, you might need to explicitly set those permissions.</span></span> <span data-ttu-id="a8aea-185">Jeśli uruchomienie tego kodu na serwerze dostawcy hostingu i występują błędy, skontaktuj się z dostawcy hostingu, aby dowiedzieć się, jak ustawić te uprawnienia.</span><span class="sxs-lookup"><span data-stu-id="a8aea-185">If you run this code on a hosting provider's server and get errors, check with the hosting provider to find out how to set those permissions.</span></span>

    <span data-ttu-id="a8aea-186">Na koniec należy przekazać Zapisz ścieżkę do `Save` metody `WebImage` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="a8aea-186">Finally, you pass the save path to the `Save` method of the `WebImage` helper.</span></span> <span data-ttu-id="a8aea-187">Spowoduje to zapisanie załadowanego obrazu pod nową nazwą.</span><span class="sxs-lookup"><span data-stu-id="a8aea-187">This stores the uploaded image under its new name.</span></span> <span data-ttu-id="a8aea-188">Zapisz metody wygląda następująco: `photo.Save(@"~\" + imagePath)`.</span><span class="sxs-lookup"><span data-stu-id="a8aea-188">The save method looks like this: `photo.Save(@"~\" + imagePath)`.</span></span> <span data-ttu-id="a8aea-189">Pełna ścieżka jest dołączany do `@"~\"`, która jest bieżącą lokalizację witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="a8aea-189">The complete path is appended to `@"~\"`, which is the current website location.</span></span> <span data-ttu-id="a8aea-190">(Aby uzyskać informacje o `~` operatora, zobacz [wprowadzenie do platformy ASP.NET Web programowania przy użyciu składni Razor](https://go.microsoft.com/fwlink/?LinkId=202890#ID_WorkingWithFileAndFolderPaths).)</span><span class="sxs-lookup"><span data-stu-id="a8aea-190">(For information about the `~` operator, see [Introduction to ASP.NET Web Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890#ID_WorkingWithFileAndFolderPaths).)</span></span>

    <span data-ttu-id="a8aea-191">Tak jak w poprzednim przykładzie ciała strony zawiera `<img>` element, aby wyświetlić obraz.</span><span class="sxs-lookup"><span data-stu-id="a8aea-191">As in the previous example, the body of the page contains an `<img>` element to display the image.</span></span> <span data-ttu-id="a8aea-192">Jeśli `imagePath` została ustawiona, `<img>` element jest renderowany i jego `src` atrybut ma ustawioną `imagePath` wartość.</span><span class="sxs-lookup"><span data-stu-id="a8aea-192">If `imagePath` has been set, the `<img>` element is rendered and its `src` attribute is set to the `imagePath` value.</span></span>
3. <span data-ttu-id="a8aea-193">Uruchom strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="a8aea-193">Run the page in a browser.</span></span>
4. <span data-ttu-id="a8aea-194">Przekaż obraz i upewnij się, że jest wyświetlany na stronie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-194">Upload an image and make sure it's displayed in the page.</span></span>
5. <span data-ttu-id="a8aea-195">W witrynie, otwórz *obrazów* folderu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-195">In your site, open the *images* folder.</span></span> <span data-ttu-id="a8aea-196">Zobacz, czy plik został dodany których nazwa pliku wygląda następująco:</span><span class="sxs-lookup"><span data-stu-id="a8aea-196">You see that a new file has been added whose file name looks something like this::</span></span> 

    <span data-ttu-id="a8aea-197">*45ea4527-7ddd-4965-b9ca-c6444982b342\_MyPhoto.png*</span><span class="sxs-lookup"><span data-stu-id="a8aea-197">*45ea4527-7ddd-4965-b9ca-c6444982b342\_MyPhoto.png*</span></span>

    <span data-ttu-id="a8aea-198">Jest to obraz, który został przekazany za pomocą identyfikatora GUID prefiksem nazwy.</span><span class="sxs-lookup"><span data-stu-id="a8aea-198">This is the image that you uploaded with a GUID prefixed to the name.</span></span> <span data-ttu-id="a8aea-199">(Własny plik ma inny identyfikator GUID i prawdopodobnie nosi nazwę inną niż *MyPhoto.png*.)</span><span class="sxs-lookup"><span data-stu-id="a8aea-199">(Your own file will have a different GUID and probably is named something different than *MyPhoto.png*.)</span></span>

> [!TIP] 
> 
> <a id="SB_AboutGUIDs"></a>
> ### <a name="about-guids"></a><span data-ttu-id="a8aea-200">Identyfikatory GUID — informacje</span><span class="sxs-lookup"><span data-stu-id="a8aea-200">About GUIDs</span></span>
> 
> <span data-ttu-id="a8aea-201">Identyfikator GUID (globalnie unikatowy identyfikator) jest identyfikatorem, który zazwyczaj jest renderowany w formacie następująco: `936DA01F-9ABD-4d9d-80C7-02AF85C822A8`.</span><span class="sxs-lookup"><span data-stu-id="a8aea-201">A GUID (globally-unique ID) is an identifier that's usually rendered in a format like this: `936DA01F-9ABD-4d9d-80C7-02AF85C822A8`.</span></span> <span data-ttu-id="a8aea-202">Cyfry i litery (od A do F) są różne dla każdego identyfikatora GUID, ale wszystkie one oparte na wzorcu przy użyciu grup 8-4-4-4-12 znaków.</span><span class="sxs-lookup"><span data-stu-id="a8aea-202">The numbers and letters (from A-F) differ for each GUID, but they all follow the pattern of using groups of 8-4-4-4-12 characters.</span></span> <span data-ttu-id="a8aea-203">(Technicznie, identyfikator GUID jest liczbą 16-bajtowych/128-bitowe). Jeśli potrzebujesz identyfikatora GUID należy wywołać wyspecjalizowany kod, który generuje identyfikator GUID dla Ciebie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-203">(Technically, a GUID is a 16-byte/128-bit number.) When you need a GUID, you can call specialized code that generates a GUID for you.</span></span> <span data-ttu-id="a8aea-204">Ideą identyfikatorów GUID jest to, że między znaczne rozmiary numer (3,4 x 10<sup>38</sup>) i algorytm generowania go wynikowa liczba jest praktycznie gwarancji jednego rodzaju.</span><span class="sxs-lookup"><span data-stu-id="a8aea-204">The idea behind GUIDs is that between the enormous size of the number (3.4 x 10<sup>38</sup>) and the algorithm for generating it, the resulting number is virtually guaranteed to be one of a kind.</span></span> <span data-ttu-id="a8aea-205">Identyfikatory GUID w związku z tym to dobry sposób na potrzeby generowania nazw elementów, gdy użytkownik gwarantuje, że nie używasz takiej samej nazwie dwa razy.</span><span class="sxs-lookup"><span data-stu-id="a8aea-205">GUIDs therefore are a good way to generate names for things when you must guarantee that you won't use the same name twice.</span></span> <span data-ttu-id="a8aea-206">Wadą interfejsu, jest, że identyfikatory GUID nie są szczególnie przyjazny dla użytkownika, więc mają zwykle można użyć, gdy nazwa jest używana tylko w kodzie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-206">The downside, of course, is that GUIDs aren't particularly user friendly, so they tend to be used when the name is used only in code.</span></span>


<a id="Resizing_an_Image"></a>
## <a name="resizing-an-image"></a><span data-ttu-id="a8aea-207">Zmiana rozmiaru obrazu</span><span class="sxs-lookup"><span data-stu-id="a8aea-207">Resizing an Image</span></span>

<span data-ttu-id="a8aea-208">Witryny sieci Web akceptuje obrazów z użytkownikiem, można zmienić rozmiar obrazów, aby wyświetlić lub zapisać je.</span><span class="sxs-lookup"><span data-stu-id="a8aea-208">If your website accepts images from a user, you might want to resize the images before you display or save them.</span></span> <span data-ttu-id="a8aea-209">Można ponownie użyć `WebImage` pomocnika dla tego.</span><span class="sxs-lookup"><span data-stu-id="a8aea-209">You can again use the `WebImage` helper for this.</span></span>

<span data-ttu-id="a8aea-210">W tej procedurze pokazano, jak zmienić rozmiar załadowanego obrazu, aby utworzyć miniaturę, a następnie zapisać miniatur i oryginalnego obrazu w witrynie internetowej.</span><span class="sxs-lookup"><span data-stu-id="a8aea-210">This procedure shows how to resize an uploaded image to create a thumbnail and then save the thumbnail and original image in the website.</span></span> <span data-ttu-id="a8aea-211">Wyświetlania miniatury na stronie i używać hiperłącza do przekierowywania użytkowników do obrazu w pełnym rozmiarze.</span><span class="sxs-lookup"><span data-stu-id="a8aea-211">You display the thumbnail on the page and use a hyperlink to redirect users to the full-sized image.</span></span>

<span data-ttu-id="a8aea-212">![[Obraz] ](9-working-with-images/_static/image3.jpg "ch9images 3.jpg")</span><span class="sxs-lookup"><span data-stu-id="a8aea-212">![[image]](9-working-with-images/_static/image3.jpg "ch9images-3.jpg")</span></span>

1. <span data-ttu-id="a8aea-213">Dodaj nową stronę o nazwie *Thumbnail.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-213">Add a new page named *Thumbnail.cshtml*.</span></span>
2. <span data-ttu-id="a8aea-214">W *obrazów* folderu, utwórz podfolder o nazwie *Kciuki*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-214">In the *images* folder, create a subfolder named *thumbs*.</span></span>
3. <span data-ttu-id="a8aea-215">Zastąp istniejącą zawartość na stronie następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="a8aea-215">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample5.cshtml)]

    <span data-ttu-id="a8aea-216">Ten kod jest podobny do kodu z poprzedniego przykładu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-216">This code is similar to the code from the previous example.</span></span> <span data-ttu-id="a8aea-217">Różnica polega na ten kod zapisuje obraz, dwa razy, raz normalnie i jeden raz po utworzeniu miniatur kopię obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-217">The difference is that this code saves the image twice, once normally and once after you create a thumbnail copy of the image.</span></span> <span data-ttu-id="a8aea-218">Najpierw uzyskać załadowanego obrazu i zapisać ją w *obrazów* folderu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-218">First you get the uploaded image and save it in the *images* folder.</span></span> <span data-ttu-id="a8aea-219">Następnie utworzymy nową ścieżkę dla obraz miniatury.</span><span class="sxs-lookup"><span data-stu-id="a8aea-219">You then construct a new path for the thumbnail image.</span></span> <span data-ttu-id="a8aea-220">Aby utworzyć faktycznie miniaturę, należy wywołać `WebImage` przez pomocnika `Resize` metodę, aby utworzyć obraz 60 pikseli przez 60 pikseli.</span><span class="sxs-lookup"><span data-stu-id="a8aea-220">To actually create the thumbnail, you call the `WebImage` helper's `Resize` method to create a 60-pixel by 60-pixel image.</span></span> <span data-ttu-id="a8aea-221">W przykładzie pokazano sposób zachowania proporcji i jak można zapobiec obrazu trwa powiększenia (w przypadku nowego rozmiaru czy rzeczywiście powiększenia obrazu).</span><span class="sxs-lookup"><span data-stu-id="a8aea-221">The example shows how you preserve the aspect ratio and how you can prevent the image from being enlarged (in case the new size would actually make the image larger).</span></span> <span data-ttu-id="a8aea-222">Obraz o zmienionym rozmiarze jest następnie zapisywana w *Kciuki* podfolderu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-222">The resized image is then saved in the *thumbs* subfolder.</span></span>

    <span data-ttu-id="a8aea-223">Na koniec kod znaczników, używać tego samego `<img>` element z dynamicznej `src` atrybut, który przedstawiono w poprzednich przykładach warunkowo pokazanie obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-223">At the end of the markup, you use the same `<img>` element with the dynamic `src` attribute that you've seen in the previous examples to conditionally show the image.</span></span> <span data-ttu-id="a8aea-224">W takim przypadku możesz wyświetlić miniatury.</span><span class="sxs-lookup"><span data-stu-id="a8aea-224">In this case, you display the thumbnail.</span></span> <span data-ttu-id="a8aea-225">Można również użyć `<a>` elementu, aby utworzyć hiperłącza do big wersję obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-225">You also use an `<a>` element to create a hyperlink to the big version of the image.</span></span> <span data-ttu-id="a8aea-226">Jak `src` atrybutu `<img>` element, należy ustawić `href` atrybutu `<a>` element dynamicznie, tak aby niezależnie od znajduje się w `imagePath`.</span><span class="sxs-lookup"><span data-stu-id="a8aea-226">As with the `src` attribute of the `<img>` element, you set the `href` attribute of the `<a>` element dynamically to whatever is in `imagePath`.</span></span> <span data-ttu-id="a8aea-227">Aby upewnić się, że ścieżka może działać jako adres URL, Przekaż `imagePath` do `Html.AttributeEncode` metodę, która konwertuje znaki, które są ok w adresie URL zarezerwowanych znaków w ścieżce.</span><span class="sxs-lookup"><span data-stu-id="a8aea-227">To make sure that the path can work as a URL, you pass `imagePath` to the `Html.AttributeEncode` method, which converts reserved characters in the path to characters that are ok in a URL.</span></span>
4. <span data-ttu-id="a8aea-228">Uruchom strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="a8aea-228">Run the page in a browser.</span></span>
5. <span data-ttu-id="a8aea-229">Przekaż zdjęcie i sprawdź, czy jest wyświetlany miniatury.</span><span class="sxs-lookup"><span data-stu-id="a8aea-229">Upload a photo and verify that the thumbnail is shown.</span></span>
6. <span data-ttu-id="a8aea-230">Kliknij miniaturę, aby wyświetlić obraz w pełnym rozmiarze.</span><span class="sxs-lookup"><span data-stu-id="a8aea-230">Click the thumbnail to see the full-size image.</span></span>
7. <span data-ttu-id="a8aea-231">W *obrazów* i *obrazów/Kciuki*, należy pamiętać, że zostały dodane nowe pliki.</span><span class="sxs-lookup"><span data-stu-id="a8aea-231">In the *images* and *images/thumbs*, note that new files have been added.</span></span>

<a id="Rotating_and_Flipping"></a>
## <a name="rotating-and-flipping-an-image"></a><span data-ttu-id="a8aea-232">Obracanie i przerzucanie obrazu</span><span class="sxs-lookup"><span data-stu-id="a8aea-232">Rotating and Flipping an Image</span></span>

<span data-ttu-id="a8aea-233">`WebImage` Pomocnika umożliwia także Przerzucanie i obracanie obrazów.</span><span class="sxs-lookup"><span data-stu-id="a8aea-233">The `WebImage` helper also lets you flip and rotate images.</span></span> <span data-ttu-id="a8aea-234">W tej procedurze pokazano, jak pobrać obrazu z serwera, Przerzucanie obrazu odwrócona (pionowo), zapisz go, a następnie Wyświetl odwrócony obraz na stronie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-234">This procedure shows how to get an image from the server, flip the image upside down (vertically), save it, and then display the flipped image on the page.</span></span> <span data-ttu-id="a8aea-235">W tym przykładzie tylko używasz plik istnieje już na serwerze (*Photo2.jpg*).</span><span class="sxs-lookup"><span data-stu-id="a8aea-235">In this example, you're just using a file you already have on the server (*Photo2.jpg*).</span></span> <span data-ttu-id="a8aea-236">W rzeczywistej aplikacji będzie prawdopodobnie Przerzucanie obrazu o nazwie otrzymasz dynamicznie, tak jak w poprzednich przykładach.</span><span class="sxs-lookup"><span data-stu-id="a8aea-236">In a real application, you'd probably flip an image whose name you get dynamically, like you did in previous examples.</span></span>

<span data-ttu-id="a8aea-237">![[Obraz] ](9-working-with-images/_static/image4.jpg "ch9images 4.jpg")</span><span class="sxs-lookup"><span data-stu-id="a8aea-237">![[image]](9-working-with-images/_static/image4.jpg "ch9images-4.jpg")</span></span>

1. <span data-ttu-id="a8aea-238">Dodaj nową stronę o nazwie *FlipImage.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-238">Add a new page named *FlipImage.cshtml*.</span></span>
2. <span data-ttu-id="a8aea-239">Zastąp istniejącą zawartość na stronie następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="a8aea-239">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample6.cshtml)]

    <span data-ttu-id="a8aea-240">W kodzie użyto `WebImage` pomocnika można pobrać obrazu z serwera.</span><span class="sxs-lookup"><span data-stu-id="a8aea-240">The code uses the `WebImage` helper to get an image from the server.</span></span> <span data-ttu-id="a8aea-241">Utwórz ścieżkę do obrazu przy użyciu tę samą metodę używany w przykładach wcześniejszych zapisywania obrazów i przekazania tej ścieżki, podczas tworzenia obrazu przy użyciu `WebImage`:</span><span class="sxs-lookup"><span data-stu-id="a8aea-241">You create the path to the image using the same technique you used in earlier examples for saving images, and you pass that path when you create an image using `WebImage`:</span></span>

    [!code-javascript[Main](9-working-with-images/samples/sample7.js)]

    <span data-ttu-id="a8aea-242">Jeśli obraz zostanie znaleziony, utworzymy nową ścieżkę i nazwę, jak w starszych przykłady.</span><span class="sxs-lookup"><span data-stu-id="a8aea-242">If an image is found, you construct a new path and file name, like you did in earlier examples.</span></span> <span data-ttu-id="a8aea-243">Aby Przerzucanie obrazu, należy wywołać `FlipVertical` metody, a następnie zapisać go ponownie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-243">To flip the image, you call the `FlipVertical` method, and then you save the image again.</span></span>

    <span data-ttu-id="a8aea-244">Obraz jest ponownie wyświetlana na stronie przy użyciu `<img>` element z `src` ustawić atrybutu `imagePath`.</span><span class="sxs-lookup"><span data-stu-id="a8aea-244">The image is again displayed on the page by using the `<img>` element with the `src` attribute set to `imagePath`.</span></span>
3. <span data-ttu-id="a8aea-245">Uruchom strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="a8aea-245">Run the page in a browser.</span></span> <span data-ttu-id="a8aea-246">Obraz dla *Photo2.jpg* przedstawiono odwrócony.</span><span class="sxs-lookup"><span data-stu-id="a8aea-246">The image for *Photo2.jpg* is shown upside down.</span></span>
4. <span data-ttu-id="a8aea-247">Odśwież stronę lub żądania strony ponownie, aby zobaczyć, że obraz jest odwrócony po prawej stronie się ponownie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-247">Refresh the page or request the page again to see the image is flipped right side up again.</span></span>

<span data-ttu-id="a8aea-248">Obracanie obrazów, możesz użyć tego samego kodu, z wyjątkiem zamiast wywoływać metodę `FlipVertical` lub `FlipHorizontal`, należy wywołać `RotateLeft` lub `RotateRight`.</span><span class="sxs-lookup"><span data-stu-id="a8aea-248">To rotate an image, you use the same code, except that instead of calling the `FlipVertical` or `FlipHorizontal`, you call `RotateLeft` or `RotateRight`.</span></span>

<a id="Adding_a_Watermark"></a>
## <a name="adding-a-watermark-to-an-image"></a><span data-ttu-id="a8aea-249">Dodawanie do obrazu znaku wodnego</span><span class="sxs-lookup"><span data-stu-id="a8aea-249">Adding a Watermark to an Image</span></span>

<span data-ttu-id="a8aea-250">Po dodaniu obrazów do witryny sieci Web, można dodać do obrazu znaku wodnego, zanim zostanie zapisany lub wyświetl ją na stronie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-250">When you add images to your website, you might want to add a watermark to the image before you save it or display it on a page.</span></span> <span data-ttu-id="a8aea-251">Aby dodać informacje o prawach autorskich do obrazu lub anonsowanie ich firma ludzie często używają znaków wodnych.</span><span class="sxs-lookup"><span data-stu-id="a8aea-251">People often use watermarks to add copyright information to an image or to advertise their business name.</span></span>

<span data-ttu-id="a8aea-252">![[Obraz] ](9-working-with-images/_static/image5.jpg "ch9images 5.jpg")</span><span class="sxs-lookup"><span data-stu-id="a8aea-252">![[image]](9-working-with-images/_static/image5.jpg "ch9images-5.jpg")</span></span>

1. <span data-ttu-id="a8aea-253">Dodaj nową stronę o nazwie *Watermark.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-253">Add a new page named *Watermark.cshtml*.</span></span>
2. <span data-ttu-id="a8aea-254">Zastąp istniejącą zawartość na stronie następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="a8aea-254">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample8.cshtml)]

    <span data-ttu-id="a8aea-255">Ten kod jest podobny kod w *FlipImage.cshtml* strony z wcześniej (ale tym razem używa *Photo3.jpg* pliku).</span><span class="sxs-lookup"><span data-stu-id="a8aea-255">This code is like the code in the *FlipImage.cshtml* page from earlier (although this time it uses the *Photo3.jpg* file).</span></span> <span data-ttu-id="a8aea-256">Aby dodać znaku wodnego, należy wywołać `WebImage` przez pomocnika `AddTextWatermark` metody, aby zapisać obraz.</span><span class="sxs-lookup"><span data-stu-id="a8aea-256">To add the watermark, you call the `WebImage` helper's `AddTextWatermark` method before you save the image.</span></span> <span data-ttu-id="a8aea-257">W wywołaniu `AddTextWatermark`, Przekaż tekst &quot;test&quot;Ustaw kolor czcionki żółty, a wartość Arial rodziny czcionek.</span><span class="sxs-lookup"><span data-stu-id="a8aea-257">In the call to `AddTextWatermark`, you pass the text &quot;My Watermark&quot;, set the font color to yellow, and set the font family to Arial.</span></span> <span data-ttu-id="a8aea-258">(Mimo że nie jest w tym miejscu pokazano `WebImage` Pomocnik pozwala także określić nieprzezroczystość rodziny czcionek i rozmiar czcionki i położenie tekstu znaku wodnego.) Podczas zapisywania obrazu nie może być tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-258">(Although it's not shown here, the `WebImage` helper also lets you specify opacity, font family and font size, and the position of the watermark text.) When you save the image it must not be read-only.</span></span>

    <span data-ttu-id="a8aea-259">Jak przedstawiono przed, obraz jest wyświetlany na stronie przy użyciu `<img>` element z atrybutem src ustawiony na wartość `@imagePath`.</span><span class="sxs-lookup"><span data-stu-id="a8aea-259">As you've seen before, the image is displayed on the page by using the `<img>` element with the src attribute set to `@imagePath`.</span></span>
3. <span data-ttu-id="a8aea-260">Uruchom strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="a8aea-260">Run the page in a browser.</span></span> <span data-ttu-id="a8aea-261">Zwróć uwagę tekst "Test" w prawym dolnym rogu obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-261">Notice the text "My Watermark" at the bottom-right corner of the image.</span></span>

<a id="Using_an_Image_as_a_Watermark"></a>
## <a name="using-an-image-as-a-watermark"></a><span data-ttu-id="a8aea-262">Jako znaku wodnego przy użyciu obrazu</span><span class="sxs-lookup"><span data-stu-id="a8aea-262">Using an Image As a Watermark</span></span>

<span data-ttu-id="a8aea-263">Zamiast przy użyciu tekstu znaku wodnego, możesz użyć innego obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-263">Instead of using text for a watermark, you can use another image.</span></span> <span data-ttu-id="a8aea-264">Osoby czasami użyć obrazów, takich jak logo firmy jako znaku wodnego lub ich obraz znaku wodnego zamiast tekstu informacjami o prawach autorskich.</span><span class="sxs-lookup"><span data-stu-id="a8aea-264">People sometimes use images like a company logo as a watermark, or they use a watermark image instead of text for copyright information.</span></span>

<span data-ttu-id="a8aea-265">![[Obraz] ](9-working-with-images/_static/image6.jpg "ch9images 6.jpg")</span><span class="sxs-lookup"><span data-stu-id="a8aea-265">![[image]](9-working-with-images/_static/image6.jpg "ch9images-6.jpg")</span></span>

1. <span data-ttu-id="a8aea-266">Dodaj nową stronę o nazwie *ImageWatermark.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-266">Add a new page named *ImageWatermark.cshtml*.</span></span>
2. <span data-ttu-id="a8aea-267">Dodawanie obrazu do *obrazów* folderu używany jako logo i Zmień nazwę obrazu *MyCompanyLogo.jpg*.</span><span class="sxs-lookup"><span data-stu-id="a8aea-267">Add an image to the *images* folder that you can use as a logo, and rename the image *MyCompanyLogo.jpg*.</span></span> <span data-ttu-id="a8aea-268">Ten obraz powinien być obrazu, który widać wyraźnie po ustawieniu 80 pikseli szerokości i wysokości 20 pikseli.</span><span class="sxs-lookup"><span data-stu-id="a8aea-268">This image should be an image that you can see clearly when it's set to 80 pixels wide and 20 pixels high.</span></span>
3. <span data-ttu-id="a8aea-269">Zastąp istniejącą zawartość na stronie następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="a8aea-269">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample9.cshtml)]

    <span data-ttu-id="a8aea-270">Jest to inny wariant kod w przykładach wcześniejszych.</span><span class="sxs-lookup"><span data-stu-id="a8aea-270">This is another variation on the code from earlier examples.</span></span> <span data-ttu-id="a8aea-271">W takim przypadku należy wywołać `AddImageWatermark` można dodać obrazu znaku wodnego do obrazu docelowego (*Photo3.jpg*) przed zapisaniem obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-271">In this case, you call `AddImageWatermark` to add the watermark image to the target image (*Photo3.jpg*) before you save the image.</span></span> <span data-ttu-id="a8aea-272">Podczas wywoływania `AddImageWatermark`, ustaw jej szerokość 80 pikseli i wysokości do 20 pikseli.</span><span class="sxs-lookup"><span data-stu-id="a8aea-272">When you call `AddImageWatermark`, you set its width to 80 pixels and the height to 20 pixels.</span></span> <span data-ttu-id="a8aea-273">*MyCompanyLogo.jpg* obraz jest wyrównania w poziomie w Centrum i wyrównane w pionie w dolnej części obrazu docelowego.</span><span class="sxs-lookup"><span data-stu-id="a8aea-273">The *MyCompanyLogo.jpg* image is horizontally aligned in the center and vertically aligned at the bottom of the target image.</span></span> <span data-ttu-id="a8aea-274">Przezroczystość ma ustawioną wartość 100% i uzupełnienie jest równa 10 pikseli.</span><span class="sxs-lookup"><span data-stu-id="a8aea-274">The opacity is set to 100% and the padding is set to 10 pixels.</span></span> <span data-ttu-id="a8aea-275">Jeśli obraz znaku wodnego jest większy niż obrazu docelowego, nic się nie stanie.</span><span class="sxs-lookup"><span data-stu-id="a8aea-275">If the watermark image is bigger than the target image, nothing will happen.</span></span> <span data-ttu-id="a8aea-276">Jeśli obraz znaku wodnego jest większy niż obrazu docelowego i ustawić dopełnienie obrazu znaku wodnego do zera, znak wodny zostanie zignorowany.</span><span class="sxs-lookup"><span data-stu-id="a8aea-276">If the watermark image is bigger than the target image and you set the padding for the image watermark to zero, the watermark is ignored.</span></span>

    <span data-ttu-id="a8aea-277">Jak wcześniej, wyświetlić przy użyciu obrazu `<img>` elementu i dynamicznym `src` atrybutu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-277">As before, you display the image using the `<img>` element and a dynamic `src` attribute.</span></span>
4. <span data-ttu-id="a8aea-278">Uruchom strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="a8aea-278">Run the page in a browser.</span></span> <span data-ttu-id="a8aea-279">Należy zauważyć, że obrazu znaku wodnego, który znajduje się w dolnej części głównego obrazu.</span><span class="sxs-lookup"><span data-stu-id="a8aea-279">Notice that the watermark image appears at the bottom of the main image.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="a8aea-280">Dodatkowe zasoby</span><span class="sxs-lookup"><span data-stu-id="a8aea-280">Additional Resources</span></span>


[<span data-ttu-id="a8aea-281">Praca z plikami w witrynie stron sieci Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="a8aea-281">Working with Files in an ASP.NET Web Pages Site</span></span>](https://go.microsoft.com/fwlink/?LinkId=202896)

[<span data-ttu-id="a8aea-282">Wprowadzenie do programowania przy użyciu składni Razor strony sieci Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="a8aea-282">Introduction to ASP.NET Web Pages Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=251587)
