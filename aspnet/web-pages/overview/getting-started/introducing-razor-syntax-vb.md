---
uid: web-pages/overview/getting-started/introducing-razor-syntax-vb
title: "Wprowadzenie do programowania sieci Web ASP.NET przy użyciu składni Razor (Visual Basic) | Dokumentacja firmy Microsoft"
author: tfitzmac
description: "Ten dodatek umożliwia omówienie programowania w języku strony sieci Web ASP.NET w języku Visual Basic, przy użyciu składni Razor."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: 5da59646-e973-41cd-88a9-c6b2c0594027
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-vb
msc.type: authoredcontent
ms.openlocfilehash: 8f5d223a5944d8adb9fe65c89e87829d18d1c7ee
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/24/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-visual-basic"></a><span data-ttu-id="3831d-103">Wprowadzenie do programowania sieci Web ASP.NET przy użyciu składni Razor (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="3831d-103">Introduction to ASP.NET Web Programming Using the Razor Syntax (Visual Basic)</span></span>
====================
<span data-ttu-id="3831d-104">przez [FitzMacken niestandardowy](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="3831d-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="3831d-105">W tym artykule zapewnia przegląd programowania ze strony sieci Web ASP.NET przy użyciu składni Razor i Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="3831d-105">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax and Visual Basic.</span></span> <span data-ttu-id="3831d-106">Program ASP.NET to technologia firmy Microsoft do uruchamiania dynamicznych stron sieci web na serwerach sieci web.</span><span class="sxs-lookup"><span data-stu-id="3831d-106">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span>
> 
> <span data-ttu-id="3831d-107">**Dowiesz się**:</span><span class="sxs-lookup"><span data-stu-id="3831d-107">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="3831d-108">8 top, programowania porady dotyczące wprowadzenie do programowania przy użyciu składni Razor strony sieci Web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="3831d-108">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="3831d-109">Podstawowe pojęcia programowania, które będą potrzebne.</span><span class="sxs-lookup"><span data-stu-id="3831d-109">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="3831d-110">Jakie kodu serwera ASP.NET o składni Razor są wszystkie informacje.</span><span class="sxs-lookup"><span data-stu-id="3831d-110">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="3831d-111">Wersje oprogramowania</span><span class="sxs-lookup"><span data-stu-id="3831d-111">Software versions</span></span>
> 
> 
> - <span data-ttu-id="3831d-112">ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="3831d-112">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="3831d-113">W tym samouczku współdziała również z programu ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="3831d-113">This tutorial also works with ASP.NET Web Pages 2.</span></span>


<span data-ttu-id="3831d-114">Większość przykładów przy użyciu stron ASP.NET Web Pages o składni Razor Użyj C#.</span><span class="sxs-lookup"><span data-stu-id="3831d-114">Most examples of using ASP.NET Web Pages with Razor syntax use C#.</span></span> <span data-ttu-id="3831d-115">Jednak ze składni Razor obsługuje również Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="3831d-115">But the Razor syntax also supports Visual Basic.</span></span> <span data-ttu-id="3831d-116">Do programowania w języku Visual Basic strony sieci web ASP.NET, tworzenia strony sieci web z *.vbhtml* rozszerzenie nazwy pliku, a następnie dodaj kod Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="3831d-116">To program an ASP.NET web page in Visual Basic, you create a web page with a *.vbhtml* filename extension, and then add Visual Basic code.</span></span> <span data-ttu-id="3831d-117">W tym artykule przedstawiono omówienie pracy z języka Visual Basic i tworzenia stron sieci Web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="3831d-117">This article gives you an overview of working with the Visual Basic language and syntax to create ASP.NET Webpages.</span></span>

> [!NOTE]
> <span data-ttu-id="3831d-118">Domyślne szablony witryny sieci Web programu Microsoft WebMatrix (**Bakery**, **galerii fotografii**, i **witryny początkowej**, itd.) są dostępne w wersjach C# i Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="3831d-118">The default website templates for Microsoft WebMatrix (**Bakery**, **Photo Gallery**, and **Starter Site**, etc.) are available in C# and Visual Basic versions.</span></span> <span data-ttu-id="3831d-119">Szablony programu Visual Basic przez można zainstalować jako pakietów NuGet.</span><span class="sxs-lookup"><span data-stu-id="3831d-119">You can install the Visual Basic templates by as NuGet packages.</span></span> <span data-ttu-id="3831d-120">Szablony witryny sieci Web są instalowane w folderze głównym lokacji w folderze o nazwie *Templates Microsoft*.</span><span class="sxs-lookup"><span data-stu-id="3831d-120">Website templates are installed in the root folder of your site in a folder named *Microsoft Templates*.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="3831d-121">Górny 8 porady dotyczące programowania</span><span class="sxs-lookup"><span data-stu-id="3831d-121">The Top 8 Programming Tips</span></span>

<span data-ttu-id="3831d-122">W tej sekcji przedstawiono kilka wskazówek, które należy bezwzględnie znać rozpoczęcie pisania kodu serwera ASP.NET przy użyciu składni Razor.</span><span class="sxs-lookup"><span data-stu-id="3831d-122">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="3831d-123">1. Dodaj kod do strony przy użyciu znaku @</span><span class="sxs-lookup"><span data-stu-id="3831d-123">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="3831d-124">`@` Znak uruchamia wyrażenia w tekście, pojedynczą instrukcją bloki, a wielu instrukcji:</span><span class="sxs-lookup"><span data-stu-id="3831d-124">The `@` character starts inline expressions, single-statement blocks, and multi-statement blocks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample1.vbhtml)]

<span data-ttu-id="3831d-125">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="3831d-125">The result displayed in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-vb/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="3831d-127">**Kodowanie HTML**</span><span class="sxs-lookup"><span data-stu-id="3831d-127">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="3831d-128">Podczas wyświetlania zawartości na stronie za pomocą `@` znak w powyższych przykładach ASP.NET HTML-koduje dane wyjściowe.</span><span class="sxs-lookup"><span data-stu-id="3831d-128">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="3831d-129">Spowoduje to zastąpienie zarezerwowanych znaków HTML (takich jak `<` i `>` i `&`) z kodami umożliwiających znaków, które mają być wyświetlane jako znaki na stronie sieci web, a nie interpretowany jako tagów HTML lub jednostek.</span><span class="sxs-lookup"><span data-stu-id="3831d-129">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="3831d-130">Zakodowany w formacie HTML, dane wyjściowe w kodzie serwera może nie wyświetlać się poprawnie, a może spowodować narażenie strony na zagrożenia bezpieczeństwa.</span><span class="sxs-lookup"><span data-stu-id="3831d-130">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="3831d-131">Celem użytkownika jest output kod znaczników HTML, który renderuje tagi jako kod znaczników (na przykład `<p></p>` akapitu lub `<em></em>` aby podkreślić tekst), zobacz sekcję [łączenie tekstu, znaczników i kodu w bloki kodu](#BM_CombiningTextMarkupAndCode) dalszej części tego artykułu.</span><span class="sxs-lookup"><span data-stu-id="3831d-131">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="3831d-132">Więcej o kodowanie HTML w [Praca z formularzy HTML w witrynach stron sieci Web platformy ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="3831d-132">You can read more about HTML encoding in [Working with HTML Forms in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-with-codeend-code"></a><span data-ttu-id="3831d-133">2. Należy ująć bloków kodu za pomocą kodu... Kod zakończenia:</span><span class="sxs-lookup"><span data-stu-id="3831d-133">2. You enclose code blocks with Code...End Code</span></span>

<span data-ttu-id="3831d-134">Blok kodu zawiera jedną lub więcej instrukcji kodu i znajduje się ze słowami kluczowymi `Code` i `End Code`.</span><span class="sxs-lookup"><span data-stu-id="3831d-134">A code block includes one or more code statements and is enclosed with the keywords `Code` and `End Code`.</span></span> <span data-ttu-id="3831d-135">Umieść otwierający `Code` — słowo kluczowe natychmiast po `@` znaku &#8212; nie może być spacji między nimi.</span><span class="sxs-lookup"><span data-stu-id="3831d-135">Place the opening `Code` keyword immediately after the `@` character &#8212; there can't be whitespace between them.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample2.vbhtml)]

<span data-ttu-id="3831d-136">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="3831d-136">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-vb/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-line-break"></a><span data-ttu-id="3831d-138">3. W bloku kończyć każda instrukcja kodu z podział wiersza</span><span class="sxs-lookup"><span data-stu-id="3831d-138">3. Inside a block, you end each code statement with a line break</span></span>

<span data-ttu-id="3831d-139">W bloku kodu języka Visual Basic każda instrukcja kończy podział wiersza.</span><span class="sxs-lookup"><span data-stu-id="3831d-139">In a Visual Basic code block, each statement ends with a line break.</span></span> <span data-ttu-id="3831d-140">(W dalszej części tego artykułu zobaczysz sposób do opakowywania instrukcję kodu długa na wiele wierszy, jeśli to konieczne.)</span><span class="sxs-lookup"><span data-stu-id="3831d-140">(Later in the article you'll see a way to wrap a long code statement into multiple lines if needed.)</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample3.vbhtml)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="3831d-141">4. W przypadku używania zmiennych do przechowywania wartości</span><span class="sxs-lookup"><span data-stu-id="3831d-141">4. You use variables to store values</span></span>

<span data-ttu-id="3831d-142">Można zapisać wartości w *zmiennej*, w tym ciągów, liczb i daty, itp. Można utworzyć nowej zmiennej przy użyciu `Dim` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="3831d-142">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `Dim` keyword.</span></span> <span data-ttu-id="3831d-143">Można wstawić wartości zmiennych bezpośrednio za pomocą strony `@`.</span><span class="sxs-lookup"><span data-stu-id="3831d-143">You can insert variable values directly in a page using `@`.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample4.vbhtml)]

<span data-ttu-id="3831d-144">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="3831d-144">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-vb/_static/image3.jpg)

### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="3831d-146">5. To ująć w podwójny cudzysłów wartości literału ciągu</span><span class="sxs-lookup"><span data-stu-id="3831d-146">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="3831d-147">A *ciąg* jest sekwencji znaków, które są traktowane jako tekst.</span><span class="sxs-lookup"><span data-stu-id="3831d-147">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="3831d-148">Aby określić ciąg, ująć w podwójny cudzysłów:</span><span class="sxs-lookup"><span data-stu-id="3831d-148">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample5.vbhtml)]

<span data-ttu-id="3831d-149">Aby osadzić podwójny cudzysłów wewnątrz wartości ciągu, Wstaw dwa znaki cudzysłowu.</span><span class="sxs-lookup"><span data-stu-id="3831d-149">To embed double quotation marks within a string value, insert two double quotation mark characters.</span></span> <span data-ttu-id="3831d-150">Znak cudzysłowu pojawią się raz w danych wyjściowych strony, należy wprowadzić go w formie `""` w cudzysłowie ciągu, a jeśli ma być wyświetlany dwa razy, wprowadź go jako `""""` ciągu ujętego w cudzysłów.</span><span class="sxs-lookup"><span data-stu-id="3831d-150">If you want the double quotation character to appear once in the page output, enter it as `""` within the quoted string, and if you want it to appear twice, enter it as `""""` within the quoted string.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample6.vbhtml)]

<span data-ttu-id="3831d-151">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="3831d-151">The result displayed in a browser:</span></span>

![Razor-Img4](introducing-razor-syntax-vb/_static/image4.jpg)

### <a name="6-visual-basic-code-is-not-case-sensitive"></a><span data-ttu-id="3831d-153">6. Kod Visual Basic nie jest uwzględniana wielkość liter</span><span class="sxs-lookup"><span data-stu-id="3831d-153">6. Visual Basic code is not case sensitive</span></span>

<span data-ttu-id="3831d-154">Język Visual Basic nie jest uwzględniana wielkość liter.</span><span class="sxs-lookup"><span data-stu-id="3831d-154">The Visual Basic language is not case sensitive.</span></span> <span data-ttu-id="3831d-155">Słowa kluczowe programowania (takich jak `Dim`, `If`, i `True`) i nazwy zmiennych (takich jak `myString`, lub `subTotal`) mogą być zapisywane w każdym przypadku.</span><span class="sxs-lookup"><span data-stu-id="3831d-155">Programming keywords (like `Dim`, `If`, and `True`) and variable names (like `myString`, or `subTotal`) can be written in any case.</span></span>

<span data-ttu-id="3831d-156">Następujące wiersze kodu przypisać wartości do zmiennej `lastname` przy użyciu małych nazwy i dane wyjściowe wartości zmiennej do strony przy użyciu nazwy wielkie litery.</span><span class="sxs-lookup"><span data-stu-id="3831d-156">The following lines of code assign a value to the variable `lastname` using a lowercase name, and then output the variable value to the page using an uppercase name.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample7.vbhtml)]

<span data-ttu-id="3831d-157">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="3831d-157">The result displayed in a browser:</span></span>

![vb-syntax-5](introducing-razor-syntax-vb/_static/image5.jpg)

### <a name="7-much-of-your-coding-involves-working-with-objects"></a><span data-ttu-id="3831d-159">7. Większość kodowania obejmuje pracy z obiektami</span><span class="sxs-lookup"><span data-stu-id="3831d-159">7. Much of your coding involves working with objects</span></span>

<span data-ttu-id="3831d-160">Obiekt reprezentuje element, który zostanie z &#8212; Strona, pola tekstowego, pliku, obrazu, żądania sieci web, wiadomości e-mail, rekord klienta (wiersza bazy danych), itp. Obiekty mają właściwości, które opisują ich właściwości &#8212; Obiekt pola tekstowego ma `Text` właściwość, obiekt żądania ma `Url` właściwość, wiadomości e-mail ma `From` właściwości oraz obiektu klienta ma `FirstName` właściwości.</span><span class="sxs-lookup"><span data-stu-id="3831d-160">An object represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics &#8212; a text box object has a `Text` property, a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="3831d-161">Obiekty mają również metody, które są &quot;zleceń&quot; mogą wykonywać.</span><span class="sxs-lookup"><span data-stu-id="3831d-161">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="3831d-162">Przykładami obiektu pliku `Save` metodę, obiekt obrazu `Rotate` — metoda i obiektu poczty e-mail `Send` metody.</span><span class="sxs-lookup"><span data-stu-id="3831d-162">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="3831d-163">Często będzie współpracować `Request` obiektu, który zawiera informacje, takie jak wartości formularza pola na stronie (pola tekstowe itp.), jaki typ przeglądarki zgłosił żądanie, adres URL strony tożsamości użytkownika, itp. W tym przykładzie pokazano, jak uzyskać dostępu do właściwości `Request` obiektów i wywoływania `MapPath` metody `Request` obiektu, który daje użytkownikowi bezwzględna ścieżka strony na serwerze:</span><span class="sxs-lookup"><span data-stu-id="3831d-163">You'll often work with the `Request` object, which gives you information like the values of form fields on the page (text boxes, etc.), what type of browser made the request, the URL of the page, the user identity, etc. This example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-vb/samples/sample8.html)]

<span data-ttu-id="3831d-164">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="3831d-164">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-vb/_static/image6.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="3831d-166">8. Można napisać kod, który podejmowania decyzji w procesie</span><span class="sxs-lookup"><span data-stu-id="3831d-166">8. You can write code that makes decisions</span></span>

<span data-ttu-id="3831d-167">Kluczową funkcją dynamicznej stron sieci web jest, że można określić, co zrobić, na podstawie warunków.</span><span class="sxs-lookup"><span data-stu-id="3831d-167">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="3831d-168">Najczęściej w tym celu jest z `If` instrukcji (i opcjonalnie `Else` instrukcji).</span><span class="sxs-lookup"><span data-stu-id="3831d-168">The most common way to do this is with the `If` statement (and optional `Else` statement).</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample9.vbhtml)]

<span data-ttu-id="3831d-169">Instrukcja `If IsPost` jest skrócona sposobem zapisu `If IsPost = True`.</span><span class="sxs-lookup"><span data-stu-id="3831d-169">The statement `If IsPost` is a shorthand way of writing `If IsPost = True`.</span></span> <span data-ttu-id="3831d-170">Wraz z programem `If` instrukcji, istnieje wiele sposobów, aby przetestować warunki, powtórz bloki kodu, i itd., które są opisane w dalszej części tego artykułu.</span><span class="sxs-lookup"><span data-stu-id="3831d-170">Along with `If` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="3831d-171">Wynik wyświetlony w przeglądarce (po kliknięciu przycisku **przesyłania**):</span><span class="sxs-lookup"><span data-stu-id="3831d-171">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor-Img6](introducing-razor-syntax-vb/_static/image7.jpg)

> [!TIP] 
> 
> <span data-ttu-id="3831d-173">**HTTP GET i POST metod i właściwości IsPost**</span><span class="sxs-lookup"><span data-stu-id="3831d-173">**HTTP GET and POST Methods and the IsPost Property**</span></span>
> 
> <span data-ttu-id="3831d-174">Protokół używany do stron sieci web (HTTP) obsługuje bardzo ograniczoną liczbę metod (&quot;zleceń&quot;) służące do wysyłać żądania do serwera.</span><span class="sxs-lookup"><span data-stu-id="3831d-174">The protocol used for web pages (HTTP) supports a very limited number of methods (&quot;verbs&quot;) that are used to make requests to the server.</span></span> <span data-ttu-id="3831d-175">Dwa najbardziej typowe to GET, który jest używany do odczytu stronę, i POST, który jest używany do przesyłania strony.</span><span class="sxs-lookup"><span data-stu-id="3831d-175">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="3831d-176">Ogólnie rzecz biorąc po raz pierwszy użytkownik zażąda strony, strony jest wymagany przy użyciu GET.</span><span class="sxs-lookup"><span data-stu-id="3831d-176">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="3831d-177">Jeśli użytkownik wpisuje do formularza, a następnie klika przycisk **przesyłania**, przeglądarka wysyła żądanie POST do serwera.</span><span class="sxs-lookup"><span data-stu-id="3831d-177">If the user fills in a form and then clicks **Submit**, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="3831d-178">W programowanie dla sieci web często jest grupowaniu można sprawdzić, czy strona jest wymagany jako GET lub POST, aby wiedzieć, jak przetwarzania tej strony.</span><span class="sxs-lookup"><span data-stu-id="3831d-178">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="3831d-179">W składniku ASP.NET Web Pages można użyć `IsPost` właściwości, aby zobaczyć, czy żądanie jest GET lub POST.</span><span class="sxs-lookup"><span data-stu-id="3831d-179">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="3831d-180">Jeśli żądanie jest żądaniem POST `IsPost` właściwości zwróci wartość true, a użytkownik może wykonywać następujące czynności odczytu wartości pól tekstowych w formularzu.</span><span class="sxs-lookup"><span data-stu-id="3831d-180">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="3831d-181">Wiele przykładów zobaczysz opisano sposób przetwarzania strony inaczej w zależności od wartości `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="3831d-181">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="3831d-182">Prosty przykład kodu</span><span class="sxs-lookup"><span data-stu-id="3831d-182">A Simple Code Example</span></span>

<span data-ttu-id="3831d-183">Tej procedury przedstawiono sposób tworzenia strony, która ilustruje podstawowych technik programowania.</span><span class="sxs-lookup"><span data-stu-id="3831d-183">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="3831d-184">W tym przykładzie utworzysz strony, który pozwala użytkownikom na wprowadzanie dwóch liczb, a następnie dodanie ich i wyświetla wyniki.</span><span class="sxs-lookup"><span data-stu-id="3831d-184">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="3831d-185">W edytorze, Utwórz nowy plik i nadaj jej nazwę *AddNumbers.vbhtml*.</span><span class="sxs-lookup"><span data-stu-id="3831d-185">In your editor, create a new file and name it *AddNumbers.vbhtml*.</span></span>
2. <span data-ttu-id="3831d-186">Skopiuj następujący kod i znaczników do strony, zastępując wszystko już na stronie.</span><span class="sxs-lookup"><span data-stu-id="3831d-186">Copy the following code and markup into the page, replacing anything already in the page.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample10.vbhtml)]

    <span data-ttu-id="3831d-187">Poniżej przedstawiono niektóre czynności umożliwiające należy zwrócić uwagę:</span><span class="sxs-lookup"><span data-stu-id="3831d-187">Here are some things for you to note:</span></span>

    - <span data-ttu-id="3831d-188">`@` Znak rozpoczyna się pierwszego bloku kodu na stronie i poprzedza `totalMessage` zmiennej osadzonych u dołu.</span><span class="sxs-lookup"><span data-stu-id="3831d-188">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable embedded near the bottom.</span></span>
    - <span data-ttu-id="3831d-189">Blok w górnej części strony jest ujęta w `Code...End Code`.</span><span class="sxs-lookup"><span data-stu-id="3831d-189">The block at the top of the page is enclosed in `Code...End Code`.</span></span>
    - <span data-ttu-id="3831d-190">Zmienne `total`, `num1`, `num2`, i `totalMessage` przechowywać wiele numerów i ciąg.</span><span class="sxs-lookup"><span data-stu-id="3831d-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="3831d-191">Wartość literału ciągu przypisane do `totalMessage` zmienna jest w podwójny cudzysłów.</span><span class="sxs-lookup"><span data-stu-id="3831d-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="3831d-192">Ponieważ kod Visual Basic nie jest uwzględniana wielkość liter, gdy `totalMessage` zmienna jest używana w dolnej części strony, jego nazwa tylko musi być zgodna pisownię deklaracja zmiennej w górnej części strony.</span><span class="sxs-lookup"><span data-stu-id="3831d-192">Because Visual Basic code is not case sensitive, when the `totalMessage` variable is used near the bottom of the page, its name only needs to match the spelling of the variable declaration at the top of the page.</span></span> <span data-ttu-id="3831d-193">Wielkość liter nie ma znaczenia.</span><span class="sxs-lookup"><span data-stu-id="3831d-193">The casing doesn't matter.</span></span>
    - <span data-ttu-id="3831d-194">Wyrażenie `num1.AsInt()`  +  `num2.AsInt()` pokazano, jak pracować z obiektów i metod.</span><span class="sxs-lookup"><span data-stu-id="3831d-194">The expression `num1.AsInt()` + `num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="3831d-195">`AsInt` Metody dla każdej zmiennej konwertuje ciąg wprowadzonej przez użytkownika na liczbę całkowitą (liczba całkowita), który można dodać.</span><span class="sxs-lookup"><span data-stu-id="3831d-195">The `AsInt` method on each variable converts the string entered by a user to a whole number (an integer) that can be added.</span></span>
    - <span data-ttu-id="3831d-196">`<form>` Zawiera tag `method="post"` atrybutu.</span><span class="sxs-lookup"><span data-stu-id="3831d-196">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="3831d-197">Oznacza to, że gdy użytkownik kliknie **Dodaj**, strony zostaną wysłane do serwera przy użyciu metody POST protokołu HTTP.</span><span class="sxs-lookup"><span data-stu-id="3831d-197">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="3831d-198">Po przesłaniu strony, kod `If IsPost` daje w wyniku wartość true i warunkowe kod działa wyświetlania wyniku dodawania liczb.</span><span class="sxs-lookup"><span data-stu-id="3831d-198">When the page is submitted, the code `If IsPost` evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="3831d-199">Strony i uruchom go w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="3831d-199">Save the page and run it in a browser.</span></span> <span data-ttu-id="3831d-200">(Upewnij się, że strona jest zaznaczona w **pliki** obszar roboczy przed jej uruchomieniem.) Wprowadź dwie liczb całkowitych, a następnie kliknij przycisk **Dodaj** przycisku.</span><span class="sxs-lookup"><span data-stu-id="3831d-200">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span>

    ![Razor-Img7](introducing-razor-syntax-vb/_static/image8.jpg)

## <a name="visual-basic-language-and-syntax"></a><span data-ttu-id="3831d-202">Język Visual Basic i składni</span><span class="sxs-lookup"><span data-stu-id="3831d-202">Visual Basic Language and Syntax</span></span>

<span data-ttu-id="3831d-203">Widać wcześniej podstawowy przykład sposobu tworzenia strony sieci web platformy ASP.NET i jak można dodać kod serwera do kod znaczników HTML.</span><span class="sxs-lookup"><span data-stu-id="3831d-203">Earlier you saw a basic example of how to create an ASP.NET web page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="3831d-204">Tutaj dowiesz się podstawy pisanie kodu serwera ASP.NET przy użyciu składni Razor &#8212; przy użyciu języka Visual Basic oznacza to, że programowania reguł języka.</span><span class="sxs-lookup"><span data-stu-id="3831d-204">Here you'll learn the basics of using Visual Basic to write ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="3831d-205">Jeśli masz doświadczenia w pracy z programowania (zwłaszcza, jeśli używano C, C++, C#, Visual Basic lub JavaScript), większość tutaj odczytu jest znane.</span><span class="sxs-lookup"><span data-stu-id="3831d-205">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="3831d-206">Prawdopodobnie należy zaznajomić się tylko z jak program WebMatrix kodu jest dodawana do kodu znaczników w *.vbhtml* plików.</span><span class="sxs-lookup"><span data-stu-id="3831d-206">You'll probably need to familiarize yourself only with how WebMatrix code is added to markup in *.vbhtml* files.</span></span>

### <a id="BM_CombiningTextMarkupAndCode"></a><span data-ttu-id="3831d-207">Łączenie tekstu, znaczników i kodu w bloki kodu</span><span class="sxs-lookup"><span data-stu-id="3831d-207">Combining text, markup, and code in code blocks</span></span>

<span data-ttu-id="3831d-208">W blokach kodu serwera należy często output tekst i znacznik ze stroną.</span><span class="sxs-lookup"><span data-stu-id="3831d-208">In server code blocks, you'll often want to output text and markup to the page.</span></span> <span data-ttu-id="3831d-209">Jeśli blok kodu serwera zawiera tekst, który nie jest kodem i który zamiast tego ma być renderowany jako jest, ASP.NET musi mieć możliwość rozróżnienia ten tekst z kodu.</span><span class="sxs-lookup"><span data-stu-id="3831d-209">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="3831d-210">Istnieje kilka sposobów, aby to zrobić.</span><span class="sxs-lookup"><span data-stu-id="3831d-210">There are several ways to do this.</span></span>

- <span data-ttu-id="3831d-211">Tekst należy ująć w elemencie bloku HTML, takie jak `<p></p>` lub `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="3831d-211">Enclose the text in an HTML block element like `<p></p>` or `<em></em>`:</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample11.vbhtml)]

    <span data-ttu-id="3831d-212">HTML element mogą zawierać tekst, dodatkowe elementy HTML i wyrażenia kod serwera.</span><span class="sxs-lookup"><span data-stu-id="3831d-212">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="3831d-213">Gdy ASP.NET widzi otwierający HTML tag (na przykład `<p>`), wszystko renderowania elementu i jego zawartości jako polega na przeglądarce (i jest rozpoznawana wyrażenia kod serwera).</span><span class="sxs-lookup"><span data-stu-id="3831d-213">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything the element and its content as is to the browser (and resolves the server-code expressions).</span></span>

- <span data-ttu-id="3831d-214">Użyj `@:` operator lub `<text>` elementu.</span><span class="sxs-lookup"><span data-stu-id="3831d-214">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="3831d-215">`@:` Generuje pojedynczy wiersz zawartości zawierającej zwykłego tekstu lub niedopasowane znaczniki HTML; `<text>` elementu obejmuje wiele wierszy danych wyjściowych.</span><span class="sxs-lookup"><span data-stu-id="3831d-215">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="3831d-216">Te opcje są przydatne, jeśli nie chcesz do renderowania elementu HTML jako część danych wyjściowych.</span><span class="sxs-lookup"><span data-stu-id="3831d-216">These options are useful when you don't want to render an HTML element as part of the output.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample12.vbhtml)]

    <span data-ttu-id="3831d-217">W poniższym przykładzie jest powtarzany poprzednim przykładzie, ale używa jednej pary `<text>` znaczniki, aby umieścić tekst do renderowania.</span><span class="sxs-lookup"><span data-stu-id="3831d-217">The following example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample13.vbhtml)]

    <span data-ttu-id="3831d-218">W poniższym przykładzie `<text>` i `</text>` tagi należy ująć w trzy wiersze, które mają niektóre uncontained tekst i niedopasowane znaczniki HTML (`<br />`), wraz z kodu serwera i pasujących tagów HTML.</span><span class="sxs-lookup"><span data-stu-id="3831d-218">In the following example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="3831d-219">Ponownie, można także poprzedzić każdego wiersza za `@:` operator; albo sposób działania.</span><span class="sxs-lookup"><span data-stu-id="3831d-219">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample14.vbhtml)]

    > [!NOTE]
    > <span data-ttu-id="3831d-220">Podczas drukowania tekstu opisane w tej sekcji &#8212; za pomocą elementu HTML `@:` , operator lub `<text>` element &#8212; Program ASP.NET nie kodowanie HTML dane wyjściowe.</span><span class="sxs-lookup"><span data-stu-id="3831d-220">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="3831d-221">(Jak wspomniano wcześniej, ASP.NET zakodować dane wyjściowe wyrażenia kodu serwera i serwera bloki kodu, które są poprzedzone `@`, z wyjątkiem przypadków wymienionych w tej sekcji.)</span><span class="sxs-lookup"><span data-stu-id="3831d-221">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="3831d-222">Odstępu</span><span class="sxs-lookup"><span data-stu-id="3831d-222">Whitespace</span></span>

<span data-ttu-id="3831d-223">Dodatkowe spacje w instrukcji (i poza literału ciągu) nie wpływają na instrukcji:</span><span class="sxs-lookup"><span data-stu-id="3831d-223">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample15.vbhtml)]

### <a name="breaking-long-statements-into-multiple-lines"></a><span data-ttu-id="3831d-224">Fundamentalne długi instrukcje do wielu linii</span><span class="sxs-lookup"><span data-stu-id="3831d-224">Breaking long statements into multiple lines</span></span>

<span data-ttu-id="3831d-225">Instrukcję kodu długo można podzielić wielu wierszy za pomocą znaku podkreślenia `_` (w języku Visual Basic nazywany *znak kontynuacji*) po każdym wierszu kodu.</span><span class="sxs-lookup"><span data-stu-id="3831d-225">You can break a long code statement into multiple lines by using the underscore character `_` (which in Visual Basic is called the *continuation character*) after each line of code.</span></span> <span data-ttu-id="3831d-226">Aby przerwać instrukcję do następnego wiersza na koniec wiersza dodać miejsce, a następnie znak kontynuacji.</span><span class="sxs-lookup"><span data-stu-id="3831d-226">To break a statement onto the next line, at the end of the line add a space and then the continuation character.</span></span> <span data-ttu-id="3831d-227">Continue — instrukcja w następnym wierszu.</span><span class="sxs-lookup"><span data-stu-id="3831d-227">Continue the statement on the next line.</span></span> <span data-ttu-id="3831d-228">Można zawijać instrukcje na musisz poprawić czytelność dowolną liczbę wierszy.</span><span class="sxs-lookup"><span data-stu-id="3831d-228">You can wrap statements onto as many lines as you need to improve readability.</span></span> <span data-ttu-id="3831d-229">Poniższe instrukcje są takie same:</span><span class="sxs-lookup"><span data-stu-id="3831d-229">The following statements are the same:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample16.vbhtml)]

<span data-ttu-id="3831d-230">Jednak nie można powielać wiersza środku literału ciągu.</span><span class="sxs-lookup"><span data-stu-id="3831d-230">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="3831d-231">Poniższy przykład nie działa:</span><span class="sxs-lookup"><span data-stu-id="3831d-231">The following example doesn't work:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample17.vbhtml)]

<span data-ttu-id="3831d-232">Aby połączyć długi ciąg, który opakowuje do wielu linii, takich jak powyżej kodu, należy użyć *operatora łączenia* (`&`), który pojawi się w dalszej części tego artykułu.</span><span class="sxs-lookup"><span data-stu-id="3831d-232">To combine a long string that wraps to multiple lines like the above code, you would need to use the *concatenation operator* (`&`), which you'll see later in this article.</span></span>

### <a name="code-comments"></a><span data-ttu-id="3831d-233">Komentarze w kodzie</span><span class="sxs-lookup"><span data-stu-id="3831d-233">Code comments</span></span>

<span data-ttu-id="3831d-234">Komentarze pozwalają pozostaw notatek dla siebie lub innych użytkowników.</span><span class="sxs-lookup"><span data-stu-id="3831d-234">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="3831d-235">Komentarze składni razor mają przedrostek `@*` się i kończyć `*@`.</span><span class="sxs-lookup"><span data-stu-id="3831d-235">Razor syntax comments are prefixed with `@*` and end with `*@`.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-vb/samples/sample18.cshtml)]

<span data-ttu-id="3831d-236">Wewnątrz bloków kodu można użyć składni Razor komentarze lub skorzystać z zwykłej Visual Basic komentarz znak pojedynczego cudzysłowu (`'`) i jest poprzedzony do każdego wiersza.</span><span class="sxs-lookup"><span data-stu-id="3831d-236">Within code blocks you can use the Razor syntax comments, or you can use ordinary Visual Basic comment character, which is a single quote (`'`) prefixed to each line.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample19.vbhtml)]

## <a name="variables"></a><span data-ttu-id="3831d-237">Zmienne</span><span class="sxs-lookup"><span data-stu-id="3831d-237">Variables</span></span>

<span data-ttu-id="3831d-238">Zmienna jest nazwanego obiektu, który służy do przechowywania danych.</span><span class="sxs-lookup"><span data-stu-id="3831d-238">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="3831d-239">Zmienne można nazwy wszystko, ale nazwa musi zaczynać się znakiem alfabetycznym i nie może zawierać spacji ani znaków zastrzeżonych.</span><span class="sxs-lookup"><span data-stu-id="3831d-239">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span> <span data-ttu-id="3831d-240">W języku Visual Basic jak przedstawiono wcześniej, nie ma znaczenia wielkość liter w nazwie zmiennej.</span><span class="sxs-lookup"><span data-stu-id="3831d-240">In Visual Basic, as you saw earlier, the case of the letters in a variable name doesn't matter.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="3831d-241">Zmienne i typy danych</span><span class="sxs-lookup"><span data-stu-id="3831d-241">Variables and data types</span></span>

<span data-ttu-id="3831d-242">Zmienna może mieć określonego typu danych, co oznacza, jakiego rodzaju dane są przechowywane w zmiennej.</span><span class="sxs-lookup"><span data-stu-id="3831d-242">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="3831d-243">Mogą mieć zmiennych ciągu, których są przechowywane wartości ciągów (takich jak &quot;Hello world&quot;), zmienne liczba całkowita, które przechowują liczby całkowitej wartości (na przykład 3 lub 79) i zmienne Data przechowywanie wartości daty w różnych formatach (na przykład 4 12/2012 lub marca 2009 ).</span><span class="sxs-lookup"><span data-stu-id="3831d-243">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="3831d-244">I istnieje wiele typów danych, w których można użyć.</span><span class="sxs-lookup"><span data-stu-id="3831d-244">And there are many other data types you can use.</span></span>

<span data-ttu-id="3831d-245">Jednak nie trzeba określać typ zmiennej.</span><span class="sxs-lookup"><span data-stu-id="3831d-245">However, you don't have to specify a type for a variable.</span></span> <span data-ttu-id="3831d-246">W większości przypadków ASP.NET można ustalić typu, w oparciu sposobu używania danych za pomocą zmiennej.</span><span class="sxs-lookup"><span data-stu-id="3831d-246">In most cases ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="3831d-247">(Czasami musi określać typ; pojawi się przykłady gdzie to PRAWDA).</span><span class="sxs-lookup"><span data-stu-id="3831d-247">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="3831d-248">Aby zadeklarować zmiennej bez określania typu, należy użyć `Dim` oraz nazwę zmiennej (na przykład `Dim myVar`).</span><span class="sxs-lookup"><span data-stu-id="3831d-248">To declare a variable without specifying a type, use `Dim` plus the variable name (for instance, `Dim myVar`).</span></span> <span data-ttu-id="3831d-249">Aby zadeklarować zmiennej typu, należy użyć `Dim` oraz nazwę zmiennej, a następnie `As` , a następnie wpisz nazwę (na przykład `Dim myVar As String`).</span><span class="sxs-lookup"><span data-stu-id="3831d-249">To declare a variable with a type, use `Dim` plus the variable name, followed by `As` and then the type name (for instance, `Dim myVar As String`).</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample20.vbhtml)]

<span data-ttu-id="3831d-250">W poniższym przykładzie przedstawiono niektóre wbudowane wyrażeń zmienne na stronie sieci web.</span><span class="sxs-lookup"><span data-stu-id="3831d-250">The following example shows some inline expressions that use the variables in a web page.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample21.vbhtml)]

<span data-ttu-id="3831d-251">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="3831d-251">The result displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-vb/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="3831d-253">Konwertowanie i testowania typy danych</span><span class="sxs-lookup"><span data-stu-id="3831d-253">Converting and testing data types</span></span>

<span data-ttu-id="3831d-254">Mimo że program ASP.NET zazwyczaj można automatycznie określić typu danych, czasami nie jest.</span><span class="sxs-lookup"><span data-stu-id="3831d-254">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="3831d-255">W związku z tym konieczne może pomóc ASP.NET, wykonując jawnej konwersji.</span><span class="sxs-lookup"><span data-stu-id="3831d-255">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="3831d-256">Nawet jeśli nie masz konwersji typów, czasami warto test, aby sprawdzić, jakiego typu dane mogą być pracy z.</span><span class="sxs-lookup"><span data-stu-id="3831d-256">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="3831d-257">Najbardziej często zdarza się, czy masz do przekonwertowania ciągu na inny typ, takich jak do liczby całkowitej lub daty.</span><span class="sxs-lookup"><span data-stu-id="3831d-257">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="3831d-258">W poniższym przykładzie przedstawiono typowy przypadek, w którym należy przekonwertować ciąg na liczbę.</span><span class="sxs-lookup"><span data-stu-id="3831d-258">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample22.vbhtml)]

<span data-ttu-id="3831d-259">Z reguły dane wejściowe użytkownika dostarczany jako ciągi.</span><span class="sxs-lookup"><span data-stu-id="3831d-259">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="3831d-260">Nawet jeśli została monituje użytkownika o wprowadzenie numeru, a nawet wtedy, gdy została wprowadzona cyfr, po przesłaniu danych wejściowych użytkownika, a następnie go odczytać w kodzie, dane są w formacie ciągu.</span><span class="sxs-lookup"><span data-stu-id="3831d-260">Even if you've prompted the user to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="3831d-261">W związku z tym należy przekonwertować ciągu na liczbę.</span><span class="sxs-lookup"><span data-stu-id="3831d-261">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="3831d-262">W tym przykładzie podjęcie próby wykonania arytmetycznego na wartościach bez konwertowania, następujący błąd wyników, ponieważ program ASP.NET nie można dodać dwa ciągi:</span><span class="sxs-lookup"><span data-stu-id="3831d-262">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

`Cannot implicitly convert type 'string' to 'int'.`

<span data-ttu-id="3831d-263">Aby dokonać konwersji wartości na liczby całkowite, należy wywołać `AsInt` metody.</span><span class="sxs-lookup"><span data-stu-id="3831d-263">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="3831d-264">Konwersja zakończy się pomyślnie, można następnie dodać liczb.</span><span class="sxs-lookup"><span data-stu-id="3831d-264">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="3831d-265">W poniższej tabeli wymieniono niektóre typowe metody konwersji i testowania dla zmiennych.</span><span class="sxs-lookup"><span data-stu-id="3831d-265">The following table lists some common conversion and test methods for variables.</span></span>

| <span data-ttu-id="3831d-266">**— Metoda**</span><span class="sxs-lookup"><span data-stu-id="3831d-266">**Method**</span></span> | <span data-ttu-id="3831d-267">**Opis**</span><span class="sxs-lookup"><span data-stu-id="3831d-267">**Description**</span></span> | <span data-ttu-id="3831d-268">**Przykład**</span><span class="sxs-lookup"><span data-stu-id="3831d-268">**Example**</span></span> |
| --- | --- | --- |
| `AsInt(), IsInt()` | <span data-ttu-id="3831d-269">Konwertuje ciąg reprezentujący liczbę całkowitą z zakresu (takie jak &quot;593&quot;) na liczbę całkowitą.</span><span class="sxs-lookup"><span data-stu-id="3831d-269">Converts a string that represents a whole number (like &quot;593&quot;) to an integer.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample23.vb)] |
| `AsBool(), IsBool()` | <span data-ttu-id="3831d-270">Konwertuje ciąg, takich jak &quot;true&quot; lub &quot;false&quot; na typ Boolean.</span><span class="sxs-lookup"><span data-stu-id="3831d-270">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample24.vb)] |
| `AsFloat(), IsFloat()` | <span data-ttu-id="3831d-271">Konwertuje ciąg o wartości dziesiętnej, takich jak &quot;1.3&quot; lub &quot;7.439&quot; liczby zmiennoprzecinkowej.</span><span class="sxs-lookup"><span data-stu-id="3831d-271">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample25.vb)] |
| `AsDecimal(), IsDecimal()` | <span data-ttu-id="3831d-272">Konwertuje ciąg o wartości dziesiętnej, takich jak &quot;1.3&quot; lub &quot;7.439&quot; na liczbę dziesiętną.</span><span class="sxs-lookup"><span data-stu-id="3831d-272">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="3831d-273">(W programie ASP.NET, liczbą dziesiętną jest bardziej dokładne niż liczba zmiennoprzecinkowa).</span><span class="sxs-lookup"><span data-stu-id="3831d-273">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample26.vb)] |
| `AsDateTime(), IsDateTime()` | <span data-ttu-id="3831d-274">Konwertuje ciąg reprezentujący wartość daty i godziny do platformy ASP.NET `DateTime` typu.</span><span class="sxs-lookup"><span data-stu-id="3831d-274">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample27.vb)] |
| `ToString()` | <span data-ttu-id="3831d-275">Konwertuje ciąg inny typ danych.</span><span class="sxs-lookup"><span data-stu-id="3831d-275">Converts any other data type to a string.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample28.vb)] |

## <a name="operators"></a><span data-ttu-id="3831d-276">Operatory</span><span class="sxs-lookup"><span data-stu-id="3831d-276">Operators</span></span>

<span data-ttu-id="3831d-277">Operator jest słowo kluczowe lub znak, który informuje ASP.NET, jakiego rodzaju polecenie do wykonania w wyrażeniu.</span><span class="sxs-lookup"><span data-stu-id="3831d-277">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="3831d-278">Visual Basic obsługuje wielu operatorów, ale musisz rozpoznać kilka, aby rozpocząć tworzenie stron sieci web programu ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="3831d-278">Visual Basic supports many operators, but you only need to recognize a few to get started developing ASP.NET web pages.</span></span> <span data-ttu-id="3831d-279">Poniższa tabela zawiera podsumowanie typowych operatorów.</span><span class="sxs-lookup"><span data-stu-id="3831d-279">The following table summarizes the most common operators.</span></span>

| <span data-ttu-id="3831d-280">**Operator**</span><span class="sxs-lookup"><span data-stu-id="3831d-280">**Operator**</span></span> | <span data-ttu-id="3831d-281">**Opis**</span><span class="sxs-lookup"><span data-stu-id="3831d-281">**Description**</span></span> | <span data-ttu-id="3831d-282">**Przykłady**</span><span class="sxs-lookup"><span data-stu-id="3831d-282">**Examples**</span></span> |
| --- | --- | --- |
| `+ - * /` | <span data-ttu-id="3831d-283">Operatory matematyczne używać w wyrażeniach numerycznych.</span><span class="sxs-lookup"><span data-stu-id="3831d-283">Math operators used in numerical expressions.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample29.vb)] |
| `=` | <span data-ttu-id="3831d-284">Przypisanie i równości.</span><span class="sxs-lookup"><span data-stu-id="3831d-284">Assignment and equality.</span></span> <span data-ttu-id="3831d-285">W zależności od kontekstu przypisuje wartość po prawej stronie instrukcji do obiektu po lewej stronie, lub sprawdza wartości pod kątem równości.</span><span class="sxs-lookup"><span data-stu-id="3831d-285">Depending on context, either assigns the value on the right side of a statement to the object on the left side, or checks the values for equality.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample30.vb)] |
| `<>` | <span data-ttu-id="3831d-286">Nierówności.</span><span class="sxs-lookup"><span data-stu-id="3831d-286">Inequality.</span></span> <span data-ttu-id="3831d-287">Zwraca `True` wartości nie są równe.</span><span class="sxs-lookup"><span data-stu-id="3831d-287">Returns `True` if the values are not equal.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample31.vb)] |
| `< > <= >=` | <span data-ttu-id="3831d-288">Mniej niż większe niż, mniejsze niż lub równy, a mniejsza.</span><span class="sxs-lookup"><span data-stu-id="3831d-288">Less than, greater than, less than or equal, and greater than or equal.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample32.vb)] |
| `&` | <span data-ttu-id="3831d-289">Łączenie, który jest używany do przyłączenia ciągów.</span><span class="sxs-lookup"><span data-stu-id="3831d-289">Concatenation, which is used to join strings.</span></span> | [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample33.vbhtml)] |
| `+= -=` | <span data-ttu-id="3831d-290">Operatory inkrementacji i dekrementacji, które dodawania i odejmowania 1 (odpowiednio) ze zmienną.</span><span class="sxs-lookup"><span data-stu-id="3831d-290">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample34.vb)] |
| `.` | <span data-ttu-id="3831d-291">Kropki.</span><span class="sxs-lookup"><span data-stu-id="3831d-291">Dot.</span></span> <span data-ttu-id="3831d-292">Pozwala odróżnić obiektów i ich właściwości i metody.</span><span class="sxs-lookup"><span data-stu-id="3831d-292">Used to distinguish objects and their properties and methods.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample35.vb)] |
| `()` | <span data-ttu-id="3831d-293">Nawiasy.</span><span class="sxs-lookup"><span data-stu-id="3831d-293">Parentheses.</span></span> <span data-ttu-id="3831d-294">Używany do wyrażenia grupy do przekazania parametrów do metod i do dostępu do członków kolekcji i tablic.</span><span class="sxs-lookup"><span data-stu-id="3831d-294">Used to group expressions, to pass parameters to methods, and to access members of arrays and collections.</span></span> | [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample36.vbhtml)] |
| `Not` | <span data-ttu-id="3831d-295">Nie.</span><span class="sxs-lookup"><span data-stu-id="3831d-295">Not.</span></span> <span data-ttu-id="3831d-296">Odwraca wartość true, FALSE i odwrotnie.</span><span class="sxs-lookup"><span data-stu-id="3831d-296">Reverses a true value to false and vice versa.</span></span> <span data-ttu-id="3831d-297">Zazwyczaj używany jako sposób skrócona do testowania `False` (oznacza to, aby nie `True`).</span><span class="sxs-lookup"><span data-stu-id="3831d-297">Typically used as a shorthand way to test for `False` (that is, for not `True`).</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample37.vb)] |
| `AndAlso OrElse` | <span data-ttu-id="3831d-298">Logiczny AND i lub, w którym są używane do łączenia ze sobą warunki.</span><span class="sxs-lookup"><span data-stu-id="3831d-298">Logical AND and OR, which are used to link conditions together.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample38.vb)] |

## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="3831d-299">Praca z pliku i ścieżki folderu w kodzie</span><span class="sxs-lookup"><span data-stu-id="3831d-299">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="3831d-300">Ścieżki plików i folderów będzie często współpracować w kodzie.</span><span class="sxs-lookup"><span data-stu-id="3831d-300">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="3831d-301">Oto przykład struktury folderu fizycznego dla witryny sieci Web może pojawiać się na komputerze deweloperskim:</span><span class="sxs-lookup"><span data-stu-id="3831d-301">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="3831d-302">Oto niektóre istotne szczegóły dotyczące adresów URL i ścieżki:</span><span class="sxs-lookup"><span data-stu-id="3831d-302">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="3831d-303">Adres URL rozpoczyna się od jedną nazwę domeny (`http://www.example.com`) lub nazwy serwera (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="3831d-303">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="3831d-304">Adres URL odnosi się do ścieżki fizycznej na komputerze hosta.</span><span class="sxs-lookup"><span data-stu-id="3831d-304">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="3831d-305">Na przykład `http://myserver` może odpowiadać do folderu *C:\websites\mywebsite* na serwerze.</span><span class="sxs-lookup"><span data-stu-id="3831d-305">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="3831d-306">Ścieżka wirtualna jest skrócona do reprezentowania ścieżek w kodzie bez konieczności określania pełną ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="3831d-306">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="3831d-307">Zawiera części adresu URL, który następuje nazwa domeny lub tego serwera.</span><span class="sxs-lookup"><span data-stu-id="3831d-307">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="3831d-308">Użycie ścieżki wirtualne, można przenieść do innej domeny lub serwer kodu bez konieczności aktualizacji ścieżki.</span><span class="sxs-lookup"><span data-stu-id="3831d-308">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="3831d-309">Oto przykład, aby ułatwić zrozumienie różnic:</span><span class="sxs-lookup"><span data-stu-id="3831d-309">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="3831d-310">Pełny adres URL</span><span class="sxs-lookup"><span data-stu-id="3831d-310">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="3831d-311">Nazwa serwera</span><span class="sxs-lookup"><span data-stu-id="3831d-311">Server name</span></span> | <span data-ttu-id="3831d-312">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="3831d-312">*mycompanyserver*</span></span> |
| <span data-ttu-id="3831d-313">Ścieżka wirtualna</span><span class="sxs-lookup"><span data-stu-id="3831d-313">Virtual path</span></span> | <span data-ttu-id="3831d-314">*/humanresources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="3831d-314">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="3831d-315">Ścieżka fizyczna</span><span class="sxs-lookup"><span data-stu-id="3831d-315">Physical path</span></span> | <span data-ttu-id="3831d-316">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="3831d-316">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="3831d-317">Wirtualnego katalogu głównego jest /, podobnie jak w folderze głównym dysku c dysku \.</span><span class="sxs-lookup"><span data-stu-id="3831d-317">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="3831d-318">(Folder wirtualny ścieżek zawsze używać ukośniki). Ścieżka wirtualna folderu nie musi mieć taką samą nazwę jak folder fizycznych; może to być alias.</span><span class="sxs-lookup"><span data-stu-id="3831d-318">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="3831d-319">(Na serwerach produkcyjnych, ścieżka wirtualna rzadko zgodna dokładnej ścieżki fizycznej.)</span><span class="sxs-lookup"><span data-stu-id="3831d-319">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="3831d-320">Podczas pracy z plikami i folderami w kodzie, czasami należy odwoływać się do ścieżki fizycznej, a czasami ścieżki wirtualnej, w zależności od tego, jakie obiekty użytkownik pracuje.</span><span class="sxs-lookup"><span data-stu-id="3831d-320">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="3831d-321">ASP.NET udostępnia te narzędzia do pracy z ścieżek plików i folderów w kodzie: `Server.MapPath` metody i `~` operatora i `Href` metody.</span><span class="sxs-lookup"><span data-stu-id="3831d-321">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="3831d-322">Konwertowanie ścieżki wirtualnej do fizycznego: Metoda Server.MapPath</span><span class="sxs-lookup"><span data-stu-id="3831d-322">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="3831d-323">`Server.MapPath` Metoda konwertuje ścieżki wirtualnej (takie jak */default.cshtml*) do bezwzględna ścieżka fizyczna (takich jak *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="3831d-323">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="3831d-324">Możesz użyć tej metody w każdej chwili należy pełną ścieżkę fizyczną.</span><span class="sxs-lookup"><span data-stu-id="3831d-324">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="3831d-325">Typowym przykładem jest podczas odczytywania lub zapisywania pliku tekstowego lub pliku obrazu na serwerze sieci web.</span><span class="sxs-lookup"><span data-stu-id="3831d-325">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="3831d-326">Zwykle nie wiadomo bezwzględna ścieżka fizyczna witryny na serwerze lokacji hostingu, więc tej metody można przekonwertować ścieżki wiadomo, — ścieżka wirtualna — do odpowiedniego ścieżki na serwerze dla Ciebie.</span><span class="sxs-lookup"><span data-stu-id="3831d-326">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="3831d-327">Ścieżka wirtualna są przekazywane do pliku lub folderu do metody i zwraca ścieżkę fizyczną:</span><span class="sxs-lookup"><span data-stu-id="3831d-327">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample39.vbhtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="3831d-328">Odwołanie do wirtualnego katalogu głównego: ~ operatora i Href — metoda</span><span class="sxs-lookup"><span data-stu-id="3831d-328">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="3831d-329">W *.cshtml* lub *.vbhtml* plików, można odwoływać się przy użyciu ścieżka wirtualnego katalogu głównego `~` operatora.</span><span class="sxs-lookup"><span data-stu-id="3831d-329">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="3831d-330">Jest to bardzo przydatne, ponieważ można przenosić stron w witrynie, a wszystkie łącza, które zawierają do innych stron będzie przerwane.</span><span class="sxs-lookup"><span data-stu-id="3831d-330">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="3831d-331">Możliwe jest również przydatne w przypadku gdy przeniesiesz kiedykolwiek witryny sieci Web w innej lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="3831d-331">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="3831d-332">Oto kilka przykładów:</span><span class="sxs-lookup"><span data-stu-id="3831d-332">Here are some examples:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample40.vbhtml)]

<span data-ttu-id="3831d-333">Jeśli witryna sieci Web jest `http://myserver/myapp`, Oto jak ASP.NET, będą traktować te ścieżki po uruchomieniu na stronie:</span><span class="sxs-lookup"><span data-stu-id="3831d-333">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="3831d-334">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="3831d-334">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="3831d-335">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="3831d-335">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="3831d-336">(Te ścieżki faktycznie nie będzie widoczny jako wartości zmiennej, ale ASP.NET będzie traktować ścieżki tak, jakby to, jakie były).</span><span class="sxs-lookup"><span data-stu-id="3831d-336">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="3831d-337">Można użyć `~` operator zarówno w kodzie serwera (jak powyżej), jak i w znaczniku w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="3831d-337">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-vb/samples/sample41.html)]

<span data-ttu-id="3831d-338">W znaczniku, użyj `~` operator do tworzenia ścieżek do zasobów, takich jak pliki obrazów, innych stron sieci web i pliki CSS.</span><span class="sxs-lookup"><span data-stu-id="3831d-338">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="3831d-339">Po uruchomieniu strony ASP.NET sprawdza za pośrednictwem strony (kod i znaczników) i usuwa wszystkie `~` odwołania do danej ścieżce.</span><span class="sxs-lookup"><span data-stu-id="3831d-339">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="3831d-340">Logikę warunkową i pętli</span><span class="sxs-lookup"><span data-stu-id="3831d-340">Conditional Logic and Loops</span></span>

<span data-ttu-id="3831d-341">Kodu serwera ASP.NET umożliwia wykonywanie zadań na podstawie warunków i zapisu kod który powtarza instrukcje określoną liczbę razy oznacza to, że kod uruchamiany w pętli).</span><span class="sxs-lookup"><span data-stu-id="3831d-341">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="3831d-342">Testowanie warunków</span><span class="sxs-lookup"><span data-stu-id="3831d-342">Testing conditions</span></span>

<span data-ttu-id="3831d-343">Aby przetestować prosty warunek użyjesz `If...Then` instrukcja, która zwraca `True` lub `False` na podstawie testu, należy określić:</span><span class="sxs-lookup"><span data-stu-id="3831d-343">To test a simple condition you use the `If...Then` statement, which returns `True` or `False` based on a test you specify:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample42.vbhtml)]

<span data-ttu-id="3831d-344">`If` — Słowo kluczowe uruchamia blok.</span><span class="sxs-lookup"><span data-stu-id="3831d-344">The `If` keyword starts a block.</span></span> <span data-ttu-id="3831d-345">Rzeczywiste testu (warunek) jest zgodna `If` — słowo kluczowe i zwraca wartość PRAWDA lub FAŁSZ.</span><span class="sxs-lookup"><span data-stu-id="3831d-345">The actual test (condition) follows the `If` keyword and returns true or false.</span></span> <span data-ttu-id="3831d-346">`If` Kończy instrukcji `Then`.</span><span class="sxs-lookup"><span data-stu-id="3831d-346">The `If` statement ends with `Then`.</span></span> <span data-ttu-id="3831d-347">Znajdują się wewnątrz instrukcji, które zostanie uruchomione, gdy test jest true `If` i `End If`.</span><span class="sxs-lookup"><span data-stu-id="3831d-347">The statements that will run if the test is true are enclosed by `If` and `End If`.</span></span> <span data-ttu-id="3831d-348">`If` Może zawierać instrukcji `Else` bloku, który określa instrukcje do uruchomienia, jeśli wynikiem warunku jest FAŁSZ:</span><span class="sxs-lookup"><span data-stu-id="3831d-348">An `If` statement can include an `Else` block that specifies statements to run if the condition is false:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample43.vbhtml)]

<span data-ttu-id="3831d-349">Jeśli `If` instrukcji rozpoczyna blok kodu, nie trzeba używać normalnej `Code...End Code` instrukcje, aby zawierała bloki.</span><span class="sxs-lookup"><span data-stu-id="3831d-349">If an `If` statement starts a code block, you don't have to use the normal `Code...End Code` statements to include the blocks.</span></span> <span data-ttu-id="3831d-350">Można dodać `@` blok, i będzie ona działać.</span><span class="sxs-lookup"><span data-stu-id="3831d-350">You can just add `@` to the block, and it will work.</span></span> <span data-ttu-id="3831d-351">Ta metoda działa z `If` oraz innych programowania słów kluczowych, które zostaną wykonane przez bloki kodu, w tym Visual Basic `For`, `For Each`, `Do While`itp.</span><span class="sxs-lookup"><span data-stu-id="3831d-351">This approach works with `If` as well as other Visual Basic programming keywords that are followed by code blocks, including `For`, `For Each`, `Do While`, etc.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample44.vbhtml)]

<span data-ttu-id="3831d-352">Można dodać wiele warunków przy użyciu co najmniej jednego `ElseIf` bloków:</span><span class="sxs-lookup"><span data-stu-id="3831d-352">You can add multiple conditions using one or more `ElseIf` blocks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample45.vbhtml)]

<span data-ttu-id="3831d-353">W tym przykładzie, jeśli pierwszy warunek w `If` bloku nie jest prawdziwe, `ElseIf` warunku jest zaznaczony.</span><span class="sxs-lookup"><span data-stu-id="3831d-353">In this example, if the first condition in the `If` block is not true, the `ElseIf` condition is checked.</span></span> <span data-ttu-id="3831d-354">Jeśli ten warunek jest spełniony, instrukcje w `ElseIf` bloku są wykonywane.</span><span class="sxs-lookup"><span data-stu-id="3831d-354">If that condition is met, the statements in the `ElseIf` block are executed.</span></span> <span data-ttu-id="3831d-355">Jeśli żaden z warunków nie jest spełniony, instrukcje w `Else` bloku są wykonywane.</span><span class="sxs-lookup"><span data-stu-id="3831d-355">If none of the conditions are met, the statements in the `Else` block are executed.</span></span> <span data-ttu-id="3831d-356">Można dodać dowolną liczbę `ElseIf` blokuje, a następnie Zamknij z `Else` zablokować jako &quot;wszystkich pozostałych&quot; warunku.</span><span class="sxs-lookup"><span data-stu-id="3831d-356">You can add any number of `ElseIf` blocks, and then close with an `Else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="3831d-357">Aby przetestować wiele warunków, użyj `Select Case` bloku:</span><span class="sxs-lookup"><span data-stu-id="3831d-357">To test a large number of conditions, use a `Select Case` block:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample46.vbhtml)]

<span data-ttu-id="3831d-358">Wartość do sprawdzenia jest w nawiasach (w przykładzie zmienna dzień tygodnia).</span><span class="sxs-lookup"><span data-stu-id="3831d-358">The value to test is in parentheses (in the example, the weekday variable).</span></span> <span data-ttu-id="3831d-359">Każdy test poszczególnych używa `Case` instrukcji, która zawiera wartości.</span><span class="sxs-lookup"><span data-stu-id="3831d-359">Each individual test uses a `Case` statement that lists a value.</span></span> <span data-ttu-id="3831d-360">Jeśli wartość `Case` instrukcji jest zgodna z wartością testu, kod w tym `Case` bloku jest wykonywana.</span><span class="sxs-lookup"><span data-stu-id="3831d-360">If the value of a `Case` statement matches the test value, the code in that `Case` block is executed.</span></span>

<span data-ttu-id="3831d-361">Wynik ostatnich dwóch bloków warunkowych wyświetlany w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="3831d-361">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-vb/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="3831d-363">Kod pętli</span><span class="sxs-lookup"><span data-stu-id="3831d-363">Looping code</span></span>

<span data-ttu-id="3831d-364">Często konieczne jest cyklicznie uruchamiany tej samej instrukcji.</span><span class="sxs-lookup"><span data-stu-id="3831d-364">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="3831d-365">W tym celu zapętlenia.</span><span class="sxs-lookup"><span data-stu-id="3831d-365">You do this by looping.</span></span> <span data-ttu-id="3831d-366">Na przykład często uruchomieniu same instrukcje dla każdego elementu w kolekcji danych.</span><span class="sxs-lookup"><span data-stu-id="3831d-366">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="3831d-367">Jeśli wiesz, dokładnie tak jak często chcesz pętli, możesz użyć `For` pętli.</span><span class="sxs-lookup"><span data-stu-id="3831d-367">If you know exactly how many times you want to loop, you can use a `For` loop.</span></span> <span data-ttu-id="3831d-368">Tego rodzaju pętli jest szczególnie przydatna w przypadku licząc w lub inwentaryzacji:</span><span class="sxs-lookup"><span data-stu-id="3831d-368">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample47.vbhtml)]

<span data-ttu-id="3831d-369">Rozpoczyna się od pętli `For` — słowo kluczowe, a następnie trzy elementy:</span><span class="sxs-lookup"><span data-stu-id="3831d-369">The loop begins with the `For` keyword, followed by three elements:</span></span>

- <span data-ttu-id="3831d-370">Natychmiast po `For` instrukcja, deklaruje zmienną licznika (nie trzeba używać `Dim`), a następnie określić zakres, podobnie jak w `i = 10 to 20`.</span><span class="sxs-lookup"><span data-stu-id="3831d-370">Immediately after the `For` statement, you declare a counter variable (you don't have to use `Dim`) and then indicate the range, as in `i = 10 to 20`.</span></span> <span data-ttu-id="3831d-371">Oznacza to, że zmienna `i` uruchomi, licząc od 10 i kontynuować dopóki nie osiągnie 20 (włącznie).</span><span class="sxs-lookup"><span data-stu-id="3831d-371">This means the variable `i` will start counting at 10 and continue until it reaches 20 (inclusive).</span></span>
- <span data-ttu-id="3831d-372">Między `For` i `Next` instrukcje znajduje się odpowiednia zawartość bloku.</span><span class="sxs-lookup"><span data-stu-id="3831d-372">Between the `For` and `Next` statements is the content of the block.</span></span> <span data-ttu-id="3831d-373">Ten plik może zawierać co najmniej jeden kod instrukcji, które wykonania w każdej iteracji.</span><span class="sxs-lookup"><span data-stu-id="3831d-373">This can contain one or more code statements that execute with each loop.</span></span>
- <span data-ttu-id="3831d-374">`Next i` Instrukcji kończy się pętli.</span><span class="sxs-lookup"><span data-stu-id="3831d-374">The `Next i` statement ends the loop.</span></span> <span data-ttu-id="3831d-375">Zwiększa licznik, a uruchamia następnej iteracji pętli.</span><span class="sxs-lookup"><span data-stu-id="3831d-375">It increments the counter and starts the next iteration of the loop.</span></span>

<span data-ttu-id="3831d-376">Wiersz kodu między `For` i `Next` wierszy zawiera kod, który jest uruchamiana dla każdej iteracji pętli.</span><span class="sxs-lookup"><span data-stu-id="3831d-376">The line of code between the `For` and `Next` lines contains the code that runs for each iteration of the loop.</span></span> <span data-ttu-id="3831d-377">Kod znaczników, tworzy nowy akapit (`<p>` element) każdego czasu i dodaje wiersz danych wyjściowych, wyświetlanie wartości i (licznik).</span><span class="sxs-lookup"><span data-stu-id="3831d-377">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of i (the counter).</span></span> <span data-ttu-id="3831d-378">Po uruchomieniu tej strony w przykładzie jest tworzony 11 wiersza wyświetlania danych wyjściowych, z tekstem w każdym wierszu wskazującą liczbę elementów.</span><span class="sxs-lookup"><span data-stu-id="3831d-378">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-vb/_static/image11.jpg)

<span data-ttu-id="3831d-380">Jeśli pracujesz z kolekcji lub tablicy, często używasz `For Each` pętli.</span><span class="sxs-lookup"><span data-stu-id="3831d-380">If you're working with a collection or array, you often use a `For Each` loop.</span></span> <span data-ttu-id="3831d-381">Kolekcja jest grupą podobne obiekty i `For Each` pętli pozwala przeprowadzić zadanie dla każdego elementu w kolekcji.</span><span class="sxs-lookup"><span data-stu-id="3831d-381">A collection is a group of similar objects, and the `For Each` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="3831d-382">Ten typ pętli jest wygodne dla kolekcji, ponieważ w odróżnieniu od `For` pętli, nie trzeba zwiększyć licznik lub ustawienie limitu.</span><span class="sxs-lookup"><span data-stu-id="3831d-382">This type of loop is convenient for collections, because unlike a `For` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="3831d-383">Zamiast tego `For Each` pętli kod wykonywany po prostu za pomocą kolekcji dopiero po jej zakończeniu.</span><span class="sxs-lookup"><span data-stu-id="3831d-383">Instead, the `For Each` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="3831d-384">W tym przykładzie zwraca elementy `Request.ServerVariables` kolekcji (zawierający informacje o serwerze sieci web).</span><span class="sxs-lookup"><span data-stu-id="3831d-384">This example returns the items in the `Request.ServerVariables` collection (which contains information about your web server).</span></span> <span data-ttu-id="3831d-385">Używa `For Each` pętli w celu wyświetlenia nazwy poszczególnych elementów, tworząc nowe `<li>` element na liście punktowanej HTML.</span><span class="sxs-lookup"><span data-stu-id="3831d-385">It uses a `For Each` loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample48.vbhtml)]

<span data-ttu-id="3831d-386">`For Each` Słowie kluczowym występuje zmienna, która reprezentuje jeden element w kolekcji (w tym przykładzie `myItem`), a następnie `In` — słowo kluczowe, a następnie według kolekcji ma pętli.</span><span class="sxs-lookup"><span data-stu-id="3831d-386">The `For Each` keyword is followed by a variable that represents a single item in the collection (in the example, `myItem`), followed by the `In` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="3831d-387">W treści `For Each` pętli, są dostępne bieżącego elementu przy użyciu zgłoszonego wcześniej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="3831d-387">In the body of the `For Each` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-vb/_static/image12.jpg)

<span data-ttu-id="3831d-389">Aby utworzyć bardziej ogólnego przeznaczenia pętli, użyj `Do While` instrukcji:</span><span class="sxs-lookup"><span data-stu-id="3831d-389">To create a more general-purpose loop, use the `Do While` statement:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample49.vbhtml)]

<span data-ttu-id="3831d-390">Ta pętla rozpoczyna się od `Do While` — słowo kluczowe, a następnie wybierz warunek, a następnie bloku powtórzeń.</span><span class="sxs-lookup"><span data-stu-id="3831d-390">This loop begins with the `Do While` keyword, followed by a condition, followed by the block to repeat.</span></span> <span data-ttu-id="3831d-391">Zwykle zwiększenie pętli (dodać) lub zmniejszenie (odejmowanie) zmiennej lub obiekt używany do zliczania.</span><span class="sxs-lookup"><span data-stu-id="3831d-391">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="3831d-392">W tym przykładzie `+=` operator dodaje 1 wartość zmiennej z każdym uruchomieniu pętli.</span><span class="sxs-lookup"><span data-stu-id="3831d-392">In the example, the `+=` operator adds 1 to the value of a variable each time the loop runs.</span></span> <span data-ttu-id="3831d-393">(Aby zmniejszyć zmiennej w pętli, które zlicza w dół, użyje operator dekrementacji `-=`.)</span><span class="sxs-lookup"><span data-stu-id="3831d-393">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`.)</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="3831d-394">Obiektów i kolekcji</span><span class="sxs-lookup"><span data-stu-id="3831d-394">Objects and Collections</span></span>

<span data-ttu-id="3831d-395">Prawie wszystko w witrynie sieci Web platformy ASP.NET jest obiektem, w tym strona sieci web.</span><span class="sxs-lookup"><span data-stu-id="3831d-395">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="3831d-396">W tej sekcji omówiono niektóre obiekty ważne, często będą korzystać z kodu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="3831d-396">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="3831d-397">Obiekty strony</span><span class="sxs-lookup"><span data-stu-id="3831d-397">Page objects</span></span>

<span data-ttu-id="3831d-398">Najbardziej podstawowa obiektu w programie ASP.NET jest to strona.</span><span class="sxs-lookup"><span data-stu-id="3831d-398">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="3831d-399">Można uzyskać dostępu do właściwości obiektu strony bezpośrednio, bez żadnych obiektów kwalifikujących.</span><span class="sxs-lookup"><span data-stu-id="3831d-399">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="3831d-400">Poniższy kod pobiera ścieżkę do pliku, przy użyciu `Request` obiekt strony:</span><span class="sxs-lookup"><span data-stu-id="3831d-400">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample50.vbhtml)]

<span data-ttu-id="3831d-401">Można użyć właściwości `Page` obiekt, aby pobrać wiele informacji, takich jak:</span><span class="sxs-lookup"><span data-stu-id="3831d-401">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="3831d-402">`Request`.</span><span class="sxs-lookup"><span data-stu-id="3831d-402">`Request`.</span></span> <span data-ttu-id="3831d-403">Jak już przeczytane, to zbiór informacji o bieżącym żądaniu, w tym, jakiego rodzaju przeglądarki zgłosił żądanie, adres URL strony tożsamości użytkownika, itp.</span><span class="sxs-lookup"><span data-stu-id="3831d-403">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="3831d-404">`Response`.</span><span class="sxs-lookup"><span data-stu-id="3831d-404">`Response`.</span></span> <span data-ttu-id="3831d-405">To jest zbiorem informacji na temat odpowiedzi (strona), który będzie wysyłany do przeglądarki, jeśli kod serwera zakończył działanie.</span><span class="sxs-lookup"><span data-stu-id="3831d-405">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="3831d-406">Na przykład służy tej właściwości można zapisać informacji do odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="3831d-406">For example, you can use this property to write information into the response.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample51.vbhtml)]

### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="3831d-407">Kolekcja obiektów (tablice i słowników)</span><span class="sxs-lookup"><span data-stu-id="3831d-407">Collection objects (arrays and dictionaries)</span></span>

<span data-ttu-id="3831d-408">Kolekcja jest grupy obiektów tego samego typu, takich jak kolekcja `Customer` obiektów z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="3831d-408">A collection is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="3831d-409">Program ASP.NET zawiera wiele kolekcji wbudowanych, tak samo, jak `Request.Files` kolekcji.</span><span class="sxs-lookup"><span data-stu-id="3831d-409">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="3831d-410">Często będzie współpracować danych w kolekcjach.</span><span class="sxs-lookup"><span data-stu-id="3831d-410">You'll often work with data in collections.</span></span> <span data-ttu-id="3831d-411">Istnieją dwa typy kolekcji wspólnej *tablicy* i *słownika*.</span><span class="sxs-lookup"><span data-stu-id="3831d-411">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="3831d-412">Tablica jest przydatne, gdy chcesz przechowywanie kolekcji podobnych elementów, ale nie chcesz utworzyć oddzielne zmienną do przechowywania każdego elementu:</span><span class="sxs-lookup"><span data-stu-id="3831d-412">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample52.vbhtml)]

<span data-ttu-id="3831d-413">Dla tablic, należy zadeklarować określonego typu danych, takich jak `String`, `Integer`, lub `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="3831d-413">With arrays, you declare a specific data type, such as `String`, `Integer`, or `DateTime`.</span></span> <span data-ttu-id="3831d-414">Aby wskazać, że zmienna może zawierać tablicę, Dodaj nawiasy do nazwy zmiennej w deklaracji (takie jak `Dim myVar() As String`).</span><span class="sxs-lookup"><span data-stu-id="3831d-414">To indicate that the variable can contain an array, you add parentheses to the variable name in the declaration (such as `Dim myVar() As String`).</span></span> <span data-ttu-id="3831d-415">Można uzyskać dostępu do elementów w tablicy przy użyciu ich pozycji (indeks) lub za pomocą `For Each` instrukcji.</span><span class="sxs-lookup"><span data-stu-id="3831d-415">You can access items in an array using their position (index) or by using the `For Each` statement.</span></span> <span data-ttu-id="3831d-416">Indeksy tablicy są liczony od zera &#8212; oznacza to, że pierwszy element jest w pozycji 0, drugi element znajduje się na pozycji 1 i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="3831d-416">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample53.vbhtml)]

<span data-ttu-id="3831d-417">Można określić liczbę elementów w tablicy, pobierając jego `Length` właściwości.</span><span class="sxs-lookup"><span data-stu-id="3831d-417">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="3831d-418">Aby uzyskać położenie konkretny element w tablicy (oznacza to, aby wyszukać), użyj `Array.IndexOf` — metoda.</span><span class="sxs-lookup"><span data-stu-id="3831d-418">To get the position of a specific item in the array (that is, to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="3831d-419">Użytkownik może również wykonywać następujące czynności wstecznego zawartości tablicy ( `Array.Reverse` — metoda) i sortować zawartość ( `Array.Sort` metody).</span><span class="sxs-lookup"><span data-stu-id="3831d-419">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="3831d-420">Dane wyjściowe kod tablicy ciąg wyświetlany w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="3831d-420">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-vb/_static/image13.jpg)

<span data-ttu-id="3831d-422">Słownik jest kolekcją par klucz/wartość, gdzie Podaj klucz (lub nazwę) można ustawić lub pobrać odpowiadającej jej wartości:</span><span class="sxs-lookup"><span data-stu-id="3831d-422">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample54.vbhtml)]

<span data-ttu-id="3831d-423">Aby utworzyć słownika, należy użyć `New` — słowo kluczowe, aby wskazać, czy tworzysz nową `Dictionary` obiektu.</span><span class="sxs-lookup"><span data-stu-id="3831d-423">To create a dictionary, you use the `New` keyword to indicate that you're creating a new `Dictionary` object.</span></span> <span data-ttu-id="3831d-424">Słownik można przypisać do zmiennej przy użyciu `Dim` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="3831d-424">You can assign a dictionary to a variable using the `Dim` keyword.</span></span> <span data-ttu-id="3831d-425">Wskazuje typy danych elementów w słowniku za pomocą nawiasów ( `( )` ).</span><span class="sxs-lookup"><span data-stu-id="3831d-425">You indicate the data types of the items in the dictionary using parentheses ( `( )` ).</span></span> <span data-ttu-id="3831d-426">Na końcu deklaracji należy dodać kolejną parę nawiasów, ponieważ jest to rzeczywiście metodę, która tworzy nowy słownik.</span><span class="sxs-lookup"><span data-stu-id="3831d-426">At the end of the declaration, you must add another pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="3831d-427">Aby dodać elementy do słownika, należy wywołać `Add` metody na zmienną słownika (`myScores` w takim przypadku), a następnie określ klucz i wartość.</span><span class="sxs-lookup"><span data-stu-id="3831d-427">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="3831d-428">Alternatywnie można Użyj nawiasów do wskazywania klucz i czy przypisanie proste, jak w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="3831d-428">Alternatively, you can use parentheses to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample55.vbhtml)]

<span data-ttu-id="3831d-429">Aby uzyskać wartość ze słownika, należy określić klucz w nawiasach:</span><span class="sxs-lookup"><span data-stu-id="3831d-429">To get a value from the dictionary, you specify the key in parentheses:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample56.vbhtml)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="3831d-430">Wywoływanie metody z parametrami</span><span class="sxs-lookup"><span data-stu-id="3831d-430">Calling Methods with Parameters</span></span>

<span data-ttu-id="3831d-431">Jak przedstawiono wcześniej w tym artykule, obiekty, które program z ma metody.</span><span class="sxs-lookup"><span data-stu-id="3831d-431">As you saw earlier in this article, the objects that you program with have methods.</span></span> <span data-ttu-id="3831d-432">Na przykład `Database` obiekt może mieć `Database.Connect` metody.</span><span class="sxs-lookup"><span data-stu-id="3831d-432">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="3831d-433">Wiele metod również mieć co najmniej jeden parametr.</span><span class="sxs-lookup"><span data-stu-id="3831d-433">Many methods also have one or more parameters.</span></span> <span data-ttu-id="3831d-434">A *parametru* jest wartością, który jest przekazywany do metody Aby włączyć metodę zakończy się.</span><span class="sxs-lookup"><span data-stu-id="3831d-434">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="3831d-435">Na przykład przyjrzeć się deklaracji `Request.MapPath` metodę, która przyjmuje trzy parametry:</span><span class="sxs-lookup"><span data-stu-id="3831d-435">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-vb[Main](introducing-razor-syntax-vb/samples/sample57.vb)]

<span data-ttu-id="3831d-436">Ta metoda zwraca ścieżkę fizyczną na serwerze, który odpowiada do określonej ścieżki wirtualnej.</span><span class="sxs-lookup"><span data-stu-id="3831d-436">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="3831d-437">Są trzy parametry dla metody `virtualPath`, `baseVirtualDir`, i `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="3831d-437">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="3831d-438">(Zwróć uwagę, że w deklaracji, parametry są wyświetlane z typami danych dane, które akceptują będzie). Gdy ta metoda jest wywoływana, należy podać wartości parametrów wszystkich trzech.</span><span class="sxs-lookup"><span data-stu-id="3831d-438">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="3831d-439">Podczas korzystania z języka Visual Basic z użyciem składni Razor, masz dwie opcje przekazywanie parametrów do metody: *parametrów pozycyjnych* lub *parametrów nazwanych*.</span><span class="sxs-lookup"><span data-stu-id="3831d-439">When you're using Visual Basic with the Razor syntax, you have two options for passing parameters to a method: *positional parameters* or *named parameters*.</span></span> <span data-ttu-id="3831d-440">Aby wywołać metodę przy użyciu parametrów pozycyjnych, parametry przekazywane w kolejności strict, który jest określony w deklaracji metody.</span><span class="sxs-lookup"><span data-stu-id="3831d-440">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="3831d-441">(Będzie zazwyczaj znasz to zamówienie przeczytaj dokumentację metody.) Wykonaj kolejności i nie można pominąć wszystkie parametry &#8212; Jeśli to konieczne, możesz przekazać pusty ciąg (`""`) lub wartość null dla parametrów pozycyjnych, które nie mają wartości.</span><span class="sxs-lookup"><span data-stu-id="3831d-441">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or null for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="3831d-442">W poniższym przykładzie założono istnieje folder o nazwie *skryptów* w witrynie sieci Web.</span><span class="sxs-lookup"><span data-stu-id="3831d-442">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="3831d-443">Kod wywołuje `Request.MapPath` — metoda i przekazuje wartości parametrów trzy we właściwej kolejności.</span><span class="sxs-lookup"><span data-stu-id="3831d-443">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="3831d-444">Wyświetla zamapowanej ścieżki wynikowej.</span><span class="sxs-lookup"><span data-stu-id="3831d-444">It then displays the resulting mapped path.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample58.vbhtml)]

<span data-ttu-id="3831d-445">Gdy istnieje wiele parametrów dla metody, można zachować swój kod czyszczący i bardziej czytelny przy użyciu parametrów nazwanych.</span><span class="sxs-lookup"><span data-stu-id="3831d-445">When there are many parameters for a method, you can keep your code cleaner and more readable by using named parameters.</span></span> <span data-ttu-id="3831d-446">Aby wywołać metodę przy użyciu nazwane parametry, określ nazwę parametru, a następnie `:=` , a następnie wprowadź wartość.</span><span class="sxs-lookup"><span data-stu-id="3831d-446">To call a method using named parameters, specify the parameter name followed by `:=` and then provide the value.</span></span> <span data-ttu-id="3831d-447">Zaletą nazwane parametry jest, że możesz dodać je w dowolnej kolejności, która ma.</span><span class="sxs-lookup"><span data-stu-id="3831d-447">An advantage of named parameters is that you can add them in any order you want.</span></span> <span data-ttu-id="3831d-448">(Wadą jest wywołanie metody nie jest możliwie kompaktowego).</span><span class="sxs-lookup"><span data-stu-id="3831d-448">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="3831d-449">Poniższy przykład wywołuje tę samą metodę jak powyżej, ale używa nazwanych parametrów umożliwiają określanie wartości:</span><span class="sxs-lookup"><span data-stu-id="3831d-449">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample59.vbhtml)]

<span data-ttu-id="3831d-450">Jak widać, parametry są przekazywane w innej kolejności.</span><span class="sxs-lookup"><span data-stu-id="3831d-450">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="3831d-451">Jednak po uruchomieniu poprzedni przykład, jak i w tym przykładzie zostanie zwrócona taką samą wartość.</span><span class="sxs-lookup"><span data-stu-id="3831d-451">However, if you run the previous example and this example, they'll return the same value.</span></span>

## <a name="handling-errors"></a><span data-ttu-id="3831d-452">Obsługa błędów</span><span class="sxs-lookup"><span data-stu-id="3831d-452">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="3831d-453">Instrukcje try-Catch</span><span class="sxs-lookup"><span data-stu-id="3831d-453">Try-Catch statements</span></span>

<span data-ttu-id="3831d-454">Konieczne będzie często instrukcje w kodzie, który może zakończyć się niepowodzeniem ze względów poza formantu.</span><span class="sxs-lookup"><span data-stu-id="3831d-454">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="3831d-455">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="3831d-455">For example:</span></span>

- <span data-ttu-id="3831d-456">Jeśli kod spróbuje otworzyć, utworzyć, odczytać lub zapisać plik, wszystkie rodzaje błędów mogą wystąpić.</span><span class="sxs-lookup"><span data-stu-id="3831d-456">If your code tries to open, create, read, or write a file, all sorts of errors might occur.</span></span> <span data-ttu-id="3831d-457">Żądany plik nie istnieje, może być zablokowane, kod może nie mieć uprawnień i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="3831d-457">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="3831d-458">Podobnie jeśli kod spróbuje do aktualizowania rekordów w bazie danych, mogą być problemy z uprawnieniami, mogą być opuszczane połączenia z bazą danych, można zapisać danych może być nieprawidłowa i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="3831d-458">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="3831d-459">W terminologii programistycznej tych sytuacji są nazywane *wyjątki*.</span><span class="sxs-lookup"><span data-stu-id="3831d-459">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="3831d-460">Jeśli kod napotkał wyjątek, generuje (zgłasza) komunikatu o błędzie oznacza to, co najlepiej irytujących dla użytkowników.</span><span class="sxs-lookup"><span data-stu-id="3831d-460">If your code encounters an exception, it generates (throws) an error message that is, at best, annoying to users.</span></span>

![Razor-Img14](introducing-razor-syntax-vb/_static/image14.jpg)

<span data-ttu-id="3831d-462">W sytuacjach, gdy kod może wystąpić wyjątki, a w celu uniknięcia komunikaty o błędach tego typu, można użyć `Try/Catch` instrukcje.</span><span class="sxs-lookup"><span data-stu-id="3831d-462">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `Try/Catch` statements.</span></span> <span data-ttu-id="3831d-463">W `Try` instrukcji, uruchom kod, który jest sprawdzanie.</span><span class="sxs-lookup"><span data-stu-id="3831d-463">In the `Try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="3831d-464">W co najmniej jednej `Catch` instrukcji, można wyszukać określonych błędów (określonych typów wyjątków), które mogły wystąpić.</span><span class="sxs-lookup"><span data-stu-id="3831d-464">In one or more `Catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="3831d-465">Może zawierać tyle `Catch` instrukcje jako użytkownik musiał błędów, które są przewidywania.</span><span class="sxs-lookup"><span data-stu-id="3831d-465">You can include as many `Catch` statements as you need to look for errors that you're anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="3831d-466">Zaleca się unikać używania `Response.Redirect` metoda `Try/Catch` instrukcji, ponieważ może spowodować wyjątek na stronie.</span><span class="sxs-lookup"><span data-stu-id="3831d-466">We recommend that you avoid using the `Response.Redirect` method in `Try/Catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="3831d-467">W poniższym przykładzie przedstawiono strona, która tworzy plik tekstowy na pierwsze żądanie, a następnie wyświetla przycisku, który umożliwia użytkownikowi otwieranie pliku.</span><span class="sxs-lookup"><span data-stu-id="3831d-467">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="3831d-468">W przykładzie użyto celowo Nieprawidłowa nazwa pliku, aby go spowoduje, że wystąpił wyjątek.</span><span class="sxs-lookup"><span data-stu-id="3831d-468">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="3831d-469">Kod zawiera `Catch` instrukcji na dwa wyjątki to możliwe: `FileNotFoundException`, który występuje, jeśli nazwa pliku jest nieprawidłowa, i `DirectoryNotFoundException`, które występuje, gdy ASP.NET nawet nie można odnaleźć folderu.</span><span class="sxs-lookup"><span data-stu-id="3831d-469">The code includes `Catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="3831d-470">(Użytkownik może usuń znaczniki komentarza do instrukcji w tym przykładzie aby zobaczyć, jak jest uruchamiana po wszystko działa prawidłowo.)</span><span class="sxs-lookup"><span data-stu-id="3831d-470">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="3831d-471">Jeśli kod nie obsłużyć wyjątek, zobaczysz stronę błędu, takie jak poprzednie zrzut ekranu.</span><span class="sxs-lookup"><span data-stu-id="3831d-471">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="3831d-472">Jednak `Try/Catch` sekcja pomoże uniemożliwienia użytkownika tego typu błędów.</span><span class="sxs-lookup"><span data-stu-id="3831d-472">However, the `Try/Catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample60.vbhtml)]

## <a name="additional-resources"></a><span data-ttu-id="3831d-473">Dodatkowe zasoby</span><span class="sxs-lookup"><span data-stu-id="3831d-473">Additional Resources</span></span>

### <a name="reference-documentation"></a><span data-ttu-id="3831d-474">Dokumentacji</span><span class="sxs-lookup"><span data-stu-id="3831d-474">Reference Documentation</span></span>

- [<span data-ttu-id="3831d-475">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="3831d-475">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)
- [<span data-ttu-id="3831d-476">Język Visual Basic</span><span class="sxs-lookup"><span data-stu-id="3831d-476">Visual Basic Language</span></span>](https://msdn.microsoft.com/library/2x7h1hfk.aspx)
