---
uid: web-pages/overview/getting-started/introducing-razor-syntax-c
title: "Wprowadzenie do programowania sieci Web ASP.NET przy użyciu składni Razor (C#) | Dokumentacja firmy Microsoft"
author: tfitzmac
description: "W tym rozdziale zapewnia przegląd programowania ze strony sieci Web ASP.NET przy użyciu składni Razor. ASP.NET to technologia firmy Microsoft dotyczące uruchamiania dynamicznych sieci Web..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: aa67d304-583b-4bf8-a231-195656cfb587
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-c
msc.type: authoredcontent
ms.openlocfilehash: 32cdd8d524d783d7ccc3ab076de636ce4a868132
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/24/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-c"></a><span data-ttu-id="0f655-104">Wprowadzenie do programowania sieci Web ASP.NET przy użyciu składni Razor (C#)</span><span class="sxs-lookup"><span data-stu-id="0f655-104">Introduction to ASP.NET Web Programming Using the Razor Syntax (C#)</span></span>
====================
<span data-ttu-id="0f655-105">przez [FitzMacken niestandardowy](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="0f655-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="0f655-106">W tym artykule zapewnia przegląd programowania ze strony sieci Web ASP.NET przy użyciu składni Razor.</span><span class="sxs-lookup"><span data-stu-id="0f655-106">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax.</span></span> <span data-ttu-id="0f655-107">Program ASP.NET to technologia firmy Microsoft do uruchamiania dynamicznych stron sieci web na serwerach sieci web.</span><span class="sxs-lookup"><span data-stu-id="0f655-107">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span> <span data-ttu-id="0f655-108">Ten artykuł zespoły przy użyciu języka programowania w języku C#.</span><span class="sxs-lookup"><span data-stu-id="0f655-108">This articles focuses on using the C# programming language.</span></span>
> 
> <span data-ttu-id="0f655-109">**Dowiesz się**:</span><span class="sxs-lookup"><span data-stu-id="0f655-109">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="0f655-110">8 top, programowania porady dotyczące wprowadzenie do programowania przy użyciu składni Razor strony sieci Web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0f655-110">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="0f655-111">Podstawowe pojęcia programowania, które będą potrzebne.</span><span class="sxs-lookup"><span data-stu-id="0f655-111">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="0f655-112">Jakie kodu serwera ASP.NET o składni Razor są wszystkie informacje.</span><span class="sxs-lookup"><span data-stu-id="0f655-112">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="0f655-113">Wersje oprogramowania</span><span class="sxs-lookup"><span data-stu-id="0f655-113">Software versions</span></span>
> 
> 
> - <span data-ttu-id="0f655-114">ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="0f655-114">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="0f655-115">W tym samouczku współdziała również z programu ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="0f655-115">This tutorial also works with ASP.NET Web Pages 2.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="0f655-116">Górny 8 porady dotyczące programowania</span><span class="sxs-lookup"><span data-stu-id="0f655-116">The Top 8 Programming Tips</span></span>

<span data-ttu-id="0f655-117">W tej sekcji przedstawiono kilka wskazówek, które należy bezwzględnie znać rozpoczęcie pisania kodu serwera ASP.NET przy użyciu składni Razor.</span><span class="sxs-lookup"><span data-stu-id="0f655-117">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

> [!NOTE]
> <span data-ttu-id="0f655-118">Składnia Razor jest oparta na język programowania C#, a to język, który jest najczęściej używany z ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="0f655-118">The Razor syntax is based on the C# programming language, and that's the language that's used most often with ASP.NET Web Pages.</span></span> <span data-ttu-id="0f655-119">Jednak ze składni Razor obsługuje również języka Visual Basic i wszystkie obiekty, które można zobaczyć, że możesz również wykonać w języku Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="0f655-119">However, the Razor syntax also supports the Visual Basic language, and everything you see you can also do in Visual Basic.</span></span> <span data-ttu-id="0f655-120">Aby uzyskać więcej informacji, zobacz dodatek [języka Visual Basic i składni](https://go.microsoft.com/fwlink/?LinkId=202908).</span><span class="sxs-lookup"><span data-stu-id="0f655-120">For details, see the appendix [Visual Basic Language and Syntax](https://go.microsoft.com/fwlink/?LinkId=202908).</span></span>


<span data-ttu-id="0f655-121">W dalszej części tego artykułu można znaleźć więcej szczegółów na temat większości technik programowania.</span><span class="sxs-lookup"><span data-stu-id="0f655-121">You can find more details about most of these programming techniques later in the article.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="0f655-122">1. Dodaj kod do strony przy użyciu znaku @</span><span class="sxs-lookup"><span data-stu-id="0f655-122">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="0f655-123">`@` Znak uruchamia wyrażenia wbudowany, jednej instrukcji bloki, a wielu instrukcji:</span><span class="sxs-lookup"><span data-stu-id="0f655-123">The `@` character starts inline expressions, single statement blocks, and multi-statement blocks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample1.html)]

<span data-ttu-id="0f655-124">Jest to, jak te instrukcje wyglądać po uruchomieniu na stronie w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="0f655-124">This is what these statements look like when the page runs in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-c/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="0f655-126">**Kodowanie HTML**</span><span class="sxs-lookup"><span data-stu-id="0f655-126">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="0f655-127">Podczas wyświetlania zawartości na stronie za pomocą `@` znak w powyższych przykładach ASP.NET HTML-koduje dane wyjściowe.</span><span class="sxs-lookup"><span data-stu-id="0f655-127">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="0f655-128">Spowoduje to zastąpienie zarezerwowanych znaków HTML (takich jak `<` i `>` i `&`) z kodami umożliwiających znaków, które mają być wyświetlane jako znaki na stronie sieci web, a nie interpretowany jako tagów HTML lub jednostek.</span><span class="sxs-lookup"><span data-stu-id="0f655-128">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="0f655-129">Zakodowany w formacie HTML, dane wyjściowe w kodzie serwera może nie wyświetlać się poprawnie, a może spowodować narażenie strony na zagrożenia bezpieczeństwa.</span><span class="sxs-lookup"><span data-stu-id="0f655-129">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="0f655-130">Celem użytkownika jest output kod znaczników HTML, który renderuje tagi jako kod znaczników (na przykład `<p></p>` akapitu lub `<em></em>` aby podkreślić tekst), zobacz sekcję [łączenie tekstu, znaczników i kodu w bloki kodu](#BM_CombiningTextMarkupAndCode) dalszej części tego artykułu.</span><span class="sxs-lookup"><span data-stu-id="0f655-130">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="0f655-131">Możesz przeczytać więcej na temat kodowania HTML w [pracy z formularzami](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="0f655-131">You can read more about HTML encoding in [Working with Forms](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-in-braces"></a><span data-ttu-id="0f655-132">2. Umieść bloki kodu w nawiasach klamrowych</span><span class="sxs-lookup"><span data-stu-id="0f655-132">2. You enclose code blocks in braces</span></span>

<span data-ttu-id="0f655-133">A *blok kodu* zawiera jedną lub więcej instrukcji kodu i jest ujęta w nawiasy klamrowe.</span><span class="sxs-lookup"><span data-stu-id="0f655-133">A *code block* includes one or more code statements and is enclosed in braces.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample2.html)]

<span data-ttu-id="0f655-134">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="0f655-134">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-c/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-semicolon"></a><span data-ttu-id="0f655-136">3. W bloku kończyć każda instrukcja kodu średnikami</span><span class="sxs-lookup"><span data-stu-id="0f655-136">3. Inside a block, you end each code statement with a semicolon</span></span>

<span data-ttu-id="0f655-137">W bloku kodu każda instrukcja kompletny kod musi być zakończona średnikiem.</span><span class="sxs-lookup"><span data-stu-id="0f655-137">Inside a code block, each complete code statement must end with a semicolon.</span></span> <span data-ttu-id="0f655-138">Wbudowane wyrażenia nie kończyć się średnikiem.</span><span class="sxs-lookup"><span data-stu-id="0f655-138">Inline expressions don't end with a semicolon.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample3.html)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="0f655-139">4. W przypadku używania zmiennych do przechowywania wartości</span><span class="sxs-lookup"><span data-stu-id="0f655-139">4. You use variables to store values</span></span>

<span data-ttu-id="0f655-140">Można zapisać wartości w *zmiennej*, w tym ciągów, liczb i daty, itp. Można utworzyć nowej zmiennej przy użyciu `var` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="0f655-140">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `var` keyword.</span></span> <span data-ttu-id="0f655-141">Można wstawić wartości zmiennych bezpośrednio za pomocą strony `@`.</span><span class="sxs-lookup"><span data-stu-id="0f655-141">You can insert variable values directly in a page using `@`.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample4.html)]

<span data-ttu-id="0f655-142">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="0f655-142">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-c/_static/image3.jpg)

<a id="ID_StringLiterals"></a>
### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="0f655-144">5. To ująć w podwójny cudzysłów wartości literału ciągu</span><span class="sxs-lookup"><span data-stu-id="0f655-144">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="0f655-145">A *ciąg* jest sekwencji znaków, które są traktowane jako tekst.</span><span class="sxs-lookup"><span data-stu-id="0f655-145">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="0f655-146">Aby określić ciąg, ująć w podwójny cudzysłów:</span><span class="sxs-lookup"><span data-stu-id="0f655-146">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample5.cshtml)]

<span data-ttu-id="0f655-147">Jeśli ciąg, który chcesz wyświetlić zawiera znak ukośnika odwrotnego ( `\` ) lub podwójny cudzysłów ( `"` ), użyj *literał ciągu dosłownego wyrażenia* który prefiksem `@` operatora.</span><span class="sxs-lookup"><span data-stu-id="0f655-147">If the string that you want to display contains a backslash character ( `\` ) or double quotation marks ( `"` ), use a *verbatim string literal* that's prefixed with the `@` operator.</span></span> <span data-ttu-id="0f655-148">(W języku C#, \ znak ma specjalnego znaczenia, chyba że za pomocą literału ciągu dosłownego wyrażenia.)</span><span class="sxs-lookup"><span data-stu-id="0f655-148">(In C#, the \ character has special meaning unless you use a verbatim string literal.)</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample6.html)]

<span data-ttu-id="0f655-149">Aby osadzić znaki cudzysłowu, użyj literał ciągu dosłownego wyrażenia i powtórz znaki cudzysłowu:</span><span class="sxs-lookup"><span data-stu-id="0f655-149">To embed double quotation marks, use a verbatim string literal and repeat the quotation marks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample7.html)]

<span data-ttu-id="0f655-150">W tym miejscu jest wynikiem za pomocą obu tych przykładach na stronie:</span><span class="sxs-lookup"><span data-stu-id="0f655-150">Here's the result of using both of these examples in a page:</span></span>

![Razor-Img4](introducing-razor-syntax-c/_static/image4.jpg)

> [!NOTE]
> <span data-ttu-id="0f655-152">Zwróć uwagę, że `@` znak jest używany do oznaczyć literał ciągu dosłownego wyrażenia w C# i oznacz kodu w stron ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0f655-152">Notice that the `@` character is used both to mark verbatim string literals in C# and to mark code in ASP.NET pages.</span></span>


### <a name="6-code-is-case-sensitive"></a><span data-ttu-id="0f655-153">6. Kod jest rozróżniana wielkość liter</span><span class="sxs-lookup"><span data-stu-id="0f655-153">6. Code is case sensitive</span></span>

<span data-ttu-id="0f655-154">W języku C#, słowa kluczowe (takich jak `var`, `true`, i `if`) i nazwy zmiennych jest uwzględniana wielkość liter.</span><span class="sxs-lookup"><span data-stu-id="0f655-154">In C#, keywords (like `var`, `true`, and `if`) and variable names are case sensitive.</span></span> <span data-ttu-id="0f655-155">Następujące wiersze kodu utworzyć dwie różne zmienne, `lastName` i`LastName.`</span><span class="sxs-lookup"><span data-stu-id="0f655-155">The following lines of code create two different variables, `lastName` and `LastName.`</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample8.cshtml)]

<span data-ttu-id="0f655-156">Deklarowanie zmiennej jako `var lastName = "Smith";` i podjęcie próby odwołanie do tej zmiennej na stronie jako `@LastName`, powoduje błąd, ponieważ `LastName` nie zostanie rozpoznane.</span><span class="sxs-lookup"><span data-stu-id="0f655-156">If you declare a variable as `var lastName = "Smith";` and if you try to reference that variable in your page as `@LastName`, an error results because `LastName` won't be recognized.</span></span>

> [!NOTE]
> <span data-ttu-id="0f655-157">W języku Visual Basic, słowa kluczowe i zmienne są *nie* z uwzględnieniem wielkości liter.</span><span class="sxs-lookup"><span data-stu-id="0f655-157">In Visual Basic, keywords and variables are *not* case sensitive.</span></span>


### <a name="7-much-of-your-coding-involves-objects"></a><span data-ttu-id="0f655-158">7. Obejmuje większość kodowania obiektów</span><span class="sxs-lookup"><span data-stu-id="0f655-158">7. Much of your coding involves objects</span></span>

<span data-ttu-id="0f655-159">*Obiektu* reprezentuje element, który zostanie z &#8212; strony, pola tekstowego, pliku, obraz, żądania sieci web, wiadomości e-mail, rekord klienta (wiersza bazy danych), itp. Obiekty mają właściwości, które opisują ich właściwości i że może odczytać lub Zmień &#8212; Obiekt pola tekstowego ma `Text` właściwość (między innymi), obiekt żądania ma `Url` właściwość, wiadomości e-mail ma `From` właściwości oraz obiektu klienta ma `FirstName` właściwości.</span><span class="sxs-lookup"><span data-stu-id="0f655-159">An *object* represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics and that you can read or change &#8212; a text box object has a `Text` property (among others), a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="0f655-160">Obiekty mają również metody, które są &quot;zleceń&quot; mogą wykonywać.</span><span class="sxs-lookup"><span data-stu-id="0f655-160">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="0f655-161">Przykładami obiektu pliku `Save` metodę, obiekt obrazu `Rotate` — metoda i obiektu poczty e-mail `Send` metody.</span><span class="sxs-lookup"><span data-stu-id="0f655-161">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="0f655-162">Często będzie współpracować `Request` obiektów, które zapewnia informacje, takie jak wartości pól tekstowych (pola formularza) na stronie, jakiego rodzaju przeglądarki zgłosił żądanie, adres URL strony tożsamości użytkownika, itp. Poniższy przykład przedstawia sposób uzyskać dostępu do właściwości `Request` obiektów i wywoływania `MapPath` metody `Request` obiektu, który daje użytkownikowi bezwzględna ścieżka strony na serwerze:</span><span class="sxs-lookup"><span data-stu-id="0f655-162">You'll often work with the `Request` object, which gives you information like the values of text boxes (form fields) on the page, what type of browser made the request, the URL of the page, the user identity, etc. The following example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample9.html)]

<span data-ttu-id="0f655-163">Wynik wyświetlony w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="0f655-163">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-c/_static/image5.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="0f655-165">8. Można napisać kod, który podejmowania decyzji w procesie</span><span class="sxs-lookup"><span data-stu-id="0f655-165">8. You can write code that makes decisions</span></span>

<span data-ttu-id="0f655-166">Kluczową funkcją dynamicznej stron sieci web jest, że można określić, co zrobić, na podstawie warunków.</span><span class="sxs-lookup"><span data-stu-id="0f655-166">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="0f655-167">Najczęściej w tym celu jest z `if` instrukcji (i opcjonalnie `else` instrukcji).</span><span class="sxs-lookup"><span data-stu-id="0f655-167">The most common way to do this is with the `if` statement (and optional `else` statement).</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample10.cshtml)]

<span data-ttu-id="0f655-168">Instrukcja `if(IsPost)` jest skrócona sposobem zapisu `if(IsPost == true)`.</span><span class="sxs-lookup"><span data-stu-id="0f655-168">The statement `if(IsPost)` is a shorthand way of writing `if(IsPost == true)`.</span></span> <span data-ttu-id="0f655-169">Wraz z programem `if` instrukcji, istnieje wiele sposobów, aby przetestować warunki, powtórz bloki kodu, i itd., które są opisane w dalszej części tego artykułu.</span><span class="sxs-lookup"><span data-stu-id="0f655-169">Along with `if` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="0f655-170">Wynik wyświetlony w przeglądarce (po kliknięciu przycisku **przesyłania**):</span><span class="sxs-lookup"><span data-stu-id="0f655-170">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor-Img6](introducing-razor-syntax-c/_static/image6.jpg)

> [!TIP] 
> 
> <a id="SB_HttpGetPost"></a>
> ### <a name="http-get-and-post-methods-and-the-ispost-property"></a><span data-ttu-id="0f655-172">HTTP GET i POST metod i właściwości IsPost</span><span class="sxs-lookup"><span data-stu-id="0f655-172">HTTP GET and POST Methods and the IsPost Property</span></span>
> 
> <span data-ttu-id="0f655-173">Protokół używany do stron sieci web (HTTP) obsługuje bardzo ograniczoną liczbę metod (zleceń), które są używane na wysyłanie żądań do serwera.</span><span class="sxs-lookup"><span data-stu-id="0f655-173">The protocol used for web pages (HTTP) supports a very limited number of methods (verbs) that are used to make requests to the server.</span></span> <span data-ttu-id="0f655-174">Dwa najbardziej typowe to GET, który jest używany do odczytu stronę, i POST, który jest używany do przesyłania strony.</span><span class="sxs-lookup"><span data-stu-id="0f655-174">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="0f655-175">Ogólnie rzecz biorąc po raz pierwszy użytkownik zażąda strony, strony jest wymagany przy użyciu GET.</span><span class="sxs-lookup"><span data-stu-id="0f655-175">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="0f655-176">Jeśli użytkownik wypełnia formularza, a następnie klika przycisk Prześlij, przeglądarka wysyła żądanie POST do serwera.</span><span class="sxs-lookup"><span data-stu-id="0f655-176">If the user fills in a form and then clicks a submit button, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="0f655-177">W programowanie dla sieci web często jest grupowaniu można sprawdzić, czy strona jest wymagany jako GET lub POST, aby wiedzieć, jak przetwarzania tej strony.</span><span class="sxs-lookup"><span data-stu-id="0f655-177">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="0f655-178">W składniku ASP.NET Web Pages można użyć `IsPost` właściwości, aby zobaczyć, czy żądanie jest GET lub POST.</span><span class="sxs-lookup"><span data-stu-id="0f655-178">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="0f655-179">Jeśli żądanie jest żądaniem POST `IsPost` właściwości zwróci wartość true, a użytkownik może wykonywać następujące czynności odczytu wartości pól tekstowych w formularzu.</span><span class="sxs-lookup"><span data-stu-id="0f655-179">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="0f655-180">Wiele przykładów zobaczysz opisano sposób przetwarzania strony inaczej w zależności od wartości `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="0f655-180">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="0f655-181">Prosty przykład kodu</span><span class="sxs-lookup"><span data-stu-id="0f655-181">A Simple Code Example</span></span>

<span data-ttu-id="0f655-182">Tej procedury przedstawiono sposób tworzenia strony, która ilustruje podstawowych technik programowania.</span><span class="sxs-lookup"><span data-stu-id="0f655-182">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="0f655-183">W tym przykładzie utworzysz strony, który pozwala użytkownikom na wprowadzanie dwóch liczb, a następnie dodanie ich i wyświetla wyniki.</span><span class="sxs-lookup"><span data-stu-id="0f655-183">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="0f655-184">W edytorze, Utwórz nowy plik i nadaj jej nazwę *AddNumbers.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0f655-184">In your editor, create a new file and name it *AddNumbers.cshtml*.</span></span>
2. <span data-ttu-id="0f655-185">Skopiuj następujący kod i znaczników do strony, zastępując wszystko już na stronie.</span><span class="sxs-lookup"><span data-stu-id="0f655-185">Copy the following code and markup into the page, replacing anything already in the page.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample11.cshtml)]

    <span data-ttu-id="0f655-186">Poniżej przedstawiono niektóre czynności umożliwiające należy zwrócić uwagę:</span><span class="sxs-lookup"><span data-stu-id="0f655-186">Here are some things for you to note:</span></span>

    - <span data-ttu-id="0f655-187">`@` Znak rozpoczyna się pierwszego bloku kodu na stronie i poprzedza `totalMessage` zmiennej, która jest osadzony w dolnej części strony.</span><span class="sxs-lookup"><span data-stu-id="0f655-187">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable that's embedded near the bottom of the page.</span></span>
    - <span data-ttu-id="0f655-188">Blok w górnej części strony jest ujęta w nawiasy klamrowe.</span><span class="sxs-lookup"><span data-stu-id="0f655-188">The block at the top of the page is enclosed in braces.</span></span>
    - <span data-ttu-id="0f655-189">W bloku u góry wszystkie linie kończyć średnikiem.</span><span class="sxs-lookup"><span data-stu-id="0f655-189">In the block at the top, all lines end with a semicolon.</span></span>
    - <span data-ttu-id="0f655-190">Zmienne `total`, `num1`, `num2`, i `totalMessage` przechowywać wiele numerów i ciąg.</span><span class="sxs-lookup"><span data-stu-id="0f655-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="0f655-191">Wartość literału ciągu przypisane do `totalMessage` zmienna jest w podwójny cudzysłów.</span><span class="sxs-lookup"><span data-stu-id="0f655-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="0f655-192">Ponieważ kod jest rozróżniana wielkość liter, gdy `totalMessage` zmienna jest używana w dolnej części strony, jego nazwa musi być zgodny zmiennej u góry.</span><span class="sxs-lookup"><span data-stu-id="0f655-192">Because the code is case-sensitive, when the `totalMessage` variable is used near the bottom of the page, its name must match the variable at the top exactly.</span></span>
    - <span data-ttu-id="0f655-193">Wyrażenie `num1.AsInt() + num2.AsInt()` pokazano, jak pracować z obiektów i metod.</span><span class="sxs-lookup"><span data-stu-id="0f655-193">The expression `num1.AsInt() + num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="0f655-194">`AsInt` Metody dla każdej zmiennej konwertuje ciąg wprowadzony przez użytkownika na liczbę (liczba całkowita) tak, aby można było wykonać operacje arytmetyczne na nim.</span><span class="sxs-lookup"><span data-stu-id="0f655-194">The `AsInt` method on each variable converts the string entered by a user to a number (an integer) so that you can perform arithmetic on it.</span></span>
    - <span data-ttu-id="0f655-195">`<form>` Zawiera tag `method="post"` atrybutu.</span><span class="sxs-lookup"><span data-stu-id="0f655-195">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="0f655-196">Oznacza to, że gdy użytkownik kliknie **Dodaj**, strony zostaną wysłane do serwera przy użyciu metody POST protokołu HTTP.</span><span class="sxs-lookup"><span data-stu-id="0f655-196">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="0f655-197">Po przesłaniu strony `if(IsPost)` testu daje w wyniku wartość true i warunkowe kod działa wyświetlania wyniku dodawania liczb.</span><span class="sxs-lookup"><span data-stu-id="0f655-197">When the page is submitted, the `if(IsPost)` test evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="0f655-198">Strony i uruchom go w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="0f655-198">Save the page and run it in a browser.</span></span> <span data-ttu-id="0f655-199">(Upewnij się, że strona jest zaznaczona w **pliki** obszar roboczy przed jej uruchomieniem.) Wprowadź dwie liczb całkowitych, a następnie kliknij przycisk **Dodaj** przycisku.</span><span class="sxs-lookup"><span data-stu-id="0f655-199">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span> 

    ![Razor-Img7](introducing-razor-syntax-c/_static/image7.jpg)

## <a name="basic-programming-concepts"></a><span data-ttu-id="0f655-201">Podstawowe koncepcje programowania</span><span class="sxs-lookup"><span data-stu-id="0f655-201">Basic Programming Concepts</span></span>

<span data-ttu-id="0f655-202">Ten artykuł zawiera omówienie programowania w języku sieci web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0f655-202">This article provides you with an overview of ASP.NET web programming.</span></span> <span data-ttu-id="0f655-203">Nie jest kompletną badania, po prostu krótki przewodnik za pośrednictwem pojęcia dotyczące programowania, które będą używane najczęściej.</span><span class="sxs-lookup"><span data-stu-id="0f655-203">It isn't an exhaustive examination, just a quick tour through the programming concepts you'll use most often.</span></span> <span data-ttu-id="0f655-204">Mimo tego obejmują prawie wszystko, które będą potrzebne, aby zacząć korzystać z ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="0f655-204">Even so, it covers almost everything you'll need to get started with ASP.NET Web Pages.</span></span>

<span data-ttu-id="0f655-205">Ale pierwszy, małego informacje techniczne.</span><span class="sxs-lookup"><span data-stu-id="0f655-205">But first, a little technical background.</span></span>

### <a name="the-razor-syntax-server-code-and-aspnet"></a><span data-ttu-id="0f655-206">Składnia Razor kodu serwera i platformy ASP.NET</span><span class="sxs-lookup"><span data-stu-id="0f655-206">The Razor Syntax, Server Code, and ASP.NET</span></span>

<span data-ttu-id="0f655-207">Składnia razor jest proste programowania składnię osadzania kodu na serwerze, na stronie sieci web.</span><span class="sxs-lookup"><span data-stu-id="0f655-207">Razor syntax is a simple programming syntax for embedding server-based code in a web page.</span></span> <span data-ttu-id="0f655-208">Na stronie sieci web, która używa składni Razor, istnieją dwa rodzaje zawartości: Kod klienta zawartości i serwera.</span><span class="sxs-lookup"><span data-stu-id="0f655-208">In a web page that uses the Razor syntax, there are two kinds of content: client content and server code.</span></span> <span data-ttu-id="0f655-209">Zawartość klienta jest rzeczy użyto do na stronach sieci web: kod znaczników HTML (elementy), informacje, takie jak CSS, styl może być niektórych skryptu klienta, takich jak JavaScript i zwykły tekst.</span><span class="sxs-lookup"><span data-stu-id="0f655-209">Client content is the stuff you're used to in web pages: HTML markup (elements), style information such as CSS, maybe some client script such as JavaScript, and plain text.</span></span>

<span data-ttu-id="0f655-210">Składnia razor pozwala dodać kod serwera do tej zawartości klienta.</span><span class="sxs-lookup"><span data-stu-id="0f655-210">Razor syntax lets you add server code to this client content.</span></span> <span data-ttu-id="0f655-211">Jeśli na stronie jest kod serwera, na serwerze działa ten kod najpierw przed wysłaniem strony do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="0f655-211">If there's server code in the page, the server runs that code first, before it sends the page to the browser.</span></span> <span data-ttu-id="0f655-212">Uruchamiając na serwerze, kod mogą wykonywać zadania, które mogą być znacznie bardziej złożone, aby zrobić przy użyciu klienta zawartość samodzielnie, takich jak uzyskiwanie dostępu na serwerze baz danych.</span><span class="sxs-lookup"><span data-stu-id="0f655-212">By running on the server, the code can perform tasks that can be a lot more complex to do using client content alone, like accessing server-based databases.</span></span> <span data-ttu-id="0f655-213">Przede wszystkim kod serwera można dynamicznie utworzyć klienta, zawartość &#8212; go wygenerować kod znaczników HTML lub innej zawartości w locie, a następnie wyślij ją w przeglądarce wraz z statycznych kodu HTML, który może zawierać strony.</span><span class="sxs-lookup"><span data-stu-id="0f655-213">Most importantly, server code can dynamically create client content &#8212; it can generate HTML markup or other content on the fly and then send it to the browser along with any static HTML that the page might contain.</span></span> <span data-ttu-id="0f655-214">Z perspektywy przeglądarki nie różni się od innej zawartości klient się zawartość klienta, która jest generowana przez kod serwera.</span><span class="sxs-lookup"><span data-stu-id="0f655-214">From the browser's perspective, client content that's generated by your server code is no different than any other client content.</span></span> <span data-ttu-id="0f655-215">Jak już przeczytane, kod serwera, która jest wymagana jest bardzo proste.</span><span class="sxs-lookup"><span data-stu-id="0f655-215">As you've already seen, the server code that's required is quite simple.</span></span>

<span data-ttu-id="0f655-216">Strony sieci web ASP.NET zawierających składnię Razor mają rozszerzenie pliku specjalne (*.cshtml* lub *.vbhtml*).</span><span class="sxs-lookup"><span data-stu-id="0f655-216">ASP.NET web pages that include the Razor syntax have a special file extension (*.cshtml* or *.vbhtml*).</span></span> <span data-ttu-id="0f655-217">Serwer rozpoznaje tych rozszerzeń, uruchamia kod, który jest oznaczony atrybutem składni Razor, a następnie wysyła strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="0f655-217">The server recognizes these extensions, runs the code that's marked with Razor syntax, and then sends the page to the browser.</span></span>

### <a name="where-does-aspnet-fit-in"></a><span data-ttu-id="0f655-218">Gdy program ASP.NET pasują do?</span><span class="sxs-lookup"><span data-stu-id="0f655-218">Where does ASP.NET fit in?</span></span>

<span data-ttu-id="0f655-219">Składnia razor jest oparta na technologii firmy Microsoft o nazwie platformę ASP.NET, która z kolei jest oparta na programie Microsoft .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0f655-219">Razor syntax is based on a technology from Microsoft called ASP.NET, which in turn is based on the Microsoft .NET Framework.</span></span> <span data-ttu-id="0f655-220">Środowiska.NET Framework jest duży, kompleksowe struktury programistycznej firmy Microsoft związane z opracowywaniem praktycznie dowolnego typu aplikacji komputera.</span><span class="sxs-lookup"><span data-stu-id="0f655-220">The.NET Framework is a big, comprehensive programming framework from Microsoft for developing virtually any type of computer application.</span></span> <span data-ttu-id="0f655-221">Program ASP.NET jest częścią programu .NET Framework, przeznaczone do tworzenia aplikacji sieci web.</span><span class="sxs-lookup"><span data-stu-id="0f655-221">ASP.NET is the part of the .NET Framework that's specifically designed for creating web applications.</span></span> <span data-ttu-id="0f655-222">Deweloperzy użyto ASP.NET można utworzyć wiele witryn sieci Web z najwyższą ruchu i największa na świecie.</span><span class="sxs-lookup"><span data-stu-id="0f655-222">Developers have used ASP.NET to create many of the largest and highest-traffic websites in the world.</span></span> <span data-ttu-id="0f655-223">(Dowolnej chwili wyświetlić rozszerzenie nazwy pliku *.aspx* jako część adresu URL w witrynie, będzie wiadomo, czy lokacji została opracowana za pomocą programu ASP.NET.)</span><span class="sxs-lookup"><span data-stu-id="0f655-223">(Any time you see the file-name extension *.aspx* as part of the URL in a site, you'll know that the site was written using ASP.NET.)</span></span>

<span data-ttu-id="0f655-224">Składnia Razor umożliwia wszystkie możliwości platformy ASP.NET, ale przy użyciu składni uproszczoną ułatwiający dowiedzieć się użytkownik początkujący i dzięki temu można efektywniej, jeśli jesteś eksperta.</span><span class="sxs-lookup"><span data-stu-id="0f655-224">The Razor syntax gives you all the power of ASP.NET, but using a simplified syntax that's easier to learn if you're a beginner and that makes you more productive if you're an expert.</span></span> <span data-ttu-id="0f655-225">Mimo że ta składnia jest łatwy w użyciu, jej rodziny relacji z platformy ASP.NET i .NET Framework oznacza, że jako witryny sieci Web staną się bardziej zaawansowanych, być zasilania większych struktur, dostępne dla Ciebie.</span><span class="sxs-lookup"><span data-stu-id="0f655-225">Even though this syntax is simple to use, its family relationship to ASP.NET and the .NET Framework means that as your websites become more sophisticated, you have the power of the larger frameworks available to you.</span></span>

![Razor-Img8](introducing-razor-syntax-c/_static/image8.jpg)

> [!TIP] 
> 
> <span data-ttu-id="0f655-227">**Klasy i wystąpienia**</span><span class="sxs-lookup"><span data-stu-id="0f655-227">**Classes and Instances**</span></span>
> 
> <span data-ttu-id="0f655-228">Obiekty, które z kolei są tworzone z myślą klasy korzysta z kodu serwera ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0f655-228">ASP.NET server code uses objects, which are in turn built on the idea of classes.</span></span> <span data-ttu-id="0f655-229">Klasa jest definicja lub szablon dla obiekt.</span><span class="sxs-lookup"><span data-stu-id="0f655-229">The class is the definition or template for an object.</span></span> <span data-ttu-id="0f655-230">Na przykład aplikacja może zawierać `Customer` klasa, która definiuje właściwości i metody, które wymaga dowolnego obiektu klienta.</span><span class="sxs-lookup"><span data-stu-id="0f655-230">For example, an application might contain a `Customer` class that defines the properties and methods that any customer object needs.</span></span>
> 
> <span data-ttu-id="0f655-231">Kiedy aplikacja musi działać z informacjami o klientach rzeczywiste, tworzy wystąpienia (lub *tworzy*) obiektu klienta.</span><span class="sxs-lookup"><span data-stu-id="0f655-231">When the application needs to work with actual customer information, it creates an instance of (or *instantiates*) a customer object.</span></span> <span data-ttu-id="0f655-232">Poszczególnych klientów jest oddzielnego wystąpienia `Customer` klasy.</span><span class="sxs-lookup"><span data-stu-id="0f655-232">Each individual customer is a separate instance of the `Customer` class.</span></span> <span data-ttu-id="0f655-233">Każde wystąpienie obsługuje te same właściwości i metody, ale wartości właściwości dla każdego wystąpienia różnią się zwykle, ponieważ każdy obiekt klienta jest unikatowa.</span><span class="sxs-lookup"><span data-stu-id="0f655-233">Every instance supports the same properties and methods, but the property values for each instance are typically different, because each customer object is unique.</span></span> <span data-ttu-id="0f655-234">W obiekcie jednego klienta `LastName` właściwość może być "Smith"; w innym obiekcie klienta, `LastName` właściwość może być "Nowak".</span><span class="sxs-lookup"><span data-stu-id="0f655-234">In one customer object, the `LastName` property might be "Smith"; in another customer object, the `LastName` property might be "Jones."</span></span>
> 
> <span data-ttu-id="0f655-235">Podobnie jest indywidualnych stronach sieci web w swojej witrynie `Page` obiekt, który jest wystąpieniem `Page` klasy.</span><span class="sxs-lookup"><span data-stu-id="0f655-235">Similarly, any individual web page in your site is a `Page` object that's an instance of the `Page` class.</span></span> <span data-ttu-id="0f655-236">Przycisk na stronie jest `Button` obiekt, który jest wystąpieniem `Button` klasy i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="0f655-236">A button on the page is a `Button` object that is an instance of the `Button` class, and so on.</span></span> <span data-ttu-id="0f655-237">Każde wystąpienie ma własne charakterystyki, ale wszystkie one są oparte na to, co jest określona w definicji klasy obiektu.</span><span class="sxs-lookup"><span data-stu-id="0f655-237">Each instance has its own characteristics, but they all are based on what's specified in the object's class definition.</span></span>


## <a name="basic-syntax"></a><span data-ttu-id="0f655-238">Podstawowa składnia</span><span class="sxs-lookup"><span data-stu-id="0f655-238">Basic Syntax</span></span>

<span data-ttu-id="0f655-239">Widać wcześniej podstawowy przykład sposobu tworzenia strony ASP.NET Web Pages i jak można dodać kod serwera do kod znaczników HTML.</span><span class="sxs-lookup"><span data-stu-id="0f655-239">Earlier you saw a basic example of how to create an ASP.NET Web Pages page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="0f655-240">Tutaj dowiesz się podstawowe informacje dotyczące pisania kodu serwera ASP.NET przy użyciu składni Razor &#8212; oznacza to, że programowania reguł języka.</span><span class="sxs-lookup"><span data-stu-id="0f655-240">Here you'll learn the basics of writing ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="0f655-241">Jeśli masz doświadczenia w pracy z programowania (zwłaszcza, jeśli używano C, C++, C#, Visual Basic lub JavaScript), większość tutaj odczytu jest znane.</span><span class="sxs-lookup"><span data-stu-id="0f655-241">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="0f655-242">Prawdopodobnie należy zapoznać się z tylko sposób kod serwera jest dodawana do kodu znaczników w *.cshtml* plików.</span><span class="sxs-lookup"><span data-stu-id="0f655-242">You'll probably need to familiarize yourself only with how server code is added to markup in *.cshtml* files.</span></span>

<a id="BM_CombiningTextMarkupAndCode"></a>
### <a name="combining-text-markup-and-code-in-code-blocks"></a><span data-ttu-id="0f655-243">Łączenie tekstu, znaczników i kodu w bloki kodu</span><span class="sxs-lookup"><span data-stu-id="0f655-243">Combining Text, Markup, and Code in Code Blocks</span></span>

<span data-ttu-id="0f655-244">W serwerze bloki kodu ma często dane wyjściowe tekstu lub znacznika (lub obie) do strony.</span><span class="sxs-lookup"><span data-stu-id="0f655-244">In server code blocks, you often want to output text or markup (or both) to the page.</span></span> <span data-ttu-id="0f655-245">Jeśli blok kodu serwera zawiera tekst, który nie jest kodem i który zamiast tego ma być renderowany jako jest, ASP.NET musi mieć możliwość rozróżnienia ten tekst z kodu.</span><span class="sxs-lookup"><span data-stu-id="0f655-245">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="0f655-246">Istnieje kilka sposobów, aby to zrobić.</span><span class="sxs-lookup"><span data-stu-id="0f655-246">There are several ways to do this.</span></span>

- <span data-ttu-id="0f655-247">Tekst należy ująć w elemencie HTML, takie jak `<p></p>` lub `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="0f655-247">Enclose the text in an HTML element like `<p></p>` or `<em></em>`:</span></span>   

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample12.cshtml)]

    <span data-ttu-id="0f655-248">HTML element mogą zawierać tekst, dodatkowe elementy HTML i wyrażenia kod serwera.</span><span class="sxs-lookup"><span data-stu-id="0f655-248">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="0f655-249">Gdy ASP.NET widzi otwierający HTML tag (na przykład `<p>`), wszystkie elementy w tym elemencie renderowania i zawartość jako jest w przeglądarce, rozpoznawania kod serwera wyrażenia, ponieważ przechodzi ona.</span><span class="sxs-lookup"><span data-stu-id="0f655-249">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything including the element and its content as is to the browser, resolving server-code expressions as it goes.</span></span>
- <span data-ttu-id="0f655-250">Użyj `@:` operator lub `<text>` elementu.</span><span class="sxs-lookup"><span data-stu-id="0f655-250">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="0f655-251">`@:` Generuje pojedynczy wiersz zawartości zawierającej zwykłego tekstu lub niedopasowane znaczniki HTML; `<text>` elementu obejmuje wiele wierszy danych wyjściowych.</span><span class="sxs-lookup"><span data-stu-id="0f655-251">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="0f655-252">Te opcje są przydatne, jeśli nie chcesz do renderowania elementu HTML jako część danych wyjściowych.</span><span class="sxs-lookup"><span data-stu-id="0f655-252">These options are useful when you don't want to render an HTML element as part of the output.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample13.cshtml)]

    <span data-ttu-id="0f655-253">Chcesz output wiele wierszy tekstu lub niedopasowane znaczniki HTML, mogą występować przed każdej linii `@:`, lub może być częścią linię w `<text>` elementu.</span><span class="sxs-lookup"><span data-stu-id="0f655-253">If you want to output multiple lines of text or unmatched HTML tags, you can precede each line with `@:`, or you can enclose the line in a `<text>` element.</span></span> <span data-ttu-id="0f655-254">Podobnie jak `@:` operatora`<text>` tagi są używane przez program ASP.NET do identyfikowania zawartości tekstowej i nigdy nie są renderowane w danych wyjściowych strony.</span><span class="sxs-lookup"><span data-stu-id="0f655-254">Like the `@:` operator,`<text>` tags are used by ASP.NET to identify text content and are never rendered in the page output.</span></span>

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample14.cshtml)]

    <span data-ttu-id="0f655-255">Pierwszym przykładzie powtarza poprzednim przykładzie, ale używa jednej pary `<text>` znaczniki, aby umieścić tekst do renderowania.</span><span class="sxs-lookup"><span data-stu-id="0f655-255">The first example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span> <span data-ttu-id="0f655-256">W drugim przykładzie `<text>` i `</text>` tagi należy ująć w trzy wiersze, które mają niektóre uncontained tekst i niedopasowane znaczniki HTML (`<br />`), wraz z kodu serwera i pasujących tagów HTML.</span><span class="sxs-lookup"><span data-stu-id="0f655-256">In the second example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="0f655-257">Ponownie, można także poprzedzić każdego wiersza za `@:` operator; albo sposób działania.</span><span class="sxs-lookup"><span data-stu-id="0f655-257">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    > [!NOTE]
    > <span data-ttu-id="0f655-258">Podczas drukowania tekstu opisane w tej sekcji &#8212; za pomocą elementu HTML `@:` , operator lub `<text>` element &#8212; Program ASP.NET nie kodowanie HTML dane wyjściowe.</span><span class="sxs-lookup"><span data-stu-id="0f655-258">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="0f655-259">(Jak wspomniano wcześniej, ASP.NET zakodować dane wyjściowe wyrażenia kodu serwera i serwera bloki kodu, które są poprzedzone `@`, z wyjątkiem przypadków wymienionych w tej sekcji.)</span><span class="sxs-lookup"><span data-stu-id="0f655-259">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="0f655-260">Odstępu</span><span class="sxs-lookup"><span data-stu-id="0f655-260">Whitespace</span></span>

<span data-ttu-id="0f655-261">Dodatkowe spacje w instrukcji (i poza literału ciągu) nie wpływają na instrukcji:</span><span class="sxs-lookup"><span data-stu-id="0f655-261">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample15.cshtml)]

<span data-ttu-id="0f655-262">Podział wiersza w instrukcji nie ma wpływu na instrukcję i może zawijać się instrukcje dla czytelności.</span><span class="sxs-lookup"><span data-stu-id="0f655-262">A line break in a statement has no effect on the statement, and you can wrap statements for readability.</span></span> <span data-ttu-id="0f655-263">Poniższe instrukcje są takie same:</span><span class="sxs-lookup"><span data-stu-id="0f655-263">The following statements are the same:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample16.cshtml)]

<span data-ttu-id="0f655-264">Jednak nie można powielać wiersza środku literału ciągu.</span><span class="sxs-lookup"><span data-stu-id="0f655-264">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="0f655-265">Poniższy przykład nie działa:</span><span class="sxs-lookup"><span data-stu-id="0f655-265">The following example doesn't work:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample17.cshtml)]

<span data-ttu-id="0f655-266">Aby połączyć długi ciąg, który opakowuje do wielu linii, takich jak powyżej kodu, dostępne są dwie opcje.</span><span class="sxs-lookup"><span data-stu-id="0f655-266">To combine a long string that wraps to multiple lines like the above code, there are two options.</span></span> <span data-ttu-id="0f655-267">Można używać operatora łączenia (`+`), który pojawi się w dalszej części tego artykułu.</span><span class="sxs-lookup"><span data-stu-id="0f655-267">You can use the concatenation operator (`+`), which you'll see later in this article.</span></span> <span data-ttu-id="0f655-268">Można również użyć `@` znak można utworzyć ciągu dosłownego wyrażenia literału, jak przedstawiono wcześniej w tym artykule.</span><span class="sxs-lookup"><span data-stu-id="0f655-268">You can also use the `@` character to create a verbatim string literal, as you saw earlier in this article.</span></span> <span data-ttu-id="0f655-269">Literały ciągu dosłownego wyrażenia mogą być dzielone między wiersze:</span><span class="sxs-lookup"><span data-stu-id="0f655-269">You can break verbatim string literals across lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample18.cshtml)]

### <a name="code-and-markup-comments"></a><span data-ttu-id="0f655-270">Kod (i znaczników) komentarzy</span><span class="sxs-lookup"><span data-stu-id="0f655-270">Code (and Markup) Comments</span></span>

<span data-ttu-id="0f655-271">Komentarze pozwalają pozostaw notatek dla siebie lub innych użytkowników.</span><span class="sxs-lookup"><span data-stu-id="0f655-271">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="0f655-272">Umożliwiają również wyłączyć (*komentarz*) sekcji kodu lub kod znaczników, który nie chcesz uruchomić, ale można przechowywać na stronie w chwili obecnej.</span><span class="sxs-lookup"><span data-stu-id="0f655-272">They also allow you to disable (*comment out*) a section of code or markup that you don't want to run but want to keep in your page for the time being.</span></span>

<span data-ttu-id="0f655-273">Istnieje inny komentowania składnię Razor kodu oraz kod znaczników HTML.</span><span class="sxs-lookup"><span data-stu-id="0f655-273">There's different commenting syntax for Razor code and for HTML markup.</span></span> <span data-ttu-id="0f655-274">Podobnie jak w przypadku wszystkich kodu Razor, Razor komentarze są przetwarzane (i następnie usuwane) na serwerze przed wysłaniem strony do przeglądarki.</span><span class="sxs-lookup"><span data-stu-id="0f655-274">As with all Razor code, Razor comments are processed (and then removed) on the server before the page is sent to the browser.</span></span> <span data-ttu-id="0f655-275">W związku z tym składnia komentarza Razor umożliwia wprowadzone komentarze do kodu (lub nawet znaczników), które można zobaczyć, kiedy należy edytować plik, ale użytkownicy nie będą widzieli, nawet w źródle strony.</span><span class="sxs-lookup"><span data-stu-id="0f655-275">Therefore, the Razor commenting syntax lets you put comments into the code (or even into the markup) that you can see when you edit the file, but that users don't see, even in the page source.</span></span>

<span data-ttu-id="0f655-276">Komentarze ASP.NET Razor start komentarz z `@*` i Zakończ ją z `*@`.</span><span class="sxs-lookup"><span data-stu-id="0f655-276">For ASP.NET Razor comments, you start the comment with `@*` and end it with `*@`.</span></span> <span data-ttu-id="0f655-277">Komentarz może być w jednym lub wielu wierszy:</span><span class="sxs-lookup"><span data-stu-id="0f655-277">The comment can be on one line or multiple lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample19.cshtml)]

<span data-ttu-id="0f655-278">Poniżej przedstawiono komentarz w bloku kodu:</span><span class="sxs-lookup"><span data-stu-id="0f655-278">Here is a comment within a code block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample20.cshtml)]

<span data-ttu-id="0f655-279">W tym miejscu jest tym samym bloku kodu z wierszem kodu oznaczone jako komentarz tak, aby nie można go uruchomić:</span><span class="sxs-lookup"><span data-stu-id="0f655-279">Here is the same block of code, with the line of code commented out so that it won't run:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample21.cshtml)]

<span data-ttu-id="0f655-280">W bloku kodu zamiast przy użyciu składni komentarza Razor, za pomocą komentowania składni języka programowania, którego używasz, takich jak C#:</span><span class="sxs-lookup"><span data-stu-id="0f655-280">Inside a code block, as an alternative to using Razor comment syntax, you can use the commenting syntax of the programming language you're using, such as C#:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample22.cshtml)]

<span data-ttu-id="0f655-281">W języku C# są poprzedzone komentarz jednowierszowy `//` znaków i Komentarze wielowierszowe zaczynać `/*` się i kończyć `*/`.</span><span class="sxs-lookup"><span data-stu-id="0f655-281">In C#, single-line comments are preceded by the `//` characters, and multi-line comments begin with `/*` and end with `*/`.</span></span> <span data-ttu-id="0f655-282">(Tak jak w przypadku komentarz Razor komentarze języka C# nie są renderowane w przeglądarce.)</span><span class="sxs-lookup"><span data-stu-id="0f655-282">(As with Razor comments, C# comments are not rendered to the browser.)</span></span>

<span data-ttu-id="0f655-283">W przypadku znaczników prawdopodobnie wiesz, można utworzyć komentarz HTML:</span><span class="sxs-lookup"><span data-stu-id="0f655-283">For markup, as you probably know, you can create an HTML comment:</span></span>

[!code-xml[Main](introducing-razor-syntax-c/samples/sample23.xml)]

<span data-ttu-id="0f655-284">Komentarze HTML rozpoczynać `<!--` znaków i kończyć się `-->`.</span><span class="sxs-lookup"><span data-stu-id="0f655-284">HTML comments start with `<!--` characters and end with `-->`.</span></span> <span data-ttu-id="0f655-285">Komentarze HTML umożliwia przestrzenny nie tylko tekstu, ale również żadnych znaczników HTML, który można przechowywać na stronie, ale nie mają być renderowane.</span><span class="sxs-lookup"><span data-stu-id="0f655-285">You can use HTML comments to surround not only text, but also any HTML markup that you may want to keep in the page but don't want to render.</span></span> <span data-ttu-id="0f655-286">Ten komentarz HTML spowoduje ukrycie całej zawartości znaczniki i tekst, który zawiera:</span><span class="sxs-lookup"><span data-stu-id="0f655-286">This HTML comment will hide the entire content of the tags and the text they contain:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample24.html)]

<span data-ttu-id="0f655-287">W przeciwieństwie do komentarzy Razor, HTML, uwag *są* odwzorowywany na stronie, a użytkownik zobaczy ich wyświetlając źródło strony.</span><span class="sxs-lookup"><span data-stu-id="0f655-287">Unlike Razor comments, HTML comments *are* rendered to the page and the user can see them by viewing the page source.</span></span>

<span data-ttu-id="0f655-288">Razor ma ograniczenia w zagnieżdżonych bloków języka C#.</span><span class="sxs-lookup"><span data-stu-id="0f655-288">Razor has limitations on nested blocks of C#.</span></span> <span data-ttu-id="0f655-289">Aby uzyskać więcej informacji, zobacz [o nazwie C# zmiennych i zagnieżdżone bloki Generuj uszkodzony kod](http://aspnetwebstack.codeplex.com/workitem/1914)</span><span class="sxs-lookup"><span data-stu-id="0f655-289">For more information see [Named C# Variables and Nested Blocks Generate Broken Code](http://aspnetwebstack.codeplex.com/workitem/1914)</span></span>

## <a name="variables"></a><span data-ttu-id="0f655-290">Zmienne</span><span class="sxs-lookup"><span data-stu-id="0f655-290">Variables</span></span>

<span data-ttu-id="0f655-291">Zmienna jest nazwanego obiektu, który służy do przechowywania danych.</span><span class="sxs-lookup"><span data-stu-id="0f655-291">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="0f655-292">Zmienne można nazwy wszystko, ale nazwa musi zaczynać się znakiem alfabetycznym i nie może zawierać spacji ani znaków zastrzeżonych.</span><span class="sxs-lookup"><span data-stu-id="0f655-292">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="0f655-293">Typy danych i zmienne</span><span class="sxs-lookup"><span data-stu-id="0f655-293">Variables and Data Types</span></span>

<span data-ttu-id="0f655-294">Zmienna może mieć określonego typu danych, co oznacza, jakiego rodzaju dane są przechowywane w zmiennej.</span><span class="sxs-lookup"><span data-stu-id="0f655-294">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="0f655-295">Mogą mieć zmiennych ciągu, których są przechowywane wartości ciągów (takich jak &quot;Hello world&quot;), zmienne liczba całkowita, które przechowują liczby całkowitej wartości (na przykład 3 lub 79) i zmienne Data przechowywanie wartości daty w różnych formatach (na przykład 4 12/2012 lub marca 2009 ).</span><span class="sxs-lookup"><span data-stu-id="0f655-295">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="0f655-296">I istnieje wiele typów danych, w których można użyć.</span><span class="sxs-lookup"><span data-stu-id="0f655-296">And there are many other data types you can use.</span></span>

<span data-ttu-id="0f655-297">Jednak zwykle nie trzeba określać typ zmiennej.</span><span class="sxs-lookup"><span data-stu-id="0f655-297">However, you generally don't have to specify a type for a variable.</span></span> <span data-ttu-id="0f655-298">W większości przypadków, platformy ASP.NET można ustalić typu, w oparciu sposobu używania danych za pomocą zmiennej.</span><span class="sxs-lookup"><span data-stu-id="0f655-298">Most of the time, ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="0f655-299">(Czasami musi określać typ; pojawi się przykłady gdzie to PRAWDA).</span><span class="sxs-lookup"><span data-stu-id="0f655-299">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="0f655-300">Należy zadeklarować zmienną za pomocą `var` — słowo kluczowe (Jeśli nie chcesz określić typ) lub przy użyciu nazwy typu:</span><span class="sxs-lookup"><span data-stu-id="0f655-300">You declare a variable using the `var` keyword (if you don't want to specify a type) or by using the name of the type:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample25.cshtml)]

<span data-ttu-id="0f655-301">W poniższym przykładzie przedstawiono niektóre typowe zastosowania zmienne na stronie sieci web:</span><span class="sxs-lookup"><span data-stu-id="0f655-301">The following example shows some typical uses of variables in a web page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample26.cshtml)]

<span data-ttu-id="0f655-302">Jeśli znajdzie się na stronie poprzednich przykładach, widzisz taki komunikat wyświetlany w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="0f655-302">If you combine the previous examples in a page, you see this displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-c/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="0f655-304">Konwertowanie i testowania typy danych</span><span class="sxs-lookup"><span data-stu-id="0f655-304">Converting and Testing Data Types</span></span>

<span data-ttu-id="0f655-305">Mimo że program ASP.NET zazwyczaj można automatycznie określić typu danych, czasami nie jest.</span><span class="sxs-lookup"><span data-stu-id="0f655-305">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="0f655-306">W związku z tym konieczne może pomóc ASP.NET, wykonując jawnej konwersji.</span><span class="sxs-lookup"><span data-stu-id="0f655-306">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="0f655-307">Nawet jeśli nie masz konwersji typów, czasami warto test, aby sprawdzić, jakiego typu dane mogą być pracy z.</span><span class="sxs-lookup"><span data-stu-id="0f655-307">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="0f655-308">Najbardziej często zdarza się, czy masz do przekonwertowania ciągu na inny typ, takich jak do liczby całkowitej lub daty.</span><span class="sxs-lookup"><span data-stu-id="0f655-308">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="0f655-309">W poniższym przykładzie przedstawiono typowy przypadek, w którym należy przekonwertować ciąg na liczbę.</span><span class="sxs-lookup"><span data-stu-id="0f655-309">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample27.cshtml)]

<span data-ttu-id="0f655-310">Z reguły dane wejściowe użytkownika dostarczany jako ciągi.</span><span class="sxs-lookup"><span data-stu-id="0f655-310">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="0f655-311">Nawet jeśli została monitowany użytkownikom wprowadź liczbę z zakresu, a nawet wtedy, gdy ich wprowadzono cyfrę, po przesłaniu danych wejściowych użytkownika, a następnie go odczytać w kodzie, dane są w formacie ciągu.</span><span class="sxs-lookup"><span data-stu-id="0f655-311">Even if you've prompted users to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="0f655-312">W związku z tym należy przekonwertować ciągu na liczbę.</span><span class="sxs-lookup"><span data-stu-id="0f655-312">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="0f655-313">W tym przykładzie podjęcie próby wykonania arytmetycznego na wartościach bez konwertowania, następujący błąd wyników, ponieważ program ASP.NET nie można dodać dwa ciągi:</span><span class="sxs-lookup"><span data-stu-id="0f655-313">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

<span data-ttu-id="0f655-314">*Nie można niejawnie przekonwertować typu "string" do "int".*</span><span class="sxs-lookup"><span data-stu-id="0f655-314">*Cannot implicitly convert type 'string' to 'int'.*</span></span>

<span data-ttu-id="0f655-315">Aby dokonać konwersji wartości na liczby całkowite, należy wywołać `AsInt` metody.</span><span class="sxs-lookup"><span data-stu-id="0f655-315">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="0f655-316">Konwersja zakończy się pomyślnie, można następnie dodać liczb.</span><span class="sxs-lookup"><span data-stu-id="0f655-316">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="0f655-317">W poniższej tabeli wymieniono niektóre typowe metody konwersji i testowania dla zmiennych.</span><span class="sxs-lookup"><span data-stu-id="0f655-317">The following table lists some common conversion and test methods for variables.</span></span>

| <span data-ttu-id="0f655-318">**— Metoda**</span><span class="sxs-lookup"><span data-stu-id="0f655-318">**Method**</span></span> | <span data-ttu-id="0f655-319">**Opis**</span><span class="sxs-lookup"><span data-stu-id="0f655-319">**Description**</span></span> | <span data-ttu-id="0f655-320">**Przykład**</span><span class="sxs-lookup"><span data-stu-id="0f655-320">**Example**</span></span> |
| --- | --- | --- |
| `AsInt(), IsInt()` | <span data-ttu-id="0f655-321">Konwertuje ciąg reprezentujący liczbę całkowitą (na przykład "593") na liczbę całkowitą.</span><span class="sxs-lookup"><span data-stu-id="0f655-321">Converts a string that represents a whole number (like "593") to an integer.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample28.cs)] |
| `AsBool(), IsBool()` | <span data-ttu-id="0f655-322">Konwertuje ciąg, takich jak &quot;true&quot; lub &quot;false&quot; na typ Boolean.</span><span class="sxs-lookup"><span data-stu-id="0f655-322">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample29.cs)] |
| `AsFloat(), IsFloat()` | <span data-ttu-id="0f655-323">Konwertuje ciąg o wartości dziesiętnej, takich jak &quot;1.3&quot; lub &quot;7.439&quot; liczby zmiennoprzecinkowej.</span><span class="sxs-lookup"><span data-stu-id="0f655-323">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample30.cs)] |
| `AsDecimal(), IsDecimal()` | <span data-ttu-id="0f655-324">Konwertuje ciąg o wartości dziesiętnej, takich jak &quot;1.3&quot; lub &quot;7.439&quot; na liczbę dziesiętną.</span><span class="sxs-lookup"><span data-stu-id="0f655-324">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="0f655-325">(W programie ASP.NET, liczbą dziesiętną jest bardziej dokładne niż liczba zmiennoprzecinkowa).</span><span class="sxs-lookup"><span data-stu-id="0f655-325">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample31.cs)] |
| `AsDateTime(), IsDateTime()` | <span data-ttu-id="0f655-326">Konwertuje ciąg reprezentujący wartość daty i godziny do platformy ASP.NET `DateTime` typu.</span><span class="sxs-lookup"><span data-stu-id="0f655-326">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample32.cs)] |
| `ToString()` | <span data-ttu-id="0f655-327">Konwertuje ciąg inny typ danych.</span><span class="sxs-lookup"><span data-stu-id="0f655-327">Converts any other data type to a string.</span></span> | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample33.js)] |

## <a name="operators"></a><span data-ttu-id="0f655-328">Operatory</span><span class="sxs-lookup"><span data-stu-id="0f655-328">Operators</span></span>

<span data-ttu-id="0f655-329">Operator jest słowo kluczowe lub znak, który informuje ASP.NET, jakiego rodzaju polecenie do wykonania w wyrażeniu.</span><span class="sxs-lookup"><span data-stu-id="0f655-329">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="0f655-330">W języku C# (i składni Razor oparty na nim) obsługuje wielu operatorów, ale musisz rozpoznać kilka, aby rozpocząć pracę.</span><span class="sxs-lookup"><span data-stu-id="0f655-330">The C# language (and the Razor syntax that's based on it) supports many operators, but you only need to recognize a few to get started.</span></span> <span data-ttu-id="0f655-331">Poniższa tabela zawiera podsumowanie typowych operatorów.</span><span class="sxs-lookup"><span data-stu-id="0f655-331">The following table summarizes the most common operators.</span></span>

| <span data-ttu-id="0f655-332">**Operator**</span><span class="sxs-lookup"><span data-stu-id="0f655-332">**Operator**</span></span> | <span data-ttu-id="0f655-333">**Opis**</span><span class="sxs-lookup"><span data-stu-id="0f655-333">**Description**</span></span> | <span data-ttu-id="0f655-334">**Przykłady**</span><span class="sxs-lookup"><span data-stu-id="0f655-334">**Examples**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="0f655-335">`+` `-` `*` `/`</span><span class="sxs-lookup"><span data-stu-id="0f655-335">`+` `-` `*` `/`</span></span> | <span data-ttu-id="0f655-336">Operatory matematyczne używać w wyrażeniach numerycznych.</span><span class="sxs-lookup"><span data-stu-id="0f655-336">Math operators used in numerical expressions.</span></span> | [!code-css[Main](introducing-razor-syntax-c/samples/sample34.css)] |
| `=` | <span data-ttu-id="0f655-337">Przypisanie.</span><span class="sxs-lookup"><span data-stu-id="0f655-337">Assignment.</span></span> <span data-ttu-id="0f655-338">Przypisuje wartości po prawej stronie instrukcji obiektu po lewej stronie.</span><span class="sxs-lookup"><span data-stu-id="0f655-338">Assigns the value on the right side of a statement to the object on the left side.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample35.cs)] |
| `==` | <span data-ttu-id="0f655-339">Równości.</span><span class="sxs-lookup"><span data-stu-id="0f655-339">Equality.</span></span> <span data-ttu-id="0f655-340">Zwraca `true` Jeśli wartości są równe.</span><span class="sxs-lookup"><span data-stu-id="0f655-340">Returns `true` if the values are equal.</span></span> <span data-ttu-id="0f655-341">(Zwróć uwagę, że `=` operatora i `==` operatora.)</span><span class="sxs-lookup"><span data-stu-id="0f655-341">(Notice the distinction between the `=` operator and the `==` operator.)</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample36.cs)] |
| `!=` | <span data-ttu-id="0f655-342">Nierówności.</span><span class="sxs-lookup"><span data-stu-id="0f655-342">Inequality.</span></span> <span data-ttu-id="0f655-343">Zwraca `true` wartości nie są równe.</span><span class="sxs-lookup"><span data-stu-id="0f655-343">Returns `true` if the values are not equal.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample37.cs)] |
| `< > <= >=` | <span data-ttu-id="0f655-344">Mniej-niż większe-niż mniej niż — lub równości i większa niż lub równości.</span><span class="sxs-lookup"><span data-stu-id="0f655-344">Less-than, greater-than, less-than-or-equal, and greater-than-or-equal.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample38.cs)] |
| `+` | <span data-ttu-id="0f655-345">Łączenie, który jest używany do przyłączenia ciągów.</span><span class="sxs-lookup"><span data-stu-id="0f655-345">Concatenation, which is used to join strings.</span></span> <span data-ttu-id="0f655-346">ASP.NET wie, że różnica między Ten operator i operator dodawania na podstawie typu danych wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="0f655-346">ASP.NET knows the difference between this operator and the addition operator based on the data type of the expression.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample39.cs)] |
| <span data-ttu-id="0f655-347">`+=``-=`</span><span class="sxs-lookup"><span data-stu-id="0f655-347">`+=` `-=`</span></span> | <span data-ttu-id="0f655-348">Operatory inkrementacji i dekrementacji, które dodawania i odejmowania 1 (odpowiednio) ze zmienną.</span><span class="sxs-lookup"><span data-stu-id="0f655-348">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample40.cs)] |
| `.` | <span data-ttu-id="0f655-349">Kropki.</span><span class="sxs-lookup"><span data-stu-id="0f655-349">Dot.</span></span> <span data-ttu-id="0f655-350">Pozwala odróżnić obiektów i ich właściwości i metody.</span><span class="sxs-lookup"><span data-stu-id="0f655-350">Used to distinguish objects and their properties and methods.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample41.cs)] |
| `()` | <span data-ttu-id="0f655-351">Nawiasy.</span><span class="sxs-lookup"><span data-stu-id="0f655-351">Parentheses.</span></span> <span data-ttu-id="0f655-352">Używane do wyrażenia grupy oraz do przekazania parametrów do metod.</span><span class="sxs-lookup"><span data-stu-id="0f655-352">Used to group expressions and to pass parameters to methods.</span></span> | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample42.js)] |
| `[]` | <span data-ttu-id="0f655-353">Nawiasy kwadratowe.</span><span class="sxs-lookup"><span data-stu-id="0f655-353">Brackets.</span></span> <span data-ttu-id="0f655-354">Używane do uzyskiwania dostępu do wartości w macierzy lub kolekcji.</span><span class="sxs-lookup"><span data-stu-id="0f655-354">Used for accessing values in arrays or collections.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample43.cs)] |
| `!` | <span data-ttu-id="0f655-355">Nie.</span><span class="sxs-lookup"><span data-stu-id="0f655-355">Not.</span></span> <span data-ttu-id="0f655-356">Odwraca `true` do wartości `false` i na odwrót.</span><span class="sxs-lookup"><span data-stu-id="0f655-356">Reverses a `true` value to `false` and vice versa.</span></span> <span data-ttu-id="0f655-357">Zazwyczaj używany jako sposób skrócona do testowania `false` (oznacza to, aby nie `true`).</span><span class="sxs-lookup"><span data-stu-id="0f655-357">Typically used as a shorthand way to test for `false` (that is, for not `true`).</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample44.cs)] |
| <span data-ttu-id="0f655-358">`&&`<code>&#124;&#124;</code></span><span class="sxs-lookup"><span data-stu-id="0f655-358">`&&` <code>&#124;&#124;</code></span></span> | <span data-ttu-id="0f655-359">Logiczny AND i lub, w którym są używane do łączenia ze sobą warunki.</span><span class="sxs-lookup"><span data-stu-id="0f655-359">Logical AND and OR, which are used to link conditions together.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample45.cs)] |

<a id="ID_WorkingWithFileAndFolderPaths"></a>
## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="0f655-360">Praca z pliku i ścieżki folderu w kodzie</span><span class="sxs-lookup"><span data-stu-id="0f655-360">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="0f655-361">Ścieżki plików i folderów będzie często współpracować w kodzie.</span><span class="sxs-lookup"><span data-stu-id="0f655-361">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="0f655-362">Oto przykład struktury folderu fizycznego dla witryny sieci Web może pojawiać się na komputerze deweloperskim:</span><span class="sxs-lookup"><span data-stu-id="0f655-362">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="0f655-363">Oto niektóre istotne szczegóły dotyczące adresów URL i ścieżki:</span><span class="sxs-lookup"><span data-stu-id="0f655-363">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="0f655-364">Adres URL rozpoczyna się od jedną nazwę domeny (`http://www.example.com`) lub nazwy serwera (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="0f655-364">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="0f655-365">Adres URL odnosi się do ścieżki fizycznej na komputerze hosta.</span><span class="sxs-lookup"><span data-stu-id="0f655-365">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="0f655-366">Na przykład `http://myserver` może odpowiadać do folderu *C:\websites\mywebsite* na serwerze.</span><span class="sxs-lookup"><span data-stu-id="0f655-366">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="0f655-367">Ścieżka wirtualna jest skrócona do reprezentowania ścieżek w kodzie bez konieczności określania pełną ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="0f655-367">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="0f655-368">Zawiera części adresu URL, który następuje nazwa domeny lub tego serwera.</span><span class="sxs-lookup"><span data-stu-id="0f655-368">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="0f655-369">Użycie ścieżki wirtualne, można przenieść do innej domeny lub serwer kodu bez konieczności aktualizacji ścieżki.</span><span class="sxs-lookup"><span data-stu-id="0f655-369">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="0f655-370">Oto przykład, aby ułatwić zrozumienie różnic:</span><span class="sxs-lookup"><span data-stu-id="0f655-370">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="0f655-371">Pełny adres URL</span><span class="sxs-lookup"><span data-stu-id="0f655-371">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="0f655-372">Nazwa serwera</span><span class="sxs-lookup"><span data-stu-id="0f655-372">Server name</span></span> | <span data-ttu-id="0f655-373">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="0f655-373">*mycompanyserver*</span></span> |
| <span data-ttu-id="0f655-374">Ścieżka wirtualna</span><span class="sxs-lookup"><span data-stu-id="0f655-374">Virtual path</span></span> | <span data-ttu-id="0f655-375">*/humanresources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="0f655-375">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="0f655-376">Ścieżka fizyczna</span><span class="sxs-lookup"><span data-stu-id="0f655-376">Physical path</span></span> | <span data-ttu-id="0f655-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="0f655-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="0f655-378">Wirtualnego katalogu głównego jest /, podobnie jak w folderze głównym dysku c dysku \.</span><span class="sxs-lookup"><span data-stu-id="0f655-378">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="0f655-379">(Folder wirtualny ścieżek zawsze używać ukośniki). Ścieżka wirtualna folderu nie musi mieć taką samą nazwę jak folder fizycznych; może to być alias.</span><span class="sxs-lookup"><span data-stu-id="0f655-379">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="0f655-380">(Na serwerach produkcyjnych, ścieżka wirtualna rzadko zgodna dokładnej ścieżki fizycznej.)</span><span class="sxs-lookup"><span data-stu-id="0f655-380">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="0f655-381">Podczas pracy z plikami i folderami w kodzie, czasami należy odwoływać się do ścieżki fizycznej, a czasami ścieżki wirtualnej, w zależności od tego, jakie obiekty użytkownik pracuje.</span><span class="sxs-lookup"><span data-stu-id="0f655-381">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="0f655-382">ASP.NET udostępnia te narzędzia do pracy z ścieżek plików i folderów w kodzie: `Server.MapPath` metody i `~` operatora i `Href` metody.</span><span class="sxs-lookup"><span data-stu-id="0f655-382">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="0f655-383">Konwertowanie ścieżki wirtualnej do fizycznego: Metoda Server.MapPath</span><span class="sxs-lookup"><span data-stu-id="0f655-383">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="0f655-384">`Server.MapPath` Metoda konwertuje ścieżki wirtualnej (takie jak */default.cshtml*) do bezwzględna ścieżka fizyczna (takich jak *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="0f655-384">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="0f655-385">Możesz użyć tej metody w każdej chwili należy pełną ścieżkę fizyczną.</span><span class="sxs-lookup"><span data-stu-id="0f655-385">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="0f655-386">Typowym przykładem jest podczas odczytywania lub zapisywania pliku tekstowego lub pliku obrazu na serwerze sieci web.</span><span class="sxs-lookup"><span data-stu-id="0f655-386">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="0f655-387">Zwykle nie wiadomo bezwzględna ścieżka fizyczna witryny na serwerze lokacji hostingu, więc tej metody można przekonwertować ścieżki wiadomo, — ścieżka wirtualna — do odpowiedniego ścieżki na serwerze dla Ciebie.</span><span class="sxs-lookup"><span data-stu-id="0f655-387">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="0f655-388">Ścieżka wirtualna są przekazywane do pliku lub folderu do metody i zwraca ścieżkę fizyczną:</span><span class="sxs-lookup"><span data-stu-id="0f655-388">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample46.cshtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="0f655-389">Odwołanie do wirtualnego katalogu głównego: ~ operatora i Href — metoda</span><span class="sxs-lookup"><span data-stu-id="0f655-389">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="0f655-390">W *.cshtml* lub *.vbhtml* plików, można odwoływać się przy użyciu ścieżka wirtualnego katalogu głównego `~` operatora.</span><span class="sxs-lookup"><span data-stu-id="0f655-390">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="0f655-391">Jest to bardzo przydatne, ponieważ można przenosić stron w witrynie, a wszystkie łącza, które zawierają do innych stron będzie przerwane.</span><span class="sxs-lookup"><span data-stu-id="0f655-391">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="0f655-392">Możliwe jest również przydatne w przypadku gdy przeniesiesz kiedykolwiek witryny sieci Web w innej lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="0f655-392">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="0f655-393">Oto kilka przykładów:</span><span class="sxs-lookup"><span data-stu-id="0f655-393">Here are some examples:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample47.cshtml)]

<span data-ttu-id="0f655-394">Jeśli witryna sieci Web jest `http://myserver/myapp`, Oto jak ASP.NET, będą traktować te ścieżki po uruchomieniu na stronie:</span><span class="sxs-lookup"><span data-stu-id="0f655-394">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="0f655-395">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="0f655-395">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="0f655-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="0f655-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="0f655-397">(Te ścieżki faktycznie nie będzie widoczny jako wartości zmiennej, ale ASP.NET będzie traktować ścieżki tak, jakby to, jakie były).</span><span class="sxs-lookup"><span data-stu-id="0f655-397">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="0f655-398">Można użyć `~` operator zarówno w kodzie serwera (jak powyżej), jak i w znaczniku w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="0f655-398">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample48.html)]

<span data-ttu-id="0f655-399">W znaczniku, użyj `~` operator do tworzenia ścieżek do zasobów, takich jak pliki obrazów, innych stron sieci web i pliki CSS.</span><span class="sxs-lookup"><span data-stu-id="0f655-399">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="0f655-400">Po uruchomieniu strony ASP.NET sprawdza za pośrednictwem strony (kod i znaczników) i usuwa wszystkie `~` odwołania do danej ścieżce.</span><span class="sxs-lookup"><span data-stu-id="0f655-400">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="0f655-401">Logikę warunkową i pętli</span><span class="sxs-lookup"><span data-stu-id="0f655-401">Conditional Logic and Loops</span></span>

<span data-ttu-id="0f655-402">Kodu serwera ASP.NET umożliwia wykonywanie zadań na podstawie warunków i pisania kodu, który powtarza instrukcje określoną liczbę razy (to znaczy kodu wykonywanego w pętli).</span><span class="sxs-lookup"><span data-stu-id="0f655-402">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times (that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="0f655-403">Testowanie warunków</span><span class="sxs-lookup"><span data-stu-id="0f655-403">Testing Conditions</span></span>

<span data-ttu-id="0f655-404">Aby przetestować prosty warunek użyjesz `if` instrukcji, która zwraca wartość PRAWDA lub FAŁSZ, oparta na test, możesz określić:</span><span class="sxs-lookup"><span data-stu-id="0f655-404">To test a simple condition you use the `if` statement, which returns true or false based on a test you specify:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample49.cshtml)]

<span data-ttu-id="0f655-405">`if` — Słowo kluczowe uruchamia blok.</span><span class="sxs-lookup"><span data-stu-id="0f655-405">The `if` keyword starts a block.</span></span> <span data-ttu-id="0f655-406">Rzeczywiste testu (warunek) jest w nawiasach i zwraca wartość PRAWDA lub FAŁSZ.</span><span class="sxs-lookup"><span data-stu-id="0f655-406">The actual test (condition) is in parentheses and returns true or false.</span></span> <span data-ttu-id="0f655-407">Instrukcje, które są uruchamiane wtedy test ma wartość PRAWDA są ujęte w nawiasy klamrowe.</span><span class="sxs-lookup"><span data-stu-id="0f655-407">The statements that run if the test is true are enclosed in braces.</span></span> <span data-ttu-id="0f655-408">`if` Może zawierać instrukcji `else` bloku, który określa instrukcje do uruchomienia, jeśli wynikiem warunku jest FAŁSZ:</span><span class="sxs-lookup"><span data-stu-id="0f655-408">An `if` statement can include an `else` block that specifies statements to run if the condition is false:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample50.cshtml)]

<span data-ttu-id="0f655-409">Można dodać wiele warunków przy użyciu `else if` bloku:</span><span class="sxs-lookup"><span data-stu-id="0f655-409">You can add multiple conditions using an `else if` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample51.cshtml)]

<span data-ttu-id="0f655-410">W tym przykładzie, jeśli pierwszy warunek w, gdy blok nie jest prawdziwe, `else if` warunku jest zaznaczony.</span><span class="sxs-lookup"><span data-stu-id="0f655-410">In this example, if the first condition in the if block is not true, the `else if` condition is checked.</span></span> <span data-ttu-id="0f655-411">Jeśli ten warunek jest spełniony, instrukcje w `else if` bloku są wykonywane.</span><span class="sxs-lookup"><span data-stu-id="0f655-411">If that condition is met, the statements in the `else if` block are executed.</span></span> <span data-ttu-id="0f655-412">Jeśli żaden z warunków nie jest spełniony, instrukcje w `else` bloku są wykonywane.</span><span class="sxs-lookup"><span data-stu-id="0f655-412">If none of the conditions are met, the statements in the `else` block are executed.</span></span> <span data-ttu-id="0f655-413">Można dodać dowolną liczbę else if blokuje, a następnie Zamknij z `else` zablokować jako &quot;wszystkich pozostałych&quot; warunku.</span><span class="sxs-lookup"><span data-stu-id="0f655-413">You can add any number of else if blocks, and then close with an `else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="0f655-414">Aby przetestować wiele warunków, użyj `switch` bloku:</span><span class="sxs-lookup"><span data-stu-id="0f655-414">To test a large number of conditions, use a `switch` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample52.cshtml)]

<span data-ttu-id="0f655-415">Wartość do sprawdzenia jest w nawiasach (w tym przykładzie `weekday` zmiennej).</span><span class="sxs-lookup"><span data-stu-id="0f655-415">The value to test is in parentheses (in the example, the `weekday` variable).</span></span> <span data-ttu-id="0f655-416">Każdy test poszczególnych używa `case` instrukcji, która kończy się średnikiem (;).</span><span class="sxs-lookup"><span data-stu-id="0f655-416">Each individual test uses a `case` statement that ends with a colon (:).</span></span> <span data-ttu-id="0f655-417">Jeśli wartość `case` instrukcji jest zgodna z wartością testu, wykonywany jest kod w tym przypadku bloku.</span><span class="sxs-lookup"><span data-stu-id="0f655-417">If the value of a `case` statement matches the test value, the code in that case block is executed.</span></span> <span data-ttu-id="0f655-418">Zamknij każdej instrukcji case z `break` instrukcji.</span><span class="sxs-lookup"><span data-stu-id="0f655-418">You close each case statement with a `break` statement.</span></span> <span data-ttu-id="0f655-419">(Jeśli zapomnisz do uwzględnienia podziału w każdym `case` bloku kodu z następnej `case` instrukcji uruchomi również.) A `switch` blok często ma `default` instrukcji, jak w przypadku ostatniego &quot;wszystkich pozostałych&quot; opcji, jeśli żaden z innych przypadków nie jest spełniony.</span><span class="sxs-lookup"><span data-stu-id="0f655-419">(If you forget to include break in each `case` block, the code from the next `case` statement will run also.) A `switch` block often has a `default` statement as the last case for an &quot;everything else&quot; option that runs if none of the other cases are true.</span></span>

<span data-ttu-id="0f655-420">Wynik ostatnich dwóch bloków warunkowych wyświetlany w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="0f655-420">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-c/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="0f655-422">Kod pętli</span><span class="sxs-lookup"><span data-stu-id="0f655-422">Looping Code</span></span>

<span data-ttu-id="0f655-423">Często konieczne jest cyklicznie uruchamiany tej samej instrukcji.</span><span class="sxs-lookup"><span data-stu-id="0f655-423">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="0f655-424">W tym celu zapętlenia.</span><span class="sxs-lookup"><span data-stu-id="0f655-424">You do this by looping.</span></span> <span data-ttu-id="0f655-425">Na przykład często uruchomieniu same instrukcje dla każdego elementu w kolekcji danych.</span><span class="sxs-lookup"><span data-stu-id="0f655-425">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="0f655-426">Jeśli wiesz, dokładnie tak jak często chcesz pętli, możesz użyć `for` pętli.</span><span class="sxs-lookup"><span data-stu-id="0f655-426">If you know exactly how many times you want to loop, you can use a `for` loop.</span></span> <span data-ttu-id="0f655-427">Tego rodzaju pętli jest szczególnie przydatna w przypadku licząc w lub inwentaryzacji:</span><span class="sxs-lookup"><span data-stu-id="0f655-427">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample53.html)]

<span data-ttu-id="0f655-428">Rozpoczyna się od pętli `for` — słowo kluczowe następują trzy instrukcje w nawiasach, każdy kończyć się średnikiem.</span><span class="sxs-lookup"><span data-stu-id="0f655-428">The loop begins with the `for` keyword, followed by three statements in parentheses, each terminated with a semicolon.</span></span>

- <span data-ttu-id="0f655-429">Wewnątrz nawiasów, w pierwszej instrukcji (`var i=10;`) licznik tworzy i inicjuje go do 10.</span><span class="sxs-lookup"><span data-stu-id="0f655-429">Inside the parentheses, the first statement (`var i=10;`) creates a counter and initializes it to 10.</span></span> <span data-ttu-id="0f655-430">Nie masz nazwy licznika `i` &#8212; można użyć dowolnej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="0f655-430">You don't have to name the counter `i` &#8212; you can use any variable.</span></span> <span data-ttu-id="0f655-431">Gdy `for` pętla zostanie uruchomiona, licznik jest automatycznie zwiększany.</span><span class="sxs-lookup"><span data-stu-id="0f655-431">When the `for` loop runs, the counter is automatically incremented.</span></span>
- <span data-ttu-id="0f655-432">Drugi — instrukcja (`i < 21;`) ustawia warunek dla jakim ma być liczba.</span><span class="sxs-lookup"><span data-stu-id="0f655-432">The second statement (`i < 21;`) sets the condition for how far you want to count.</span></span> <span data-ttu-id="0f655-433">W takim przypadku ma przejść do maksymalnie 20 (oznacza to, Graj dalej podczas licznik jest mniejsza niż 21).</span><span class="sxs-lookup"><span data-stu-id="0f655-433">In this case, you want it to go to a maximum of 20 (that is, keep going while the counter is less than 21).</span></span>
- <span data-ttu-id="0f655-434">Trzeci — instrukcja (`i++` ) używa operatora inkrementacji, która po prostu Określa, że licznik powinny mieć do niego dodana każdym uruchomieniu pętli 1.</span><span class="sxs-lookup"><span data-stu-id="0f655-434">The third statement (`i++` ) uses an increment operator, which simply specifies that the counter should have 1 added to it each time the loop runs.</span></span>

<span data-ttu-id="0f655-435">W nawiasach klamrowych jest kod, który będzie wykonywany dla każdej iteracji pętli.</span><span class="sxs-lookup"><span data-stu-id="0f655-435">Inside the braces is the code that will run for each iteration of the loop.</span></span> <span data-ttu-id="0f655-436">Kod znaczników, tworzy nowy akapit (`<p>` element) każdego czasu i dodaje wiersz danych wyjściowych, wyświetlanie wartość `i` (licznik).</span><span class="sxs-lookup"><span data-stu-id="0f655-436">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of `i` (the counter).</span></span> <span data-ttu-id="0f655-437">Po uruchomieniu tej strony w przykładzie jest tworzony 11 wiersza wyświetlania danych wyjściowych, z tekstem w każdym wierszu wskazującą liczbę elementów.</span><span class="sxs-lookup"><span data-stu-id="0f655-437">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-c/_static/image11.jpg)

<span data-ttu-id="0f655-439">Jeśli pracujesz z kolekcji lub tablicy, często używasz `foreach` pętli.</span><span class="sxs-lookup"><span data-stu-id="0f655-439">If you're working with a collection or array, you often use a `foreach` loop.</span></span> <span data-ttu-id="0f655-440">Kolekcja jest grupą podobne obiekty i `foreach` pętli pozwala przeprowadzić zadanie dla każdego elementu w kolekcji.</span><span class="sxs-lookup"><span data-stu-id="0f655-440">A collection is a group of similar objects, and the `foreach` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="0f655-441">Ten typ pętli jest wygodne dla kolekcji, ponieważ w odróżnieniu od `for` pętli, nie trzeba zwiększyć licznik lub ustawienie limitu.</span><span class="sxs-lookup"><span data-stu-id="0f655-441">This type of loop is convenient for collections, because unlike a `for` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="0f655-442">Zamiast tego `foreach` pętli kod wykonywany po prostu za pomocą kolekcji dopiero po jej zakończeniu.</span><span class="sxs-lookup"><span data-stu-id="0f655-442">Instead, the `foreach` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="0f655-443">Na przykład następujący kod zwraca elementy `Request.ServerVariables` kolekcji, który jest obiektem, który zawiera informacje o serwerze sieci web.</span><span class="sxs-lookup"><span data-stu-id="0f655-443">For example, the following code returns the items in the `Request.ServerVariables` collection, which is an object that contains information about your web server.</span></span> <span data-ttu-id="0f655-444">Używa `foreac` h pętli do wyświetlenia nazwy każdego elementu w celu tworzenia nowego `<li>` element na liście punktowanej HTML.</span><span class="sxs-lookup"><span data-stu-id="0f655-444">It uses a `foreac` h loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample54.html)]

<span data-ttu-id="0f655-445">`foreach` Słowie kluczowym występuje w nawiasach, gdzie zadeklarować zmiennej, która reprezentuje jeden element w kolekcji (w tym przykładzie `var item`), a następnie `in` — słowo kluczowe, a następnie według kolekcji ma pętli.</span><span class="sxs-lookup"><span data-stu-id="0f655-445">The `foreach` keyword is followed by parentheses where you declare a variable that represents a single item in the collection (in the example, `var item`), followed by the `in` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="0f655-446">W treści `foreach` pętli, są dostępne bieżącego elementu przy użyciu zgłoszonego wcześniej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="0f655-446">In the body of the `foreach` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-c/_static/image12.jpg)

<span data-ttu-id="0f655-448">Aby utworzyć bardziej ogólnego przeznaczenia pętli, użyj `while` instrukcji:</span><span class="sxs-lookup"><span data-stu-id="0f655-448">To create a more general-purpose loop, use the `while` statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample55.cshtml)]

<span data-ttu-id="0f655-449">A `while` pętli rozpoczyna się od `while` — słowo kluczowe, następuje nawiasów której określić, jak długo nadal pętli (tutaj, aby uzyskać tak długo, jak `countNum` jest mniejsza niż 50), następnie bloku powtórzeń.</span><span class="sxs-lookup"><span data-stu-id="0f655-449">A `while` loop begins with the `while` keyword, followed by parentheses where you specify how long the loop continues (here, for as long as `countNum` is less than 50), then the block to repeat.</span></span> <span data-ttu-id="0f655-450">Zwykle zwiększenie pętli (dodać) lub zmniejszenie (odejmowanie) zmiennej lub obiekt używany do zliczania.</span><span class="sxs-lookup"><span data-stu-id="0f655-450">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="0f655-451">W tym przykładzie `+=` operator dodaje od 1 do `countNum` każdym uruchomieniu pętli.</span><span class="sxs-lookup"><span data-stu-id="0f655-451">In the example, the `+=` operator adds 1 to `countNum` each time the loop runs.</span></span> <span data-ttu-id="0f655-452">(Aby zmniejszyć zmiennej w pętli, które zlicza w dół, użyje operator dekrementacji `-=`).</span><span class="sxs-lookup"><span data-stu-id="0f655-452">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`).</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="0f655-453">Obiektów i kolekcji</span><span class="sxs-lookup"><span data-stu-id="0f655-453">Objects and Collections</span></span>

<span data-ttu-id="0f655-454">Prawie wszystko w witrynie sieci Web platformy ASP.NET jest obiektem, w tym strona sieci web.</span><span class="sxs-lookup"><span data-stu-id="0f655-454">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="0f655-455">W tej sekcji omówiono niektóre obiekty ważne, często będą korzystać z kodu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="0f655-455">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="0f655-456">Obiekty strony</span><span class="sxs-lookup"><span data-stu-id="0f655-456">Page Objects</span></span>

<span data-ttu-id="0f655-457">Najbardziej podstawowa obiektu w programie ASP.NET jest to strona.</span><span class="sxs-lookup"><span data-stu-id="0f655-457">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="0f655-458">Można uzyskać dostępu do właściwości obiektu strony bezpośrednio, bez żadnych obiektów kwalifikujących.</span><span class="sxs-lookup"><span data-stu-id="0f655-458">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="0f655-459">Poniższy kod pobiera ścieżkę do pliku, przy użyciu `Request` obiekt strony:</span><span class="sxs-lookup"><span data-stu-id="0f655-459">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample56.cshtml)]

<span data-ttu-id="0f655-460">Aby była wyczyść że w przypadku odwołuje właściwości i metody dla bieżącego obiektu strony można opcjonalnie użyć słowa kluczowego `this` do reprezentowania obiektów strony w kodzie.</span><span class="sxs-lookup"><span data-stu-id="0f655-460">To make it clear that you're referencing properties and methods on the current page object, you can optionally use the keyword `this` to represent the page object in your code.</span></span> <span data-ttu-id="0f655-461">Poniżej przedstawiono w poprzednim przykładzie kodu z `this` dodane do reprezentowania strony:</span><span class="sxs-lookup"><span data-stu-id="0f655-461">Here is the previous code example, with `this` added to represent the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample57.cshtml)]

<span data-ttu-id="0f655-462">Można użyć właściwości `Page` obiekt, aby pobrać wiele informacji, takich jak:</span><span class="sxs-lookup"><span data-stu-id="0f655-462">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="0f655-463">`Request`.</span><span class="sxs-lookup"><span data-stu-id="0f655-463">`Request`.</span></span> <span data-ttu-id="0f655-464">Jak już przeczytane, to zbiór informacji o bieżącym żądaniu, w tym, jakiego rodzaju przeglądarki zgłosił żądanie, adres URL strony tożsamości użytkownika, itp.</span><span class="sxs-lookup"><span data-stu-id="0f655-464">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="0f655-465">`Response`.</span><span class="sxs-lookup"><span data-stu-id="0f655-465">`Response`.</span></span> <span data-ttu-id="0f655-466">To jest zbiorem informacji na temat odpowiedzi (strona), który będzie wysyłany do przeglądarki, jeśli kod serwera zakończył działanie.</span><span class="sxs-lookup"><span data-stu-id="0f655-466">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="0f655-467">Na przykład służy tej właściwości można zapisać informacji do odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="0f655-467">For example, you can use this property to write information into the response.</span></span> 

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample58.cshtml)]

<a id="ID_CollectionsAndObjects"></a>
### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="0f655-468">Kolekcja obiektów (tablice i słowników)</span><span class="sxs-lookup"><span data-stu-id="0f655-468">Collection Objects (Arrays and Dictionaries)</span></span>

<span data-ttu-id="0f655-469">A *kolekcji* jest grupy obiektów tego samego typu, takich jak kolekcja `Customer` obiektów z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="0f655-469">A *collection* is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="0f655-470">Program ASP.NET zawiera wiele kolekcji wbudowanych, tak samo, jak `Request.Files` kolekcji.</span><span class="sxs-lookup"><span data-stu-id="0f655-470">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="0f655-471">Często będzie współpracować danych w kolekcjach.</span><span class="sxs-lookup"><span data-stu-id="0f655-471">You'll often work with data in collections.</span></span> <span data-ttu-id="0f655-472">Istnieją dwa typy kolekcji wspólnej *tablicy* i *słownika*.</span><span class="sxs-lookup"><span data-stu-id="0f655-472">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="0f655-473">Tablica jest przydatne, gdy chcesz przechowywanie kolekcji podobnych elementów, ale nie chcesz utworzyć oddzielne zmienną do przechowywania każdego elementu:</span><span class="sxs-lookup"><span data-stu-id="0f655-473">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample59.cshtml)]

<span data-ttu-id="0f655-474">Dla tablic, należy zadeklarować określonego typu danych, takich jak `string`, `int`, lub `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="0f655-474">With arrays, you declare a specific data type, such as `string`, `int`, or `DateTime`.</span></span> <span data-ttu-id="0f655-475">Aby wskazać, że zmienna może zawierać tablicę, Dodaj nawiasy deklaracji (takich jak `string[]` lub `int[]`).</span><span class="sxs-lookup"><span data-stu-id="0f655-475">To indicate that the variable can contain an array, you add brackets to the declaration (such as `string[]` or `int[]`).</span></span> <span data-ttu-id="0f655-476">Można uzyskać dostępu do elementów w tablicy przy użyciu ich pozycji (indeks) lub za pomocą `foreach` instrukcji.</span><span class="sxs-lookup"><span data-stu-id="0f655-476">You can access items in an array using their position (index) or by using the `foreach` statement.</span></span> <span data-ttu-id="0f655-477">Indeksy tablicy są liczony od zera &#8212; oznacza to, że pierwszy element jest w pozycji 0, drugi element znajduje się na pozycji 1 i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="0f655-477">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample60.cshtml)]

<span data-ttu-id="0f655-478">Można określić liczbę elementów w tablicy, pobierając jego `Length` właściwości.</span><span class="sxs-lookup"><span data-stu-id="0f655-478">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="0f655-479">Aby uzyskać położenie konkretny element w tablicy (Aby wyszukać), należy użyć `Array.IndexOf` metody.</span><span class="sxs-lookup"><span data-stu-id="0f655-479">To get the position of a specific item in the array (to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="0f655-480">Użytkownik może również wykonywać następujące czynności wstecznego zawartości tablicy ( `Array.Reverse` — metoda) i sortować zawartość ( `Array.Sort` metody).</span><span class="sxs-lookup"><span data-stu-id="0f655-480">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="0f655-481">Dane wyjściowe kod tablicy ciąg wyświetlany w przeglądarce:</span><span class="sxs-lookup"><span data-stu-id="0f655-481">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-c/_static/image13.jpg)

<span data-ttu-id="0f655-483">Słownik jest kolekcją par klucz/wartość, gdzie Podaj klucz (lub nazwę) można ustawić lub pobrać odpowiadającej jej wartości:</span><span class="sxs-lookup"><span data-stu-id="0f655-483">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample61.cshtml)]

<span data-ttu-id="0f655-484">Aby utworzyć słownika, należy użyć `new` — słowo kluczowe, aby wskazać, czy tworzysz nowy obiekt słownika.</span><span class="sxs-lookup"><span data-stu-id="0f655-484">To create a dictionary, you use the `new` keyword to indicate that you're creating a new dictionary object.</span></span> <span data-ttu-id="0f655-485">Słownik można przypisać do zmiennej przy użyciu `var` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="0f655-485">You can assign a dictionary to a variable using the `var` keyword.</span></span> <span data-ttu-id="0f655-486">Wskazuje typy danych elementów w słowniku przy użyciu nawiasu ostrego ( `< >` ).</span><span class="sxs-lookup"><span data-stu-id="0f655-486">You indicate the data types of the items in the dictionary using angle brackets ( `< >` ).</span></span> <span data-ttu-id="0f655-487">Na końcu deklaracji należy dodać parę nawiasów, ponieważ jest to rzeczywiście metodę, która tworzy nowy słownik.</span><span class="sxs-lookup"><span data-stu-id="0f655-487">At the end of the declaration, you must add a pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="0f655-488">Aby dodać elementy do słownika, należy wywołać `Add` metody na zmienną słownika (`myScores` w takim przypadku), a następnie określ klucz i wartość.</span><span class="sxs-lookup"><span data-stu-id="0f655-488">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="0f655-489">Alternatywnie można nawiasy kwadratowe wskazuje klucz i wykonaj przypisanie proste, jak w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="0f655-489">Alternatively, you can use square brackets to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample62.cs)]

<span data-ttu-id="0f655-490">Aby uzyskać wartość ze słownika, należy określić klucz w nawiasach:</span><span class="sxs-lookup"><span data-stu-id="0f655-490">To get a value from the dictionary, you specify the key in brackets:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample63.cs)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="0f655-491">Wywoływanie metody z parametrami</span><span class="sxs-lookup"><span data-stu-id="0f655-491">Calling Methods with Parameters</span></span>

<span data-ttu-id="0f655-492">Omówione w tym artykule, obiekty, które program z może mieć metody.</span><span class="sxs-lookup"><span data-stu-id="0f655-492">As you read earlier in this article, the objects that you program with can have methods.</span></span> <span data-ttu-id="0f655-493">Na przykład `Database` obiekt może mieć `Database.Connect` metody.</span><span class="sxs-lookup"><span data-stu-id="0f655-493">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="0f655-494">Wiele metod również mieć co najmniej jeden parametr.</span><span class="sxs-lookup"><span data-stu-id="0f655-494">Many methods also have one or more parameters.</span></span> <span data-ttu-id="0f655-495">A *parametru* jest wartością, który jest przekazywany do metody Aby włączyć metodę zakończy się.</span><span class="sxs-lookup"><span data-stu-id="0f655-495">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="0f655-496">Na przykład przyjrzeć się deklaracji `Request.MapPath` metodę, która przyjmuje trzy parametry:</span><span class="sxs-lookup"><span data-stu-id="0f655-496">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample64.cs)]

<span data-ttu-id="0f655-497">(Linia jest opakowana aby był bardziej czytelny.</span><span class="sxs-lookup"><span data-stu-id="0f655-497">(The line has been wrapped to make it more readable.</span></span> <span data-ttu-id="0f655-498">Należy pamiętać, że podziałów niemal dowolnym miejscu, z wyjątkiem wewnątrz ciągów, które można umieścić są ujęte w cudzysłów.)</span><span class="sxs-lookup"><span data-stu-id="0f655-498">Remember that you can put line breaks almost any place except inside strings that are enclosed in quotation marks.)</span></span>

<span data-ttu-id="0f655-499">Ta metoda zwraca ścieżkę fizyczną na serwerze, który odpowiada do określonej ścieżki wirtualnej.</span><span class="sxs-lookup"><span data-stu-id="0f655-499">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="0f655-500">Są trzy parametry dla metody `virtualPath`, `baseVirtualDir`, i `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="0f655-500">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="0f655-501">(Zwróć uwagę, że w deklaracji, parametry są wyświetlane z typami danych dane, które akceptują będzie). Gdy ta metoda jest wywoływana, należy podać wartości parametrów wszystkich trzech.</span><span class="sxs-lookup"><span data-stu-id="0f655-501">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="0f655-502">Składnia Razor oferuje dwie opcje parametrów może być przekazywany do metody: *parametrów pozycyjnych* i *parametrów nazwanych*.</span><span class="sxs-lookup"><span data-stu-id="0f655-502">The Razor syntax gives you two options for passing parameters to a method: *positional parameters* and *named parameters*.</span></span> <span data-ttu-id="0f655-503">Aby wywołać metodę przy użyciu parametrów pozycyjnych, parametry przekazywane w kolejności strict, który jest określony w deklaracji metody.</span><span class="sxs-lookup"><span data-stu-id="0f655-503">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="0f655-504">(Będzie zazwyczaj znasz to zamówienie przeczytaj dokumentację metody.) Wykonaj kolejności i nie można pominąć wszystkie parametry &#8212; Jeśli to konieczne, możesz przekazać pusty ciąg (`""`) lub `null` dla parametrów pozycyjnych, które nie mają wartości.</span><span class="sxs-lookup"><span data-stu-id="0f655-504">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or `null` for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="0f655-505">W poniższym przykładzie założono istnieje folder o nazwie *skryptów* w witrynie sieci Web.</span><span class="sxs-lookup"><span data-stu-id="0f655-505">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="0f655-506">Kod wywołuje `Request.MapPath` — metoda i przekazuje wartości parametrów trzy we właściwej kolejności.</span><span class="sxs-lookup"><span data-stu-id="0f655-506">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="0f655-507">Wyświetla zamapowanej ścieżki wynikowej.</span><span class="sxs-lookup"><span data-stu-id="0f655-507">It then displays the resulting mapped path.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample65.cshtml)]

<span data-ttu-id="0f655-508">Gdy metoda ma wiele parametrów, można zachować swój kod był bardziej czytelny przy użyciu parametrów nazwanych.</span><span class="sxs-lookup"><span data-stu-id="0f655-508">When a method has many parameters, you can keep your code more readable by using named parameters.</span></span> <span data-ttu-id="0f655-509">Aby wywołać metodę przy użyciu parametrów nazwanych, należy określić nazwę parametru, po której następuje dwukropkiem (:) i wartość.</span><span class="sxs-lookup"><span data-stu-id="0f655-509">To call a method using named parameters, you specify the parameter name followed by a colon (:), and then the value.</span></span> <span data-ttu-id="0f655-510">Zaletą nazwane parametry jest, że można przekazać je w dowolnej kolejności, która ma.</span><span class="sxs-lookup"><span data-stu-id="0f655-510">The advantage of named parameters is that you can pass them in any order you want.</span></span> <span data-ttu-id="0f655-511">(Wadą jest wywołanie metody nie jest możliwie kompaktowego).</span><span class="sxs-lookup"><span data-stu-id="0f655-511">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="0f655-512">Poniższy przykład wywołuje tę samą metodę jak powyżej, ale używa nazwanych parametrów umożliwiają określanie wartości:</span><span class="sxs-lookup"><span data-stu-id="0f655-512">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample66.cshtml)]

<span data-ttu-id="0f655-513">Jak widać, parametry są przekazywane w innej kolejności.</span><span class="sxs-lookup"><span data-stu-id="0f655-513">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="0f655-514">Jednak po uruchomieniu poprzedni przykład, jak i w tym przykładzie zostanie zwrócona taką samą wartość.</span><span class="sxs-lookup"><span data-stu-id="0f655-514">However, if you run the previous example and this example, they'll return the same value.</span></span>

<a id="ID_HandlingErrors"></a>
## <a name="handling-errors"></a><span data-ttu-id="0f655-515">Obsługa błędów</span><span class="sxs-lookup"><span data-stu-id="0f655-515">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="0f655-516">Instrukcje try-Catch</span><span class="sxs-lookup"><span data-stu-id="0f655-516">Try-Catch Statements</span></span>

<span data-ttu-id="0f655-517">Konieczne będzie często instrukcje w kodzie, który może zakończyć się niepowodzeniem ze względów poza formantu.</span><span class="sxs-lookup"><span data-stu-id="0f655-517">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="0f655-518">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="0f655-518">For example:</span></span>

- <span data-ttu-id="0f655-519">W przypadku kodu próbuje utworzyć lub uzyskać dostępu do pliku, wszystkie rodzaje błędów mogą wystąpić.</span><span class="sxs-lookup"><span data-stu-id="0f655-519">If your code tries to create or access a file, all sorts of errors might occur.</span></span> <span data-ttu-id="0f655-520">Żądany plik nie istnieje, może być zablokowane, kod może nie mieć uprawnień i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="0f655-520">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="0f655-521">Podobnie jeśli kod spróbuje do aktualizowania rekordów w bazie danych, mogą być problemy z uprawnieniami, mogą być opuszczane połączenia z bazą danych, można zapisać danych może być nieprawidłowa i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="0f655-521">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="0f655-522">W terminologii programistycznej tych sytuacji są nazywane *wyjątki*.</span><span class="sxs-lookup"><span data-stu-id="0f655-522">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="0f655-523">Jeśli kod napotkał wyjątek, generuje (zgłasza) komunikat o błędzie to jest w najlepszym irytujących dla użytkowników:</span><span class="sxs-lookup"><span data-stu-id="0f655-523">If your code encounters an exception, it generates (throws) an error message that's, at best, annoying to users:</span></span>

![Razor-Img14](introducing-razor-syntax-c/_static/image14.jpg)

<span data-ttu-id="0f655-525">W sytuacjach, gdy kod może wystąpić wyjątki, a w celu uniknięcia komunikaty o błędach tego typu, można użyć `try/catch` instrukcje.</span><span class="sxs-lookup"><span data-stu-id="0f655-525">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `try/catch` statements.</span></span> <span data-ttu-id="0f655-526">W `try` instrukcji, uruchom kod, który jest sprawdzanie.</span><span class="sxs-lookup"><span data-stu-id="0f655-526">In the `try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="0f655-527">W co najmniej jednej `catch` instrukcji, można wyszukać określonych błędów (określonych typów wyjątków), które mogły wystąpić.</span><span class="sxs-lookup"><span data-stu-id="0f655-527">In one or more `catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="0f655-528">Może zawierać tyle `catch` instrukcje jako użytkownik musiał błędów, które są przewidywania.</span><span class="sxs-lookup"><span data-stu-id="0f655-528">You can include as many `catch` statements as you need to look for errors that you are anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="0f655-529">Zaleca się unikać używania `Response.Redirect` metoda `try/catch` instrukcji, ponieważ może spowodować wyjątek na stronie.</span><span class="sxs-lookup"><span data-stu-id="0f655-529">We recommend that you avoid using the `Response.Redirect` method in `try/catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="0f655-530">W poniższym przykładzie przedstawiono strona, która tworzy plik tekstowy na pierwsze żądanie, a następnie wyświetla przycisku, który umożliwia użytkownikowi otwieranie pliku.</span><span class="sxs-lookup"><span data-stu-id="0f655-530">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="0f655-531">W przykładzie użyto celowo Nieprawidłowa nazwa pliku, aby go spowoduje, że wystąpił wyjątek.</span><span class="sxs-lookup"><span data-stu-id="0f655-531">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="0f655-532">Kod zawiera `catch` instrukcji na dwa wyjątki to możliwe: `FileNotFoundException`, który występuje, jeśli nazwa pliku jest nieprawidłowa, i `DirectoryNotFoundException`, które występuje, gdy ASP.NET nawet nie można odnaleźć folderu.</span><span class="sxs-lookup"><span data-stu-id="0f655-532">The code includes `catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="0f655-533">(Użytkownik może usuń znaczniki komentarza do instrukcji w tym przykładzie aby zobaczyć, jak jest uruchamiana po wszystko działa prawidłowo.)</span><span class="sxs-lookup"><span data-stu-id="0f655-533">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="0f655-534">Jeśli kod nie obsłużyć wyjątek, zobaczysz stronę błędu, takie jak poprzednie zrzut ekranu.</span><span class="sxs-lookup"><span data-stu-id="0f655-534">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="0f655-535">Jednak `try/catch` sekcja pomoże uniemożliwienia użytkownika tego typu błędów.</span><span class="sxs-lookup"><span data-stu-id="0f655-535">However, the `try/catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample67.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="0f655-536">Dodatkowe zasoby</span><span class="sxs-lookup"><span data-stu-id="0f655-536">Additional Resources</span></span>

<span data-ttu-id="0f655-537">**Programowania w języku Visual Basic**</span><span class="sxs-lookup"><span data-stu-id="0f655-537">**Programming with Visual Basic**</span></span>


[<span data-ttu-id="0f655-538">Dodatku: język Visual Basic i składni</span><span class="sxs-lookup"><span data-stu-id="0f655-538">Appendix: Visual Basic Language and Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkId=202908)


<span data-ttu-id="0f655-539">**Dokumentacji**</span><span class="sxs-lookup"><span data-stu-id="0f655-539">**Reference Documentation**</span></span>


[<span data-ttu-id="0f655-540">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="0f655-540">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)

[<span data-ttu-id="0f655-541">C# — język</span><span class="sxs-lookup"><span data-stu-id="0f655-541">C# Language</span></span>](https://msdn.microsoft.com/library/kx37x362.aspx)
