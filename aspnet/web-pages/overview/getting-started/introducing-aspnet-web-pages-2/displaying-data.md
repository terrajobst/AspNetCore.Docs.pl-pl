---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: Wprowadzenie do składnika ASP.NET Web Pages — wyświetlanie danych | Dokumentacja firmy Microsoft
author: tfitzmac
description: Ten samouczek pokazuje, jak utworzyć bazę danych w programie WebMatrix i sposób wyświetlania danych bazy danych na stronie, gdy używasz stron sieci Web platformy ASP.NET (Razor). Przyjęto założenie, y...
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: 6c66e5fb0a1a49da411286e19c7954f83055c3fd
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/06/2018
ms.locfileid: "30898460"
---
<a name="introducing-aspnet-web-pages---displaying-data"></a><span data-ttu-id="4ac5d-104">Wprowadzenie do strony sieci Web ASP.NET - wyświetlanie danych</span><span class="sxs-lookup"><span data-stu-id="4ac5d-104">Introducing ASP.NET Web Pages - Displaying Data</span></span>
====================
<span data-ttu-id="4ac5d-105">przez [FitzMacken niestandardowy](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="4ac5d-106">Ten samouczek pokazuje, jak utworzyć bazę danych w programie WebMatrix i sposób wyświetlania danych bazy danych na stronie, gdy używasz stron sieci Web platformy ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-106">This tutorial shows you how to create a database in WebMatrix and how to display database data in a page when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="4ac5d-107">Przyjęto założenie, że zostały wykonane serii za pomocą [wprowadzenie do programowania stron sieci Web ASP.NET](../introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-107">It assumes you have completed the series through [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md).</span></span>
> 
> <span data-ttu-id="4ac5d-108">Zawartość:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="4ac5d-109">Jak utworzyć bazę danych i tabel bazy danych za pomocą narzędzi programu WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-109">How to use WebMatrix tools to create a database and database tables.</span></span>
> - <span data-ttu-id="4ac5d-110">Jak dodać dane do bazy danych za pomocą narzędzi programu WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-110">How to use WebMatrix tools to add data to a database.</span></span>
> - <span data-ttu-id="4ac5d-111">Jak wyświetlać dane z bazy danych na stronie.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-111">How to display data from the database on a page.</span></span>
> - <span data-ttu-id="4ac5d-112">Sposób uruchamiania poleceń SQL w programie ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-112">How to run SQL commands in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="4ac5d-113">Dostosowywanie `WebGrid` pomocnika, aby zmienić sposób wyświetlania danych i dodać stronicowania i sortowania.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-113">How to customize the `WebGrid` helper to change the data display and to add paging and sorting.</span></span>
>   
> 
> <span data-ttu-id="4ac5d-114">Funkcje/technologie omówione:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="4ac5d-115">Narzędzia bazy danych programu WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-115">WebMatrix database tools.</span></span>
> - <span data-ttu-id="4ac5d-116">`WebGrid` Pomocnik.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-116">`WebGrid` helper.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="4ac5d-117">Jakie będzie kompilacji</span><span class="sxs-lookup"><span data-stu-id="4ac5d-117">What You'll Build</span></span>

<span data-ttu-id="4ac5d-118">Poprzednie samouczka zostały wprowadzone do stron sieci Web platformy ASP.NET (*.cshtml* plików), aby podstawowe informacje o składni Razor i pomocników.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-118">In the previous tutorial, you were introduced to ASP.NET Web Pages (*.cshtml* files), to the basics of Razor syntax, and to helpers.</span></span> <span data-ttu-id="4ac5d-119">W tym samouczku będzie rozpocząć tworzenie aplikacji rzeczywistych sieci web, które będzie używane w pozostałej części serii.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-119">In this tutorial, you'll begin creating the actual web application that you'll use for the rest of the series.</span></span> <span data-ttu-id="4ac5d-120">Aplikacja jest aplikacji filmu proste, która umożliwia wyświetlanie, dodawanie, zmienianie i usuwania informacji o filmów.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-120">The app is a simple movie application that lets you view, add, change, and delete information about movies.</span></span>

<span data-ttu-id="4ac5d-121">Po zakończeniu korzystania z tego samouczka, będzie można wyświetlić listę film, która wygląda jak ta strona:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-121">When you're done with this tutorial, you'll be able to view a movie listing that looks like this page:</span></span>

![Wyświetl WebGrid, który zawiera parametry ustawioną nazwy klas CSS](displaying-data/_static/image1.png)

<span data-ttu-id="4ac5d-123">Jednak aby rozpocząć, należy utworzyć bazę danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-123">But to begin, you have to create a database.</span></span>

## <a name="a-very-brief-introduction-to-databases"></a><span data-ttu-id="4ac5d-124">Bardzo krótkie wprowadzenie do bazy danych</span><span class="sxs-lookup"><span data-stu-id="4ac5d-124">A Very Brief Introduction to Databases</span></span>

<span data-ttu-id="4ac5d-125">W tym samouczku będzie udostępniać tylko briefest wprowadzenie do bazy danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-125">This tutorial will provide only the briefest introduction to databases.</span></span> <span data-ttu-id="4ac5d-126">Jeśli masz doświadczenie w bazie danych, możesz pominąć tę sekcję krótki.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-126">If you have database experience, you can skip this short section.</span></span>

<span data-ttu-id="4ac5d-127">Baza danych zawiera co najmniej jednej tabeli, które zawierają informacje &mdash; na przykład tabel dla klientów, zamówienia i dostawców, lub dla uczniów lub studentów, nauczycieli, klasy i klas.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-127">A database contains one or more tables that contain information &mdash; for example, tables for customers, orders, and vendors, or for students, teachers, classes, and grades.</span></span> <span data-ttu-id="4ac5d-128">Strukturę tabeli bazy danych przypomina arkusz kalkulacyjny.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-128">Structurally, a database table is like a spreadsheet.</span></span> <span data-ttu-id="4ac5d-129">Wyobraź sobie książki adresowej typowych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-129">Imagine a typical address book.</span></span> <span data-ttu-id="4ac5d-130">Dla każdego wpisu w książce adresowej (to znaczy, że dla każdej osoby) ma kilka rodzajów informacji, takich jak imię, nazwisko, adres, adres e-mail i numer telefonu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-130">For each entry in the address book (that is, for each person) you have several pieces of information such as first name, last name, address, email address, and phone number.</span></span>

![Przykładowa tabela bazy danych jako proste siatki](displaying-data/_static/image2.png)

<span data-ttu-id="4ac5d-132">(Wiersze są czasami określane jako *rekordów*, i kolumny są czasami określane jako *pola*.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-132">(Rows are sometimes referred to as *records*, and columns are sometimes referred to as *fields*.)</span></span>

<span data-ttu-id="4ac5d-133">W przypadku większości tabel bazy danych Tabela musi mieć kolumnę zawierającą unikatową wartość, takich jak numer klienta, numer konta i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-133">For most database tables, the table has to have a column that contains a unique value, like a customer number, account number, and so on.</span></span> <span data-ttu-id="4ac5d-134">Ta wartość jest znana jako tabeli *klucz podstawowy*, i używa ich do identyfikacji każdego wiersza w tabeli.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-134">This value is known as the table's *primary key*, and you use it to identify each row in the table.</span></span> <span data-ttu-id="4ac5d-135">W tym przykładzie w kolumnie identyfikator jest kluczem podstawowym w poprzednim przykładzie książki adresowej.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-135">In the example, the ID column is the primary key for the address book shown in the previous example.</span></span>

<span data-ttu-id="4ac5d-136">Większość zadań wykonywanych w aplikacji sieci web obejmuje odczytu informacji z bazy danych programu i wyświetlanie go na stronie.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-136">Much of the work you do in web applications consists of reading information out of the database and displaying it on a page.</span></span> <span data-ttu-id="4ac5d-137">Zostanie również często zbierania informacji od użytkowników i dodaj go do bazy danych lub będzie modyfikowania rekordów, które już znajdują się w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-137">You'll also often gather information from users and add it to a database, or you'll modify records that are already in the database.</span></span> <span data-ttu-id="4ac5d-138">(Firma Microsoft będzie obejmować wszystkie te operacje w ramach tego samouczka zestawu.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-138">(We'll cover all of these operations in the course of this tutorial set.)</span></span>

<span data-ttu-id="4ac5d-139">Praca z bazy danych może być enormously złożona i może wymagać specjalistyczną wiedzę.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-139">Database work can be enormously complex and can require specialized knowledge.</span></span> <span data-ttu-id="4ac5d-140">Dla tego samouczka zestawu należy jednak zapoznać tylko podstawowe pojęcia, które wszystkie opisano w trakcie pracy.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-140">For this tutorial set, though, you have to understand only basic concepts, which will all be explained as you go.</span></span>

## <a name="creating-a-database"></a><span data-ttu-id="4ac5d-141">Tworzenie bazy danych</span><span class="sxs-lookup"><span data-stu-id="4ac5d-141">Creating a Database</span></span>

<span data-ttu-id="4ac5d-142">Program WebMatrix zawiera narzędzia, które ułatwiają tworzenie bazy danych oraz do tworzenia tabel w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-142">WebMatrix includes tools that make it easy to create a database and to create tables in the database.</span></span> <span data-ttu-id="4ac5d-143">(Struktury bazy danych jest określany jako bazy danych *schematu*.) Dla tego zestawu samouczka, utworzysz bazę danych, która ma tylko jedną tabelę w nim &mdash; filmów.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-143">(The structure of a database is referred to as the database's *schema*.) For this tutorial set, you'll create a database that has only one table in it &mdash; Movies.</span></span>

<span data-ttu-id="4ac5d-144">Otwórz program WebMatrix, jeśli jeszcze tego nie zrobiono, a następnie otwórz witrynę WebPagesMovies utworzoną w poprzedniej samouczka.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-144">Open WebMatrix if you haven't already done so, and open the WebPagesMovies site that you created in the previous tutorial.</span></span>

<span data-ttu-id="4ac5d-145">W okienku po lewej stronie kliknij **bazy danych** obszaru roboczego.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-145">In the left pane, click the **Database** workspace.</span></span>

![Karty Obszar roboczy bazy danych programu WebMatrix](displaying-data/_static/image3.png)

<span data-ttu-id="4ac5d-147">Zmiany wstążki Pokaż zadania związane z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-147">The ribbon changes to show database-related tasks.</span></span> <span data-ttu-id="4ac5d-148">Na wstążce kliknij **nową bazę danych**.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-148">In the ribbon, click **New Database**.</span></span>

![Przycisk nową bazę danych na wstążce programu WebMatrix](displaying-data/_static/image4.png)

<span data-ttu-id="4ac5d-150">Program WebMatrix tworzy bazę danych programu SQL Server CE ( *sdf* pliku) który ma taką samą nazwę jak witryny &mdash; *WebPagesMovies.sdf*.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-150">WebMatrix creates a SQL Server CE database (an *.sdf* file) that has the same name as your site &mdash; *WebPagesMovies.sdf*.</span></span> <span data-ttu-id="4ac5d-151">(Nie w tym w tym miejscu, ale można zmienić nazwę pliku na niczego Ci się podoba, tak długo, jak długo ma *sdf* rozszerzenia.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-151">(You won't do this here, but you can rename the file to anything you like, as long as it has an *.sdf* extension.)</span></span>

## <a name="creating-a-table"></a><span data-ttu-id="4ac5d-152">Tworzenie tabeli</span><span class="sxs-lookup"><span data-stu-id="4ac5d-152">Creating a Table</span></span>

<span data-ttu-id="4ac5d-153">Na wstążce kliknij **nową tabelę**.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-153">In the ribbon, click **New Table**.</span></span> <span data-ttu-id="4ac5d-154">Program WebMatrix otwiera projektanta tabel na nowej karcie. (Jeśli opcja Nowa tabela jest niedostępna, upewnij się, że nowa baza danych jest wybrane w widoku drzewa po lewej stronie.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-154">WebMatrix opens the table designer in a new tab. (If the New Table option isn't available, make sure that the new database is selected in the tree view on the left.)</span></span>

![Przycisk "Nowa tabela" na wstążce programu WebMatrix](displaying-data/_static/image5.png)

<span data-ttu-id="4ac5d-156">W polu tekstowym u góry (gdzie znaku wodnego mówi "Wprowadź nazwę tabeli") wprowadź "Filmy".</span><span class="sxs-lookup"><span data-stu-id="4ac5d-156">In the text box at the top (where the watermark says "Enter table name"), enter "Movies".</span></span>

![Wprowadzanie nazwy tabeli w projektancie bazy danych programu WebMatrix](displaying-data/_static/image6.png)

<span data-ttu-id="4ac5d-158">Okienku poniżej nazwy tabeli jest, gdzie został zdefiniowany poszczególnych kolumn.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-158">The pane underneath the table name is where you define individual columns.</span></span> <span data-ttu-id="4ac5d-159">Dla *filmy* tabeli w tym samouczku utworzysz tylko kilka kolumn: *identyfikator*, *tytuł*, *Genre*, i *roku*.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-159">For the *Movies* table in this tutorial, you'll create only a few columns: *ID*, *Title*, *Genre*, and *Year*.</span></span>

<span data-ttu-id="4ac5d-160">W **nazwa** wprowadź "ID".</span><span class="sxs-lookup"><span data-stu-id="4ac5d-160">In the **Name** box, enter "ID".</span></span> <span data-ttu-id="4ac5d-161">Wprowadzenie wartości w tym miejscu aktywuje wszystkie formanty dla nowej kolumny.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-161">Entering a value here activates all the controls for the new column.</span></span>

<span data-ttu-id="4ac5d-162">Karty, aby **— typ danych** listę i wybierz **int**. Ta wartość określa, czy w kolumnie identyfikator będzie zawierać danych liczb całkowitych (numer).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-162">Tab to the **Data Type** list and choose **int**. This value specifies that the ID column will contain integer (number) data.</span></span>

> [!NOTE]
> <span data-ttu-id="4ac5d-163">Nie będzie nazywamy go jakąkolwiek więcej tutaj (znacznie), ale standardowe gestów klawiatury systemu Windows umożliwia nawigowanie w tej siatce.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-163">We won't call it out any more here (much), but you can use standard Windows keyboard gestures to navigate in this grid.</span></span> <span data-ttu-id="4ac5d-164">Na przykład można karcie między polami, może po prostu zacznij wpisywać ciąg w celu wybierz element na liście i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-164">For example, you can tab between fields, you can just start typing in order to select an item in a list, and so on.</span></span>


<span data-ttu-id="4ac5d-165">Karta poza **wartość domyślną** pola (to znaczy, pozostaw to pole puste).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-165">Tab past the **Default Value** box (that is, leave it blank).</span></span> <span data-ttu-id="4ac5d-166">Karty, aby **jest kluczem podstawowym** pole wyboru i zaznacz je.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-166">Tab to the **Is Primary Key** check box and select it.</span></span> <span data-ttu-id="4ac5d-167">Ta opcja nakazuje bazy danych *identyfikator* kolumna będzie zawierać dane, które identyfikuje poszczególnych wierszy.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-167">This option tells the database that the *ID* column will contain the data that identifies individual rows.</span></span> <span data-ttu-id="4ac5d-168">(To znaczy każdy wiersz będzie mieć unikatową wartość w kolumnie identyfikator, który umożliwia znalezienie tego wiersza.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-168">(That is, each row will have a unique value in the ID column that you can use to find that row.)</span></span>

<span data-ttu-id="4ac5d-169">Wybierz **tożsamości jest** opcji.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-169">Choose the **Is Identity** option.</span></span> <span data-ttu-id="4ac5d-170">Ta opcja nakazuje bazy danych, czy powinien automatycznie generować kolejny numer sekwencyjny dla każdego nowego wiersza.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-170">This option tells the database that it should automatically generate the next sequential number for each new row.</span></span> <span data-ttu-id="4ac5d-171">( **Tożsamości jest** opcja działa tylko wtedy, gdy wybrano również **jest kluczem podstawowym** opcję.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-171">(The **Is Identity** option works only if you've also selected the **Is Primary Key** option.)</span></span>

<span data-ttu-id="4ac5d-172">Kliknij w następnym wierszu siatki, lub naciśnij klawisz Tab dwukrotnie w celu zakończenia bieżącego wiersza.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-172">Click in the next grid row, or press Tab twice to finish the current row.</span></span> <span data-ttu-id="4ac5d-173">Albo gestu zapisuje bieżący wiersz i rozpoczyna się kolejny.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-173">Either gesture saves the current row and starts the next one.</span></span> <span data-ttu-id="4ac5d-174">Zwróć uwagę, że **wartość domyślną** kolumny teraz mówi **Null**.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-174">Notice that the **Default Value** column now says **Null**.</span></span> <span data-ttu-id="4ac5d-175">(Wartość null jest wartością domyślną dla wartości domyślnej, więc do speak).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-175">(Null is the default value for the default value, so to speak.)</span></span>

<span data-ttu-id="4ac5d-176">Po zakończeniu definiowania nowej **identyfikator** kolumny, Projektant będzie wyglądać na następującej ilustracji:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-176">When you've finished defining the new **ID** column, the designer will look like this illustration:</span></span>

![Projektant baz danych programu WebMatrix po zdefiniowaniu w kolumnie Identyfikator tabeli filmy](displaying-data/_static/image7.png)

<span data-ttu-id="4ac5d-178">Aby utworzyć następnej kolumnie, kliknij pole w **nazwa** kolumny.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-178">To create the next column, click in the box in the **Name** column.</span></span> <span data-ttu-id="4ac5d-179">Wprowadź "Title" dla kolumny, a następnie wybierz **nvarchar** dla **— typ danych** wartość.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-179">Enter "Title" for the column and then select **nvarchar** for the **Data Type** value.</span></span> <span data-ttu-id="4ac5d-180">Część "var" **nvarchar** bazy danych informuje, że dane dla tej kolumny zostaną ciąg, którego rozmiar może się różnić od rekordu do rekordu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-180">The "var" part of **nvarchar** tells the database that the data for this column will be a string whose size might vary from record to record.</span></span> <span data-ttu-id="4ac5d-181">(Prefiks "n" reprezentuje "national", która wskazuje, czy pole może przechowywać dane znakowe dla dowolnego alfabetu lub zapisywanie systemu — to znaczy pole zawiera dane Unicode.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-181">(The "n" prefix represents "national," which indicates that the field can hold character data for any alphabet or writing system — that is, the field holds Unicode data.)</span></span>

<span data-ttu-id="4ac5d-182">Po wybraniu **nvarchar**, zostanie wyświetlone okno innego, za pomocą którego można wprowadzić maksymalną długość pola.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-182">When you choose **nvarchar**, another box appears where you can enter the maximum length for the field.</span></span> <span data-ttu-id="4ac5d-183">Wprowadź 50, przy założeniu, że nie będzie można pracować w tym samouczku tytuł filmu będzie więcej niż 50 znaków.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-183">Enter 50, on the assumption that no movie title that you'll work with in this tutorial will be longer than 50 characters.</span></span>

<span data-ttu-id="4ac5d-184">Pomiń **wartość domyślną** i wyczyść **Zezwalaj na wartości null** opcji.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-184">Skip **Default Value** and clear the **Allow Nulls** option.</span></span> <span data-ttu-id="4ac5d-185">Nie ma bazy danych, aby umożliwić filmy wprowadzane do bazy danych, które nie mają tytuł.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-185">You don't want the database to allow any movies to be entered into the database that don't have a title.</span></span>

<span data-ttu-id="4ac5d-186">Gdy ukończysz pracę i przejście do następnego wiersza projektanta wygląda na tej ilustracji:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-186">When you're done and move to the next row, the designer looks like this illustration:</span></span>

![Projektant baz danych programu WebMatrix po zdefiniowaniu kolumnę tabeli filmy](displaying-data/_static/image8.png)

<span data-ttu-id="4ac5d-188">Powtórz te kroki, aby utworzyć kolumnę o nazwie "Rodzaju", z wyjątkiem długość, ustaw ją na właśnie 30.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-188">Repeat these steps to create a column named "Genre", except for the length, set it to just 30.</span></span>

<span data-ttu-id="4ac5d-189">Utwórz inną kolumnę o nazwie "Year".</span><span class="sxs-lookup"><span data-stu-id="4ac5d-189">Create another column named "Year."</span></span> <span data-ttu-id="4ac5d-190">Dla typu danych, wybierz **nchar** (nie **nvarchar**) i ustawić długości na 4.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-190">For the data type, choose **nchar** (not **nvarchar**) and set the length to 4.</span></span> <span data-ttu-id="4ac5d-191">Rok możesz zacząć korzystać numer 4-cyfrowego, takich jak "1995" lub "2010", więc nie wymagają kolumny o zmiennej długości.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-191">For the year, you're going to use a 4-digit number like "1995" or "2010", so you don't require a variable-sized column.</span></span>

<span data-ttu-id="4ac5d-192">Oto, jak wygląda gotowego projektu:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-192">Here's what the finished design looks like:</span></span>

![Projektant baz danych programu WebMatrix po wszystkich pól zdefiniowanych dla tabeli filmy](displaying-data/_static/image9.png)

<span data-ttu-id="4ac5d-194">Naciśnij klawisze Ctrl + S lub kliknij przycisk **zapisać** przycisk paska narzędzi Szybki dostęp.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-194">Press Ctrl+S or click the **Save** button in the Quick Access toolbar.</span></span> <span data-ttu-id="4ac5d-195">Zamknij projektanta bazy danych przez zamknięcie karty.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-195">Close the database designer by closing the tab.</span></span>

## <a name="adding-some-example-data"></a><span data-ttu-id="4ac5d-196">Dodawanie niektóre przykładowe dane</span><span class="sxs-lookup"><span data-stu-id="4ac5d-196">Adding Some Example Data</span></span>

<span data-ttu-id="4ac5d-197">Później w tym samouczku utworzysz strony, których można wprowadzać nowości w formularzu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-197">Later in this tutorial series you'll create a page where you can enter new movies in a form.</span></span> <span data-ttu-id="4ac5d-198">Jednak obecnie można dodać niektóre przykładowe dane, które można następnie wyświetlić na stronie.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-198">For now, however, you can add some example data that you can then display on a page.</span></span>

<span data-ttu-id="4ac5d-199">W **bazy danych** obszaru roboczego w programie WebMatrix, zwróć uwagę, że istnieje drzewa, który pokazuje *sdf* wcześniej utworzony plik.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-199">In the **Database** workspace in WebMatrix, notice that there's a tree that shows you the *.sdf* file you created earlier.</span></span> <span data-ttu-id="4ac5d-200">Otwórz węzeł dla nowego *sdf* pliku, a następnie otwórz **tabel** węzła.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-200">Open the node for your new *.sdf* file, and then open the **Tables** node.</span></span>

![Obszar roboczy bazy danych programu WebMatrix z drzewa otwórz tabelę filmy](displaying-data/_static/image10.png)

<span data-ttu-id="4ac5d-202">Kliknij prawym przyciskiem myszy **filmy** węzeł, a następnie wybierz **danych**.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-202">Right-click the **Movies** node and then choose **Data**.</span></span> <span data-ttu-id="4ac5d-203">Program WebMatrix otwiera siatki, których można wprowadzać dane *filmy* tabeli:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-203">WebMatrix opens a grid where you can enter data for the *Movies* table:</span></span>

![Siatka zapisu bazy danych w programie WebMatrix (pusta)](displaying-data/_static/image11.png)

<span data-ttu-id="4ac5d-205">Kliknij przycisk **tytuł** kolumny, a następnie wprowadź "Gdy Harry spełnione Zosi".</span><span class="sxs-lookup"><span data-stu-id="4ac5d-205">Click the **Title** column and enter "When Harry Met Sally".</span></span> <span data-ttu-id="4ac5d-206">Przenieś do **Genre** kolumny (możesz użyć klawisza Tab), a następnie wprowadź "Sceny miłosne Komedia".</span><span class="sxs-lookup"><span data-stu-id="4ac5d-206">Move to the **Genre** column (you can use the Tab key) and enter "Romantic Comedy".</span></span> <span data-ttu-id="4ac5d-207">Przenieś do **roku** kolumny, a następnie wprowadź "1989":</span><span class="sxs-lookup"><span data-stu-id="4ac5d-207">Move to the **Year** column and enter "1989":</span></span>

![Siatka zapisu bazy danych w programie WebMatrix z jeden rekord](displaying-data/_static/image12.png)

<span data-ttu-id="4ac5d-209">Naciśnij klawisz Enter, a program WebMatrix jest zapisywany nowy filmu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-209">Press Enter, and WebMatrix saves the new movie.</span></span> <span data-ttu-id="4ac5d-210">Zwróć uwagę, że **identyfikator** kolumny zostały wypełnione.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-210">Notice that the **ID** column has been filled in.</span></span>

![Siatka wpis bazy danych w programie WebMatrix jeden rekord i identyfikator wygenerowany automatycznie](displaying-data/_static/image13.png)

<span data-ttu-id="4ac5d-212">Wprowadź inny movie (na przykład, "usunięty z Wind", "Teatralne", "1939").</span><span class="sxs-lookup"><span data-stu-id="4ac5d-212">Enter another movie (for example, "Gone with the Wind", "Drama", "1939").</span></span> <span data-ttu-id="4ac5d-213">W kolumnie identyfikator jest wprowadzone ponownie:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-213">The ID column is filled in again:</span></span>

![Siatka wpis bazy danych w programie WebMatrix z dwa rekordy i identyfikatorów generowanych automatycznie](displaying-data/_static/image14.png)

<span data-ttu-id="4ac5d-215">Wprowadź trzeci movie (na przykład "Ghostbusters", "Komedia").</span><span class="sxs-lookup"><span data-stu-id="4ac5d-215">Enter a third movie (for example, "Ghostbusters", "Comedy").</span></span> <span data-ttu-id="4ac5d-216">Eksperyment, pozostawienie **roku** kolumny puste, a następnie naciśnij klawisz Enter.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-216">As an experiment, leave the **Year** column blank and then press Enter.</span></span> <span data-ttu-id="4ac5d-217">Ponieważ użytkownik usunął zaznaczenie **Zezwalaj na wartości null** opcji, pokazuje błąd, bazy danych:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-217">Because you unselected the **Allow Nulls** option, the database shows an error:</span></span>

![Błąd 'Nieprawidłowe dane' wyświetlane, gdy wartość w kolumnie wymagane jest puste](displaying-data/_static/image15.png)

<span data-ttu-id="4ac5d-219">Kliknij przycisk **OK** przejść wstecz i Usuń wpis (roku "Ghostbusters" jest 1984) i naciśnij klawisz Enter.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-219">Click **OK** to go back and fix the entry (the year for "Ghostbusters" is 1984), and then press Enter.</span></span>

<span data-ttu-id="4ac5d-220">Wypełnij kilka filmów, dopóki nie uzyskasz 8 lub tak.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-220">Fill in several movies until you have 8 or so.</span></span> <span data-ttu-id="4ac5d-221">(Wprowadzanie 8 ułatwia posługiwanie się stronicowania później.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-221">(Entering 8 makes it easier to work with paging later.</span></span> <span data-ttu-id="4ac5d-222">Ale jeśli jest zbyt wiele, wprowadź kilka teraz). Nie ma znaczenia, rzeczywiste dane.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-222">But if that's too many, enter just a few for now.) The actual data doesn't matter.</span></span>

![Siatka zapisu bazy danych w programie WebMatrix z albo rekordów](displaying-data/_static/image16.png)

<span data-ttu-id="4ac5d-224">Jeśli wprowadzono wszystkie filmy bez żadnych błędów, sekwencyjne są wartości Identyfikatora.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-224">If you entered all the movies without any errors, the ID values are sequential.</span></span> <span data-ttu-id="4ac5d-225">Próbowano zapisać rekord niekompletne film, numery identyfikatorów może być kolejne.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-225">If you tried to save an incomplete movie record, the ID numbers might not be sequential.</span></span> <span data-ttu-id="4ac5d-226">Jeśli tak, że jest poprawny.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-226">If so, that's okay.</span></span> <span data-ttu-id="4ac5d-227">Liczby nie mają żadnego związanego z używaniem znaczenia, a jedynym elementem, który jest ważne jest, są one unikatowe w *filmy* tabeli.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-227">The numbers don't have any inherent meaning, and the only thing that's important is that they're unique in the *Movies* table.</span></span>

<span data-ttu-id="4ac5d-228">Zamknij kartę zawierającą projektanta bazy danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-228">Close the tab that contains the database designer.</span></span>

<span data-ttu-id="4ac5d-229">Teraz można włączyć do wyświetlania tych danych na stronie sieci web.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-229">Now you can turn to displaying this data on a web page.</span></span>

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a><span data-ttu-id="4ac5d-230">Wyświetlanie danych na stronie przy użyciu Pomocnika WebGrid</span><span class="sxs-lookup"><span data-stu-id="4ac5d-230">Displaying Data in a Page by Using the WebGrid Helper</span></span>

<span data-ttu-id="4ac5d-231">Aby wyświetlić dane na stronie, możesz zacząć używać `WebGrid` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-231">To display data in a page, you're going to use the `WebGrid` helper.</span></span> <span data-ttu-id="4ac5d-232">Tego pomocnika powoduje wyświetlenie w siatce lub tabeli (wierszy i kolumn).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-232">This helper produces a display in a grid or table (rows and columns).</span></span> <span data-ttu-id="4ac5d-233">Jak można zauważyć, będziesz w stanie Aktualizuj siatkę z formatowanie i inne funkcje.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-233">As you'll see, you'll be able refine the grid with formatting and other features.</span></span>

<span data-ttu-id="4ac5d-234">Aby uruchomić siatki, będzie konieczne Napisz kilka wierszy kodu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-234">To run the grid, you'll have to write a few lines of code.</span></span> <span data-ttu-id="4ac5d-235">Te kilka wierszy będzie służyć jako rodzaj wzorca dla prawie wszystkich dostępu do danych, jak w tym samouczku.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-235">These few lines will serve as a kind of pattern for almost all of the data access that you do in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="4ac5d-236">Faktycznie oferują wiele opcji wyświetlania danych na stronie; `WebGrid` pomocnika jest tylko jedna.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-236">You actually have many options for displaying data on a page; the `WebGrid` helper is just one.</span></span> <span data-ttu-id="4ac5d-237">Wybraliśmy go w tym samouczku, ponieważ jest najprostszym sposobem wyświetlenia danych i jest rozsądnych elastyczne.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-237">We chose it for this tutorial because it's the easiest way to display data and because it's reasonably flexible.</span></span> <span data-ttu-id="4ac5d-238">W zestawie samouczek dalej zobaczysz sposób użycia więcej "manual" sposób pracy z danymi na tej stronie, co pozwala na bardziej bezpośrednią kontrolę nad sposób wyświetlania danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-238">In the next tutorial set, you'll see how to use a more "manual" way to work with data in the page, which gives you more direct control over how to display the data.</span></span>


<span data-ttu-id="4ac5d-239">W lewym okienku w programie WebMatrix, kliknij przycisk **pliki** obszaru roboczego.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-239">In the left pane in WebMatrix, click the **Files** workspace.</span></span>

<span data-ttu-id="4ac5d-240">Nowe bazy danych utworzonej znajduje się w *aplikacji\_danych* folderu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-240">The new database you created is in the *App\_Data* folder.</span></span> <span data-ttu-id="4ac5d-241">Jeśli folder już nie istnieje, program WebMatrix jej utworzenia nowej bazy danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-241">If the folder didn't already exist, WebMatrix created it for your new database.</span></span> <span data-ttu-id="4ac5d-242">(Folder może występować Jeśli wcześniej została zainstalowana pomocników.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-242">(The folder might have existed if you'd previously installed helpers.)</span></span>

<span data-ttu-id="4ac5d-243">W widoku drzewa wybierz katalog główny witryny sieci Web.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-243">In the tree view, select the root of the website.</span></span> <span data-ttu-id="4ac5d-244">Musisz wybrać katalog główny witryny sieci Web; w przeciwnym razie nowy plik może być dodany do aplikacji\_folderem danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-244">You must select the root of the website; otherwise, the new file might be added to the App\_Data folder.</span></span>

<span data-ttu-id="4ac5d-245">Na wstążce kliknij **nowy**.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-245">In the ribbon, click **New**.</span></span> <span data-ttu-id="4ac5d-246">W **wybierz typ pliku** wybierz **CSHTML**.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-246">In the **Choose a File Type** box, choose **CSHTML**.</span></span>

<span data-ttu-id="4ac5d-247">W **nazwa** , podaj nazwę nowej strony "Movies.cshtml":</span><span class="sxs-lookup"><span data-stu-id="4ac5d-247">In the **Name** box, name the new page "Movies.cshtml":</span></span>

![Okno dialogowe "Wybierz typ pliku" przedstawiający stronę "Filmy"](displaying-data/_static/image17.png)

<span data-ttu-id="4ac5d-249">Kliknij przycisk **OK** przycisku.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-249">Click the **OK** button.</span></span> <span data-ttu-id="4ac5d-250">Program WebMatrix umożliwia otwarcie nowego pliku z niektóre elementy szkielet.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-250">WebMatrix opens a new file with some skeleton elements in it.</span></span> <span data-ttu-id="4ac5d-251">Najpierw będzie napisanie kodu, pobrać dane z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-251">First you'll write some code to go get the data from the database.</span></span> <span data-ttu-id="4ac5d-252">Następnie dodasz kod znaczników do strony, aby rzeczywiście wyświetlania danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-252">Then you'll add markup to the page to actually display the data.</span></span>

### <a name="writing-the-data-query-code"></a><span data-ttu-id="4ac5d-253">Pisanie kodu zapytania danych</span><span class="sxs-lookup"><span data-stu-id="4ac5d-253">Writing the Data Query Code</span></span>

<span data-ttu-id="4ac5d-254">W górnej części strony między `@{` i `}` znaków, wprowadź poniższy kod.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-254">At the top of the page, between the `@{` and `}` characters, enter the following code.</span></span> <span data-ttu-id="4ac5d-255">(Upewnij się, wprowadź ten kod pomiędzy otwierającym, a klamrowe nawiasy zamykające).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-255">(Make sure that you enter this code between the opening and closing braces.)</span></span>

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

<span data-ttu-id="4ac5d-256">Pierwszy wiersz otwiera bazy danych, który został utworzony wcześniej, który jest zawsze pierwszym krokiem przed wykonaniem coś z bazą danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-256">The first line opens the database that you created earlier, which is always the first step before doing something with the database.</span></span> <span data-ttu-id="4ac5d-257">Można sprawdzić `Database.Open` nazwę metody, można otworzyć bazy danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-257">You tell the `Database.Open` method name of the database to open.</span></span> <span data-ttu-id="4ac5d-258">Należy zauważyć, że nie zawierają *sdf* w nazwie.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-258">Notice that you don't include *.sdf* in the name.</span></span> <span data-ttu-id="4ac5d-259">`Open` — Metoda przyjęto założenie, że jest szuka *sdf* pliku (oznacza to, *WebPagesMovies.sdf*) oraz że *sdf* plik znajduje się w *aplikacji\_ Dane* folderu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-259">The `Open` method assumes that it's looking for an *.sdf* file (that is, *WebPagesMovies.sdf*) and that the *.sdf* file is in the *App\_Data* folder.</span></span> <span data-ttu-id="4ac5d-260">(Zauważyć firma Microsoft, który wcześniej *aplikacji\_danych* folder jest zarezerwowany; ten scenariusz jest jednym z miejsc, w którym ASP.NET sprawia, że założenia o tej nazwie.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-260">(Earlier we noted that the *App\_Data* folder is reserved; this scenario is one of the places where ASP.NET makes assumptions about that name.)</span></span>

<span data-ttu-id="4ac5d-261">Gdy baza danych jest otwarta, odwołanie do niej są umieszczane w zmiennej o nazwie `db`.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-261">When the database is opened, a reference to it is put into the variable named `db`.</span></span> <span data-ttu-id="4ac5d-262">(Która może być nazwane niczego.) `db` Zmienna jest sposób zostanie wyświetlone okno interakcji z bazą danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-262">(Which could be named anything.) The `db` variable is how you'll end up interacting with the database.</span></span>

<span data-ttu-id="4ac5d-263">Drugi wiersz faktycznie pobiera danych w bazie danych przy użyciu `Query` metody.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-263">The second line actually fetches the database data by using the `Query` method.</span></span> <span data-ttu-id="4ac5d-264">Zwróć uwagę, jak działa ten kod: `db` zmienna odwołuje się do otwartej bazy danych, i wywołuje `Query` metody przy użyciu `db` zmiennej (`db.Query`).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-264">Notice how this code works: the `db` variable has a reference to the opened database, and you invoke the `Query` method by using the `db` variable (`db.Query`).</span></span>

<span data-ttu-id="4ac5d-265">Sama kwerenda jest SQL `Select` instrukcji.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-265">The query itself is a SQL `Select` statement.</span></span> <span data-ttu-id="4ac5d-266">(Trochę tła o SQL, zobacz wyjaśnienie później). W instrukcji `Movies` identyfikuje tabeli w zapytaniu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-266">(For a little background about SQL, see the explanation later.) In the statement, `Movies` identifies the table to query.</span></span> <span data-ttu-id="4ac5d-267">`*` Znaku określa zapytanie powinno zwrócić wszystkie kolumny z tabeli.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-267">The `*` character specifies that the query should return all the columns from the table.</span></span> <span data-ttu-id="4ac5d-268">(Możesz można również listy kolumn oddzielnie, oddzielonych przecinkami.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-268">(You could also list columns individually, separated by commas.)</span></span>

<span data-ttu-id="4ac5d-269">Wyniki zapytania, jeśli taki występuje, są zwracane i udostępniona w `selectedData` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-269">The results of the query, if any, are returned and made available in the `selectedData` variable.</span></span> <span data-ttu-id="4ac5d-270">Ponownie zmienna może być nazwane żadnych czynności.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-270">Again, the variable could be named anything.</span></span>

<span data-ttu-id="4ac5d-271">Na koniec trzeciego wiersza informuje ASP.NET, czy chcesz użyć wystąpienia `WebGrid` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-271">Finally, the third line tells ASP.NET that you want to use an instance of the `WebGrid` helper.</span></span> <span data-ttu-id="4ac5d-272">Możesz utworzyć (*wystąpienia*) obiekt pomocnika za pomocą `new` — słowo kluczowe i przekaż go wyników zapytania za pomocą `selectedData` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-272">You create (*instantiate*) the helper object by using the `new` keyword and pass it the query results via the `selectedData` variable.</span></span> <span data-ttu-id="4ac5d-273">Nowy `WebGrid` obiektu oraz wyniki kwerendy bazy danych są udostępniane w `grid` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-273">The new `WebGrid` object, along with the results of the database query, are made available in the `grid` variable.</span></span> <span data-ttu-id="4ac5d-274">Wynik będziesz potrzebować na platformie chwilę, aby rzeczywiście wyświetlania danych na stronie.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-274">You'll need that result in a moment to actually display the data in the page.</span></span>

<span data-ttu-id="4ac5d-275">Na tym etapie bazy danych zostało otwarte, zaakceptowania danych ma i zostały przygotowane `WebGrid` pomocnika za pomocą tych danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-275">At this stage, the database has been opened, you've gotten the data you want, and you've prepared the `WebGrid` helper with that data.</span></span> <span data-ttu-id="4ac5d-276">Następnie jest utworzenie znaczników na stronie.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-276">Next is to create the markup in the page.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="4ac5d-277">**Structured Query Language (SQL)**</span><span class="sxs-lookup"><span data-stu-id="4ac5d-277">**Structured Query Language (SQL)**</span></span>
> 
> <span data-ttu-id="4ac5d-278">SQL jest językiem, który jest używany w większości relacyjnych baz danych do zarządzania danymi w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-278">SQL is a language that's used in most relational databases for managing data in a database.</span></span> <span data-ttu-id="4ac5d-279">Zawiera polecenia, które umożliwiają pobieranie danych i zaktualizować go i umożliwiające tworzenie, modyfikowanie i zarządzanie dane w tabelach bazy danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-279">It includes commands that let you retrieve data and update it, and that let you create, modify, and manage data in database tables.</span></span> <span data-ttu-id="4ac5d-280">SQL różni się od języka programowania (na przykład C#).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-280">SQL is different than a programming language (like C#).</span></span> <span data-ttu-id="4ac5d-281">Poinformuj bazy danych chcesz SQL, i jest zadanie bazy danych, aby dowiedzieć się, jak pobrać dane lub wykonać zadania.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-281">With SQL, you tell the database what you want, and it's the database's job to figure out how to get the data or perform the task.</span></span> <span data-ttu-id="4ac5d-282">Poniżej przedstawiono przykłady niektórych poleceń SQL i co zrobić:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-282">Here are examples of some SQL commands and what they do:</span></span>
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> <span data-ttu-id="4ac5d-283">Pierwszy `Select` instrukcji pobiera wszystkie kolumny (określonego przez `*`) z *filmy* tabeli.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-283">The first `Select` statement gets all the columns (specified by `*`) from the *Movies* table.</span></span>
> 
> <span data-ttu-id="4ac5d-284">Drugi `Select` instrukcji pobiera kolumny Identyfikatora, nazwy i Price z rekordów w *produktu* tabeli, których ceny wartość kolumny jest więcej niż 10.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-284">The second `Select` statement fetches the ID, Name, and Price columns from records in the *Product* table whose Price column value is more than 10.</span></span> <span data-ttu-id="4ac5d-285">Polecenie zwraca wyniki w kolejności alfabetycznej na podstawie wartości w kolumnie Nazwa.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-285">The command returns the results in alphabetical order based on the values of the Name column.</span></span> <span data-ttu-id="4ac5d-286">Jeśli żadne rekordy nie odpowiadają kryteria cen, to polecenie zwraca pusty zestaw.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-286">If no records match the price criteria, the command returns an empty set.</span></span>
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> <span data-ttu-id="4ac5d-287">To polecenie Wstawia nowy rekord w *produktu* tabeli ustawienie nazwy kolumny "Croissant" Opis kolumnę "A niestabilnym radością" i ceny 1.99.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-287">This command inserts a new record into the *Product* table, setting the Name column to "Croissant", the Description column to "A flaky delight", and the price to 1.99.</span></span>
> 
> <span data-ttu-id="4ac5d-288">Należy zauważyć, że jest określając wartości nieliczbowe wartość jest ujęta w znaki apostrofu (nie znaki cudzysłowu, tak jak C#).</span><span class="sxs-lookup"><span data-stu-id="4ac5d-288">Notice that when you're specifying a non-numeric value, the value is enclosed in single quotation marks (not double quotation marks, as in C#).</span></span> <span data-ttu-id="4ac5d-289">Używamy tych cudzysłowów wokół wartości tekstowe lub daty, ale nie wokół cyfry.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-289">You use these quotation marks around text or date values, but not around numbers.</span></span>
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> <span data-ttu-id="4ac5d-290">To polecenie usuwa rekordy w *produktu* tabeli, którego kolumna Data wygaśnięcia jest wcześniejsza niż 1 stycznia 2008.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-290">This command deletes records in the *Product* table whose expiration date column is earlier than January 1, 2008.</span></span> <span data-ttu-id="4ac5d-291">(Polecenia założono, że *produktu* Tabela oczywiście ma takie kolumny.) Tutaj podano datę w formacie MM/DD/RRRR, ale powinny być wprowadzane w formacie, który jest używany dla ustawień regionalnych użytkownika.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-291">(The command assumes that the *Product* table has such a column, of course.) The date is entered here in MM/DD/YYYY format, but it should be entered in the format that's used for your locale.</span></span>
> 
> <span data-ttu-id="4ac5d-292">`Insert` i `Delete` poleceń nie zwracać zestawów wyników.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-292">The `Insert` and `Delete` commands don't return result sets.</span></span> <span data-ttu-id="4ac5d-293">Zamiast tego zwracają numer, informujący o tym, jak wiele rekordów wpłynęła na polecenie.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-293">Instead, they return a number that tells you how many records were affected by the command.</span></span>
> 
> <span data-ttu-id="4ac5d-294">Dla niektórych z tych operacji (takich jak wstawianie i usuwanie rekordów) proces, który żąda operacji musi mieć odpowiednie uprawnienia w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-294">For some of these operations (like inserting and deleting records), the process that's requesting the operation has to have appropriate permissions in the database.</span></span> <span data-ttu-id="4ac5d-295">To dlatego dla baz danych produkcyjnych często konieczne podanie nazwy użytkownika i hasła podczas łączenia z bazą danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-295">That's why for production databases you often have to supply a user name and password when you connect to the database.</span></span>
> 
> <span data-ttu-id="4ac5d-296">Istnieje wielu poleceń SQL, ale wszystkie one wykonać wzorzec, takich jak polecenia widocznej w tym miejscu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-296">There are dozens of SQL commands, but they all follow a pattern like the commands you see here.</span></span> <span data-ttu-id="4ac5d-297">Poleceń SQL służy do tworzenia tabel bazy danych, liczbę rekordów w tabeli obliczyć ceny i wykonywać wiele kolejnych operacji.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-297">You can use SQL commands to create database tables, count the number of records in a table, calculate prices, and perform many more operations.</span></span>


### <a name="adding-markup-to-display-the-data"></a><span data-ttu-id="4ac5d-298">Dodawanie znaczników do wyświetlania danych</span><span class="sxs-lookup"><span data-stu-id="4ac5d-298">Adding Markup to Display the Data</span></span>

<span data-ttu-id="4ac5d-299">Wewnątrz `<head>` elementu, zawartość zestawu `<title>` elementu "Filmy":</span><span class="sxs-lookup"><span data-stu-id="4ac5d-299">Inside the `<head>` element, set contents of the `<title>` element to "Movies":</span></span>

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

<span data-ttu-id="4ac5d-300">Wewnątrz `<body>` element strony, Dodaj następujący kod:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-300">Inside the `<body>` element of the page, add the following:</span></span>

[!code-html[Main](displaying-data/samples/sample3.html)]

<span data-ttu-id="4ac5d-301">To wszystko.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-301">That's it.</span></span> <span data-ttu-id="4ac5d-302">`grid` Wartość utworzone podczas tworzenia zmiennej to `WebGrid` obiektu w kodzie wcześniej.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-302">The `grid` variable is the value you created when you created the `WebGrid` object in code earlier.</span></span>

<span data-ttu-id="4ac5d-303">W widoku drzewa programu WebMatrix, kliknij prawym przyciskiem myszy strony, a następnie wybierz **Uruchom w przeglądarce**.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-303">In the WebMatrix tree view, right-click the page and select **Launch in browser**.</span></span> <span data-ttu-id="4ac5d-304">Zostanie wyświetlony ekran podobny do tej strony:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-304">You'll see something like this page:</span></span>

![Domyślnie dane wyjściowe pomocnika WebGrid tabeli filmy](displaying-data/_static/image18.png)

<span data-ttu-id="4ac5d-306">Kliknij łącze nagłówek kolumny, aby sortować według tej kolumny.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-306">Click a column heading link to sort by that column.</span></span> <span data-ttu-id="4ac5d-307">Możliwość sortować, klikając odpowiedni nagłówek jest funkcją, która jest wbudowana w **WebGrid** pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-307">Being able to sort by clicking a heading is a feature that's built into the **WebGrid** helper.</span></span>

<span data-ttu-id="4ac5d-308">`GetHtml` Metoda, jak jego nazwa sugeruje, generuje kod znaczników, który wyświetla dane.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-308">The `GetHtml` method, as its name suggests, generates markup that displays the data.</span></span> <span data-ttu-id="4ac5d-309">Domyślnie `GetHtml` metoda generuje HTML `<table>` elementu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-309">By default, the `GetHtml` method generates an HTML `<table>` element.</span></span> <span data-ttu-id="4ac5d-310">(Jeśli chcesz, możesz sprawdzić renderowanie przez patrzeć źródło strony w przeglądarce.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-310">(If you want, you can verify the rendering by looking at the source of the page in the browser.)</span></span>

## <a name="modifying-the-look-of-the-grid"></a><span data-ttu-id="4ac5d-311">Modyfikowanie wyglądu siatki</span><span class="sxs-lookup"><span data-stu-id="4ac5d-311">Modifying the Look of the Grid</span></span>

<span data-ttu-id="4ac5d-312">Przy użyciu `WebGrid` pomocnika tak tak samo jak jest łatwe, ale jest zwykły wynikami wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-312">Using the `WebGrid` helper like you just did is easy, but the resulting display is plain.</span></span> <span data-ttu-id="4ac5d-313">`WebGrid` Pomocnika ma szerokiej gamy opcje umożliwiające sterowanie sposób wyświetlania danych.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-313">The `WebGrid` helper has all sorts of options that let you control how the data is displayed.</span></span> <span data-ttu-id="4ac5d-314">Istnieje zbyt dużo, aby zapoznać się z tego samouczka, ale w tej sekcji zostanie dają pogląd, niektóre z tych opcji.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-314">There are far too many to explore in this tutorial, but this section will give you an idea of some of those options.</span></span> <span data-ttu-id="4ac5d-315">Kilka dodatkowych opcji zostaną opisane w kolejnych samouczkach z tej serii.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-315">A few additional options will be covered in later tutorials in this series.</span></span>

### <a name="specifying-individual-columns-to-display"></a><span data-ttu-id="4ac5d-316">Określenie poszczególnych kolumn do wyświetlenia</span><span class="sxs-lookup"><span data-stu-id="4ac5d-316">Specifying Individual Columns to Display</span></span>

<span data-ttu-id="4ac5d-317">Aby rozpocząć, można określić, czy mają być wyświetlane tylko niektóre kolumny.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-317">To start, you can specify that you want to display only certain columns.</span></span> <span data-ttu-id="4ac5d-318">Domyślnie, jak zostały przeczytane na siatce są wyświetlane wszystkie cztery kolumny *filmy* tabeli.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-318">By default, as you've seen, the grid shows all four of the columns from the *Movies* table.</span></span>

<span data-ttu-id="4ac5d-319">W *Movies.cshtml* pliku, Zastąp `@grid.GetHtml()` kod znaczników, który właśnie dodano następującym kodem:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-319">In the *Movies.cshtml* file, replace the `@grid.GetHtml()` markup that you just added with the following:</span></span>

[!code-css[Main](displaying-data/samples/sample4.css)]

<span data-ttu-id="4ac5d-320">Mówić pomocnika kolumn do wyświetlenia, obejmują `columns` parametr `GetHtml` — metoda i przekazać w kolekcji kolumn.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-320">To tell the helper which columns to display, you include a `columns` parameter for the `GetHtml` method and pass in a collection of columns.</span></span> <span data-ttu-id="4ac5d-321">W kolekcji należy określić każdej kolumny w celu uwzględnienia.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-321">In the collection, you specify each column to include.</span></span> <span data-ttu-id="4ac5d-322">Określ poszczególne kolumny do wyświetlenia w tym `grid.Column` obiektów i podaj nazwę kolumny danych ma.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-322">You specify an individual column to display by including a `grid.Column` object, and pass in the name of the data column you want.</span></span> <span data-ttu-id="4ac5d-323">(Te kolumny muszą być zawarte w wynikach zapytania SQL — `WebGrid` pomocnika nie może wyświetlić kolumny, które nie zostały zwrócone przez zapytanie.)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-323">(These columns must be included in the SQL query results — the `WebGrid` helper cannot display columns that were not returned by the query.)</span></span>

<span data-ttu-id="4ac5d-324">Uruchom *Movies.cshtml* strony w przeglądarce ponownie i tym razem zostanie wyświetlony ekran podobny do następującego (Zauważ, że w kolumnie Identyfikator nie jest wyświetlana):</span><span class="sxs-lookup"><span data-stu-id="4ac5d-324">Launch the *Movies.cshtml* page in the browser again, and this time you get a display like the following one (notice that no ID column is displayed):</span></span>

![Wyświetl WebGrid przedstawiający tylko wybranych kolumn](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a><span data-ttu-id="4ac5d-326">Zmiana wyglądu siatki</span><span class="sxs-lookup"><span data-stu-id="4ac5d-326">Changing the Look of the Grid</span></span>

<span data-ttu-id="4ac5d-327">Istnieje kilka więcej opcji wyświetlania kolumn, niektóre z nich zostaną przedstawione w kolejnych samouczkach w tym zestawie.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-327">There are quite a few more options for displaying columns, some of which will be explored in later tutorials in this set.</span></span> <span data-ttu-id="4ac5d-328">Obecnie w tej sekcji przedstawiono sposoby, w którym można stylów siatki jako całość.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-328">For now, this section will introduce you to ways in which you can style the grid as a whole.</span></span>

<span data-ttu-id="4ac5d-329">Wewnątrz `<head>` części strony, bezpośrednio przed zamknięciem `</head>` tagu, należy dodać następujące `<style>` elementu:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-329">Inside the `<head>` section of the page, just before the closing `</head>` tag, add the following `<style>` element:</span></span>

[!code-css[Main](displaying-data/samples/sample5.css)]

<span data-ttu-id="4ac5d-330">Ten kod znaczników CSS definiuje klasy o nazwie `grid`, `head`i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-330">This CSS markup defines classes named `grid`, `head`, and so on.</span></span> <span data-ttu-id="4ac5d-331">Można również wprowadzić te definicje stylu w oddzielnej *.css* plików i połączona ze stroną.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-331">You could also put these style definitions in a separate *.css* file and link that to the page.</span></span> <span data-ttu-id="4ac5d-332">(W rzeczywistości należy to zrobić w dalszej części tego samouczka zestawu.) Jednak aby ułatwić czynności w tym samouczku, są one w tej samej stronie, która wyświetla dane.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-332">(In fact, you'll do that later in this tutorial set.) But to make things easy for this tutorial, they're inside the same page that displays the data.</span></span>

<span data-ttu-id="4ac5d-333">Teraz możesz uzyskać `WebGrid` pomocnika do używania tych klas stylu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-333">Now you can get the `WebGrid` helper to use these style classes.</span></span> <span data-ttu-id="4ac5d-334">Pomocnik ma wiele właściwości (na przykład `tableStyle`) właśnie w tym celu — przypisać nazwę klasy stylów CSS do nich, i że nazwa klasy jest renderowane jako część znaczników, który jest renderowany przez pomocnika.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-334">The helper has a number of properties (for example, `tableStyle`) for just this purpose — you assign a CSS style class name to them, and that class name is rendered as part of the markup that's rendered by the helper.</span></span>

<span data-ttu-id="4ac5d-335">Zmień `grid.GetHtml` znaczników, aby teraz wygląda ten kod:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-335">Change the `grid.GetHtml` markup so that it now looks like this code:</span></span>

[!code-css[Main](displaying-data/samples/sample6.css)]

<span data-ttu-id="4ac5d-336">Nowości w tym miejscu jest, że zostały dodane `tableStyle`, `headerStyle`, i `alternatingRowStyle` parametry `GetHtml` metody.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-336">What's new here is that you've added `tableStyle`, `headerStyle`, and `alternatingRowStyle` parameters to the `GetHtml` method.</span></span> <span data-ttu-id="4ac5d-337">Te parametry zostały ustawione na nazwy stylów CSS, które dodano kilka minut temu.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-337">These parameters have been set to the names of the CSS styles that you added a moment ago.</span></span>

<span data-ttu-id="4ac5d-338">Uruchom strony, a teraz Zobacz siatki, która wygląda znacznie mniej zwykły niż przed:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-338">Run the page, and this time you see a grid that looks much less plain than before:</span></span>

![Wyświetl WebGrid, który zawiera parametry ustawioną nazwy klas CSS](displaying-data/_static/image20.png)

<span data-ttu-id="4ac5d-340">Aby sprawdzić, jakie `GetHtml` wygenerowane metody, można sprawdzić źródło strony w przeglądarce.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-340">To see what the `GetHtml` method generated, you can look at the source of the page in the browser.</span></span> <span data-ttu-id="4ac5d-341">Nie możemy przejść do szczegółów w tym miejscu, ale istotne jest, określając parametry, takie jak `tableStyle`, spowodowane siatki mają być Generowanie tagi HTML podobne do poniższych:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-341">We won't go into detail here, but the important point is that by specifying parameters like `tableStyle`, you caused the grid to generate HTML tags like the following:</span></span>

`<table class="grid">`

<span data-ttu-id="4ac5d-342">`<table>` Miał znacznik `class` dodany atrybut, który odwołuje się do jednego z reguły CSS, które zostały wcześniej dodane.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-342">The `<table>` tag has had a `class` attribute added to it that references one of the CSS rules that you added earlier.</span></span> <span data-ttu-id="4ac5d-343">Ten kod przedstawia podstawowy wzorzec &mdash; różnych parametrów `GetHtml` metody umożliwiają możesz odwołanie CSS klas, że z nich metoda generuje następnie wraz ze znaczników.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-343">This code shows you the basic pattern &mdash; different parameters for the `GetHtml` method let you reference CSS classes that the method then generates along with the markup.</span></span> <span data-ttu-id="4ac5d-344">Co należy zrobić z klas CSS zależy od użytkownika.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-344">What you do with the CSS classes is up to you.</span></span>

## <a name="adding-paging"></a><span data-ttu-id="4ac5d-345">Dodawanie stronicowania</span><span class="sxs-lookup"><span data-stu-id="4ac5d-345">Adding Paging</span></span>

<span data-ttu-id="4ac5d-346">Jako ostatnie zadanie dla tego samouczka warto stronicowania siatki.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-346">As the last task for this tutorial, you'll add paging to the grid.</span></span> <span data-ttu-id="4ac5d-347">W tej chwili jest żadnych problemów, aby wyświetlić wszystkie filmów jednocześnie.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-347">Right now it's no problem to display all your movies at once.</span></span> <span data-ttu-id="4ac5d-348">Jednak jeśli dodano setki filmów wyświetlania stron, jak długo.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-348">But if you added hundreds of movies, the page display would get long.</span></span>

<span data-ttu-id="4ac5d-349">W kodzie strony, zmień wiersz, który tworzy `WebGrid` obiektu poniższy kod:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-349">In the page code, change the line that creates the `WebGrid` object to the following code:</span></span>

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

<span data-ttu-id="4ac5d-350">Jedyną różnicą z przed jest, że zostały dodane `rowsPerPage` parametr, który ma ustawioną wartość 3.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-350">The only difference from before is that you've added a `rowsPerPage` parameter that's set to 3.</span></span>

<span data-ttu-id="4ac5d-351">Uruchom strony.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-351">Run the page.</span></span> <span data-ttu-id="4ac5d-352">Siatka wyświetla 3 wiersze na czas, oraz łączy nawigacji, które umożliwiają przeglądanie filmów w bazie danych:</span><span class="sxs-lookup"><span data-stu-id="4ac5d-352">The grid displays 3 rows at a time, plus navigation links that let you page through the movies in your database:</span></span>

![Wyświetl WebGrid z stronicowania](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a><span data-ttu-id="4ac5d-354">Powtarzający się dalej</span><span class="sxs-lookup"><span data-stu-id="4ac5d-354">Coming Up Next</span></span>

<span data-ttu-id="4ac5d-355">W następnym samouczku nauczysz się, jak pobrać dane wejściowe użytkownika w postaci za pomocą kodu Razor i C#.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-355">In the next tutorial, you'll learn how to use Razor and C# code to get user input in a form.</span></span> <span data-ttu-id="4ac5d-356">Pole wyszukiwania będzie dodany do strony filmów, dzięki czemu można znaleźć filmy tytuł lub rodzaju.</span><span class="sxs-lookup"><span data-stu-id="4ac5d-356">You'll add a search box to the Movies page so that you can find movies by title or genre.</span></span>

## <a name="complete-listing-for-movies-page"></a><span data-ttu-id="4ac5d-357">Pełna lista filmów strony</span><span class="sxs-lookup"><span data-stu-id="4ac5d-357">Complete Listing for Movies Page</span></span>

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="4ac5d-358">Dodatkowe zasoby</span><span class="sxs-lookup"><span data-stu-id="4ac5d-358">Additional Resources</span></span>

- [<span data-ttu-id="4ac5d-359">Wprowadzenie do programowania sieci Web ASP.NET przy użyciu składni Razor</span><span class="sxs-lookup"><span data-stu-id="4ac5d-359">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> <span data-ttu-id="4ac5d-360">[Poprzednie](intro-to-web-pages-programming.md)
> [dalej](form-basics.md)</span><span class="sxs-lookup"><span data-stu-id="4ac5d-360">[Previous](intro-to-web-pages-programming.md)
[Next](form-basics.md)</span></span>
