---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: "Wprowadzenie do składnika ASP.NET Web Pages — aktualizowanie bazy danych | Dokumentacja firmy Microsoft"
author: tfitzmac
description: "Ten samouczek pokazuje, jak zaktualizować wpis (Zmień) istniejącej bazy danych, korzystając z stron sieci Web platformy ASP.NET (Razor). Przyjęto założenie, że zostały wykonane serii th..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 01/02/2018
ms.topic: article
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: 6fc8631c323dbb82f4f33b78128ba8de8bc9bfdd
ms.sourcegitcommit: 2d23ea501e0213bbacf65298acf1c8bd17209540
ms.translationtype: HT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/09/2018
---
<a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="53f61-104">Wprowadzenie do strony sieci Web ASP.NET — aktualizowanie bazy danych</span><span class="sxs-lookup"><span data-stu-id="53f61-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>
====================
<span data-ttu-id="53f61-105">przez [FitzMacken niestandardowy](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="53f61-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="53f61-106">Ten samouczek pokazuje, jak zaktualizować wpis (Zmień) istniejącej bazy danych, korzystając z stron sieci Web platformy ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="53f61-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="53f61-107">Przyjęto założenie, że zostały wykonane serii za pomocą [wprowadzanie danych przez za pomocą formularzy przy użyciu stron ASP.NET Web Pages](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="53f61-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="53f61-108">Zawartość:</span><span class="sxs-lookup"><span data-stu-id="53f61-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="53f61-109">Jak wybrać rekord `WebGrid` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="53f61-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="53f61-110">Jak można odczytać pojedynczego rekordu z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="53f61-111">Jak wstępnie formularza z wartości rekordu bazy danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="53f61-112">Jak zaktualizować istniejący rekord w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="53f61-113">Sposób przechowywania informacji na stronie bez ich wyświetlania.</span><span class="sxs-lookup"><span data-stu-id="53f61-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="53f61-114">Jak używać do przechowywania informacji o ukryte pole.</span><span class="sxs-lookup"><span data-stu-id="53f61-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="53f61-115">Funkcje/technologie omówione:</span><span class="sxs-lookup"><span data-stu-id="53f61-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="53f61-116">`WebGrid` Pomocnika.</span><span class="sxs-lookup"><span data-stu-id="53f61-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="53f61-117">SQL `Update` polecenia.</span><span class="sxs-lookup"><span data-stu-id="53f61-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="53f61-118">`Database.Execute` Metody.</span><span class="sxs-lookup"><span data-stu-id="53f61-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="53f61-119">Ukryte pola (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="53f61-119">Hidden fields (`<input type="hidden">`).</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="53f61-120">Jakie będzie kompilacji</span><span class="sxs-lookup"><span data-stu-id="53f61-120">What You'll Build</span></span>

<span data-ttu-id="53f61-121">W poprzednich samouczka przedstawiono sposób dodać rekordu do bazy danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="53f61-122">W tym miejscu dowiesz się, jak można wyświetlić rekordu do edycji.</span><span class="sxs-lookup"><span data-stu-id="53f61-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="53f61-123">W *filmy* strony, będzie aktualizowana `WebGrid` Pomocnik, którego nie wyświetla **Edytuj** link obok każdego filmu:</span><span class="sxs-lookup"><span data-stu-id="53f61-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![Wyświetl WebGrid tym łącze "Edytuj" dla każdego filmu](updating-data/_static/image1.png)

<span data-ttu-id="53f61-125">Po kliknięciu **Edytuj** łącza, spowoduje to przejście do innej strony, gdy informacje filmu są już w postaci:</span><span class="sxs-lookup"><span data-stu-id="53f61-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Edytuj stronę Film przedstawiający filmu do edycji](updating-data/_static/image2.png)

<span data-ttu-id="53f61-127">Można zmienić wartości.</span><span class="sxs-lookup"><span data-stu-id="53f61-127">You can change any of the values.</span></span> <span data-ttu-id="53f61-128">Po przesłaniu zmian kodu na stronie aktualizuje bazę danych i powrót do listy filmów.</span><span class="sxs-lookup"><span data-stu-id="53f61-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="53f61-129">Ta część procesu prawie dokładnie tak samo, jak działa *AddMovie.cshtml* utworzonego w poprzedniej samouczek, dlatego większość tego samouczka należy zapoznać się strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="53f61-130">Istnieje kilka metod, które można zaimplementować sposób edytowania indywidualne filmów.</span><span class="sxs-lookup"><span data-stu-id="53f61-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="53f61-131">Podejście pokazano została wybrana, ponieważ jest łatwa do wdrożenia i łatwy do zrozumienia.</span><span class="sxs-lookup"><span data-stu-id="53f61-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="53f61-132">Dodawanie do listy filmów łącza edycji</span><span class="sxs-lookup"><span data-stu-id="53f61-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="53f61-133">Aby rozpocząć, będą aktualizowane *filmy* strony, tak aby zawierała każdego filmu również wyświetlanie **Edytuj** łącza.</span><span class="sxs-lookup"><span data-stu-id="53f61-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="53f61-134">Otwórz *Movies.cshtml* pliku.</span><span class="sxs-lookup"><span data-stu-id="53f61-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="53f61-135">W treści strony Zmień `WebGrid` znaczników przez dodanie kolumny.</span><span class="sxs-lookup"><span data-stu-id="53f61-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="53f61-136">Oto modyfikacji kod znaczników:</span><span class="sxs-lookup"><span data-stu-id="53f61-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="53f61-137">Nowa kolumna jest ta:</span><span class="sxs-lookup"><span data-stu-id="53f61-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="53f61-138">Punkt w tej kolumnie jest do wyświetlenia łącza (`<a>` element) którego tekst mówi "Edytuj".</span><span class="sxs-lookup"><span data-stu-id="53f61-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="53f61-139">Interesuje nas jest utworzenie łącza, który wygląda jak poniżej, po uruchomieniu na stronie, z `id` wartość różne dla każdego filmu:</span><span class="sxs-lookup"><span data-stu-id="53f61-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="53f61-140">To łącze wywoła stronę o nazwie *EditMovie*, i zostanie przekazany ciąg zapytania `?id=7` do tej strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="53f61-141">Składnia dla nowej kolumny, która może wyglądać nieco złożonego, ale jest to tylko, ponieważ umieszcza je ze sobą kilka elementów.</span><span class="sxs-lookup"><span data-stu-id="53f61-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="53f61-142">Każdy element poszczególnych jest prosta.</span><span class="sxs-lookup"><span data-stu-id="53f61-142">Each individual element is straightforward.</span></span> <span data-ttu-id="53f61-143">Jeśli możesz skoncentrować się na właśnie `<a>` elementu, zobacz ten kod znaczników:</span><span class="sxs-lookup"><span data-stu-id="53f61-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="53f61-144">Pewną wiedzę na temat działania siatki: Siatka wyświetla wiersze, po jednej dla każdego rekordu bazy danych, i wyświetla kolumny dla każdego pola w rekordzie bazy danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="53f61-145">Podczas każdego wiersza siatki jest tworzona, `item` obiektu zawiera rekord bazy danych (element) dla tego wiersza.</span><span class="sxs-lookup"><span data-stu-id="53f61-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="53f61-146">To rozmieszczenie umożliwia kod w celu pobrania danych dla tego wiersza.</span><span class="sxs-lookup"><span data-stu-id="53f61-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="53f61-147">Jest widoczna w tym miejscu: wyrażenie `item.ID` otrzymuje wartość Identyfikatora bieżącego elementu bazy danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="53f61-148">Można uzyskać wartości bazy danych (tytuł, genre lub rok) taki sam sposób jak przy użyciu `item.Title`, `item.Genre`, lub `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="53f61-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="53f61-149">Wyrażenie `"~/EditMovie?id=@item.ID` łączy część ustalony docelowy adres URL (`~/EditMovie?id=`) z tym identyfikatorem dynamicznie pochodnych.</span><span class="sxs-lookup"><span data-stu-id="53f61-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="53f61-150">(Widać `~` operatora w poprzednim samouczek; jest operatora programu ASP.NET, który reprezentuje bieżącego katalogu głównego witryny sieci Web.)</span><span class="sxs-lookup"><span data-stu-id="53f61-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="53f61-151">Wynik jest, że ta część znacznika w kolumnie po prostu tworzy przypominać następujący kod w czasie wykonywania:</span><span class="sxs-lookup"><span data-stu-id="53f61-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="53f61-152">Oczywiście rzeczywistej wartości `id` będą inne dla każdego wiersza.</span><span class="sxs-lookup"><span data-stu-id="53f61-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="53f61-153">Tworzenie niestandardowego wyświetlania kolumny siatki</span><span class="sxs-lookup"><span data-stu-id="53f61-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="53f61-154">Teraz z powrotem do kolumn siatki.</span><span class="sxs-lookup"><span data-stu-id="53f61-154">Now back to the grid column.</span></span> <span data-ttu-id="53f61-155">Trzy kolumny trzeba pierwotnie było w siatce wyświetlane tylko wartości danych (tytuł, genre i rok).</span><span class="sxs-lookup"><span data-stu-id="53f61-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="53f61-156">Ten ekran jest określony przez nazwa kolumny bazy danych &mdash; na przykład `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="53f61-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="53f61-157">Nowy **Edytuj** kolumny łącza jest inna.</span><span class="sxs-lookup"><span data-stu-id="53f61-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="53f61-158">Zamiast określania nazwy kolumny, przechodząc `format` parametru.</span><span class="sxs-lookup"><span data-stu-id="53f61-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="53f61-159">Ten parametr umożliwia definiowanie znaczników który `WebGrid` pomocnika spowoduje, że wraz z programem `item` wartość do wyświetlania danych kolumny jako pogrubiony lub zielony lub w dowolnym formacie tego.</span><span class="sxs-lookup"><span data-stu-id="53f61-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="53f61-160">Na przykład jeśli potrzebujesz tytuł, aby wyświetlone pogrubioną można utworzyć kolumnę, tak jak ten przykład:</span><span class="sxs-lookup"><span data-stu-id="53f61-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="53f61-161">(Różne `@` znaki widoczne w `format` właściwości oznaczyć przejścia między znaczników i wartość kodu.)</span><span class="sxs-lookup"><span data-stu-id="53f61-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="53f61-162">Po wiedzieć o `format` właściwości, łatwiej zrozumieć, jak nowy **Edytuj** Podsumowując kolumny łącza:</span><span class="sxs-lookup"><span data-stu-id="53f61-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="53f61-163">Kolumna zawiera *tylko* znaczników, który renderuje łącze, oraz niektóre informacje (ID) który został wyodrębniony rekordu bazy danych dla wiersza.</span><span class="sxs-lookup"><span data-stu-id="53f61-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="53f61-164">**Parametrów nazwanych i pozycyjnych parametrów metody**</span><span class="sxs-lookup"><span data-stu-id="53f61-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="53f61-165">Wiele razy po utworzeniu wywołuje metodę i przekazania parametrów do niego, po prostu zawartych wartości parametrów oddzielonych przecinkami.</span><span class="sxs-lookup"><span data-stu-id="53f61-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="53f61-166">Oto kilka przykładów:</span><span class="sxs-lookup"><span data-stu-id="53f61-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="53f61-167">Firma Microsoft nie wspomina problem, gdy najpierw był wyświetlany ten kod, ale w każdym przypadku jest przekazywanie parametrów do metod w określonej kolejności &mdash; mianowicie, kolejność, w której parametry są zdefiniowane w tej metodzie.</span><span class="sxs-lookup"><span data-stu-id="53f61-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="53f61-168">Aby uzyskać `db.Execute` i `Validation.RequireFields`, jeśli zamienione wartości należy przekazać, otrzyma komunikat o błędzie po uruchomieniu na stronie lub co najmniej pewnych dziwne wyników.</span><span class="sxs-lookup"><span data-stu-id="53f61-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="53f61-169">Wyraźnie widać trzeba znać do przekazania parametrów w kolejności.</span><span class="sxs-lookup"><span data-stu-id="53f61-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="53f61-170">(W programie WebMatrix, IntelliSense ułatwiają ustalenie potrzebnej nazwę, typ i kolejność parametrów.)</span><span class="sxs-lookup"><span data-stu-id="53f61-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="53f61-171">Zamiast wartości przekazywanie w kolejności, można użyć *parametrów nazwanych*.</span><span class="sxs-lookup"><span data-stu-id="53f61-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="53f61-172">(Przekazywanie parametrów w kolejności nazywa się przy użyciu *parametrów pozycyjnych*.) Dla parametrów nazwanych jawnie uwzględniono nazwę parametru podczas przekazywania jego wartość.</span><span class="sxs-lookup"><span data-stu-id="53f61-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="53f61-173">Używano nazwane parametry już kilka razy w tych samouczkach.</span><span class="sxs-lookup"><span data-stu-id="53f61-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="53f61-174">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="53f61-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="53f61-175">and</span><span class="sxs-lookup"><span data-stu-id="53f61-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="53f61-176">Parametry nazwane jest skorzystać z kilku sytuacjach, szczególnie w przypadku, gdy metoda pobiera dużo parametrów.</span><span class="sxs-lookup"><span data-stu-id="53f61-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="53f61-177">Jest jeden, gdy chcesz przekazać tylko jeden lub dwa parametry, ale wśród pierwszej pozycji na liście parametrów nie są wartości, które chcesz przekazać.</span><span class="sxs-lookup"><span data-stu-id="53f61-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="53f61-178">Innej sytuacji jest, aby kod był bardziej czytelny przekazując parametry w kolejności priorytetów dla Ciebie.</span><span class="sxs-lookup"><span data-stu-id="53f61-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="53f61-179">Oczywiście Aby użyć parametrów nazwanych, trzeba znać nazwy parametrów.</span><span class="sxs-lookup"><span data-stu-id="53f61-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="53f61-180">Program WebMatrix IntelliSense może *Pokaż* nazwy, ale nie można obecnie wypełnieniu je automatycznie.</span><span class="sxs-lookup"><span data-stu-id="53f61-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>


## <a name="creating-the-edit-page"></a><span data-ttu-id="53f61-181">Tworzenie edycji strony</span><span class="sxs-lookup"><span data-stu-id="53f61-181">Creating the Edit Page</span></span>

<span data-ttu-id="53f61-182">Teraz możesz utworzyć *EditMovie* strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="53f61-183">Po kliknięciu **Edytuj** łącza, są one będą umieszczane na tej stronie.</span><span class="sxs-lookup"><span data-stu-id="53f61-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="53f61-184">Utwórz stronę o nazwie *EditMovie.cshtml* i Zastąp nowości w pliku o następujący kod:</span><span class="sxs-lookup"><span data-stu-id="53f61-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="53f61-185">Ten kod znaczników i kodu jest podobny do masz *AddMovie* strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="53f61-186">Istnieje niewielka różnica w tekst dla przycisku Prześlij.</span><span class="sxs-lookup"><span data-stu-id="53f61-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="53f61-187">Jak *AddMovie* strony, jest `Html.ValidationSummary` wywołaniu, które będą wyświetlane błędy sprawdzania poprawności, jeśli istnieją.</span><span class="sxs-lookup"><span data-stu-id="53f61-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="53f61-188">Teraz możemy Cię pomijając wywołania `Validation.Message`, ponieważ błędy będą wyświetlane w podsumowania weryfikacji.</span><span class="sxs-lookup"><span data-stu-id="53f61-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="53f61-189">Jak wspomniano w poprzedniej samouczek, można użyć podsumowania weryfikacji i komunikaty o poszczególnych w różnych kombinacjach.</span><span class="sxs-lookup"><span data-stu-id="53f61-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="53f61-190">Zwróć uwagę, ponownie że `method` atrybutu `<form>` element jest ustawiony na wartość `post`.</span><span class="sxs-lookup"><span data-stu-id="53f61-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="53f61-191">Jak *AddMovie.cshtml* strony, strony dokonuje zmian w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="53f61-192">W związku z tym należy wykonać ten formularz `POST` operacji.</span><span class="sxs-lookup"><span data-stu-id="53f61-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="53f61-193">(Aby uzyskać więcej informacji o różnicach między `GET` i `POST` operacji, zobacz [GET, POST i bezpieczeństwa zlecenie HTTP](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) paska bocznego w samouczku w formularzach HTML.)</span><span class="sxs-lookup"><span data-stu-id="53f61-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="53f61-194">Jak przedstawiono wcześniej samouczka `value` atrybutów pola tekstowe są ustawiane z kodu Razor w celu wstępnego załadowania je.</span><span class="sxs-lookup"><span data-stu-id="53f61-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="53f61-195">Teraz, jednak używasz zmiennych, na przykład `title` i `genre` dla tego zadania, a nie `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="53f61-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="53f61-196">Jako przed, ten kod znaczników zostaną wstępnie wartości pole tekstowe z wartościami filmu.</span><span class="sxs-lookup"><span data-stu-id="53f61-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="53f61-197">Pojawi się za chwilę, dlaczego warto używać zmiennych w tej chwili zamiast `Request` obiektu.</span><span class="sxs-lookup"><span data-stu-id="53f61-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="53f61-198">Istnieje również `<input type="hidden">` elementu na tej stronie.</span><span class="sxs-lookup"><span data-stu-id="53f61-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="53f61-199">Ten element przechowuje identyfikator filmu bez staje się widoczny na stronie.</span><span class="sxs-lookup"><span data-stu-id="53f61-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="53f61-200">Identyfikator jest początkowo przekazany do strony przy przy użyciu wartości ciągu zapytania (`?id=7` lub podobne w adresie URL).</span><span class="sxs-lookup"><span data-stu-id="53f61-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="53f61-201">Ustawiając wartość Identyfikatora w ukrytym polu, można upewnij się, że jest dostępny po przesłaniu formularza, nawet jeśli nie masz już dostępu do strony została wywołana z oryginalnego adresu URL.</span><span class="sxs-lookup"><span data-stu-id="53f61-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="53f61-202">W odróżnieniu od *AddMovie* strony kod *EditMovie* strony ma dwa różne funkcje.</span><span class="sxs-lookup"><span data-stu-id="53f61-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="53f61-203">Pierwszej funkcji jest fakt, że ta strona jest wyświetlana po raz pierwszy (i *tylko* następnie), kod pobiera identyfikator film z ciągu zapytania.</span><span class="sxs-lookup"><span data-stu-id="53f61-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="53f61-204">Kod, a następnie używa Identyfikatora do odczytywania odpowiedniego film z bazy danych programu i wyświetlić (wstępnie) go w polach tekstowych.</span><span class="sxs-lookup"><span data-stu-id="53f61-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="53f61-205">Druga funkcja występuje wtedy, gdy użytkownik kliknie **Prześlij zmiany** przycisku kod ma odczytać wartości pola tekstowe i sprawdzania ich poprawności.</span><span class="sxs-lookup"><span data-stu-id="53f61-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="53f61-206">Ten kod musi zaktualizować element bazy danych przy użyciu nowych wartości.</span><span class="sxs-lookup"><span data-stu-id="53f61-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="53f61-207">Ta technika jest podobne do dodawania rekordów, jak przedstawiono w *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="53f61-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="53f61-208">Dodawanie kodu do odczytu pojedynczego filmu</span><span class="sxs-lookup"><span data-stu-id="53f61-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="53f61-209">Aby wykonać pierwszej funkcji, Dodaj ten kod do górnej części strony:</span><span class="sxs-lookup"><span data-stu-id="53f61-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="53f61-210">Większość ten kod znajduje się wewnątrz bloku, która rozpoczyna się `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="53f61-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="53f61-211">`!` Operatora "not" oznacza, że oznacza wyrażenie *Jeśli to żądanie nie jest przesyłanie post*, która jest pośredni sposób z informacją o tym *Jeśli to żądanie jest ta strona została uruchomiona po raz pierwszy*.</span><span class="sxs-lookup"><span data-stu-id="53f61-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="53f61-212">Jak wspomniano wcześniej, należy uruchamiać ten kod *tylko* przy pierwszym uruchomieniu strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="53f61-213">Jeśli nie umieść kod w `if(!IsPost)`, czy uruchamiać za każdym razem strony, zostanie wywołany, czy po raz pierwszy, lub w odpowiedzi na przycisku kliknij.</span><span class="sxs-lookup"><span data-stu-id="53f61-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="53f61-214">Należy zauważyć, że kod zawiera `else` blokowania w tej chwili.</span><span class="sxs-lookup"><span data-stu-id="53f61-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="53f61-215">Jak możemy powiedzieć, gdy wprowadzono `if` bloków, czasami chcesz uruchomić kod alternatywnego, jeśli testowany warunek nie jest spełniony.</span><span class="sxs-lookup"><span data-stu-id="53f61-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="53f61-216">To sytuacji, w tym miejscu.</span><span class="sxs-lookup"><span data-stu-id="53f61-216">That's the case here.</span></span> <span data-ttu-id="53f61-217">Jeśli warunek przekazuje (jeśli przekazany do strony identyfikator jest prawidłowy), przeczytanie wiersz z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="53f61-218">Jednak jeśli warunek nie przeszło, `else` uruchamia blok i kod ustawia komunikat o błędzie.</span><span class="sxs-lookup"><span data-stu-id="53f61-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="53f61-219">Sprawdzanie poprawności wartości przekazane do strony</span><span class="sxs-lookup"><span data-stu-id="53f61-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="53f61-220">W kodzie użyto `Request.QueryString["id"]` można uzyskać Identyfikatora, który jest przekazywany do strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="53f61-221">Kod upewnia się, że wartość rzeczywiście została przekazana dla identyfikatora.</span><span class="sxs-lookup"><span data-stu-id="53f61-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="53f61-222">Jeśli wartość nie została przekazana, kod ustawia błąd sprawdzania poprawności.</span><span class="sxs-lookup"><span data-stu-id="53f61-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="53f61-223">Ten kod zawiera inną metodę weryfikacji informacji.</span><span class="sxs-lookup"><span data-stu-id="53f61-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="53f61-224">W samouczku poprzednie doświadczenie z `Validation` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="53f61-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="53f61-225">Rejestrowane pola do sprawdzania poprawności i ASP.NET została sprawdzania poprawności i automatycznie wyświetlone błędy przy użyciu `Html.ValidationMessage` i `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="53f61-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="53f61-226">W takim przypadku jednak możesz jest nie naprawdę sprawdzanie poprawności danych wejściowych użytkownika.</span><span class="sxs-lookup"><span data-stu-id="53f61-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="53f61-227">Zamiast tego jest sprawdzanie poprawności wartość, która została przekazana do strony z innej lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="53f61-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="53f61-228">`Validation` Pomocnika nie pracę za użytkownika.</span><span class="sxs-lookup"><span data-stu-id="53f61-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="53f61-229">Należy więc wybrać wartość samodzielnie, testując go przy użyciu `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="53f61-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="53f61-230">Jeśli występuje problem, można wyświetlić błąd, za pomocą `Html.ValidationSummary`, jak w przypadku `Validation` pomocnika.</span><span class="sxs-lookup"><span data-stu-id="53f61-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="53f61-231">Aby to zrobić, należy wywołać `Validation.AddFormError` i przekaż go komunikat do wyświetlenia.</span><span class="sxs-lookup"><span data-stu-id="53f61-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="53f61-232">`Validation.AddFormError`to wbudowana metoda, która pozwala zdefiniować niestandardowe wiadomości, które wiąże się przy użyciu systemu sprawdzania poprawności, które znasz już.</span><span class="sxs-lookup"><span data-stu-id="53f61-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="53f61-233">(W dalszej części tego samouczka będziesz omawianiu jak ten proces sprawdzania poprawności nieco bardziej niezawodne.)</span><span class="sxs-lookup"><span data-stu-id="53f61-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="53f61-234">Po upewnieniu się, że jest Identyfikatorem filmu, kod odczytuje bazę danych, Wyszukiwanie elementu pojedynczej bazy danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="53f61-235">(Prawdopodobnie zauważenia ogólne wzorca operacji bazy danych: otworzyć bazy danych, Zdefiniuj instrukcję SQL i uruchom instrukcję.) Teraz, SQL `Select` instrukcja zawiera `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="53f61-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="53f61-236">Identyfikator jest unikatowy, mogą być zwracane tylko jeden rekord.</span><span class="sxs-lookup"><span data-stu-id="53f61-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="53f61-237">Zapytanie jest wykonywane przy użyciu `db.QuerySingle` (nie `db.Query`, jak użyć listy film), i kod umieszcza wynik w `row` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="53f61-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="53f61-238">Nazwa `row` dowolnej; można określić nazwę zmienne niczego chcesz.</span><span class="sxs-lookup"><span data-stu-id="53f61-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="53f61-239">Zmienne zainicjować u góry są wypełnione ze szczegółami film, aby te wartości mogą być wyświetlane w polach tekstowych.</span><span class="sxs-lookup"><span data-stu-id="53f61-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="53f61-240">Testowanie edycji strony (wykonanej do tej pory)</span><span class="sxs-lookup"><span data-stu-id="53f61-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="53f61-241">Jeśli chcesz testować stronę, uruchom *filmy* teraz i kliknij przycisk **Edytuj** link obok żadnych filmu.</span><span class="sxs-lookup"><span data-stu-id="53f61-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="53f61-242">Zobaczysz *EditMovie* strony Szczegóły wypełnione dla filmu wybrano:</span><span class="sxs-lookup"><span data-stu-id="53f61-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Edytuj stronę Film przedstawiający filmu do edycji](updating-data/_static/image3.png)

<span data-ttu-id="53f61-244">Zwróć uwagę, że adres URL strony zawiera przypominać `?id=10` (lub inny numer).</span><span class="sxs-lookup"><span data-stu-id="53f61-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="53f61-245">Do tej pory przetestowaniu który **Edytuj** łączy w *film* strony pracy, czy strony odczytuje identyfikator z ciągu zapytania i że bazy danych zapytania można pobrać filmu pojedynczego rekordu działa.</span><span class="sxs-lookup"><span data-stu-id="53f61-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="53f61-246">Można zmienić informacje film, ale nic się nie dzieje po kliknięciu **Prześlij zmiany**.</span><span class="sxs-lookup"><span data-stu-id="53f61-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="53f61-247">Dodawanie kodu do zaktualizowania film z zmian wprowadzonych przez użytkownika</span><span class="sxs-lookup"><span data-stu-id="53f61-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="53f61-248">W *EditMovie.cshtml* plików, aby zaimplementować funkcji second (zapisywanie zmian), Dodaj następujący kod wewnątrz zamykającym `@` bloku.</span><span class="sxs-lookup"><span data-stu-id="53f61-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="53f61-249">(Jeśli nie masz pewności, dokładnie, gdzie umieścić kod, można przyjrzeć się [kompletny kod dla strony Edytuj film](#Complete_Page_Listing_for_EditMovie) który pojawia się na końcu tego samouczka.)</span><span class="sxs-lookup"><span data-stu-id="53f61-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="53f61-250">Ponownie, jest podobny do kodu, w tym znaczników i kodu *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="53f61-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="53f61-251">Kod znajduje się w `if(IsPost)` zablokować, ponieważ ten kod działa tylko wtedy, gdy użytkownik kliknie **Prześlij zmiany** przycisku &mdash; oznacza to, kiedy (i tylko wtedy, gdy) wysłał formularza.</span><span class="sxs-lookup"><span data-stu-id="53f61-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="53f61-252">W takim przypadku nie używasz testu, takie jak `if(IsPost && Validation.IsValid())`— to znaczy nie łączenia zarówno testy przy użyciu AND.</span><span class="sxs-lookup"><span data-stu-id="53f61-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="53f61-253">Na tej stronie, należy najpierw określić, czy jest przesłanie formularza (`if(IsPost)`), a następnie zarejestruj pól do sprawdzenia poprawności.</span><span class="sxs-lookup"><span data-stu-id="53f61-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="53f61-254">Następnie można przetestować wyniki sprawdzania poprawności (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="53f61-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="53f61-255">Przepływ są nieco inne niż w *AddMovie.cshtml* strony, ale efekt jest taki sam.</span><span class="sxs-lookup"><span data-stu-id="53f61-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="53f61-256">Pobiera wartości pól tekstowych przy użyciu `Request.Form["title"]` i podobny kod dla siebie `<input>` elementów.</span><span class="sxs-lookup"><span data-stu-id="53f61-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="53f61-257">Zwróć uwagę, tym razem kod pobiera identyfikator filmu ukryte pola (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="53f61-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="53f61-258">Po stronie pierwszego uruchomienia, kod uzyskano identyfikator poza ciągu zapytania.</span><span class="sxs-lookup"><span data-stu-id="53f61-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="53f61-259">Pobiera wartość z pola ukrytego upewnij się, że jesteś już identyfikator film, który pierwotnie został wyświetlony, jeśli ciąg zapytania jakiś sposób został zmieniony od tego momentu.</span><span class="sxs-lookup"><span data-stu-id="53f61-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="53f61-260">Naprawdę istotną różnicą między *AddMovie* kodu i ten kod jest w tym kodzie wykorzystania SQL `Update` instrukcji zamiast `Insert Into` instrukcji.</span><span class="sxs-lookup"><span data-stu-id="53f61-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="53f61-261">W poniższym przykładzie przedstawiono składnię SQL `Update` instrukcji:</span><span class="sxs-lookup"><span data-stu-id="53f61-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="53f61-262">Można określić kolumn w dowolnej kolejności i niekoniecznie nie trzeba zaktualizować każdej kolumny podczas `Update` operacji.</span><span class="sxs-lookup"><span data-stu-id="53f61-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="53f61-263">(Nie można zaktualizować Identyfikatora, ponieważ który będzie obowiązywać zapisać rekord jako nowego rekordu, a nie jest dozwolona dla `Update` operacji.)</span><span class="sxs-lookup"><span data-stu-id="53f61-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="53f61-264">**Ważne** `Where` klauzuli o identyfikatorze jest bardzo ważne, ponieważ jest to, jak bazy danych zna bazę danych, która rekordów, które chcesz zaktualizować.</span><span class="sxs-lookup"><span data-stu-id="53f61-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="53f61-265">Jeśli została pozostawiona `Where` klauzuli bazy danych będzie aktualizował *co* rekordów w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="53f61-266">W większości przypadków, które będą awarii.</span><span class="sxs-lookup"><span data-stu-id="53f61-266">In most cases, that would be a disaster.</span></span>


<span data-ttu-id="53f61-267">W kodzie można zaktualizować wartości są przekazywane do instrukcji SQL przy użyciu symboli zastępczych.</span><span class="sxs-lookup"><span data-stu-id="53f61-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="53f61-268">Powtarzaj, co zostało możemy powiedzieć przed: ze względów bezpieczeństwa *tylko* użycia symbole zastępcze w celu przekazania wartości do instrukcji SQL.</span><span class="sxs-lookup"><span data-stu-id="53f61-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="53f61-269">Po kodzie użyto `db.Execute` do uruchomienia `Update` instrukcji przekierowuje wróć do strony listy, w którym można zobaczyć zmiany.</span><span class="sxs-lookup"><span data-stu-id="53f61-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="53f61-270">**Instrukcje SQL różnych, różnych metod**</span><span class="sxs-lookup"><span data-stu-id="53f61-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="53f61-271">Zwróć uwagę, czy nieco inne metody używane do uruchamiania różnych instrukcji SQL.</span><span class="sxs-lookup"><span data-stu-id="53f61-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="53f61-272">Aby uruchomić `Select` zapytanie to potencjalnie zwraca wiele rekordów, możesz użyć `Query` metody.</span><span class="sxs-lookup"><span data-stu-id="53f61-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="53f61-273">Aby uruchomić `Select` zapytania, który zwróci tylko jeden element bazy danych, możesz użyć `QuerySingle` metody.</span><span class="sxs-lookup"><span data-stu-id="53f61-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="53f61-274">Aby uruchomić polecenia, który wprowadzić zmiany, ale które nie zwracają elementy bazy danych, należy użyć `Execute` metody.</span><span class="sxs-lookup"><span data-stu-id="53f61-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="53f61-275">Musisz mieć różne metody, ponieważ każdy z nich zwraca różne wyniki, jak przedstawiono już różnic między `Query` i `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="53f61-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="53f61-276">( `Execute` Metoda również faktycznie zwraca wartość &mdash; mianowicie, liczbę wierszy bazy danych, które miały wpływ polecenia &mdash; , ale można już został ignorowanie który wykonanej do tej pory.)</span><span class="sxs-lookup"><span data-stu-id="53f61-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="53f61-277">Oczywiście `Query` metoda może zwracać tylko jeden wiersz bazy danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="53f61-278">Jednak ASP.NET zawsze traktuje wyniki `Query` metody jako kolekcja.</span><span class="sxs-lookup"><span data-stu-id="53f61-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="53f61-279">Nawet wtedy, gdy metoda zwróci wartość tylko jeden wiersz, należy wyodrębnić ten pojedynczy wiersz z kolekcji.</span><span class="sxs-lookup"><span data-stu-id="53f61-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="53f61-280">W związku z tym w sytuacjach, w którym możesz *wiedzieć* zostanie wyświetlony ponownie tylko jeden wiersz, jest bardziej wygodne bit `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="53f61-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="53f61-281">Istnieje kilka metod, wykonujących określone typy operacji w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="53f61-282">Można znaleźć listę metod bazy danych w [interfejsu API stron sieci Web platformy ASP.NET — dokumentacja szybkie](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="53f61-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>


## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="53f61-283">Tworzenie weryfikacji dla Identyfikatora więcej niezawodne</span><span class="sxs-lookup"><span data-stu-id="53f61-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="53f61-284">Po raz pierwszy uruchamia strony, otrzymasz identyfikator film z ciągu zapytania, dzięki czemu możesz pobrać ten film z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="53f61-285">Wprowadzone należy się upewnić, że faktycznie wystąpił wartość przejść, sprawdź, czy przy użyciu tego kodu:</span><span class="sxs-lookup"><span data-stu-id="53f61-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="53f61-286">Ten kod jest używany do upewnij się, że jeśli użytkownik pobiera do *EditMovies* strony bez zaznaczania filmu w *filmy* strony, strona będzie wyświetlana przyjazna dla użytkownika komunikat.</span><span class="sxs-lookup"><span data-stu-id="53f61-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="53f61-287">(W przeciwnym razie użytkownicy czy Zobacz błąd, który będzie prawdopodobnie właśnie mylić je).</span><span class="sxs-lookup"><span data-stu-id="53f61-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="53f61-288">Jednak stopniu niezawodną nie tej weryfikacji.</span><span class="sxs-lookup"><span data-stu-id="53f61-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="53f61-289">Strony może również być wywoływane z tych błędów:</span><span class="sxs-lookup"><span data-stu-id="53f61-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="53f61-290">Identyfikator nie jest liczbą.</span><span class="sxs-lookup"><span data-stu-id="53f61-290">The ID isn't a number.</span></span> <span data-ttu-id="53f61-291">Na przykład strony można wywołać przy użyciu adresu URL, takie jak `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="53f61-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="53f61-292">Identyfikator jest liczbą, lecz przywołuje film, który nie istnieje (na przykład `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="53f61-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="53f61-293">Jeśli Cię to ciekawi wyświetlić błędy wynikające z tych adresów URL, uruchom *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="53f61-294">Wybierz opcję film, aby edytować, a następnie zmień adres URL *EditMovie* strony do adresu URL, który zawiera alfabetyczną identyfikator lub identyfikator filmu nie istnieje.</span><span class="sxs-lookup"><span data-stu-id="53f61-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="53f61-295">Dlatego co należy zrobić?</span><span class="sxs-lookup"><span data-stu-id="53f61-295">So what should you do?</span></span> <span data-ttu-id="53f61-296">Pierwsze rozwiązanie polega na upewnij się, że nie tylko jest identyfikator przekazywany do strony, ale czy identyfikator jest liczbą całkowitą.</span><span class="sxs-lookup"><span data-stu-id="53f61-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="53f61-297">Zmień kod `!IsPost` testu, aby wyglądały jak w tym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="53f61-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="53f61-298">Dodano drugi warunek do `IsEmpty` testu, połączony z `&&` (logiczne i):</span><span class="sxs-lookup"><span data-stu-id="53f61-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="53f61-299">Pamiętasz z [wprowadzenie do programowania stron sieci Web ASP.NET](../introducing-razor-syntax-c.md) samouczka dotyczącego metod, takich jak `AsBool` `AsInt` przekonwertować ciągu znaków na inny typ danych.</span><span class="sxs-lookup"><span data-stu-id="53f61-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="53f61-300">`IsInt` — Metoda (i innych osób, takich jak `IsBool` i `IsDateTime`) są podobne.</span><span class="sxs-lookup"><span data-stu-id="53f61-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="53f61-301">Jednak sprawdzają one tylko czy możesz *można* przekonwertować ciągu, bez rzeczywistego wykonania konwersji.</span><span class="sxs-lookup"><span data-stu-id="53f61-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="53f61-302">Dlatego w tym miejscu możesz jest zasadniczo informujący o tym *Jeśli można przekonwertować na liczbę całkowitą wartość ciągu kwerendy...* .</span><span class="sxs-lookup"><span data-stu-id="53f61-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="53f61-303">Potencjalny problem szuka film, który nie istnieje.</span><span class="sxs-lookup"><span data-stu-id="53f61-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="53f61-304">Kod w celu uzyskania filmu wygląda ten kod:</span><span class="sxs-lookup"><span data-stu-id="53f61-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="53f61-305">W przypadku przekazania `movieId` do wartości `QuerySingle` metodę, która nie jest zgodny z rzeczywistego film, nic nie zostanie zwrócone i instrukcje wykonaj (na przykład `title=row.Title`) spowodować błędy.</span><span class="sxs-lookup"><span data-stu-id="53f61-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="53f61-306">Ponownie jest łatwe.</span><span class="sxs-lookup"><span data-stu-id="53f61-306">Again there's an easy fix.</span></span> <span data-ttu-id="53f61-307">Jeśli `db.QuerySingle` — metoda nie zwróciło żadnych wyników `row` zmienna będzie mieć wartość null.</span><span class="sxs-lookup"><span data-stu-id="53f61-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="53f61-308">Dlatego można sprawdzić, czy `row` zmienna ma wartość null, zanim spróbujesz uzyskać wartości z niego.</span><span class="sxs-lookup"><span data-stu-id="53f61-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="53f61-309">Poniższy kod dodaje `if` bloku wokół instrukcji, które pobiera wartości z `row` obiektu:</span><span class="sxs-lookup"><span data-stu-id="53f61-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="53f61-310">Z tych dwóch testów weryfikacji dodatkowe strony staje się bardziej punktor potwierdzenia.</span><span class="sxs-lookup"><span data-stu-id="53f61-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="53f61-311">Kompletny kod dla `!IsPost` gałęzi teraz wygląda następująco:</span><span class="sxs-lookup"><span data-stu-id="53f61-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="53f61-312">Firma Microsoft będzie należy ponownie to zadanie dobre wykorzystanie dla `else` bloku.</span><span class="sxs-lookup"><span data-stu-id="53f61-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="53f61-313">Jeśli nie, `else` bloki ustawić komunikaty o błędach.</span><span class="sxs-lookup"><span data-stu-id="53f61-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="53f61-314">Dodanie łącz, aby powrócić do strony filmy</span><span class="sxs-lookup"><span data-stu-id="53f61-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="53f61-315">Szczegóły ostateczne i przydatne jest aby dodać łącze do *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="53f61-316">W zwykłym przepływu zdarzeń, użytkownicy zostaną uruchomione w *filmy* i kliknij przycisk **Edytuj** łącza.</span><span class="sxs-lookup"><span data-stu-id="53f61-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="53f61-317">Który wprowadzono do *EditMovie* strony, w którym można edytować filmu i kliknij przycisk.</span><span class="sxs-lookup"><span data-stu-id="53f61-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="53f61-318">Po kodzie przetwarza zmiany, przekierowuje do *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="53f61-319">Jednak:</span><span class="sxs-lookup"><span data-stu-id="53f61-319">However:</span></span>

- <span data-ttu-id="53f61-320">Użytkownik może zrezygnować z wprowadzanie zmian.</span><span class="sxs-lookup"><span data-stu-id="53f61-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="53f61-321">Użytkownik może stają się coraz do tej strony bez najpierw kliknij **Edytuj** łącze w *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="53f61-322">W obu przypadkach należy ułatwiają je, aby powrócić do listy głównego.</span><span class="sxs-lookup"><span data-stu-id="53f61-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="53f61-323">Jest łatwy poprawka &mdash; Dodaj następujący kod znaczników zaraz po upływie `</form>` tag w kod znaczników:</span><span class="sxs-lookup"><span data-stu-id="53f61-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="53f61-324">Ten kod znaczników używa tej samej składni `<a>` element, który w tym samouczku w innym miejscu.</span><span class="sxs-lookup"><span data-stu-id="53f61-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="53f61-325">Adres URL zawiera `~` oznacza "root witryny sieci Web".</span><span class="sxs-lookup"><span data-stu-id="53f61-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="53f61-326">Testowanie procesu aktualizacji film</span><span class="sxs-lookup"><span data-stu-id="53f61-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="53f61-327">Teraz możesz przetestować.</span><span class="sxs-lookup"><span data-stu-id="53f61-327">Now you can test.</span></span> <span data-ttu-id="53f61-328">Uruchom *filmy* , a następnie kliknij przycisk **Edytuj** obok filmu.</span><span class="sxs-lookup"><span data-stu-id="53f61-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="53f61-329">Gdy *EditMovie* zostanie wyświetlona strona, wprowadzić zmiany do filmu i kliknij przycisk **Prześlij zmiany**.</span><span class="sxs-lookup"><span data-stu-id="53f61-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="53f61-330">Zostanie wyświetlona na liście film, upewnij się, że zmiany są pokazywane.</span><span class="sxs-lookup"><span data-stu-id="53f61-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="53f61-331">Aby upewnić się, czy działa sprawdzania poprawności, kliknij przycisk **Edytuj** dla innego filmu.</span><span class="sxs-lookup"><span data-stu-id="53f61-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="53f61-332">Po przejściu do *EditMovie* wyczyść **Genre** pola (lub **roku** pola lub oba), a następnie spróbuj przesłać zmiany.</span><span class="sxs-lookup"><span data-stu-id="53f61-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="53f61-333">Zostanie wyświetlony błąd, jak można oczekiwać:</span><span class="sxs-lookup"><span data-stu-id="53f61-333">You'll see an error, as you'd expect:</span></span>

![Edytuj stronę Film przedstawiający błędy sprawdzania poprawności](updating-data/_static/image4.png)

<span data-ttu-id="53f61-335">Kliknij przycisk **powrócić do listy filmów** łącze, aby odrzucić zmiany i wróć do *filmy* strony.</span><span class="sxs-lookup"><span data-stu-id="53f61-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="53f61-336">Powtarzający się dalej</span><span class="sxs-lookup"><span data-stu-id="53f61-336">Coming Up Next</span></span>

<span data-ttu-id="53f61-337">W następnym samouczku pojawi się, jak usunąć rekord filmu.</span><span class="sxs-lookup"><span data-stu-id="53f61-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="53f61-338">Pełna lista strony Movie (aktualizowane wraz z łączami Edycja)</span><span class="sxs-lookup"><span data-stu-id="53f61-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="53f61-339">Zakończenie listę strony dla strony filmu edycji</span><span class="sxs-lookup"><span data-stu-id="53f61-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="53f61-340">Dodatkowe zasoby</span><span class="sxs-lookup"><span data-stu-id="53f61-340">Additional Resources</span></span>

- [<span data-ttu-id="53f61-341">Wprowadzenie do programowania sieci Web ASP.NET przy użyciu składni Razor</span><span class="sxs-lookup"><span data-stu-id="53f61-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](../introducing-razor-syntax-c.md)
- <span data-ttu-id="53f61-342">[Instrukcji SQL UPDATE](http://www.w3schools.com/sql/sql_update.asp) w witrynie W3Schools</span><span class="sxs-lookup"><span data-stu-id="53f61-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

>[!div class="step-by-step"]
<span data-ttu-id="53f61-343">[Poprzednie](entering-data.md)
[dalej](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="53f61-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
